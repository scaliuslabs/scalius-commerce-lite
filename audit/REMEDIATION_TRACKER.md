# Remediation Tracker

Status values: `Not Started`, `In Progress`, `Blocked`, `Fixed`, `Verified`, `Won't Fix`.

Update this file when a future agent starts or finishes a slice. Keep evidence short and link the PR/commit when available.

## Priority Queue

| ID | Severity | Status | Owner | Summary | Primary Verification |
| --- | --- | --- | --- | --- | --- |
| SEC-001 | P0 | Verified | API/Auth | Enforce 2FA verification in admin API middleware. | API route tests for unverified 2FA session. |
| AUTH-002 | P0 | Verified | API/Admin Auth | Remove direct 2FA `mark-verified` bypass; require Better Auth session-token proof before setting `session.twoFactorVerified`. | Focused API auth tests, API/admin typechecks, root gates, deploy, and live admin smoke. |
| AUTH-003 | P0 | Verified | Admin/Auth Setup | Delete browser-callable first-user super-admin promotion helper and rely on guarded `/api/v1/setup`. | Stale-reference scan, admin typecheck, deploy, and live admin smoke. |
| AUTH-005 | P1 | Verified | Database/Auth | Add Better Auth `twoFactor.verified` schema/migration support for Better Auth 1.6. | Core auth tests, local/remote migration `0040`, remote table-info check, deploy, and live admin auth smoke. |
| AUTH-006 | P1 | Verified | API/Admin Auth | Require target-method code proof before changing preferred admin 2FA method. | Focused auth-management route tests, SDK regeneration, admin/API typechecks, deploy, and live admin smoke. |
| AUTH-007 | P1 | Verified | API/Auth Setup | Verify an existing duplicate setup account password before first-admin promotion recovery. | Focused setup duplicate-recovery tests, deploy, and live setup/auth smoke. |
| AUTH-009 | P1 | Verified | API/Admin Auth | Harden legacy `/2fa/verify` so invalid/expired codes return 400 and token proofs must match current session/user. | Focused auth-management tests, root tests, API deploy, and live admin smoke. |
| AUTH-010 | P1 | Verified | Admin/API Auth | Password change and first-time 2FA setup can rotate Better Auth sessions through the API worker without propagating `Set-Cookie` back to the dashboard domain. | API/admin cookie-relay tests, first-time email/TOTP method tests, typechecks, local/live smoke, and deploy. |
| AUTH-011 | P2 | Verified | API/Admin Auth UX | Current conservative trusted-device policy is enforced at both the legacy API verification path and same-origin Better Auth catch-all paths; login 2FA now uses pending Better Auth method hints instead of silently guessing email. | Focused admin/API auth tests, root gates, local running-stack smoke, deploy, live trusted-device rejection checks, demo sign-in API check, and live browser smoke. |
| SEC-002 | P0 | Verified | Admin/Auth/RBAC | Require inventory permission before scanner token minting. | Low-permission admin cannot mint scanner session. |
| SEC-006 | P0 | Verified | Admin/Auth/RBAC | Same-origin scanner-token minting verifies RBAC but did not require a fully 2FA-verified admin session. | Scanner token minting now rejects pending-2FA sessions before RBAC/KV work, keeps products view+edit or super-admin RBAC after 2FA, and is verified with focused/root gates, admin deploy, live unauthenticated fail-closed smoke, and deployed server-bundle marker checks. |
| SEC-003 | P0 | Verified | API/Settings | Split public checkout-language reads from admin mutations. | Public mutation returns 404; admin mutation remains registered. |
| PRIV-001 | P0 | Verified | Storefront/Orders | Protect order receipt PII with receipt token/minimal DTO. | Private browser cannot view PII by order ID alone. |
| PRIV-003 | P1 | Verified | Storefront/Orders/Payments | Receipt proof bearer tokens still travel in URLs across `/order-success`, receipt API query strings, hosted-payment callbacks/redirects, and admin recovery copy links, exposing sensitive recovery proof through browser history, referers, logs, and screenshots. | Focused receipt/payment callback tests prove no `token`, `receipt_token`, or `receiptToken` appears in public URLs; proof moves to a short-lived same-origin cookie/header/POST handoff; source and live read-only smokes confirm receipt access still works. |
| POSTSALE-021 | P1 | Verified | Storefront/Orders/Auth | Merchant-sendable cross-browser hosted-payment recovery needs a buyer-verified handoff now that raw receipt proof is banned from URLs. | A buyer can recover a failed SSLCommerz/Polar guest order from a new browser only after email/SMS/WhatsApp OTP proof; tests and live smokes prove leaked URLs alone cannot open receipt PII or expose raw receipt proof. |
| POSTSALE-022 | P2 | Verified | Release/Post-sale Smoke | The local post-sale checkout smoke still used a `chk_` receipt proof in the receipt URL after production intentionally banned bearer proof in URLs, so the release harness was stale and could encourage regressions. | `scripts/dev-postsale.mjs` now builds receipt lookup requests with `X-Receipt-Token`, no longer prints raw receipt proof in success JSON, and still proves checkout replay plus support-request creation. Verification: focused post-sale smoke tests, local `node scripts/dev-postsale.mjs checkout-smoke --state /tmp/scalius-release-smoke` order `9EW468`, root gates, full deploy, live release check, and ops check pass. |
| SEC-004 | P0 | Verified | Storefront/Checkout | Remove `innerHTML` rendering of checkout user data. | Injection string renders safely as text. |
| SEC-007 | P1 | Verified | API/Admin Safe Methods | `GET /api/v1/admin/abandoned-checkouts` performed cleanup/archive mutations while serving a list read. | GET is now side-effect-free; old/empty abandoned-checkout pruning moved to scheduled maintenance; focused/root gates, API deploy `cae463cd-6cf3-4add-af00-5b53347d5119`, and live OpenAPI/health/401 smokes pass. |
| SEC-009 | P0 | Verified | Core/API Email Security | Missing transactional email provider fallback could log full email HTML/text, including customer OTPs, admin 2FA codes, reset links, and invite temporary passwords. | Email fallback now logs masked metadata only and remains non-delivering/retryable; focused/root gates, API deploy `185b1a67-b614-4db6-a193-dc58fc2cd0c1`, admin deploy `3be4718f-2feb-4ae0-9933-77c70224d9d1`, and live smokes pass. |
| AUTH-025 | P1 | Verified | Customer/Auth OTP Security | OTP failed-attempt accounting used read-modify-write KV updates, so concurrent wrong-code attempts could undercount brute-force attempts and correct-code races could double-consume or resurrect an OTP. | Customer OTP challenges now live in D1 `customer_auth_otp_challenges` with HMAC code hashes, channel-scoped keys, pinned contacts, guarded correct/wrong update paths, queue-handoff cleanup, scheduled cleanup, local/remote migration `0051`, focused/root gates, API deploy `2a7b6ee4-55e3-411e-8edb-c3aa099ba531`, and live D1/API smokes. |
| SEC-010 | P1 | Verified | API/CORS/Auth | CSP allowed domains were also used as credentialed API CORS origins, so adding a third-party CSP domain could grant credentialed API access to that origin. | CSP domains are now storefront CSP-only; credentialed API CORS uses first-party/exact origins only, unsafe cookie browser writes are blocked by Origin guards in API/admin/storefront proxies, focused/root gates pass, API/admin/storefront deploys pass, and live CORS/403 smokes pass. |
| AUTH-026 | P1 | Verified | API/Admin Setup | First-admin setup lock/rate-limit fail open when KV is unavailable and the KV lock is get-then-put, so concurrent setup requests can race. | D1 `admin_setup_claims` and `admin_setup_rate_limits` now own bootstrap coordination; focused/root gates, remote migration `0052`, API deploy `7977cc51-d8be-4395-a578-d13e6d50ee65`, and live setup/D1 smokes pass. |
| SEC-011 | P1 | Verified | Admin/Scanner Auth | Scanner QR token exchange reads KV, creates a scanner session, then deletes the token, so two racing exchanges can both mint scanner sessions. | D1 `scanner_token_claims` now atomically consumes unexpired QR claims before any KV scanner session is written; focused/root gates, remote migration `0053`, API deploy `50999170-909e-47cc-9bb4-128784c987b2`, admin deploy `3de21275-ea4b-4489-88c8-40104e31d64d`, and live D1/API/scanner smokes pass. |
| AUTH-027 | P1 | Verified | Customer/Auth Enumeration | Customer sign-in/sign-up OTP responses reveal whether a phone/email is already registered, and existence checks currently run before the IP limiter so enumeration avoids configured OTP/send quotas. | `sendOtp()` no longer performs account-existence lookups before challenge creation; it creates a real D1 OTP challenge with generic send copy and leaves unknown-account/duplicate-account guidance to post-OTP `verifyOtp()`. Storefront OTP errors now offer a safe alternate-intent action after proof. Focused/root gates pass; API deploy `c725f94a-8853-44f8-aa05-8a7f4211f9cf`, storefront deploy `546fe109-7220-4743-8b18-ad7ee90694f6`, live API health and deployed AuthModal marker smokes pass. |
| SEC-012 | P1 | Verified | API/CORS/Auth | Credentialed CORS and the cookie-origin guard always allow localhost/loopback origins, including production, so unsafe cookie writes from local origins can pass the same allowlist used by credentialed API CORS. | Loopback wildcards are now enabled only when a first-party runtime URL is loopback, exact explicit localhost origins remain opt-in, focused/root gates pass, API deploy `62a419af-1fd7-4613-9fbf-bf1a86554328`, and live first-party/localhost CORS smokes pass. |
| AUTH-028 | P1 | Verified | Admin/Auth Invites | Admin user creation emails a long-lived temporary password, marks the user email-verified/admin, and does not enforce password change or 2FA enrollment before RBAC access. | Temp-password invite delivery has been replaced with Better Auth reset/setup links plus D1 `mustChangePassword` and `mustEnrollTwoFactor` gates in API/admin route guards before RBAC. Focused/root gates, local browser invite smoke, remote migration `0060`, API deploy `76e564cd-fa47-4b20-9a10-3156a67d09d2`, admin deploy `a1458364-4568-4728-934a-381745934456`, live OpenAPI/D1/admin-asset/401 smokes pass. |
| AUTH-029 | P1 | Verified | Customer/Auth Sessions | Soft-deleted customers could keep using existing 30-day KV sessions because `getCustomerBySession()` trusted the cached session without checking `customers.deletedAt`. | Customer sessions are now D1-authoritative in `customer_sessions`; `getCustomerBySession()` hashes the cookie token, requires active/non-expired/non-revoked rows, joins the live `customers` row, and rejects deleted/missing customers. Admin soft/bulk/permanent deletes revoke/delete affected session rows. Focused/root gates pass; local and remote migration `0054`, API deploy `5bc3ebee-044b-4b37-b715-54ee189a2f4f`, remote D1 table/index smoke, and live `/customer-auth/me` smokes pass. |
| AUTH-030 | P1 | Verified | Customer/Auth Sessions | Customer session KV keys and values contained raw bearer session tokens, so KV key/value exposure was enough for immediate 30-day customer session takeover. | Raw customer session tokens now exist only in the httpOnly `cs_tok` cookie and transient request memory. D1 stores only HMAC-SHA256 token hashes plus customer/expiry/revocation timestamps; no session JSON is persisted to KV. Focused tests assert no raw-token persistence, root gates pass, and API deploy `5bc3ebee-044b-4b37-b715-54ee189a2f4f` is live. |
| SEC-013 | P1 | Verified | Admin/Auth Guard | Admin route guards parsed `better-auth.session_token` manually and trusted the unsigned token prefix against D1, so a leaked D1 session token could enter admin pages without a valid Better Auth signed cookie. | Hot admin guard now verifies the Better Auth HMAC signature with `BETTER_AUTH_SECRET` before D1 lookup and fails closed when the cookie is raw, tampered, wrong-secret signed, or the secret is missing. Focused/root gates pass; admin deploy `fa94e771-e058-487b-ad2e-1b2c0890df55`; live no-cookie and forged-cookie `/admin` smokes redirect to login. |
| SEC-014 | P1 | Verified | API/Admin Bearer Auth | Admin API bearer JWT fallback could bypass Better Auth session revocation, ban/deleted checks, and live DB-backed 2FA truth by trusting token claims. | Admin/cache API middleware no longer accepts JWT Bearer fallback; only live Better Auth dashboard sessions and exact scanner-session cookies are valid. Bearer-only admin requests are covered by middleware tests and live 401 smoke with no `X-New-Token`; API deploy `2ead5ab0-d7fe-4af1-91c2-989d9d8c022e`. |
| AUTH-031 | P1 | Verified | Customer/Auth Rate Limits | Customer OTP IP throttling is KV get-then-put and trusts `x-forwarded-for` outside Cloudflare, allowing races and header spoofing to exceed the send cap. | OTP send throttling is now D1-authoritative through `customer_auth_otp_rate_limits`, IP buckets are hashed, production trusts `CF-Connecting-IP` and ignores raw XFF unless running behind loopback local origins, unknown IPs share a fail-closed bucket, concurrent tests prove only the configured quota can pass, and API deploy `6e8f93f6-3ae7-4e65-9a00-7051999c97ea` is live. |
| PRIV-002 | P2 | Verified | Customer/Auth OTP PII | OTP challenge rows HMAC the code but retained raw identifier/contactEmail/phone for a short window, exposing recent auth targets and phone/email pairings to DB readers. | Challenge storage now uses opaque HMAC keys, keyed identifier hashes plus masks, encrypted pinned sign-up contacts, and tests proving no raw email/phone/code persistence. Focused/root gates pass; local and remote migration `0069`, API deploy `195d32b4-7848-43e3-9a7e-8f1a00f76180`, and live D1/API smokes pass. |
| AUTH-032 | P2 | Verified | Customer/Auth Cookies | Customer cookie domain scoping derived the root from the last two hostname labels, which was brittle for public-suffix domains and could over-broaden sibling-subdomain access. | Customer auth cookies are host-only by default; only explicit `CUSTOMER_AUTH_COOKIE_DOMAIN` can add a `Domain=` attribute. Focused/root gates pass; API deploy `70acbcd1-5ef0-468b-97a1-71b0ff500c60`, storefront deploy `ba54c128-ed53-4f1c-871d-dd48364b30c3`, and live API/storefront logout cookie smokes prove no implicit domain attribute. |
| AUTH-033 | P1 | Verified | Admin/Auth Login | If the admin login form is submitted before React hydration attaches, the browser can fall back to native `GET /auth/login?email=...&password=...`, leaking credentials into URL history/log surfaces and failing login. | Login SSR now renders a `method="post"` form with controls disabled until hydration, so no-JS/pre-hydration submit cannot put credentials in the URL. Verified with route-boundary coverage, admin typecheck/lint/build, a local no-JavaScript Chrome smoke showing POST form action plus disabled email/password/submit controls, admin deploy `0096d1fd-78b1-4b0b-8e79-b5aad52fa951`, and live dashboard login HTML smoke (`200`, POST form, four disabled controls, no `password=` marker). |
| AUTH-034 | P1 | Verified | Auth/No-JS Form Safety | Cross-surface audit found password reset and several auth/account forms were safe only through React interception or missing input names; storefront discount submit was also a brittle default-GET form. | Admin sensitive auth/account forms now use route-bound POST actions, `noValidate`, and hydration-gated controls; reset-password removes native password field names. Storefront discount form is POST-only and cart checkout POSTs require `formIntent=checkout`; AuthModal remains non-form based. Focused source-boundary tests, admin/storefront typecheck/lint/build, local and live no-JavaScript Chrome smokes with screenshots verify no credential/OTP/phone/email/password/discount value enters URLs or gets mistaken for checkout. Admin deploy `eab5b3b6-2821-45c7-9dbd-14d40d20b6a7`; storefront deploy `389b24ac-512e-4259-9946-a70c32432eed`. |
| AUTH-035 | P1 | Verified | Auth/No-JS Form Regression Audit | User independently observed a historical login submit fallback that encoded credentials into the URL, and re-raised the same browser-observed class on 2026-06-22. Even though `AUTH-033`/`AUTH-034` hardened the known surfaces, every new or touched auth/contact/payment form must be treated as suspect until no-JS and pre-hydration fallback behavior is re-proven. | Admin app forms now have explicit submit methods; sensitive mutation/config forms use POST + `noValidate`, and broad route-boundary coverage fails on implicit methods. Public discount validation is POST-only JSON, the generated SDK no longer exposes `getApiV1DiscountsValidate`, storefront discount validation sends buyer/cart/customer data in the request body, cart checkout has explicit `action="/cart"`, and the visible discount input stays nameless. Focused/root gates, local API GET/POST smokes, local rendered `/cart` HTML smoke, deploys (API `0c735555-c581-41b8-8683-4493073313c4`, admin `00678717-62a2-4c70-b126-07dd0c990244`, storefront `9a05bfc2-5586-4574-9593-cdcc0cc7ba7c`), and live API/OpenAPI/storefront cart smokes pass. |
| AUTH-036 | P1 | Verified | API/Admin Setup | A completed bootstrap claim could block first-admin recovery after local/dev user rows were reset, while `/api/v1/setup` also treated Better Auth `twoFactorRedirect` as proof that an existing duplicate setup account password was verified. | Setup recovery now lets D1 reclaim a completed setup claim only when no admin user exists, and duplicate-account password proof requires an actual Better Auth session token, not a 2FA redirect. Focused setup claim tests, API auth-management tests, root typecheck/lint/test/build, env/dist/migration/diff checks, and local setup/login browser smoke pass. |
| AUTH-037 | P2 | Verified | API/Admin Invites | Team invite creation validated runtime URL configuration after mutating auth/user state, so a bad URL setting could create a partially-onboarded admin without a usable invite link. | Invite URL prerequisites are validated before Better Auth/user mutation. Focused auth-management tests, route-boundary coverage, root typecheck/lint/test/build, and local admin login/setup smokes pass. |
| AUTH-038 | P2 | Verified | Admin/Auth Login Navigation | Fresh live login could succeed but still log an aborted `adminRouteGuard()` `createServerFn` fetch because the Better Auth sign-in request carried a `/admin` `callbackURL` while the component also navigated to `/admin`. | Login success is now router-owned: `callbackURL` is omitted, 2FA/admin transitions use replace navigation, and route-boundary coverage guards against reintroducing the double-navigation shape. Verification: focused route-boundary test (`38` tests), admin typecheck/lint/build, `pnpm check:dist-secrets`, `git diff --check`, local fresh-context browser login smoke with all `_serverFn` calls `200`, admin deploy `5c866377-ae28-4de9-a5a4-080e40849a95`, and live fresh-context login smoke with `adminRouteGuard()` `200` and no dashboard `Failed to fetch` console error. |
| AUTH-039 | P2 | Verified | Admin/Auth Login | Login still gave native controls `name="email"` and `name="password"`, so any future accidental native GET regression would have serializable credentials even though the form is POST/hydration-gated today. | Login controls now keep `id`/`autoComplete` but have no native sensitive `name` keys; focused auth-form URL safety tests model accidental GET serialization across admin auth and storefront AuthModal surfaces. Focused tests, admin/storefront typecheck/lint/build, root gates, admin deploy `51127823-1626-4b1a-b726-3f36852cc682` / version `fa00baa0-4ff5-4d7d-8171-7ff9e6c93896`, and live dashboard login HTML smoke prove a POST login form with no native sensitive names. |
| AUTH-050 | P1 | Verified | Customer/Auth OTP Security | Customer-login and hosted-payment recovery OTP queue messages could carry raw OTP codes, and WhatsApp OTP dispatch used a template shape that could mismatch Meta Cloud API templates, increasing secret exposure and delivery fragility. | OTP codes are now derived at send time from opaque challenge/delivery keys plus the configured signing key, customer-auth and payment-recovery queue payloads carry no raw code for new messages, legacy payloads remain readable, and WhatsApp OTP sends the single body parameter expected by the current template. Focused OTP/recovery/queue tests, root typecheck/lint/test (`381` files / `2599` tests), build/env/dist/migration/drizzle/audit/peer/frozen-install/diff gates, deploy API `71d37e84-446a-4c90-9dbb-b1f22dba66b7`, admin `76e716b9-3826-48db-a035-5b132b511bf1`, storefront `bfa7c0d9-208b-4ec5-bb12-267a625275d2`, ops `885b385b-a3db-40e8-8ae9-865160785a4b`, and live ops check `ops-check-20260706t032822007z-b4c34239` pass. |
| AUTH-051 | P1 | Verified | Admin/Auth Email Credentials | Better Auth email delivery still let Resend provider credentials decrypt through the legacy `JWT_SECRET` fallback path, so an old or wrong credential key could make unreadable ciphertext look usable in password reset/verification/2FA mail flows. | Better Auth email runtime now passes only the dedicated `CREDENTIAL_ENCRYPTION_KEY` into provider credential reads; a regression test proves a Resend API key encrypted with only `JWT_SECRET` stays unreadable. Verification: focused core auth test, root typecheck/lint/test (`381` files / `2619` tests), build/env/dist/migration/drizzle/audit/peer/frozen-install/diff gates, full deploy API `d4da4b57-a37c-4c0b-b777-f7f0f3185331`, admin `fbe06d25-5b34-447d-ab3a-07091d646877`, storefront `8bb81458-f570-4e6c-8c2f-956f13e65f99`, ops `0ee75238-49f9-44cf-afe7-77c9c7a2b610`, and live ops check `ops-check-20260706t041347670z-23eecc06` pass. |
| AUTH-052 | P1 | Verified | Auth/OTP Queue Privacy | New customer-auth and payment-recovery OTP queue messages still carried raw recipient identifiers/display names after `AUTH-050` removed raw OTP codes. | Migration `0079_auth_otp_delivery_targets` stores encrypted delivery target/name on the D1 challenge rows; new `auth.send_otp` queue payloads carry only opaque challenge/delivery references; the queue consumer resolves/decrypts the target at send time and terminally skips missing/decrypt-failed targets without provider calls or hot retries. Verification: focused OTP/queue tests, root typecheck/lint/test/build/security gates, remote D1 `PRAGMA table_info` proof, deploy API `5f4fe9a5-672b-4a35-a43d-c2c12ff345d9`, admin `16c0aa27-835f-409c-b431-cc70c6bb2800`, storefront `9e2cfb69-b7c8-4cf4-bdcb-e507b86145da`, ops `60612cda-df46-497a-82ad-5853e7c14416`, and live ops check `ops-check-20260706t181040901z-b360a3d8` pass. |
| CONF-002 | P1 | Verified | Core/Credentials | Several provider reads can fall back from `CREDENTIAL_ENCRYPTION_KEY` to `JWT_SECRET` and graceful decrypt can return undecryptable ciphertext as a non-empty configured secret. | Provider-boundary reads for payment gateways, Resend, SMS, WhatsApp readiness, payment sessions, webhooks, refunds, checkout config, and notification queues now use strict `CREDENTIAL_ENCRYPTION_KEY` resolution. New generic encrypted settings write `enc:`-prefixed values; missing/wrong-key ciphertext produces explicit readiness errors instead of configured secrets. Focused/root gates, SDK regeneration, full deploy, and live OpenAPI/checkout/storefront smokes pass. |
| CONF-003 | P1 | Verified | Core/Credentials | Follow-up audit found residual provider paths still using credential reads that could accept the legacy JWT fallback or unreadable ciphertext: delivery providers, fraud checker, webhook auth, FCM, and notification dispatch setup. | Delivery, fraud-checker, FCM/notification, and webhook-auth reads now use strict `CREDENTIAL_ENCRYPTION_KEY` provider boundaries and surface unreadable credentials as readiness/setup failures instead of configured secrets. Focused credential-boundary tests, root gates, deploys, and live ops check listed in `AUTH-050` pass. |
| SEC-008 | P1 | Verified | API/CORS | Credentialed CORS wildcard origin matching did not escape regex metacharacters before replacing `*`, so lookalike origins could match trusted wildcard domains. | CORS origin matching now normalizes candidate origins, escapes non-wildcard characters, supports only exact origins, `*.domain` subdomain wildcards, and `host:*` dev ports, and rejects lookalike domains. Focused shared CORS tests, root gates, API deploy, and live CORS smokes pass. |
| SUPPLY-001 | P1 | Verified | Platform/Dependencies | Clear open `pnpm audit` advisories for Hono/Turbo/esbuild/shell-quote/devalue/grpc/ws/qs/js-yaml. | `pnpm audit --audit-level moderate` returns no known vulnerabilities. |
| ORDER-001 | P1 | Verified | Inventory/Orders | Reservation expiry can release live order reservations. | Expiry test with live reserved order. |
| ORDER-002 | P1 | Verified | Orders/Fulfillment | Inventory/COD/provider side effects can precede local CAS/commit. | CAS/provider failure-ordering tests. |
| ORDER-003 | P1 | Verified | Orders/Queue | Batch stock/discount failure can poison unrelated messages. | Mixed batch failure isolation tests. |
| ORDER-004 | P1 | Verified | API/Orders | Queue send happens before checkout KV write. | Route test for queue success plus KV failure. |
| ORDER-005 | P1 | Verified | Admin/Orders/Inventory | Abandoned-checkout cleanup can strand reserved inventory. | Old incomplete reserved-order cleanup releases stock before archive/delete. |
| PAY-001 | P1 | Verified | Payments/Webhooks | Stripe/SSL idempotency is KV-only and recorded after queue send. | Durable webhook claim tests plus payment duplicate tests. |
| PAY-002 | P1 | Verified | Payments/Orders | Polar full refund skips order status transition. | Polar refund state-machine test. |
| PAY-003 | P1 | Verified | Payments/Checkout | Payment session creation trusts caller-controlled order/redirect data. | `payment-session.test.ts` rejects missing/foreign tokens and verifies trusted gateway URLs. |
| DEL-001 | P1 | Verified | Delivery | Webhook idempotency and shipment status semantics drift. | Steadfast durable claim and delivery tracking tests. |
| STORE-001 | P1 | Verified | Storefront/Cache | Checkout/shipping cache purge can leave stale L2 entries. | Selective purge policy and checkout purge payload tests. |
| OPS-001 | P1 | Verified | Platform/Storefront | Ignored generated `BUILD_ID` can break clean typecheck/deploy. | Storefront typecheck generation and deterministic build-id check. |
| OPS-002 | P1 | Verified | Platform/Deploy | Single-worker deploy scripts skip safety gates. | `node --check scripts/deploy.mjs`; root deploy shortcuts route through `scripts/deploy.mjs --only ...`. |
| OPS-003 | P1 | Verified | Platform/Deploy | Deploy dry-run mode can still apply migrations and deploy. | `pnpm run deploy:api -- --dry-run` skips mutation steps after validation. |
| OPS-009 | P2 | Verified | Platform/Release Readiness | Release readiness sampled API `/readyz` less conservatively than deploy/ops checks, so transient dependency variance could produce noisier go/no-go judgment. | `scripts/release-check.mjs` now uses the same four-sample readiness window as deploy verification, requiring the final sample plus at least two samples ready; tests cover transient recovery and persistent/final degraded states. Verification: focused release-check tests, root gates, deploy API `5f4fe9a5-672b-4a35-a43d-c2c12ff345d9`, admin `16c0aa27-835f-409c-b431-cc70c6bb2800`, storefront `9e2cfb69-b7c8-4cf4-bdcb-e507b86145da`, ops `60612cda-df46-497a-82ad-5853e7c14416`, and live ops check `ops-check-20260706t181040901z-b360a3d8` pass. |
| OPS-010 | P2 | Blocked | Platform/Queue Hygiene | Live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` on 2026-07-07 passed required queue producers/consumers but warned that stale `worker:testdash` producers are still attached to `payment-events`, `order-notifications`, and `auth-otp`. A read-only Wrangler investigation confirmed `worker:testdash` is old external Cloudflare state, still has producer bindings for those queues plus legacy `order-ingest`, and is not owned by current repo source. Follow-up proof on 2026-07-07 showed `testdash` latest deployment `0f3f0b9a-6af9-458a-b1b6-09a7b2408b6a` is from 2026-03-12 and has fetch+queue handlers; fresh 2026-07-08 proof clarified `dash.wrygo.com` routes directly to `testdash`, while `store.wrygo.com` routes to `storefront-test` with a `BACKEND_API` service binding to `testdash`, so deleting it would break active legacy hosts. Latest ops/release proof: `ops-check-20260708t011934777z-a67e57d0` passed required checks with the same warnings and emits required actions for each unexpected producer, while live `pnpm release:check --timeout-ms 30000` now runs queue metadata too, prints `queues 8 checked`, and surfaces the same queue cleanup actions. Storefront-cache and storefront-cache-dlq producer/consumer wiring stayed clean. A parallel read-only repo audit found `testdash` appears only in docs/comments/test fixtures; `scripts/ops-check.mjs` computes unexpected actors from Cloudflare provider state and does not hardcode this worker. | Do not delete `worker:testdash` blindly. Either migrate `store.wrygo.com`/`dash.wrygo.com` to the current `scalius-storefront`/`scalius-api` and dashboard architecture, or recover the `testdash`/`storefront-test` source/config and redeploy legacy Workers without production queue producer bindings, then rerun `pnpm ops:check --queues` and `pnpm release:check`. Do not allowlist `worker:testdash` as an expected production queue actor unless it is intentionally source-owned and redeployed as a production actor. This remains warning-level because required `worker:scalius-api` wiring and `worker:scalius-ops-monitor` monitoring producers are present. |
| MCP-001 | P2 | In Progress | Platform/MCP Agents | Admin and storefront assistants need a durable MCP/agent architecture before code lands, or they risk becoming parallel auth, catalog, checkout, and provider-secret surfaces. | Parallel read-only audits completed on 2026-07-08 for Admin MCP, Storefront MCP/UCP, Flue/Cloudflare agent architecture, and assistant UI direction. Durable guidance is now in `docs/codex/MCP-AGENT-ARCHITECTURE.md`: one tracked `apps/agent` Cloudflare Worker, API as authority, Admin MCP behind Better Auth/onboarding/2FA/RBAC, Storefront MCP catalog/page-context/read-only cart-validation only, existing AI settings for model profiles, and explicit verification gates. First implementation prerequisites are verified: admin-v2 page access now delegates to the core page-permission source with a route-boundary guard against reintroduced local maps, and Storefront UCP route tests now prove catalog-only capabilities plus feed-backed search/lookup. Verification: focused admin/UCP route tests, admin/storefront typechecks and lints, admin build, `pnpm check:dist-secrets`, `git diff --check`, admin deploy `e78bec32-b4db-4292-9899-9c880b91562e`, live `pnpm release:check --timeout-ms 30000`, and live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` (`ops-check-20260707t215115225z-1d6b164f`) passing with only known `worker:testdash` queue warnings. |
| MCP-002 | P2 | Verified | Agent/Storefront MCP | First storefront-only `@scalius/agent` Cloudflare Worker slice exposed a stateless `/mcp` Streamable HTTP endpoint plus `/health`, with only read-only storefront catalog MCP tools backed by existing UCP catalog/profile endpoints. That slice had no Durable Object, D1, R2, KV, queue, admin, checkout, cart mutation, order, payment, fulfillment, customer, support, recovery, or provider-secret bindings/tools. | Verification: blocker scans for database/core imports, commerce mutation terms, credentials, auth headers, and queue/storage bindings were clean; `pnpm --filter @scalius/agent typecheck`, `pnpm --filter @scalius/agent lint`, `pnpm --filter @scalius/agent test` (`7` tests), deploy/turbo config tests (`8` tests), `pnpm --filter @scalius/agent build`, `pnpm check:env`, `pnpm install --frozen-lockfile`, `git diff --check`, and `pnpm run deploy:agent -- --dry-run` passed. Deployed `scalius-agent` version `2e53ea83-72be-4866-abb5-89ce88653595`; live `/health` returned `200` no-store, unknown routes returned no-store JSON `404`, MCP tools/list returned exactly `catalog_search`, `catalog_lookup`, `catalog_product`, and `catalog_profile` with read-only annotations, and live `catalog_profile`, `catalog_search`, `catalog_lookup`, and `catalog_product` calls succeeded against storefront UCP. The original storefront-only deploy dry-run and live deploy showed only `STOREFRONT_URL`, `AGENT_PROFILE_URL`, `AGENT_NAME`, and `AGENT_VERSION` env vars. Release-hardening follow-up: `scripts/release-check.mjs` and `scripts/deploy.mjs` now automate the deployed agent `/health` plus MCP initialize/tools-list catalog-only smoke, skipping it only with `--skip-live`/`--skip-wrangler`; focused release/deploy script tests and live `pnpm release:check --timeout-ms 30000` passed. Post-deploy `pnpm release:check --timeout-ms 30000` passed and `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` passed as `ops-check-20260707t230804721z-87fd5721` with only the known logs-only Email alert and stale external `worker:testdash` warnings. |
| MCP-003 | P2 | Verified | Agent/Admin MCP | First authenticated Admin MCP foundation is live: dashboard `/api/assistant/mcp` proxies to agent `/mcp/admin`, forwards only the active dashboard cookie plus MCP protocol headers, strips bearer auth, and exposes exactly one read-only tool, `admin_session_context`, backed by `/api/v1/admin/rbac/my-permissions` through the API service binding. The agent still has no direct D1, KV, R2, queue, provider-secret, checkout, order, payment, inventory, or customer-data bindings; admin authority remains in the API middleware. | Verification: Cloudflare MCP docs reviewed for stateless Streamable HTTP/`createMcpHandler` fit; focused `pnpm --filter @scalius/agent test` (`15` tests), admin proxy/release/deploy gate tests (`59` tests), agent/admin typechecks, agent/admin lints, agent/admin builds, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, and `pnpm release:check --skip-live --json` passed. Deployed agent `38a75356-f7ba-4896-afc9-67429aa7b86d` and admin `fe3c357d-8cb4-4fd8-be09-ad1e4ecc3b4a`. Deploy smokes proved public catalog MCP still executes `catalog_profile` and dashboard Admin MCP rejects unauthenticated requests with `401 no-store`. Full live `pnpm release:check --timeout-ms 30000` passed. Authenticated live demo-admin probe signed in via `/api/auth/sign-in/email`, initialized `/api/assistant/mcp`, listed only `admin_session_context`, and called it successfully with `no-store` on initialize/list/call responses and a non-super-admin context containing `1` role and `81` permissions. |
| MCP-004 | P2 | Verified | Admin/Storefront Assistant Context | First page-context bridge layer is in place without widening MCP authority. Admin publishes a sanitized browser-only page-state snapshot from the dashboard shell with pathname, title, heading, scroll state, and explicitly registered visible surfaces. Storefront publishes a sanitized public page-context snapshot from the shared layout with route/canonical/title/page kind plus a bounded allowlisted cart summary. Neither bridge adds APIs, database access, provider credentials, hidden form reads, checkout/order/payment mutations, raw storage/cookie reads, receipt proofs, contact details, or discount codes. | Verification: focused admin assistant tests (`3` files / `9` tests), storefront assistant tests (`3` files / `7` tests), admin/storefront typechecks, admin/storefront lints/builds, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, and `pnpm release:check --skip-live --json` passed. Source-boundary guards prove no arbitrary form-control scraping, no whole-admin-content observer, no admin API/domain imports, no raw browser storage/cookie reads in the bridge, safe inline JSON serialization, and no cart/order/payment/customer mutation imports. Deployed admin `72b49911-42e9-4bea-8ea8-65efa47e5343` and storefront `9a87f72f-2681-48f7-8b96-747774535cf9`; live storefront HTML emits the context seed and hydration chunk, live admin bundle contains the page-state bridge without the rejected content observer, and live `pnpm release:check --timeout-ms 30000` passed with only known OPS-005/OPS-010 warnings/actions. |
| MCP-005 | P2 | Verified | Agent/Admin MCP Navigation Context | Admin MCP needed a useful next context tool without becoming a second dashboard router. | `admin_navigation_context` is now the second Admin MCP tool, using the same cookie-only dashboard session preflight and `/api/v1/admin/rbac/my-permissions` API service-binding path as `admin_session_context`. It returns a bounded static entry-page catalog filtered by effective API permissions plus a compact session summary; it does not navigate, mutate, expose dynamic routes, read provider secrets, or import database/core route guards into the agent Worker. Verification: focused `pnpm --filter @scalius/agent test` (`21` tests), `pnpm --filter @scalius/agent typecheck`, `pnpm --filter @scalius/agent lint`, `pnpm --filter @scalius/agent build`, focused release/deploy script tests (`54` tests), `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm release:check --skip-live --json`, `git diff --check`, agent deploy `bcb16c5a-02ec-4f16-95bf-ce14f95cea69`, and live `pnpm release:check --timeout-ms 30000` passed. |
| MCP-006 | P2 | Verified | Agent/Storefront MCP Cart Validation | Storefront MCP needed read-only stale-cart explanation without exposing cart mutation, checkout, order, payment, customer, support, recovery, or provider-secret capabilities. | `cart_validate` is now allowlisted beside the public catalog tools. It accepts only a bounded allowlisted cart snapshot, sends no cookies/auth/storage values, calls the existing storefront `/api/checkout/validate-cart` proxy, returns compact `PRODUCT_UNAVAILABLE` / `VARIANT_REQUIRED` / `VARIANT_UNAVAILABLE` / `VARIANT_MISMATCH` / `QUANTITY_UNAVAILABLE` / `PRICE_CHANGED` repair semantics, forwards abort signals, and fails closed on upstream errors without raw leakage. Release/deploy gates now expect exactly `cart_validate`, `catalog_lookup`, `catalog_product`, `catalog_profile`, and `catalog_search`, reject checkout/order/payment/customer/recovery and cart-mutation terms, call `catalog_profile`, and call `cart_validate` with a missing-product smoke. UCP discovery remains catalog-only. Verification: focused `pnpm --filter @scalius/agent test` (`21` tests), agent typecheck/lint/build, focused release/deploy script tests (`54` tests), `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm release:check --skip-live --json`, `git diff --check`, `pnpm run deploy:agent` to `bcb16c5a-02ec-4f16-95bf-ce14f95cea69`, deploy smoke proving `catalog_profile` and `cart_validate` calls, manual live `cart_validate` smoke returning `PRODUCT_UNAVAILABLE/remove`, and live `pnpm release:check --timeout-ms 30000` passed with only known OPS-005/OPS-010 warnings/actions. |
| MCP-007 | P1 | Verified | Agent/Admin MCP Public Surface | The Admin MCP route existed on the public agent Worker host as well as the dashboard service-binding path. Even though it still required a valid admin cookie, exposing `/mcp/admin` on `workers.dev` was unnecessary attack surface for an admin-only tool group. | `/mcp/admin` now accepts only the internal service-binding URL `http://agent.internal/mcp/admin`; public agent-host requests return bland no-store JSON `404` before cookie/API preflight. The dashboard proxy remains the only live entry point and still strips bearer auth, forwards only cookies/MCP protocol headers, and marks responses no-store. `pnpm release:check` now has an optional authenticated Admin MCP smoke using `SCALIUS_RELEASE_ADMIN_EMAIL` / `SCALIUS_RELEASE_ADMIN_PASSWORD`; when provided it signs in, lists exactly `admin_session_context` and `admin_navigation_context`, and calls `admin_navigation_context` with no-store proof. Verification: focused agent tests (`23`), release/deploy script tests (`58`), agent/core/API/storefront typechecks and builds, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, agent deploy `34eb9a9e-7429-48c5-a3b9-64121f670039`, live public `/mcp/admin` probe returned `404 no-store`, live release check passed with authenticated Admin MCP, and ops check `ops-check-20260708t040959006z-a1e7f653` passed with only OPS-005/OPS-010 warnings. |
| MCP-008 | P2 | Verified | Storefront Assistant Page Context | Storefront assistant page-context installation called `hydrateCartFromStorage()`, which read localStorage, mutated `cartStore`, enabled persistence, and dispatched `cart-updated`. The assistant bridge should observe already-hydrated public page/cart state, not cause cart hydration side effects. | The storefront assistant page-context client now imports only `cartStore`, reads the current store snapshot, and never calls `hydrateCartFromStorage` or raw browser storage APIs. Source-boundary tests ban `hydrateCartFromStorage`, raw storage/cookie reads, cart mutation functions, and private order/customer APIs in the bridge. Verification: focused storefront assistant tests (`3` files / `8` tests), storefront typecheck/lint/build, storefront deploy `b93eef24-b287-4f8f-b468-228b7c004cb1`, cache warm, and live release check passed. Follow-up closed by `MCP-009`. |
| MCP-009 | P2 | Verified | Storefront Assistant Option Labels | Storefront assistant cart summaries still reflected the legacy cart `size`/`color` shape, which can mislead agents and buyers because merchant options are generic axes such as Weight, Style, Pack, Size, or Color. | Storefront cart items now preserve an optional `options: { name, label }[]` pair list while retaining legacy `size`/`color` for checkout/key compatibility. Product detail add-to-cart, product shortcodes, and quick-buy cart payloads populate those pairs from saved product option labels; assistant page context prefers the pair list and falls back to safe `Option 1`/`Option 2` legacy labels only for older carts. Verification: focused storefront tests (`6` files / `27` tests), storefront typecheck/lint/build, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm release:check --skip-live --json`, `git diff --check`, storefront deploy `e1477045-abf6-40a3-8a52-80ea5fc5edf2` with build `src-6ef1a15d47c9cdf4`, live product-page smoke proving assistant seed and option-label data with no old `(size/weight)` / `(color/style)` copy, live `pnpm release:check --timeout-ms 30000`, and live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` (`ops-check-20260708t043638637z-2516fb98`) with only known OPS-005/OPS-010 warnings. |
| MCP-010 | P2 | Verified | Agent/Admin MCP Product Search | Admin MCP needed a first useful domain read tool without becoming a parallel admin API, leaking product rich content/stock/SKU data, or widening mutation authority. | `admin_product_search` is now a bounded read-only Admin MCP tool behind the same dashboard cookie preflight and API service-binding authority as the context tools. It accepts only `{ query, page, limit }`, calls `GET /api/v1/admin/products` with fixed `updatedAt desc` ordering and no bearer auth, and returns compact product identifiers/status/category/count/timestamp data while omitting descriptions, prices, discounts, SKUs, primary images, deleted fields, stock, barcodes, unknown upstream fields, and upstream error bodies. Release-check now expects and executes the tool in the authenticated Admin MCP smoke. Verification: explorer review, focused agent tests (`1` file / `27` tests), focused release-check tests (`1` file / `48` tests), agent typecheck/lint/build, `node --check scripts/release-check.mjs`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm release:check --skip-live --json`, `git diff --check`, agent deploy `8cb5b558-7aac-47c7-8491-22037391d4d0`, live authenticated `pnpm release:check --timeout-ms 30000`, and live ops check `ops-check-20260708t051556118z-dd5375f0` passed with only known OPS-005/OPS-010 warnings. |
| MCP-011 | P2 | Verified | Agent/Admin MCP Order Search | Admin MCP needed a bounded order lookup helper without exposing order detail, buyer PII, payment evidence, provider secrets, or mutation authority. | `admin_order_search` is now a read-only Admin MCP tool behind the same dashboard cookie preflight and API service-binding authority as the existing Admin MCP tools. It accepts only bounded `{ query, page, limit }`, calls `GET /api/v1/admin/orders` through the API service binding with fixed `updatedAt desc` ordering and no bearer auth, returns compact order identifiers/status/timestamp/amount/count summaries, and omits raw customer name/phone/email, secrets, provider payloads, receipt/recovery/session tokens, items/item lines, addresses, tracking data, raw payment evidence, unknown upstream fields, and upstream error bodies. Release-check expects and executes the tool in authenticated Admin MCP smoke. Verification: focused agent tests (`1` file / `31` tests), focused release-check tests (`1` file / `49` tests), agent typecheck/lint/build, `node --check scripts/release-check.mjs`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm release:check --skip-live --json`, `git diff --check`, agent deploy `40d0d03d-8392-46fc-b4fa-680172850c87`, live authenticated `pnpm release:check --timeout-ms 30000`, and live ops check `ops-check-20260708t053007095z-b76c39db` passed with only known OPS-005/OPS-010 warnings. |
| MCP-012 | P2 | Verified | Agent/Storefront MCP Categories | Public Storefront MCP needed a compact category discovery tool without turning the agent Worker into a catalog backend or adding UCP/cart/checkout authority. | `catalog_categories` is now a public read-only Storefront MCP tool beside `cart_validate`, `catalog_lookup`, `catalog_product`, `catalog_profile`, and `catalog_search`. It is API service-binding-only, sends no cookies/auth/private customer/order/payment/checkout/session data, calls `GET /api/v1/categories` or `GET /api/v1/categories/{slug}`, and returns compact category discovery facts with bounded/sanitized descriptions and same-origin category paths. Release-check expects it in public agent MCP `tools/list` and calls it in catalog-tool smoke with bounded `{ limit: 1 }`, requiring no-store, non-error, non-empty structured content. UCP advertised capabilities remain catalog-only search/lookup/product; `catalog_categories` is not a UCP advertised capability. Verification: focused agent tests (`1` file / `35` tests), focused release-check tests (`1` file / `50` tests), agent typecheck/lint/build, `node --check scripts/release-check.mjs`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm release:check --skip-live --json`, `git diff --check`, agent deploy `c9ce8d8d-cf25-4fe6-85d9-85963e64551f`, live authenticated `pnpm release:check --timeout-ms 30000`, and live ops check `ops-check-20260708t054320788z-c901cd42` passed with only known OPS-005/OPS-010 warnings. |
| MCP-013 | P2 | Verified | Agent/Admin MCP Category Search | Admin MCP needed a bounded category lookup helper without becoming a parallel category API or widening mutation authority. | `admin_category_search` is now a read-only, bounded Admin MCP tool behind the same dashboard cookie preflight and API service-binding authority as the other Admin MCP tools. It accepts only `{ query, page, limit }`, calls `GET /api/v1/admin/categories` through the API service binding with fixed `updatedAt desc` ordering and no bearer auth, returns compact category identifiers/product-count/discovery metadata, and omits descriptions, meta fields, image facts, deleted rows/fields, unknown upstream fields, upstream error bodies, and any create/update/delete authority. Release-check expects it in authenticated Admin MCP `tools/list` and calls it with demo-safe `{ query: "test", limit: 1, page: 1 }`, requiring `Cache-Control: no-store`, no MCP tool error, and at least one content block. Verification: focused agent tests (`1` file / `39` tests), focused release-check tests (`1` file / `51` tests), agent typecheck/lint/build, `node --check scripts/release-check.mjs`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm release:check --skip-live --json`, `git diff --check`, agent deploy `7c8f089a-8c1e-4dba-a6a0-93f048cd2787`, live authenticated `pnpm release:check --timeout-ms 30000`, and live ops check `ops-check-20260708t055740392z-b572bc27` passed with only known OPS-005/OPS-010 warnings. |
| MCP-014 | P2 | Verified | Agent/Admin MCP Collection Search | Admin MCP needs a bounded collection lookup helper without becoming a parallel collection API, exposing product records/lists/media/rich metadata, or widening mutation authority. | `admin_collection_search` is now a read-only, bounded Admin MCP tool behind the same dashboard cookie preflight and API service-binding authority as the other Admin MCP tools. It accepts only `{ query, page, limit }`, calls `GET /api/v1/admin/collections` through the API service binding with fixed `updatedAt desc` ordering and no bearer auth, returns compact collection id/name/product-count/discovery metadata, and includes canonical paths only when they exactly match the current ID-routed `/collections/<collectionId>` path. It omits slugs, descriptions, meta fields, image facts, product records/lists, deleted rows/fields, unknown upstream fields, upstream error bodies, and any create/update/delete authority. Release-check expects it in authenticated Admin MCP `tools/list` and calls it with demo-safe `{ query: "test", limit: 1, page: 1 }`, requiring `Cache-Control: no-store`, no MCP tool error, and at least one content block without requiring non-empty demo collections. Verification: focused agent tests (`1` file / `43` tests), focused release-check tests (`1` file / `52` tests), agent typecheck/lint/build, `node --check scripts/release-check.mjs`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm release:check --skip-live --json`, `git diff --check`, agent deploy `bcde5820-5453-4b18-a96a-c272b2378186`, live authenticated `pnpm release:check --timeout-ms 30000`, and live ops check `ops-check-20260708t061520383z-35aa791e` passed with only known OPS-005/OPS-010 warnings. |
| MCP-015 | P2 | Verified | Agent/Admin MCP Page Search | Admin MCP needs a bounded CMS page lookup helper without becoming a parallel page API, exposing page content/meta/media, or widening mutation authority. | `admin_page_search` is now a read-only, bounded Admin MCP tool behind the same dashboard cookie preflight and API service-binding authority as the other Admin MCP tools. It accepts only `{ query, page, limit }`, calls `GET /api/v1/admin/pages` through the API service binding with fixed `updatedAt desc` ordering and no bearer auth, returns compact CMS page identifiers plus publish/discovery/layout flags, includes canonical paths only when they are valid non-reserved one-segment CMS page paths, and omits HTML content, meta copy, images, deleted rows/fields, unknown upstream fields, upstream error bodies, and mutation authority. Release-check expects it in authenticated Admin MCP `tools/list` and calls it with demo-safe `{ query: "test", limit: 1, page: 1 }`. Verification: focused agent tests (`1` file / `47` tests), focused release-check tests (`1` file / `53` tests), agent typecheck/lint/build, `node --check scripts/release-check.mjs`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm release:check --skip-live --json`, `git diff --check`, deploy dry-run, agent deploy `811aa425-7265-4fd5-9b17-842cdfbe98a8`, live authenticated `pnpm release:check --timeout-ms 30000` proving `admin_navigation_context`, `admin_category_search`, `admin_collection_search`, `admin_page_search`, `admin_product_search`, and `admin_order_search`, and live ops check `ops-check-20260708t063120208z-f051790e` passed with only known OPS-005/OPS-010 warnings. |
| MCP-016 | P2 | Verified | Agent/Admin MCP Media Search | Admin MCP needs a bounded media lookup helper without exposing upload/delete/move/folder mutation authority, storage internals, unsafe URLs, deleted/unknown fields, or upstream error bodies. | `admin_media_search` is now a read-only, bounded Admin MCP tool behind the same dashboard cookie preflight and API service-binding authority as the other Admin MCP tools. It accepts `{ query, page, limit, folderId, mimeType }`, calls `GET /api/v1/admin/media` with fixed `createdAt desc` ordering and no bearer auth, returns compact media file metadata with safe URLs only, and omits upload/delete/move/folder mutation authority, storage internals, deleted/unknown fields, unsafe URLs, and upstream error bodies. Release-check expects it in authenticated Admin MCP `tools/list` and calls it with demo-safe `{ query: "test", limit: 1, page: 1 }`. Verification: focused agent tests (`1` file / `52` tests), focused release-check tests (`1` file / `54` tests), agent typecheck/lint/build, `node --check scripts/release-check.mjs`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm release:check --skip-live --json`, `git diff --check`, deploy dry-run, agent deploy `dd1b5455-08d6-4dda-a40c-dc0bd83795e6`, live authenticated `pnpm release:check --timeout-ms 30000` proving `admin_navigation_context`, `admin_category_search`, `admin_collection_search`, `admin_page_search`, `admin_media_search`, `admin_product_search`, and `admin_order_search`, and live ops check `ops-check-20260708t071043411z-c3bb1975` passed with only known OPS-005/OPS-010 warnings. |
| MCP-017 | P2 | Verified | Agent/Admin MCP Inventory Lookup | Admin MCP needs a bounded inventory lookup helper without exposing inventory adjustment authority, movement history, scanner lookup, prices, barcodes, order IDs, notes, version fields, or upstream error bodies. | `admin_inventory_lookup` is now a read-only, bounded Admin MCP tool behind the same dashboard cookie preflight and API service-binding authority as the other Admin MCP tools. It calls `GET /api/v1/admin/inventory` with fixed `section=variants`, bounded `{ query, page, limit, status, sort, order }`, and no bearer auth, returns compact tracked-variant stock fields plus aggregate stats only, and omits movements, alerts, barcodes, prices, version fields, adjustment authority, scanner lookup, order IDs, notes, unknown upstream fields, and upstream error bodies. Release-check expects it in authenticated Admin MCP `tools/list` and calls it with demo-safe `{ query: "test", limit: 1, page: 1 }`. Verification: focused agent tests (`1` file / `58` tests), focused release-check tests (`1` file / `55` tests), agent typecheck/lint/build, `node --check scripts/release-check.mjs`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm release:check --skip-live --json`, `git diff --check`, deploy dry-run, agent deploy `aa0e8ea0-b95d-40bf-bde7-14606b4bf536`, live authenticated `pnpm release:check --timeout-ms 30000` proving `admin_navigation_context`, `admin_category_search`, `admin_collection_search`, `admin_page_search`, `admin_media_search`, `admin_product_search`, `admin_order_search`, and `admin_inventory_lookup`, and live ops check `ops-check-20260708t072511786z-dd175635` passed with only known OPS-005/OPS-010 warnings. |
| MCP-018 | P2 | Verified | Agent/Admin MCP Dashboard Summary | Admin MCP needed a bounded dashboard summary helper without exposing recent orders, customer PII/contact data, order IDs, lifetime revenue, daily activity, payment evidence, provider payloads, mutation authority, or upstream error bodies. | `admin_dashboard_summary` is now a read-only, strict empty-input Admin MCP tool behind the same dashboard cookie preflight and API service-binding authority as the other Admin MCP tools. A new API-authoritative `GET /api/v1/admin/dashboard/metrics-summary` route returns only `data.stats` from `getDashboardSummaryStats(db)` and never calls recent orders, full dashboard stats, or activity queries. The MCP tool calls that route with dashboard cookie/User-Agent only, no bearer auth and no body, returns compact aggregate stats plus explicit privacy/mutation limits, and fails closed on partial/malformed upstream data. Release-check expects it in authenticated Admin MCP `tools/list` and calls it with `{}` while requiring no-store, nested numeric stats, safe source path, and no sensitive dashboard/order/payment/provider leakage. Verification: focused API dashboard tests (`92` files / `760` tests), focused agent tests (`1` file / `62` tests), focused release-check tests (`1` file / `56` tests), API/agent/API-client typechecks, API/agent lints/builds, `pnpm generate:sdk`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm release:check --skip-live --json`, `git diff --check`, deploy dry-runs, API deploy `38c96afb-976a-4ad5-8cd7-b5c6e7b7c3ce`, agent deploy `45261550-7d7e-43b1-a7c6-6d503a37f7e8`, live authenticated `pnpm release:check --timeout-ms 30000` proving `admin_dashboard_summary`, and live ops check `ops-check-20260708t074650650z-88fc72f2` passed with only known OPS-005/OPS-010 warnings. |
| MCP-019 | P2 | Verified | Agent/Admin MCP Settings Summary | Admin MCP needed settings awareness without exporting sensitive settings forms, masked secrets, provider account identifiers, snippets, logs, contacts, or mutation authority. | `admin_settings_summary` is now a read-only, strict empty-input Admin MCP tool behind the same dashboard cookie preflight and API service-binding authority as the other Admin MCP tools. A new API-authoritative `GET /api/v1/admin/settings/mcp-summary` route is guarded by `settings.general.view` and returns only booleans, counts, bounded checkout issues, sanitized absolute storefront URL, country/currency, checkout policy, payment method IDs/status, SEO discovery toggles, return-policy facts, provider readiness counts/status, and explicit redaction/mutation limits. It hard-codes `phoneCollectionRequired: true`, fails closed on malformed upstream data, and omits credential values, masked secrets, provider identifiers, business contact fields, analytics snippets/custom code, raw logs, provider payloads, customer PII, receipt proofs, OTP/session/cookie material, unknown upstream fields, and upstream error bodies. Release-check expects the tool in authenticated Admin MCP `tools/list`, calls it with `{}`, requires no-store, safe source path/permission, explicit false limits, and sensitive key/value leak checks. Verification: focused API settings summary tests (`93` files / `763` tests), focused agent tests (`1` file / `67` tests), focused release-check tests (`1` file / `58` tests), API/agent typechecks, API/agent lints, `pnpm generate:sdk`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm release:check --skip-live --json`, `git diff --check`, deploy dry-runs, API deploy `da00a237-b112-4cef-8935-2db4d21cf501`, agent deploy `08d14ddc-9e98-49dc-986d-865da824e909`, live authenticated `pnpm release:check --timeout-ms 30000` proving `admin_settings_summary` plus full public Storefront MCP tool execution, and live ops check `ops-check-20260708t083221474z-92d0941a` passed with only known OPS-005/OPS-010 warnings. |
| MCP-020 | P2 | Verified | Agent/Storefront MCP Release Coverage | Public Storefront MCP release proof could pass if `tools/list`, `catalog_profile`, `catalog_categories`, and `cart_validate` worked while `catalog_search`, `catalog_lookup`, or `catalog_product` wrappers silently regressed. | `pnpm release:check` now requires `Cache-Control: no-store` on Agent MCP initialize, initialized notification, tools/list, and every public tool call. The public agent smoke executes `catalog_profile`, `catalog_categories`, `catalog_search`, and `cart_validate`, then derives a product candidate from `catalog_search` and executes `catalog_lookup` plus `catalog_product` when the catalog is non-empty; empty catalogs skip those two with an explicit reason instead of fabricating a product. Category responses must include structured `catalogCategories.categories`, search must return structured product content when present, lookup must return at least one product for the derived candidate, and product must return structured product content. Verification: `node --check scripts/release-check.mjs`, `node --check scripts/release-check.test.mjs`, focused release-check tests (`1` file / `58` tests), `pnpm release:check --skip-live --json`, live authenticated `pnpm release:check --timeout-ms 30000` proving deployed Agent MCP `/health`, exact tools/list, and successful `catalog_profile`, `catalog_categories`, `catalog_search`, `catalog_lookup`, `catalog_product`, and `cart_validate` calls against agent version `08d14ddc-9e98-49dc-986d-865da824e909`, and live ops check `ops-check-20260708t083221474z-92d0941a` passed with only known OPS-005/OPS-010 warnings. |
| MCP-021 | P2 | Verified | Agent/Admin MCP Analytics Summary | Admin MCP needed analytics readiness awareness without exposing saved browser snippets, custom code, credential material, provider account identifiers, raw issue strings, provider messages, provider payloads, or mutation authority. | `admin_analytics_summary` is now a read-only, strict empty-input Admin MCP tool behind the same dashboard cookie preflight and API service-binding authority as the other Admin MCP tools. It calls only API-authoritative `GET /api/v1/admin/analytics/health`, guarded by `analytics.view`, and returns aggregate readiness counts plus compact browser/server status/counts for each analytics type. It omits script config, snippets, custom code, raw issues, provider messages, account identifiers, credentials, provider payloads, unknown upstream fields, upstream error bodies, and mutation authority. Release-check expects the tool in authenticated Admin MCP `tools/list`, calls it with `{}`, requires no-store, safe source path/permission/version, explicit false limits, bounded provider rows, and leak checks for config/snippet/token/pixel/message/issue keys and values. Verification: focused agent tests (`1` file / `71` tests), focused release-check tests (`1` file / `59` tests), agent typecheck/lint/build, `node --check scripts/release-check.mjs scripts/release-check.test.mjs`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm release:check --skip-live --json`, `git diff --check`, deploy dry-run, agent deploy `d09ac855-bec2-4df0-9831-02b56c5b67d9`, live authenticated `pnpm release:check --timeout-ms 30000` proving `admin_analytics_summary` plus all other Admin and Storefront MCP tool calls, and live ops check `ops-check-20260708t085257543z-4e5f8ada` passed with only known OPS-005/OPS-010 warnings/actions. |
| MCP-022 | P2 | Verified | Agent/Admin MCP Customer Search | Admin MCP needed customer lookup, but customer search terms can be phone numbers or email addresses, so wrapping the existing GET customer list would put buyer PII in URLs and could leak raw contact/address/history fields. | `admin_customer_search` is now a read-only Admin MCP tool behind the dashboard proxy/internal service-binding route. It calls API-owned `POST /api/v1/admin/customers/mcp-search`, guarded by `customers.view`, with strict body-only input so phone/email searches never enter URLs. The API route calls `listCustomers()` with `showTrashed:false` and fixed `updatedAt desc`, then returns only customer IDs, order/spend aggregates, timestamps, redacted request metadata, pagination, and explicit privacy limits. The MCP wrapper posts `{ query, page, limit }` only, forwards dashboard cookie/User-Agent without bearer auth, discards unexpected upstream fields, and omits raw query, names, email, phone, addresses, locations, history, order lists, trashed rows, unknown upstream fields, upstream error bodies, and mutation authority. Release-check expects and executes the tool in authenticated Admin MCP smoke and fails on URL/query/contact leaks. Verification: focused API customer MCP search tests (`1` file / `4` tests), core RBAC route-permission tests (`1` file / `7` tests), focused agent tests (`1` file / `75` tests), focused release-check tests (`1` file / `60` tests), syntax checks, API/core/agent/API-client typechecks, API/core/agent lints, API/agent builds, `pnpm generate:sdk`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm release:check --skip-live --json`, `git diff --check`, deploy dry-runs, API deploy `ae2cd944-ac16-4af8-a257-b95ddecd68c1`, Agent deploy `5aac506d-32be-446b-ae46-ab42b8a073ff`, authenticated live `pnpm release:check --timeout-ms 30000` proving `admin_customer_search`, and live ops check `ops-check-20260708t093248945z-efa8fb2a` passed with only known OPS-005/OPS-010 warnings/actions. |
| MCP-023 | P2 | Verified | Agent/Storefront MCP Discovery Policy | Public Storefront MCP should answer sitemap/feed/robots/schema/return-policy questions without adding checkout, payment, order, customer, cart-mutation, or provider authority. | `storefront_discovery_policy` is now a strict empty-input public read-only Storefront MCP tool. It calls only API-authoritative `GET /api/v1/seo` through the API service binding, sends no cookies/auth, builds only absolute storefront discovery URLs from validated `STOREFRONT_URL`, returns compact sitemap/feed/robots/structured-data toggles plus merchant-saved return-policy facts, and includes explicit read-only/no-private-data limits. It omits raw robots text, checkout/payment/customer/order/session data, analytics snippets, provider payloads, upstream error bodies, and hidden mutation authority. Release-check expects and executes the tool in public Agent MCP smoke. Verification: focused agent worker tests (`1` file / `78` tests), focused release-check tests (`1` file / `60` tests), `node --check scripts/release-check.mjs scripts/release-check.test.mjs`, agent typecheck/lint/build, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm release:check --skip-live --json`, `git diff --check`, deploy dry-run, Agent deploy `edeb084d-a4f2-4a3c-9b0e-e8109af5de83`, live authenticated `pnpm release:check --timeout-ms 30000` proving `storefront_discovery_policy`, and live ops check `ops-check-20260708t100418940z-2c12576d` passed with only known OPS-005/OPS-010 warnings/actions. |
| AI-001 | P2 | Verified | Core/API AI Credentials | Widget AI provider key reads silently dropped encrypted rows when `CREDENTIAL_ENCRYPTION_KEY` was missing or wrong, making dashboard/API behavior look like a missing key instead of an unreadable saved credential. Future Admin/Storefront assistants need the shared AI settings layer to fail closed with clear readiness. | AI provider key reads now use `readStoredCredentialStrict()`. Runtime settings expose safe per-provider `credentialErrors`, admin settings include masked per-provider `credentialError`, unreadable encrypted keys do not count as configured, legacy plaintext keys remain readable, and Cloudflare Workers AI binding still counts as the Cloudflare-native credential path. Verification: focused core AI settings test (`12`), core/API typechecks and API build, API deploy `b6a6db2e-1cb0-4c54-9eea-3d28f1549d51` with four ready `/readyz` samples, live release check, and ops check `ops-check-20260708t040959006z-a1e7f653`. Model-profile follow-up closed by `AI-002`. |
| AI-002 | P2 | Verified | AI Model Profiles | Admin, storefront, widget, image, and voice assistant work needed a first-class model profile contract before any durable assistant calls models, otherwise future MCP/agent code could invent ad hoc provider/model selection and secret readiness paths. | AI settings now normalize canonical profiles `adminChat`, `storefrontChat`, `widgetGeneration`, `imageGeneration`, and `voice`; future profiles default disabled with empty models, `widgetGeneration` preserves the current provider default, profile resolution requires an enabled profile, enabled provider, strict credential readiness, and provider allowlist membership, and the admin API/UI can read/save profiles without accepting secrets in the profile payload. Verification: core AI settings tests (`95` files / `780` tests), API AI settings tests (`92` files / `759` tests), admin Widget AI boundary test (`1` file / `3` tests), core/API/admin/API-client typechecks, API/admin lints, API/admin builds, `pnpm generate:sdk`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm check:admin-perf`, `pnpm release:check --skip-live --json`, `git diff --check`, API deploy `e6e387ef-0daa-4434-a4ef-df6809893d53`, admin deploy `a816af7e-a7b0-41fb-9b39-0af5efa8de13`, live authenticated `pnpm release:check --timeout-ms 30000`, and live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` (`ops-check-20260708t050035076z-3432426b`) passed with only known OPS-005/OPS-010 warnings. |
| OPS-011 | P2 | Verified | API/Readiness Storage Variance | Live release/ops smokes on 2026-07-07 showed intermittent `/readyz` degradation because remote D1/KV/R2 probes did not share one realistic dependency budget: R2 and shared auth KV samples could succeed after the old 1500ms default, while R2 was observed succeeding as late as `4185ms`. | D1, KV, and R2 readiness probes now share one 5s bounded remote-storage budget, while D1 keeps transient-overload retries. Focused readiness tests prove slow-but-successful D1, KV, and R2 samples within budget remain ready. Verification: `pnpm --dir apps/api exec vitest run src/routes/readiness.test.ts` (`1` file / `6` tests), `pnpm --filter @scalius/api typecheck`, `pnpm --filter @scalius/api lint`, `pnpm --filter @scalius/api build`, API deploy `0e761897-1455-4d10-ab8c-ef301cd76204` with deploy readiness `4/4` ready including R2 `4185ms`, live `pnpm release:check --timeout-ms 30000`, and live `pnpm ops:check --queues --samples 4 --timeout-ms 30000 --json` (`ops-check-20260707t172849560z-04869389`) passing with recovered transient R2 and known `worker:testdash` queue warnings. |
| OPS-012 | P2 | Verified | Platform/Release Readiness | `pnpm release:check` could pass while dropping warning/action details returned by `pnpm ops:check`, and later still did not request queue metadata, hiding the still-open logs-only ops Email alert configuration and stale `worker:testdash` producer cleanup actions from the compact release gate. | `scripts/release-check.mjs` now propagates ops-check warnings, required actions, alert-channel metadata, and queue metadata into release-check JSON/text output whenever Wrangler proof is enabled, with regression coverage for warning/action rendering and unexpected queue producers. Verification: focused release/ops tests (`57`), syntax checks, `git diff --check`, admin deploy `765d1478-88b2-428b-a727-a6455a8a6458`, live `pnpm release:check --timeout-ms 30000` showing `queues 8 checked` plus OPS-005 and OPS-010 actions, and live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` (`ops-check-20260708t011934777z-a67e57d0`) pass with the known logs-only Email and `worker:testdash` warnings visible. |
| AUTH-053 | P2 | Verified | Admin/Auth Settings UX | Auth & Access could swallow SMS readiness read failures, leaving merchants with ambiguous SMS OTP status when the provider check itself failed. | `AuthSettingsBuilder` now keeps a first-class SMS readiness error, surfaces actionable copy, and blocks SMS OTP policy saves while readiness cannot be checked instead of pretending the channel is usable. Verification: focused sensitive-settings boundary test, admin typecheck/lint/build, admin deploy `765d1478-88b2-428b-a727-a6455a8a6458`, live release check, and live ops check `ops-check-20260707t223230238z-2a2ffd63` pass. |
| ADMIN-030 | P2 | Verified | Admin/Product Editing UX | Product edit regressed toward eagerly mounting the primary Tiptap description editor and variant-management UI still leaked generic Size/Color assumptions despite merchant-defined option axes. | Product edit uses `DeferredTiptapEditor` again and source-boundary tests now reject direct primary-editor imports on the route. Product/variant option labels are threaded through image alt labels, variant headers, display badges, spreadsheet rows, sort/reorder UI, and bulk generation previews so defaults remain understandable while merchant-defined axes such as weight/style/color are honored. Verification: focused product edit/variant boundary tests, admin typecheck/lint/build, admin deploy `765d1478-88b2-428b-a727-a6455a8a6458`, live release check, and live ops check `ops-check-20260707t223230238z-2a2ffd63` pass. |
| TEST-001 | P1 | Verified | Admin/Widgets | Root test suite fails widget script extraction test. | `pnpm test` passes. |
| DEPLOY-001 | P1 | Verified | Admin/Deploy | Stale deployed route chunks can strand `/admin` in the error boundary after deploy. | Admin browser checks plus Worker tail after full redeploy. |
| BUILD-003 | P1 | Verified | Platform/Deploy | Local env files can remain in app `dist/` outputs after framework builds/cache restores. | `pnpm check:dist-secrets` and focused script test pass. |
| AUTH-001 | P1 | Verified | Admin/Auth/Local Dev | Better Auth session-field mapping and partial setup state could block admin login/setup. | Auth config test, local setup recovery, and browser dashboard login. |
| PAY-004 | P1 | Verified | Payments/Checkout | Public payment sessions still trust caller-selected deposit/manual-capture fields. | Session routes derive payment mode/amount/capture server-side. |
| PAY-005 | P1 | Verified | Payments/Webhooks | SSLCommerz webhook uses form `tran_id`/metadata after validation. | IPN applies canonical validated transaction/order data only. |
| ORDER-006 | P1 | Verified | Storefront/Checkout | Order creation can accept missing/bogus shipping method with caller-supplied zero shipping. | Backend rejects invalid shipping and derives shipping charge. |
| ORDER-007 | P1 | Verified | Orders/Inventory | Status changes are not resumable when inventory fails after order CAS. | Retry reconciles inventory even when status already changed. |
| WEBHOOK-001 | P1 | Verified | Webhooks/Durability | Durable webhook `processing` claims can black-hole events after isolate failure. | Stale processing claims are leased/reclaimable. |
| ORDER-008 | P1 | Verified | Orders/Fulfillment | Bulk shipment claim is only a version bump before provider side effects. | Exclusive claim/state prevents concurrent updates during provider shipment. |
| ORDER-011 | P1 | Verified | Admin/Orders/Inventory | Restoring soft-deleted orders can revive invalid status/inventory combinations. | Restore matrix and central reservable-status guard prevent impossible pairs. |
| DEL-002 | P1 | Verified | Delivery/Orders | Shipment deletion can remove reconciliation evidence while an order claim remains active. | Claimed/reconcile-required shipments cannot be deleted; stale failed/cancelled claims are cleared before deletion. |
| PAY-006 | P1 | Verified | Payments/Orders | Late gateway success can mark cancelled/deleted/returned/refunded orders paid. | Shared payable-state guard tests for session routes and `processPaymentConfirmed()` pass; API deploy and live smoke verified. |
| PAY-007 | P1 | Verified | Payments/SSLCommerz | SSLCommerz deposit/balance attempts reuse `tran_id` and collide with local idempotency. | Scoped SSLCommerz transaction ids, val_id idempotency migration, deploy, and live smoke verified. |
| ORDER-012 | P1 | Verified | Admin/Orders/Inventory | Abandoned cleanup can race with late payment and overwrite a now-paid order. | Cleanup claim/payment-record guards, admin SSR fix, deploy, and live smoke verified. |
| ORDER-013 | P1 | Verified | Orders/Fulfillment | Manual fulfillment can persist order status before shipment/items commit. | Private fulfillment claim, atomic shipment/item/final-order batch, deploy, and live smoke verified. |
| ORDER-014 | P1 | Verified | Orders/COD | Generic delivered/completed status update can mark COD paid without ledger/tracking updates. | Generic COD status path requires collection ledger/tracking evidence; deploy and live smoke verified. |
| ORDER-015 | P1 | Verified | API/Core Inventory | Inventory expiry cron could process an unbounded orphan-reservation backlog and used random best-effort release movements. | Bounded sweep limit, deterministic expiry release claim, D1 batch claim+counter update, root gates, API deploy, and live scheduled smoke verified. |
| ORDER-016 | P1 | Verified | Orders/Queue/Inventory | Queued checkout ingest can leak or double-reserve stock across Worker crash/redelivery because reservation counters and movement/order writes are not a single deterministic idempotency claim. | Strict D1 movement-claim + counter batches, checkout reservation keys, active reservation reuse, root gates, API deploy, local/live smoke verified. |
| ORDER-017 | P1 | Verified | Orders/Inventory | Concurrent inventory state transitions can double deduct/release/restore when two reconciliations race the same order/items. | Movement-generation transition claims plus stock/action CAS; focused inventory tests and root gates verified. |
| BUILD-004 | P1 | Verified | Storefront/Build | `src/pages/seo-regressions.test.ts` is bundled as a public Astro route/chunk. | Test moved out of `src/pages`; storefront build/dist scan has no test route; live old route returns 404. |
| CACHE-005 | P1 | Verified | API/Storefront Cache | Auth/checkout and CSP/security settings missed public cache purges; `CACHE_TTLS.NONE` still wrote cache; warm logs counted HTTP failures as successes. | Focused cache/auth route tests, root tests/lint/typecheck/builds, API/storefront deploy, and live API/browser smoke. |
| CACHE-006 | P1 | Verified | API/Storefront Cache | Category and CMS page writes missed layout invalidation even though storefront fallback navigation can derive from public categories/pages. | Focused cache-invalidation tests, root gates, full deploy, and live dashboard/storefront smoke. |
| CACHE-007 | P1 | Verified | Storefront/Cache | Authenticated `GET /api/purge-cache` could mutate cache state by bumping KV, clearing L1, and warming pages. | Storefront route tests prove GET is non-mutating/405 and POST full/selective purge paths still work; root gates, deploy, and live GET smoke. |
| CACHE-012 | P1 | Verified | API/Storefront Cache | Catalog writes could commit and then return 500 if the downstream storefront purge failed. | `invalidateCatalogCaches()` now awaits API KV invalidation but schedules storefront purge with `waitUntil`; focused cache-invalidation tests, root gates, deploy, and local/live smoke pass. |
| CACHE-013 | P2 | Verified | API/Storefront Cache | Hero slider writes could commit and then return 500 if downstream storefront purge failed or unit tests lacked a Hono `ExecutionContext`. | Hero slider writes now await API KV invalidation and schedule storefront purge defensively; focused API cache tests, root gates, deploy, local/live hero-slider media smoke pass. |
| CACHE-014 | P2 | Verified | API/Storefront Cache | CSP/security settings save could false-500 in unit/local contexts without Hono `ExecutionContext` after the DB write. | CSP cache write now uses optional `ExecutionContext`; focused system cache-invalidation regression test, root gates, deploy, and live smoke pass. |
| ADMIN-009 | P1 | Verified | Admin/RBAC | Admin deep links allowed any permission-bearing admin into mapped pages; `/admin` could land users without `dashboard.view` on an unauthorized dashboard. | Focused `admin-access` tests, root warning-free lint/typecheck/test/build/safety gates, deploy, and live dashboard route smoke. |
| RBAC-001 | P1 | Verified | Core/API RBAC | Permission revocations could remain stale in another isolate because local RBAC memory cache was trusted before KV and mutation routes did not consistently clear KV-backed user caches. | Focused RBAC helper/API middleware tests, root gates, API deploy `822d3968-ffc0-4280-9286-f161a4096525`, live auth/API/dashboard smoke. |
| RBAC-002 | P1 | Verified | API/Admin RBAC | Permission-based admins could pass central middleware but still be blocked by handler-local legacy `user.role === "admin"` checks, and deeper Pathao/widget status routes were unmapped. | Legacy handler checks removed; team-user GET maps to team view/manage permissions; deeper import/status routes mapped; focused API middleware/auth-management tests, root gates, deploy, and live account smoke pass. |
| RBAC-003 | P2 | Verified | API/Admin RBAC | Checkout Flow readiness panel showed an amber delivery warning because `/api/v1/admin/settings/checkout-readiness` was missing from the central RBAC route map and failed closed with 403 even though public checkout readiness was healthy. | Route mapped to `settings.general.view`; focused admin-auth middleware regression passes; API deploy `cfd3f7db-c8a8-47dc-9e0d-b461b60ff22d`; admin UI now refetches readiness on mount/focus and shows the underlying API error instead of implying shipping setup is broken; admin deploy `c8f7d6bd-fdc9-41fe-94fe-8526764103a6`; live public checkout readiness returns ready. |
| RBAC-004 | P1 | Verified | API/Admin RBAC | Role-assignment paths can target non-admin Better Auth users while team listing/deletion paths still filtered `user.role = "admin"`, creating inconsistent permission-bearing principals. | Team listing/deletion now use one permission-principal predicate: legacy admin role, super admin, assigned roles, or granted direct permission overrides. Listing uses distinct RBAC joins so role-bearing and direct-permission admins appear in Team; deletion verifies the same principal set, protects super admins, and counts RBAC principals for last-admin safety. Focused API auth-management tests (`30` tests), API typecheck/lint, independent read-only subagent verification, root typecheck/lint/test (`289` files / `1856` tests), root build, env/dist/diff guards, API deploy `3293e4f1-2cd7-4baf-813d-7b50e9c4386d`, live health/readyz, and unauthenticated protected-route `401` smoke pass. |
| RBAC-005 | P1 | Verified | Admin/Auth Setup | Setup/login/admin-setup-claim existence checks used legacy `user.role`/super-admin probes, so RBAC-only admin principals could look unbootstrapped while role-only users could block recovery despite no runtime RBAC access. | Shared `adminPrincipalExists` now checks super-admin, direct granted permissions, and non-denied role-derived permissions. Focused core/API/admin tests, route-boundary tests, root gates, deploys, and live ops check listed in `AUTH-050` pass. |
| SETTINGS-001 | P1 | Verified | Admin/Sensitive Settings | Sensitive settings forms could render fallback/default values and allow saves after the original credential/settings read failed, creating a path to overwrite working provider setup during transient API or decrypt errors. | The shared settings form hook and Email, Firebase, Auth, Notification, and Payment Gateway settings surfaces now block save/edit paths on load failure, show retryable destructive-read errors, and keep per-provider credential saves decoupled from checkout visibility. Focused admin settings tests, root gates, admin deploy `76e716b9-3826-48db-a035-5b132b511bf1`, live unauth dashboard redirect, and live ops check listed in `AUTH-050` pass. |
| STORE-007 | P1 | Verified | Storefront/CSP Cache | Storefront CSP middleware read the storefront CSP endpoint as a top-level object even though the API returns the standard `{ success, data }` envelope. | Focused CSP middleware test, root gates, API/storefront deploy, live CSP endpoint and storefront browser smoke. |
| STORE-010 | P1 | Verified | Storefront/Security | Independent audit verified raw `JSON.stringify()` reaching executable inline scripts, JSON-LD/data-island `set:html`, quick-buy generated HTML, and admin theme colors reaching a style block without end-to-end validation. | Inline JSON sinks now use `serializeJsonForInlineScript()`; theme colors validate/sanitize through `@scalius/shared/storefront-theme`; focused/root gates, API deploy, storefront deploy, and live browser smoke verified. |
| STORE-011 | P2 | Verified | Storefront/Accessibility | Local cart browser smoke found Layout-owned storefront pages rendering nested `<main>` landmarks (layout plus page-level main), while `/account` rendered one. | Layout now owns the single storefront `<main>`; cart, checkout, search, product, category, and collection pages use neutral containers. Source regression, focused/root gates, local screenshots, storefront deploy `efdd9a69-d8fe-4fbb-be31-e5d47619b4cf`, and live `/cart`/`/checkout`/`/search`/`/account`/product/category/collection landmark smokes pass. |
| CUST-001 | P1 | Verified | Customer/Post-sale UX | Customer account order history hid API/session failures as “No orders yet” and did not expose shipment/tracking state despite backend shipment data existing. | Latest shipment summaries are returned/documented; `/account` renders tracking/error states; focused/root/build/deploy/live read-only smoke passed. |
| ORDER-018 | P1 | Verified | Payments/Refunds | Verified refund lifecycle gap: provider-successful refunds can leave order status/inventory unreconciled if final order CAS loses to concurrent status/shipment/COD mutation. | D1 `refund_attempts` now provides active refund-operation state, scheduled reconciliation can finish provider-accepted local state after CAS/process failure, and admin/customer order detail plus order lists surface sanitized recovery locks. Active refund attempts and active hosted-payment setup leases now block admin order edit/restore/delete/permanent/bulk delete, status/fulfillment changes, shipment create/refresh/delete, shipment-driven status sync, COD collection/failure/return, refunds, and returns before provider/payment/inventory side effects. Verification includes focused/root gates, API deploys `4ef7298c-0fbb-4d47-8f19-1932eadf8999` and `4e956a17-df5e-44de-a533-267c83c2a031`, admin deploy `0039f18b-accb-451e-bee0-3d2d2573b62a`, local/live order-list browser smokes, live health/readyz, live admin 401, and live OpenAPI route smoke. |
| ORDER-019 | P1 | Verified | Orders/COD | Verified COD lifecycle gap: collection/return status and inventory transitions can commit before COD ledger evidence is durably recorded. | COD collection now requires both succeeded payment and collected tracking evidence for idempotent retry, fails closed when tracking is missing, records COD evidence before inventory reconciliation, and rolls back the delivered status claim when evidence or inventory repair fails before `inventoryAction` changes. COD return now fails closed on missing tracking, records the returned marker before inventory restoration, rolls back the returned status claim on marker/restoration failure, and retries restoration when already returned. Focused/root gates, API deploys `1306c5d2-24d9-4dca-a43a-d41db7717d8d` and `08274388-bb9e-4d9d-aea7-d76267b54aa2`, and live health/readyz/admin-auth/OpenAPI smokes pass. |
| ORDER-022 | P1 | Verified | Orders/COD | Confirmed COD collection can claim `CONFIRMED -> SHIPPED -> DELIVERED`, then roll back only to `SHIPPED` if COD ledger evidence or delivered inventory reconciliation fails, leaving a false buyer/admin-visible shipped state. | `processCodAction("collected")` now tracks the full owned status claim and rolls confirmed-origin failures back to the original `CONFIRMED` status while preserving shipped-origin retry semantics. Focused COD/full fulfillment tests, root typecheck/lint/test/build, env/migration/dist/diff guards, API deploy `5ac46eb1-adef-4de0-a1c5-893a8bb8b8bb`, and live health/readyz/OpenAPI ops smokes pass. |
| PAY-023 | P1 | Verified | Payments/SSLCommerz | SSLCommerz readiness could treat obvious placeholder credentials as checkout-configured, and payment sessions did not enforce the documented BDT transaction amount bounds before provider setup. | SSLCommerz checkout readiness/admin saves now reject dummy placeholder store IDs/passwords, while real-looking sandbox credential names are allowed; session creation and checkout-flow saves enforce BDT ৳10-৳500,000 before provider calls. Focused payment/settings/API tests, root gates, full deploy, live API health/readyz, and ops queue smoke pass. |
| PAY-024 | P1 | Verified | Payments/Polar Webhooks | Polar `order.paid` processing could persist the Polar order id as the local checkout id and use a non-canonical amount field, breaking later refund reconciliation that matches by checkout id. | Polar webhooks now map `checkout_id` and `total_amount` for `order.paid`, keep a narrow legacy checkout-shaped id fallback, and skip order/refund events that lack a usable checkout id instead of recording misleading payment state. Verification: focused Polar webhook tests, queue-consumer regression tests, API checkout tests, root gates, full deploy API `d4da4b57-a37c-4c0b-b777-f7f0f3185331`, live API health/readyz, OpenAPI `277` paths, and live ops check `ops-check-20260706t041347670z-23eecc06` pass. |
| PAY-025 | P1 | Verified | Payments/Checkout UX | Live in-flight hosted payment-session attempts could surface as immediate customer-visible failures after order commit, forcing buyers into recovery even when the D1 single-flight lease was about to replay a gateway URL/client secret. | Storefront online-payment handlers, receipt recovery, and customer account payment recovery now share a bounded payment-session retry helper that retries only explicit `202`/`status=processing` responses, honors `retryAfterSeconds`/`Retry-After`, waits at least 2s, and stops within 25s/12 attempts before falling back to recovery. Stripe fallback intent creation now uses the same policy as SSLCommerz and Polar. Verification: focused storefront payment/customer/recovery tests, root typecheck/lint/test (`406` files / `2921` tests)/build, env/dist-secret/diff guards, deploy to API `5889e403-2e19-4a42-b265-c06caf40dbf5` and storefront `6ca68c98-1159-4e8d-8433-765845605eb6`, live `release:check`, live ops check `ops-check-20260707t200417214z-b8882971`, and live storefront cache headers show current build `src-70358b3ce6be1d03` for warmed public pages. |
| PAY-026 | P1 | Verified | Payments/Gateway Readiness | Stripe and Polar readiness could still treat exact placeholder/demo credentials as configured if the required fields were non-empty, risking broken checkout exposure and noisy provider calls. | Stripe and Polar readiness now reject exact known placeholders while allowing real-looking test-mode keys/tokens; active payment-method filtering hides placeholder-backed gateways, and admin enabled saves reject submitted or stored placeholder effective values before encryption/settings writes. Verification: focused core/API payment settings tests, root typecheck/lint/test (`406` files / `2921` tests)/build, env/dist-secret/diff guards, deploy to API `5889e403-2e19-4a42-b265-c06caf40dbf5`, live `release:check`, and live ops check `ops-check-20260707t200417214z-b8882971` pass. |
| META-002 | P1 | Verified | Analytics/Meta CAPI | Browser storefront events could still call `/meta/events` when Meta CAPI was disabled or unconfigured, causing failed diagnostic logs and avoidable compute for ordinary browsing. | Layout data now exposes browser CAPI readiness, the storefront dispatcher no-ops unless enabled/credential-present, Meta admin/API saves reject missing/dummy credentials before enabling, the public event route passes only `CREDENTIAL_ENCRYPTION_KEY` for token decrypt, and the backend skips disabled/unconfigured/unreadable encrypted CAPI without calling Meta or writing noisy failure logs. Focused storefront/core/API/admin tests, root gates, API deploy `5ac46eb1-adef-4de0-a1c5-893a8bb8b8bb`, admin deploy `7f345da7-006c-44f3-9c7a-036224a21899`, storefront deploy `d5da64d3-93a6-4c3e-adfb-d6c10b92d8a2`, and live smokes pass. |
| ANALYTICS-001 | P1 | Verified | Analytics/Growth Integrations | Release-critical analytics correctness needed durable Meta Purchase delivery, correct storefront search/payment funnel events, and no buyer PII leakage from broad browser events. | Meta Purchase CAPI now has a durable D1 outbox claim at the order/payment authority boundary, browser CAPI fails closed when unconfigured, Search results emit a normalized `Search` event instead of `ViewContent`, and `AddPaymentInfo` fires only when the buyer confirms the selected payment method rather than on silent default preselection. Broad storefront events carry SKU/value/currency data without checkout/customer PII and bridge to Pixel/CAPI, GA4/GTM dataLayer, TikTok Pixel, and Zaraz where configured. Verification: parallel analytics/ops/SEO subagent audits, focused analytics/checkout/core/API tests, root typecheck/lint/test (`376` files / `2552` tests)/build/env/migration/dist-secret guards, remote D1 outbox read-only proof, API deploy `aede6cdc-321a-478c-8b58-d57d8a472f47`, ops deploy `3e480e19-7164-4bf9-b907-6fe3babf9734`, storefront deploy `fa0ddccc-157b-4cda-90c4-329543d2b0ac`, live search browser smoke with no console errors and product analytics data present, and live `ops:check` request `ops-check-20260706t013723715z-cf78e019` passing `/readyz`, OpenAPI `277` paths, and queue wiring. Remaining growth-provider enhancements moved to `ANALYTICS-003` as P2 backlog. |
| ANALYTICS-002 | P1 | Verified | Storefront/GA4 GTM Analytics | GA4/GTM could be pasted as scripts but the production storefront commerce runtime was still driven by Meta-named wrappers, so product views, search, add-to-cart, checkout, and purchase events could miss GA4/GTM ecommerce reporting even when merchants installed Google tags. | Google Tag Manager is now a first-class analytics provider, storefront commerce wrappers push GA4/GTM-compatible `dataLayer` events with event IDs, currency/value, transaction IDs, and item arrays without buyer PII, and `gclid`, `gbraid`, `wbraid`, `msclkid`, and `ttclid` are captured locally while ignored in HTML cache keys. Verification: official Google GA4/GTM and TikTok click-id docs review, focused analytics/schema/cache tests, SDK regeneration, root typecheck/lint/test/build, migration/env/dist/diff guards, full deploy to API `a7cc8e03-1f2a-4fd1-a814-9c9e700a646a`, admin `051bb505-ebdf-4e85-9c1b-9494a8de9d4f`, storefront `44134c03-4c45-4db9-92b1-0e9480793f42`, ops `c3c766ff-23aa-40a2-a5fa-c1e8a368687b`, live OpenAPI GTM enum smoke, home JSON-LD smoke, and production ops check pass. Remaining provider-health/TikTok server-side scope is now tracked in `ANALYTICS-003`. |
| ANALYTICS-003 | P2 | Not Started | Analytics/Growth Integrations | Safe test-event sends, TikTok Events API/server-side adapter, server-side click-id attribution beyond local capture, consent/runtime policy, and canonical provider adapter UX remain useful but not release-blocking after `ANALYTICS-001`/`ANALYTICS-002`; provider health cards are covered by `ANALYTICS-008`. | Future work should keep provider failures fail-closed and cheap, add explicit merchant-facing readiness/test-send diagnostics only when they are one-shot and bounded, avoid hot retries on dummy credentials, and reuse the durable local-claim/outbox pattern for any new server-side conversion provider. |
| ANALYTICS-004 | P1 | Verified | Analytics/Growth Integrations | Dummy analytics IDs and unsafe Partytown proxy targets could stay active in public injection paths, causing broken tracking, noisy diagnostics, or proxy abuse even when merchants had not entered real provider identifiers. | Admin/API validation now rejects active placeholder analytics configs, public analytics injection filters legacy placeholder rows, Meta CAPI source sanitization strips receipt-token variants, and API/storefront Partytown proxies reject non-HTTPS protocols before fetching. Follow-up hardening in commit `7427bf5e` made both API and storefront Partytown proxies HTTPS-only, with focused proxy tests (`2` files / `6` tests), root typecheck/lint/test (`383` files / `2643` tests)/build/env/dist/diff guards, API deploy `700567cc-95c2-4caa-bd63-e652c2cc856d`, admin deploy `890fb4a9-a6e6-4546-9f66-d9bbbab6465a`, storefront deploy `d4a27de9-64ea-4410-b30a-6b8b9c16424f`, ops deploy `008c6321-5b41-442a-a8b3-15b67ac23f4e`, live release-check pass, and live `403` smokes for `http`/`ftp` targets on `/api/ptproxy` and `/api/v1/__ptproxy`. |
| ANALYTICS-005 | P2 | Verified | Admin/Analytics Readiness | Cloudflare Web Analytics could be saved active with the placeholder token from the admin example, and view-only analytics operators still saw create/toggle/edit/delete controls that would only fail at the API. | Cloudflare Web Analytics placeholder tokens are now blocked by admin/core validation and legacy toggle checks, valid pasted beacon snippets are canonicalized to the platform-generated beacon snippet, and analytics list create/toggle/edit/delete controls are gated by exact RBAC permissions with accessible icon labels. Verification: focused core analytics tests, admin form/route boundary tests, storefront analytics/Partytown proxy tests, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`385` files / `2666` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, full deploy to API `5d8536d2-df6d-4df5-b9c1-17273dad1275`, admin `74e45ed0-2b1a-4500-b75d-6c4e9a683631`, storefront `9d7a8fba-6a64-48fc-98b2-d0afbf7aa301`, ops monitor `1aa6e22b-e1c7-46f9-a0a0-4324b1db243e`, `pnpm release:check --timeout-ms 30000`, and live authenticated `/admin/analytics` smoke proving the placeholder Cloudflare token is rejected with `400`, no smoke row is created, Add Script is visible for the demo admin, and icon actions have accessible labels. |
| ANALYTICS-006 | P1 | Verified | Analytics/Meta CAPI Cost Control | Follow-up analytics audits found bad Meta credentials could still make the browser event endpoint perform rate-limit/D1/provider/log work across ordinary browsing until merchants fixed settings, and main-thread Facebook Pixel snippets could be blocked by storefront CSP even though the admin allowed disabling Partytown. | The public Meta event route now validates trusted storefront origin first, skips while the KV `meta-capi:browser-events:circuit` marker is open before rate-limit/D1/provider work, opens that 15-minute marker after non-retryable provider/config failures, avoids success logs and noisy disabled/unconfigured logs, and clears the marker after Meta settings saves. Storefront CSP now includes Meta Pixel script hosts in `script-src` for main-thread snippets as well as worker/proxy paths. Verification: parallel read-only analytics/provider/UX audits, focused API/core/storefront analytics/CSP tests (`7` files / `40` tests), root `pnpm lint`, `pnpm typecheck`, `pnpm test` (`395` files / `2757` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, API deploy `01443ade-53ef-4dcf-9999-66bb13480d11`, storefront deploy `5a6b12b8-2164-410d-9141-7bb3578ba6ab`, live CSP smoke proving `connect.facebook.net`/`www.facebook.com` in `script-src`, live Meta invalid-origin smoke failing closed with `400`, live `pnpm ops:check --queues --samples 1 --timeout-ms 30000` (`ops-check-20260707t084218806z-0b2c8ae3`), and live `pnpm release:check --timeout-ms 30000` covering tracker, API, dashboard auth gate, storefront, SEO policy, robots, six sitemap checks, both feeds, and Product JSON-LD pass. Remaining analytics backlog stays explicit in `ANALYTICS-003`: consent policy/runtime gating, provider health/test sends, TikTok Events API, click-id server attribution, purchase-value semantics, and generic analytics readiness UX. |
| ANALYTICS-007 | P2 | Verified | Analytics/Provider Readiness | Active first-class analytics snippets could still be saved or toggled with provider/type mismatches, pasted Cloudflare beacon snippets could be Partytowned as custom code, browser Meta CAPI layout readiness could treat unreadable ciphertext as configured, and storefront browser events used the SDK retry transport. | Slice blocks active GA4/GTM/Facebook/TikTok placeholder/provider mismatches while keeping inactive drafts permissive, forces any Cloudflare beacon snippet onto the main thread, strict-reads Meta CAPI access-token readiness before exposing browser dispatch in layout, and sends browser Meta events through one short direct no-retry POST. Verified locally with focused core analytics/storefront-boundary tests, storefront analytics/tracking tests, ops-check tests, affected typechecks/lints, root `pnpm test`, root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, and `git diff --check`. Deployed API `4c11dc83-388a-448f-8ab9-47163705c0aa`, admin `79bc236f-a4ed-4080-a6b4-04ef58830129`, and storefront `dea43b2d-5861-4e91-9ad2-7a902eb631a0`. Live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` passed; only stale `worker:testdash` queue producers warned. Live `pnpm release:check --timeout-ms 30000` passed. |
| ANALYTICS-008 | P2 | Verified | Admin/Analytics Provider Health | Merchants could configure multiple browser/server analytics providers but had no compact read-only readiness summary that distinguished active, draft, blocked, browser-only, and Meta CAPI server-side status without calling providers or echoing secrets. | Added `getAnalyticsProviderHealth()` plus `GET /api/v1/admin/analytics/health` and a dashboard Provider Readiness card for GA4, GTM, Facebook Pixel, TikTok Pixel, Cloudflare Web Analytics, and custom snippets. The endpoint reads analytics rows plus Meta CAPI settings, strict-reads encrypted Meta CAPI credentials with `CREDENTIAL_ENCRYPTION_KEY`, returns only counts/messages/statuses, and never calls providers, sends test events, or returns script config/tokens. Mutations invalidate the provider-health query. Verification: focused provider-health/API/admin tests, OpenAPI/SDK regeneration to include `/api/v1/admin/analytics/health`, core/API/admin typechecks and lints, API/admin builds, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, API deploy `264fd086-ef30-4f65-a798-00f7de7c1d08`, admin deploy `31f5e1f3-f46b-4a17-b1c6-6bc9fbe6f56c`, live unauthenticated route smoke returning admin-session `401`, and final live release/ops smokes after OpenAPI edge propagation. |
| SEO-001 | P1 | Verified | Storefront/SEO XML | Sitemap/feed audit found `/sitemap.xml` listing `/api/facebook-feed.xml` as a sitemap document, no collection sitemap despite public collection pages, and `robots.txt` silently allowing all crawlers when SEO settings could not be read. | Sitemap index now includes only sitemap documents plus the new `/sitemap-collections.xml`, keeps product feeds separate from crawl sitemap discovery, collection sitemap fails closed on data errors, and `robots.txt` returns non-cacheable `503` when SEO settings are unavailable. Focused sitemap/robots/feed tests, root gates, storefront deploy `d5da64d3-93a6-4c3e-adfb-d6c10b92d8a2`, live `xmllint` sitemap/feed smokes, and live `robots.txt` real-sitemap smoke pass. |
| SEO-002 | P1 | Verified | Admin/SEO Discovery Controls | Release-critical SEO discovery needed dashboard-governed sitemap/feed/schema policy, fail-closed XML generation, and merchant-visible product discovery diagnostics tied to the real SKU/inventory model. | Global sitemap/feed/robots/JSON-LD discovery controls are covered by `SEO-003` through `SEO-008`. Product create/edit now warms cached SEO settings and shows a compact Discovery Readiness panel for canonical URL, sitemap XML, feed image, catalog feed inclusion/skips, buyer-resolvable SKU availability, and Product/Breadcrumb JSON-LD without creating a separate SEO truth from checkout/catalog logic. Verification: focused product SEO diagnostics/settings/route-boundary tests (`3` files / `10` tests), admin route graph boundary test (`47` tests), root typecheck/lint/test (`376` files / `2552` tests)/build/env/migration/dist-secret guards, admin deploy `57d62383-8532-4ffe-b578-be81d8958a3b`, storefront deploy `fa0ddccc-157b-4cda-90c4-329543d2b0ac`, live `/api/v1/seo` discovery smoke, live sitemap XML `xmllint` with `5` sitemap entries including `/sitemap-products.xml`, live catalog feed XML `xmllint` with `5` items, `5` absolute image links, and `5` availability nodes, live admin product-new browser smoke showing the Discovery Readiness panel under the current SEO policy with no console errors, and live `ops:check` request `ops-check-20260706t013723715z-cf78e019` pass. Richer per-resource and Merchant/AEO/AIO enhancements moved to `SEO-009` as P2 backlog. |
| SEO-003 | P1 | Verified | Admin/Storefront SEO Discovery | Merchants had no dashboard control over sitemap sections, product catalog feed exposure, robots sitemap advertising, or global Organization/WebSite JSON-LD, so XML/feed/schema generation was mostly code-owned and all-or-nothing. | Added a normalized default-on SEO discovery policy in `settings.seo/discovery`, admin SEO switches for sitemap sections/feed/robots/JSON-LD, public `/seo` + layout API exposure, sitemap/feed/robots generators that honor the policy, and SDK regeneration. Focused tests, root typecheck/lint/test/build, env/migration/dist/diff guards, API deploy `ac2de5e3-cb85-44e1-85e5-c7c55ea8e784`, admin deploy `fc667445-37cc-4c68-a65d-5c56c9b1f79d`, storefront deploy `1883cf55-6cf9-4b58-82d1-9a908ca8d4fa`, ops deploy `5afa840a-ef84-4cc6-8cd7-480934e6a27b`, live `/seo` and `/storefront/layout` discovery payload smokes, live `xmllint` sitemap/feed smokes, live `robots.txt`, and production ops check pass. |
| SEO-004 | P1 | Verified | Storefront/Product Feed Availability | Product catalog feed availability could say `in stock` for any active product even when the only buyer-resolvable tracked SKU was sold out, creating mismatch against product-page JSON-LD, landing-page stock UI, checkout validation, and Merchant/Meta catalog expectations. | Public product list now projects SKU-aware `availableForSale` from `products.public-eligibility.ts`, and `/api/facebook-feed.xml` uses it for `g:availability` while keeping inactive products out of stock. Focused/root tests, SDK regeneration, root typecheck/lint/test/build, env/migration/dist/diff guards, full deploy, API deploy `65b7d552-d690-49be-a3bb-54ee25b1afab`, storefront deploy `e10216f0-25ae-45ff-94db-08e52764d056`, live API product-list `availableForSale` smoke, live feed `xmllint`, and live feed availability smoke pass. |
| SEO-005 | P1 | Verified | SEO Discovery Fail-Closed Runtime | Admin SEO reads could fail open into default-on settings, partial discovery saves could reset sibling toggles, and sitemap/robots routes could emit relative or empty-base discovery URLs when `STOREFRONT_URL` was missing. CMS page sitemap timestamps also risked 1970-era `lastmod` when numeric Unix seconds reached the storefront. | Admin SEO GET now propagates read errors, the SEO settings UI blocks editing/saving on load failure, nested discovery saves deep-merge with stored policy, sitemap/robots routes require an absolute storefront URL and return non-cacheable `503` on missing/miswired base URL, and sitemap date formatting handles Unix seconds. Focused/root tests, SDK regeneration, root typecheck/lint/test/build, env/migration/dist/diff guards, full deploy, API deploy `65b7d552-d690-49be-a3bb-54ee25b1afab`, admin deploy `d92d0d6d-3e30-4003-963d-7e7ca30bf4d4`, storefront deploy `e10216f0-25ae-45ff-94db-08e52764d056`, ops deploy `3efa2edb-7a4c-485e-955d-d9e2ede3a684`, live `/seo` discovery smoke, live `sitemap.xml` absolute-loc smoke, live `robots.txt` absolute-sitemap smoke, live `sitemap-pages.xml` no-1970 smoke, and `pnpm ops:check --queues --samples 1 --timeout-ms 20000` pass. |
| SEO-006 | P1 | Verified | Storefront/Product Feed Compliance | Product catalog XML could emit active products without a required `g:image_link`, and the feed route validated only a non-empty storefront base URL rather than the same absolute URL invariant as sitemap/robots generation. This could create malformed Merchant/Meta catalog items or relative feed links despite the dashboard discovery controls being green. | `/api/facebook-feed.xml` now uses the sitemap base URL validator, returns non-cacheable `503` on missing/relative base URL, filters feed items to active products with a real absolute `http(s)` primary image link, maps relative optimizer output to the storefront origin, filters non-http image schemes, returns no-store `503` on unexpected generation failure, and still preserves SKU-aware `g:availability`. Verification: official Google Merchant/Search/AI optimization docs review, focused feed tests, root typecheck/lint/test/build, migration/env/dist/diff guards, full deploy to API `a7cc8e03-1f2a-4fd1-a814-9c9e700a646a`, admin `051bb505-ebdf-4e85-9c1b-9494a8de9d4f`, storefront `44134c03-4c45-4db9-92b1-0e9480793f42`, ops `c3c766ff-23aa-40a2-a5fa-c1e8a368687b`, live `xmllint` feed smoke proving 5 items with absolute `image_link` and availability, live sitemap/robots XML smokes, home JSON-LD smoke, and production ops check pass. Remaining per-resource/richer-schema scope is now tracked in `SEO-009`. |
| SEO-007 | P1 | Verified | Storefront/SEO Discovery QA | Follow-up SEO audit found the static sitemap was generated but invisible in dashboard controls, `robots.txt` could preserve invalid custom `Sitemap:` lines, public XML/text discovery assets were returned to crawlers with browser no-store headers, zero-discount feed items could fall back to original price, and page JSON-LD/canonical builders used unvalidated storefront base URLs. | Added first-class `staticPages` sitemap policy/UI/API/SDK support, robots Sitemap directive normalization, public browser TTL preservation for generated discovery XML/text, zero-price feed handling, and absolute-only SEO base URL helpers used by global/page/product/category/collection JSON-LD/canonical builders. Verification: focused SEO/feed/cache/analytics tests, root typecheck/lint/test/build, SDK regeneration, env/migration/dist/diff guards, full deploy to API `0845fd0e-1256-4f94-bb66-00485858627c`, admin `076840df-4763-4fdf-81a6-35fdec3c43b7`, storefront `8bf7cce7-0d83-4f2d-b3ad-38cd173c5182`, ops `80509042-f265-4058-b52a-1bb6b391ed33`, live OpenAPI `staticPages` smoke, live `robots.txt`/`sitemap.xml`/feed public-cache smokes, live product canonical/Product/Breadcrumb JSON-LD smoke, live `/` and `/categories/food` recovery smokes after transient warmer failures, and production ops checks `ops-check-20260706t000101621z-62bff100` plus queue check `ops-check-20260706t000134385z-f490b00b` pass. |
| SEO-008 | P1 | Verified | Admin/SEO Feed And Schema Controls | The SEO discovery dashboard still treated product/category/collection JSON-LD as a vague global switch and the product feed as all-or-nothing, so merchants could not control sold-out catalog inclusion, XML feed metadata, or page-level schema families from the dashboard. Relative image optimizer output could also leak into Open Graph/Product JSON-LD image fields on product/category/collection pages, and the public `/seo` cache could keep an old payload shape across a schema expansion. | Extended the migration-free `settings.seo/discovery` JSON policy with feed title/description, sold-out inclusion, Product/Breadcrumb/CollectionPage JSON-LD switches, admin controls, API/OpenAPI/SDK support, QA summaries, and storefront enforcement. Feed XML now honors merchant XML metadata and can skip unavailable products; product/category/collection pages emit absolute social/schema images and gate existing JSON-LD by policy. Public SEO reads now use the versioned `api:seo:v2:` cache namespace while remaining covered by the broader `api:seo:` invalidation group. Verification: independent SEO/analytics/ops subagent audits, official Google Search/Merchant/AI optimization docs review, SDK regeneration, focused shared/storefront/admin/core/API tests, root `pnpm test` (`375` files / `2535` tests), root typecheck/lint/build, env/migration/dist-secret/diff guards, full deploy to API `013a9451-ee57-47b0-a4e0-229078bfe6b9`, admin `b1cf08b1-5bf5-45ab-954f-722cd26a52c9`, storefront `d0d194a7-9d16-48db-9072-8f09de6819d3`, and ops `e6b26907-25f3-4ce3-b586-eebe03a060da`, live `/api/v1/seo` un-busted HIT smoke with the new discovery shape, live OpenAPI `277` paths, live sitemap index/static/products/categories/collections XML `xmllint` pass, live robots sitemap advertisement, live feed smoke proving 5 absolute `g:image_link` values, live Product/Breadcrumb/CollectionPage JSON-LD smoke, and production ops checks `ops-check-20260706t003402917z-d5766884` plus queue check `ops-check-20260706t003402869z-ce9e0b03` pass. Remaining per-resource/richer-schema scope is now tracked in `SEO-009`. |
| SEO-009 | P1 | Verified | Admin/SEO Variant Feed And ProductGroup | Product discovery was still product-row centric even though the platform sells SKUs/options: merchants could not choose product-row versus SKU-row feed output, the feed relied on the broad public product listing shape, product-detail JSON-LD did not model optioned products as `ProductGroup`, and sitemap XML still emitted ignored `priority`/`changefreq` hints instead of the smaller loc/lastmod truth Google recommends. | Added default-on dashboard policy for feed row strategy (`variants`/`products`) and ProductGroup JSON-LD, bumped public SEO cache to `api:seo:v3:`, added dedicated `/api/v1/products/feed` projection with feed-only description/image/category/attribute/variant data, regenerated the SDK/OpenAPI (`278` paths), defaulted optioned catalog XML to SKU rows with stable `item_group_id`, kept simple products as product rows, emitted ProductGroup/hasVariant JSON-LD on optioned product pages, and simplified sitemap XML to absolute `loc` plus truthful `lastmod`. Verification: parallel SEO/feed subagent audits, official Google Search/Merchant docs review, focused tests (`18` files / `93` tests), root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`382` files / `2631` tests), `pnpm build`, env/migration/Drizzle/dist-secret/audit/frozen-lockfile/diff guards, full deploy to API `358bede2-013f-40d3-a102-18e02047b71e`, admin `2ec357af-35d0-4a43-84e6-2b96ac74647d`, storefront `d88473e5-1058-492d-bf24-11fdec04c8d1`, ops monitor `6b375853-8e3a-4b7d-a09d-6b011e35c455`, live `/api/v1/seo` policy smoke (`variants`, ProductGroup on), live `/api/v1/products/feed` projection smoke, live `xmllint` for sitemap index/products/feed, feed smoke with `5` items, `5` availability nodes, and `3` `item_group_id` values, live ProductGroup JSON-LD smoke on `/products/mojo`, admin auth-gate smoke, and production ops check `ops-check-20260706t045622789z-4fc7d356` pass. |
| SEO-010 | P2 | Verified | Storefront/SEO Canonicals | Static sitemap advertises `/search`, but the search page had no canonical and only query searches were noindexed; sorted, paged, or filtered listing variants could compete with the canonical listing. Layout social image tags also still trusted caller-provided `ogImage` instead of forcing absolute SEO-safe URLs. | `/search` now emits a stable absolute canonical and applies `noindex,follow` to query/sort/filter/page variants, while `Layout.astro` normalizes Open Graph and Twitter image URLs through the storefront SEO absolute-URL helper before emitting tags. Focused SEO regression tests, root gates, storefront deploy `bfa7c0d9-208b-4ec5-bb12-267a625275d2`, live sitemap/feed `xmllint`, live feed absolute-image/availability smoke, and live `/search?q=shoe` canonical plus `noindex,follow` smoke pass. |
| SEO-011 | P2 | In Progress | Admin/SEO Resource Overrides | Structured-data previews beyond product readiness and richer commerce schema controls remain after the release-critical XML/feed/ProductGroup/resource-indexing/canonical-override/schema-truth/feed-truth/policy-aware-smoke/live-proof slices. Store-level return-policy schema is no longer blocked: it now has a merchant-facing source of truth in `settings.seo/return_policy`, with dashboard controls for country, return category/window, fee responsibility, method, and optional policy URL. | Completed slices now include bounded live discovery probes, aggregate product-feed diagnostics from the real SKU/image/availability policy, public business settings in `/api/v1/storefront/layout`, global OnlineStore JSON-LD backed by business/contact/social/logo data, dashboard control for offer shipping schema, Product/ProductGroup Offer `shippingDetails` from active shipping methods, schema-safe GTIN fields from variant barcode data, MerchantReturnPolicy JSON-LD sourced only from merchant-saved return-policy facts, product/category/collection/CMS page `noIndex` plus sitemap-exclusion controls verified in `SEO-017`, same-store canonical path overrides tracked in `SEO-018`, schema truth hardening tracked in `SEO-019`, catalog feed/dashboard copy truth tracked in `SEO-020`, non-product discovery previews plus policy-aware release smoke tracked in `SEO-021`, and policy-aware child-sitemap/live-proof/feed/schema smoke hardening tracked in `SEO-022`. Current verification: official Google Search/Product/return-policy structured-data guidance review, shared return-policy normalizer tests, admin SEO settings/status tests, core settings/layout tests, API invalidation tests, storefront JSON-LD regression tests, SDK regeneration, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`390` files / `2717` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration guard, `git diff --check`, local admin browser smoke proving the SEO tab can enable a finite 7-day Bangladesh return policy with same-origin `/returns` URL, full deploy to API `e8db86bf-733e-4e35-b9a7-278c45cb5ccf`, admin `3220f6d6-c226-4fec-93ad-e97744183589`, storefront `6de770ad-7d02-4641-9184-d535226097c0`, ops monitor `56a88563-0f40-4661-a237-105e566e524f`, `pnpm release:check --timeout-ms 30000`, `pnpm ops:check --queues --samples 1 --timeout-ms 30000` (`ops-check-20260706t200048634z-62ebe134`), live `/api/v1/seo` and `/api/v1/storefront/layout` return-policy payload smokes, live discovery smokes for robots, six sitemaps, canonical and compatibility feeds, and live product schema smoke on `/products/test-productss`. Remaining work: richer schema/dashboard-preview UX only where dashboard-owned data exists. Future work must keep dashboard policy as the source of truth, preserve fail-closed absolute URL/image rules, avoid AI-only gimmicks, and verify against current Google Search/Merchant structured-data/feed documentation before changing public discovery output. |
| SEO-012 | P1 | Verified | Storefront/Product Feed Pagination | Product feed XML paginated by raw product pages and then filtered/expanded rows afterward, so optioned products could have extra SKU rows sliced off page 1 and never emitted on page 2; skipped unavailable/missing-image products could also make feed pages sparse. | `/api/facebook-feed.xml` now paginates the final flattened feed rows after availability/image filtering and SKU expansion, and `/api/product-feed.xml` is the canonical neutral alias while `/api/facebook-feed.xml` remains for compatibility. SEO setting saves warm both feed URLs, the dashboard/release check point to the canonical feed, and tests prove expanded variant rows continue onto later feed pages. Verification: focused API/admin/storefront/release-check tests (`8` files / `59` tests), `pnpm typecheck`, `pnpm lint`, `pnpm test` (`383` files / `2641` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, full deploy to API `a0cefcdd-c9ab-47dd-bf0d-11502ead7d9a`, admin `61bfa7fb-2296-4332-81e4-5681e44a7c36`, storefront `05316066-6be1-4045-97b8-e7ea2484f157`, ops `49ef7659-d694-422e-9967-b2dc79a95ecf`, `pnpm release:check --timeout-ms 20000`, and live canonical/compat feed smokes with `5` items, `5` availability nodes, `5` absolute image links, and `3` `item_group_id` values. |
| SEO-013 | P1 | Verified | SEO Feed/Sitemap Freshness | Follow-up audits found stock-changing writes could leave `/api/v1/products/feed` and storefront `feed_products_*` caches stale until TTL, dashboard SEO QA could show old live-proof/feed-diagnostic data after saving, `robots.txt` could preserve stale off-origin or same-origin non-canonical `Sitemap:` lines that release checks reject, and release-check did not exercise feed page 2. | Product availability invalidation now clears public product list/search/feed API caches plus storefront product exact keys and the `feed_products_` exact-generation family, `robots.txt` keeps only the canonical current storefront sitemap URL when advertised and strips sitemap directives when disabled, feed query parsing rejects malformed page/limit values, SEO settings saves invalidate live-proof/feed-diagnostic queries, and release-check validates page-2 feed XML when it exists. Verification: focused API cache-invalidation tests, storefront exact-generation/robots/feed route tests, release-check tests, admin route-boundary guard, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`385` files / `2671` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, full deploy to API `c71ad609-a8a4-421c-84e2-a1fb92dfdf37`, admin `fbe06d25-5b34-447d-ab3a-07091d646877`, storefront `6231eeec-6415-4eaf-8609-e2a35e854f30`, ops monitor `8eacbe81-0367-4b94-82b0-39804e3130f5`, `pnpm release:check --timeout-ms 30000`, live `robots.txt` canonical sitemap smoke, live canonical/compat feed page-2 XML smokes, and live malformed feed parameter `400` smokes. |
| SEO-014 | P1 | Verified | SEO/AEO/AIO Discovery Integrity | Product feed/JSON-LD image handling could turn unsafe media strings into same-origin optimized URLs, Organization `sameAs` accepted non-HTTP social links, feed diagnostics could report ready with a missing Storefront URL, and product sitemap page parsing accepted malformed positive-looking values. | Shared media URL resolution now rejects unsafe schemes and unsafe bare keys before optimizer output; Product feed/JSON-LD only consume safe resolved media; Organization `sameAs` emits absolute `http(s)` social links only; feed diagnostics adds `storefront_url_unavailable`; product sitemap pages reject malformed or leading-zero page params. Verification: current Google Search/Merchant discovery guidance review, focused SEO/media/feed/diagnostics tests, SDK regeneration, root typecheck/lint/test/build/security gates, deploy API `5f4fe9a5-672b-4a35-a43d-c2c12ff345d9`, admin `16c0aa27-835f-409c-b431-cc70c6bb2800`, storefront `9e2cfb69-b7c8-4cf4-bdcb-e507b86145da`, ops `60612cda-df46-497a-82ad-5853e7c14416`, live OpenAPI `storefront_url_unavailable` smoke, live robots canonical sitemap smoke, live product-feed XML `xmllint`, and malformed product-sitemap `400` smokes pass. |
| SEO-015 | P1 | Verified | SEO/Sitemap And Feed Freshness | The sitemap index stamped every child sitemap with request-time `lastmod`, so crawlers could see every sitemap as freshly changed on every render. The browser XSL preview also showed priority/change-frequency columns that generated sitemap XML deliberately does not emit. Independent XML audit also found rendered `/api/product-feed.xml` and `/api/facebook-feed.xml` used the global storefront cache generation instead of the already-invalidated `feed_products_` generation family, so stock/restock truth could wait for TTL. | Sitemap index entries now omit `lastmod` until a truthful child-sitemap modified timestamp exists, `sitemap.xsl` mirrors the loc/lastmod-only XML contract with graceful "Not provided" copy, and feed XML routes now classify into the `feed_products_` exact-generation family so stock-change invalidation reaches rendered XML. Verification: focused sitemap index/XSL/cache-generation route tests (`3` files / `10` tests), `pnpm typecheck`, `pnpm lint`, `pnpm test` (`391` files / `2719` tests), `pnpm build`, `pnpm check:env`, `pnpm --filter @scalius/database check:migrations`, `pnpm check:dist-secrets`, `git diff --check`, full deploy to API `e30caa87-fb02-4797-af74-83e52c5d3718`, admin `70596d66-4f64-4c82-a4a5-342edef3ff20`, storefront `5e35bd93-9ed9-4ded-ab1e-0711d8501c0a`, ops monitor `5fdd140e-2e51-442b-90f6-9ff4aaa64f8a`, `pnpm ops:check --queues --samples 1 --timeout-ms 30000`, and live XML smokes proving `/sitemap.xml` has no index `<lastmod>`, `/sitemap.xsl` has no priority/changefreq columns, and canonical/compat feed XML responses include exact `gen=0` cache-generation headers. |
| SEO-016 | P1 | Verified | Product Discovery XML Controls | Merchants need product-level control to keep a public product page live while excluding it from product sitemap XML or catalog feed XML, without confusing those controls with noindex/page visibility or filtering after pagination. | Added product `excludeFromSitemap` and `excludeFromProductFeed` fields with admin form switches, product SEO diagnostics, feed diagnostic reason `product_feed_excluded`, dedicated `/api/v1/products/sitemap` projection that filters before pagination, storefront sitemap readers that use that projection, and `sitemap_products_` exact cache generation/invalidation alongside feed XML. The general public product-list API intentionally does not expose the sitemap-exclusion flag. Verification: SDK regeneration (`280` paths / `378` operations), focused diagnostics/route/cache tests (`8` files / `93` tests), `pnpm typecheck`, `pnpm lint`, `pnpm test` (`391` files / `2722` tests), `pnpm build`, `pnpm check:env`, migration metadata guard, `pnpm check:dist-secrets`, `git diff --check`, full deploy to API `6ab31560-7fd9-42c7-a0ea-21325d0330ed`, admin `baec5372-16e3-409c-a6c4-ec7504d0a8e9`, storefront `146ebc7b-7216-48af-9519-ec51e953e6a6`, ops monitor `23ba38df-a702-4784-8b85-f471b745737d`, `pnpm release:check --timeout-ms 30000`, production ops check `ops-check-20260706t212012172z-06ac71a2`, live OpenAPI smoke proving `/api/v1/products/sitemap`, `product_feed_excluded`, and no public product-list `excludeFromSitemap`, live sitemap projection smoke, live product sitemap XML smoke, malformed sitemap page `400` smoke, and live product feed XML smoke. |
| SEO-017 | P1 | Verified | SEO/Resource Indexing Controls | Per-resource SEO controls were still product-XML centric: categories, collections, CMS pages, and products did not share a clear public-but-noindex model, and product JSON-LD could emit invented or weak facts such as price expiry, item condition, fallback seller, or fallback brand. | Added `noIndex` and resource sitemap-exclusion storage/API/admin controls for products, categories, collections, and CMS pages. Storefront pages now render public resources with `noindex,follow`, omit noindexed resources from sitemap XML, and suppress resource-specific JSON-LD when noindexed. Product JSON-LD no longer invents `priceValidUntil` or item condition, seller comes from business settings, brand is emitted only from explicit product attributes, and business setting saves invalidate layout caches. Verification: parallel SEO/structured-data/UCP audits, SDK regeneration (`280` OpenAPI paths), focused admin/core/API/storefront tests, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`392` files / `2728` tests), `pnpm build`, local+remote migration `0081`, migration metadata/env/dist-secret/diff guards, deploy API `0bd61ed1-f191-4189-a747-c7bdc81cffd0`, admin `bf5bec43-81fe-431b-9fab-39d6d44f8707`, storefront `3f641ed2-07aa-40c5-a3d7-5d2421b4ee43`, ops monitor `b13fde69-ad7d-4c20-a36c-a73ac12c0057`, `pnpm release:check --timeout-ms 30000`, `pnpm ops:check --queues --samples 1 --timeout-ms 30000` (`ops-check-20260706t215404872z-dc576436`), live OpenAPI resource-indexing smoke (`noIndex=44`, `excludeFromSitemap=44`), live discovery XML/feed `200` smokes, product sitemap/feed public cache-header smokes, and live product schema smoke proving no `priceValidUntil`, `itemCondition`, or `NewCondition`. |
| SEO-018 | P2 | Verified | Admin/SEO Canonical Overrides | Merchants needed a safe way to consolidate duplicate, campaign, or renamed public resource paths without off-origin canonical traps or mismatched sitemap/schema signals. | Products, categories, collections, and CMS pages now persist nullable `canonicalPath` values validated by a shared same-store path normalizer. Admin forms expose a compact optional canonical path field; API/OpenAPI/SDK DTOs carry the field; storefront canonical links, Open Graph URLs, resource JSON-LD URLs/breadcrumb items, and sitemap `<loc>` use valid overrides while blank/invalid values fall back to the normal public route. Verification: official Google canonical/sitemap/Product structured-data guidance review, local and remote migration `0082_resource_canonical_paths`, shared canonical helper tests, core product/category/page/collection validation tests, admin product SEO diagnostics tests, storefront canonical/sitemap route tests, SDK regeneration (`280` OpenAPI paths), root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`394` files / `2736` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, full deploy to API `997fdb0d-1f66-48da-ad99-a72050681b00`, admin `2e3d8324-50f6-4785-aeb8-e6165efdc4bd`, storefront `31700da1-36e3-4979-9f10-2f1009b18cae`, ops monitor `02ac3fdd-5f86-4dfc-a7d6-07a886fe8752`, `pnpm release:check --timeout-ms 30000`, and `pnpm ops:check --queues --samples 1 --timeout-ms 30000` (`ops-check-20260706t223803493z-b89ee906`) pass. Remaining structured-data preview and richer-schema work stays tracked in `SEO-011`. |
| SEO-019 | P2 | Verified | Storefront/SEO Schema Truth | Product seller and global OnlineStore/WebSite schema could still fabricate identity from footer/logo/`Store`, category/collection BreadcrumbList was emitted only inside CollectionPage schema, and category CollectionPage schema ignored stored category descriptions. | OnlineStore/WebSite names and Product offer seller identity now come only from Business settings company/legal name; fragile seller identity is omitted when missing and the SEO dashboard warns merchants to set Business identity before relying on OnlineStore, site-search, or Product seller schema. Category and collection BreadcrumbList JSON-LD now follows the breadcrumb toggle independently from CollectionPage schema, and category CollectionPage description uses the stored category description flattened to plain text before meta description or generic fallback. Verification: parallel read-only storefront/admin audits, official Google Product/Breadcrumb structured-data guidance review, focused storefront/admin tests, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`394` files / `2739` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, full deploy to API `c5fa3c40-fd18-4599-95d1-10005f119b7e`, admin `501a92da-a4c6-494d-b45b-0ce81d9e934f`, storefront `ba0751b1-96b5-4ba2-942f-a39dd6a181c7`, ops monitor `023068a1-87ed-412a-9e46-7c9f3ec5a0ae`, `pnpm release:check --timeout-ms 30000`, and `pnpm ops:check --queues --samples 1 --timeout-ms 30000` (`ops-check-20260706t225803782z-52ffe0d7`) pass. Remaining SEO-011 work is schema preview UX, live child-sitemap/dashboard refinement, and richer schema only where dashboard-owned data exists. |
| SEO-020 | P2 | Verified | Storefront/SEO Feed Truth And Dashboard Copy | Canonical `/api/product-feed.xml` reused the Meta/Facebook feed verbatim, so Google/Base feeds could emit Meta availability values, feed item links ignored product canonical path overrides, and missing brand data was fabricated as `Generic`; the dashboard robots editor also told merchants to include a sitemap line even though runtime strips and normalizes sitemap directives. | Canonical product feed now uses Google/Base availability values (`in_stock`/`out_of_stock`), while `/api/facebook-feed.xml` remains a Meta compatibility feed with `in stock`/`out of stock`. Feed item links honor product `canonicalPath` overrides while preserving variant query params, the feed projection carries supported variant barcode fields so true GTINs can be emitted, simple product rows can emit the one real default-SKU GTIN when present, and catalog feeds no longer invent `Generic` brands, using `identifier_exists=no` only when no brand/GTIN is available. Admin SEO copy now explains that robots textarea is crawler rules only and sitemap lines are governed by the Advertise sitemap URL switch, and the feed output mode explains SKU/variant rows versus product rows. Verification: parallel read-only dashboard/public discovery audits, official Google Merchant/Search sitemap/Product docs review, SDK regeneration (`280` OpenAPI paths), focused storefront/core/API/release-check tests, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`394` files / `2741` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, full deploy to API `f16047f6-3b55-4eb3-b537-192d7f1f091f`, admin `4a263a14-1425-4003-9718-79f428ff1ea9`, storefront `af78d661-9693-4ac6-93fe-328ad2c2eae5`, ops monitor `a488f4aa-ee3c-431b-b8dc-c4549262ec50`, `pnpm release:check --timeout-ms 30000`, and `pnpm ops:check --queues --samples 1 --timeout-ms 30000` (`ops-check-20260706t233830562z-48bd4d7b`) pass. Remaining SEO-011 work is live child-sitemap/dashboard refinement, non-product discovery outcome previews, release-check policy-awareness for intentionally disabled feeds/sitemaps, and richer schema only where dashboard-owned data exists. |
| SEO-021 | P2 | Verified | Admin/Release SEO Discovery UX | Release readiness still treated intentionally disabled sitemap/feed surfaces as failures and skipped product-page schema smoke when feeds were off; category, collection, and CMS page forms also lacked the product form's discovery-outcome preview, so merchants could not see whether canonical/noindex/sitemap/schema settings would affect public discovery after save. | `pnpm release:check` now reads public `/api/v1/seo` first, falls back to strict defaults only on fetch/shape failure, skips disabled sitemap/feed surfaces, requires robots to omit `Sitemap:` lines when sitemap advertising is disabled, and uses product sitemap `<loc>` as the product-page JSON-LD smoke source when catalog feeds are intentionally off. Category, collection, and CMS page forms now share `ResourceDiscoveryReadiness` and the pure `buildResourceDiscoveryPreview()` helper for canonical URL/path, sitemap XML, and JSON-LD outcomes; CMS page copy stays honest that no page-specific JSON-LD is emitted today. Verification: current Google Search/Merchant discovery guidance review, `node --check scripts/release-check.mjs scripts/release-check.test.mjs`, script ESLint, focused release-check tests (`17` tests), focused resource discovery preview tests (`5` tests), admin typecheck/lint, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`395` files / `2750` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, full deploy to API `f5c12e7f-ef75-4af9-885b-31611f7bb934`, admin `c02f44c6-7d38-4654-8da2-fe48f7a5969f`, storefront `37126f0b-b770-4dcb-a921-da6491d4217e`, ops monitor `3b021edc-a849-4c75-8e71-58a991ed8be3`, live `pnpm release:check --timeout-ms 30000` proving policy-aware discovery and product route schema, live `pnpm ops:check --queues --samples 1 --timeout-ms 30000` (`ops-check-20260707t001209850z-2a07fa64`), and live Chrome smokes for category/page/collection new forms showing Discovery Readiness with no console/page errors pass. |
| SEO-022 | P2 | Verified | Admin/Storefront SEO Live Proof | Follow-up audits found the admin live proof could show false warnings for intentionally disabled feeds/robots sitemap ads, did not fetch enabled child sitemaps, OnlineStore JSON-LD could serialize a literal `null` script when helper prerequisites failed, release checks only validated feed aggregate counts, release checks forced Product JSON-LD even when Product schema was disabled, release checks rejected valid empty catalog XML/feed output, and robots could preserve canonical-equivalent non-exact Sitemap lines. | Admin SEO live proof now reads the current discovery policy, probes robots against the expected Sitemap-line count, probes the sitemap index plus enabled child sitemaps, and skips disabled feed/child-sitemap rows as policy-disabled rather than broken. OnlineStore JSON-LD is built before serialization and omitted when prerequisites fail; the dashboard warns when known Business/header-logo facts cannot support OnlineStore schema. `robots.txt` normalizes to exactly one canonical current sitemap URL when advertised. `pnpm release:check` now parses `structuredData.products`, skips Product JSON-LD validation only when that public switch is off, validates each emitted feed item for product link/image/availability, allows valid empty catalog sitemap/feed XML, and keeps exact canonical robots validation. Verification: parallel read-only SEO/UCP/structured-data audits, `node --check scripts/release-check.mjs scripts/release-check.test.mjs`, focused release/admin/storefront SEO tests (`7` files / `72` tests), root `pnpm lint`, `pnpm typecheck`, `pnpm test` (`395` files / `2754` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, admin deploy `3b4ece43-03a8-4ea2-92bf-7225afd3913e`, storefront deploy `8725d631-71db-40e4-9d8e-c06307c62b57` / build `src-04eeae338025f63f`, live `pnpm release:check --timeout-ms 30000`, direct live XML/feed probes proving robots exactly one canonical Sitemap line, sitemap index `5` locs, static sitemap `2` locs, product sitemap `27` locs, both feeds `5` items/images/availability, and `pnpm ops:check --queues --samples 1 --timeout-ms 30000` (`ops-check-20260707t004645843z-238440c5`) pass. |
| SEO-023 | P1 | Verified | Product Option Feed And Schema Truth | Merchant product options are generic business axes (`2KG`, `3KG`, `Premium`, numbers, styles, etc.), but product feed/ProductGroup code could treat Option 1/Option 2 as fixed size/color semantics. That can publish false Merchant attributes, weak ProductGroup JSON-LD, and confusing admin UX for products where size/color are only examples. Follow-up runtime audits also found storefront discovery URLs could accept path/query/hash base URLs, product breadcrumbs ignored category canonical overrides, and the feed client still had a generic-list fallback. | Product records now own `variantOption1Label`, `variantOption2Label`, `variantOption1Schema`, and `variantOption2Schema`; admin product forms expose a compact catalog option mapping section; create/update/read/feed projections and SDK/OpenAPI include the fields; canonical Google feed rows use merchant labels for `item_group_title`/`variant_option`, emit standard `size`/`color`/`material`/`pattern` only from explicit mapping, and preserve custom/non-standard axes with `none`. Product/ProductGroup JSON-LD uses the same mapping and resource-level URLs. Discovery URL helpers now fail closed unless `STOREFRONT_URL` is a clean absolute origin, product breadcrumbs honor category canonical path overrides, and the feed client no longer falls back to the broad product listing projection. Verification: official Google Search sitemap/ProductGroup/Merchant feed/variant-option docs review; parallel dashboard and runtime discovery audits; SDK regeneration (`280` OpenAPI paths); `pnpm db:generate` with the repo's known Drizzle snapshot-collision warning plus passing migration metadata/Drizzle checks; focused admin/core/API/storefront typechecks; focused tests (`7` files / `61` tests); root `pnpm lint`, `pnpm typecheck`, `pnpm test` (`395` files / `2764` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`; production D1 migration `0083_product_variant_option_discovery.sql`; deploys to API `5ce7bfc5-32cb-4ced-94a8-afb92c775df4`, admin `90d83ea7-7910-4eca-8939-c60fa2b89b16`, and storefront `67495e88-5c13-4b24-863d-9cb9ed2219e8`; live `pnpm ops:check --queues --samples 1 --timeout-ms 30000` (`ops-check-20260707t093155555z-78885383`); live `pnpm release:check --timeout-ms 30000` covering tracker/docs, API ops, dashboard auth gate, storefront, SEO policy, robots, six sitemap checks, both feeds, and Product JSON-LD; and direct live discovery smokes proving canonical `/api/product-feed.xml?limit=5` emits `5` items/images/availability nodes, `3` `item_group_id`, `3` `item_group_title`, and `6` `variant_option` nodes with Google availability values while `/api/facebook-feed.xml?limit=5` preserves Meta availability and omits Google variant-option fields. |
| SEO-024 | P1 | Verified | Admin/Core SEO Canonical Routability | Follow-up SEO/AEO/AIO audits found canonical overrides were only same-store syntax checked, so merchants could save arbitrary paths such as `/shop/linen-shirt` that storefront routes do not resolve while sitemap/feed/canonical output advertised them to crawlers. | Shared canonical validation now has resource-aware route-shape checks: products must use `/products/<slug>`, categories `/categories/<slug>`, collections currently use `/collections/<collectionId>` because the storefront collection route is ID-based, and CMS pages use one non-reserved `/<slug>` segment. Core product/category/collection/page validators, admin product/collection/category/page schemas, discovery-readiness previews, storefront canonical builders, sitemap XML, product feed URLs, and UCP catalog URLs all use the resource-aware validator, preserving blank/null fallback and ignoring old persisted off-origin/query/fragment/space/dead-path overrides before XML/feed/schema output can point at 404s. Verification: focused shared/core/admin/storefront canonical tests, affected shared/core/admin/storefront typechecks, release-check UCP tests, `git diff --check`, and live `pnpm release:check --timeout-ms 30000` proving discovery XML/feed/Product JSON-LD and UCP smoke still pass. |
| SEO-025 | P1 | Verified | Admin/Core Collection Canonical IDs | Follow-up collection-route audit found the collection canonical route-shape rule still allowed slug-like overrides even though the storefront only serves `/collections/<collectionId>`, so merchants could save a syntactically valid but dead canonical, sitemap, feed, breadcrumb, or UCP URL. | Collection canonical overrides now must be blank on create and must match the saved collection ID route on update; shared canonical helpers expose resource path segments, admin collection UI copy and discovery previews explain the ID-route rule, and invalid slug-style collection canonicals fail before persistence or discovery output. Verification: focused shared/core/admin preview and collection service tests, focused UCP/cart/post-sale regression suite (`10` files / `64` tests), root lint/typecheck/test/build/env/dist-secret/audit/Drizzle/migration/peer/frozen-install/diff gates, full deploy to API `b62e892d-2500-4a6a-9061-d1f76ce9a41e`, admin `99c50683-d4cc-498f-b7dd-dd717caabe32`, storefront `8062ecd7-70d2-4ef6-91c0-f9cdfec0dcf0`, and ops monitor `ad8b6be1-4c37-4e84-9755-19b2d9de53e8`; live `pnpm release:check --timeout-ms 30000`; and live `pnpm ops:check --queues --samples 1 --timeout-ms 30000` (`ops-check-20260707t112230469z-2946f7d4`) pass. |
| SEO-027 | P2 | Verified | Admin/Release SEO Proof | Dashboard SEO live proof could still show green for malformed feed XML when HTTP returned 200, and release checks proved Product JSON-LD but not emitted homepage OnlineStore/WebSite/SearchAction/return-policy schema. | Feed live probes now count per-item product links, image links, availability values, and absolute http(s) link/image URLs; malformed non-empty feeds warn/fail in dashboard proof instead of looking OK. Sitemap/feed/robots status rows now warn when the Store URL is not an absolute runtime URL. `pnpm release:check` now parses full public structured-data policy and validates emitted homepage OnlineStore, WebSite/SearchAction, and MerchantReturnPolicy JSON-LD without requiring merchants to fabricate missing prerequisites. Verification: official Google ecommerce structured-data, Product/ProductGroup, MerchantReturnPolicy, and sitemap guidance review; focused SEO/UCP/release tests (`6` files / `70` tests); storefront/admin typecheck+lint; root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`400` files / `2813` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm audit --audit-level moderate`, Drizzle/migration/peer/frozen-install/diff guards; admin deploy `79ba753f-40c9-4d24-be87-64d8a169b395`; storefront deploy `dbac8672-2550-4a93-865d-6ae06ce3db89`; live `pnpm release:check --timeout-ms 30000`; and live `pnpm ops:check --queues --samples 1 --timeout-ms 30000` (`ops-check-20260707t115105436z-5d2c1dc5`) pass. |
| SEO-028 | P1 | Verified | SEO Feed/Sitemap Settings Freshness | Follow-up runtime audit found SEO settings saves warmed `/api/product-feed.xml`, `/api/facebook-feed.xml`, and `/sitemap-products.xml` but invalidated only homepage/layout groups, so storefront exact-generation families `feed_products_` and `sitemap_products_` could serve old feed/sitemap policy until TTL or an unrelated product invalidation. The same audit also raised future modeling gaps: product feeds still hardcode `condition=new`, and dashboard SEO readiness needs stronger repair guidance for relative Store URL, dangerous robots rules, and product diagnostic samples. | Slice adds a dedicated `discovery` cache group with `api:seo:`, `global_seo_settings`, `feed_products_`, and `sitemap_products_`, maps `/api/v1/admin/settings/seo` to `homepage/layout/discovery`, and makes direct SEO saves invalidate that group while preserving discovery warm paths. Verified locally with focused API cache/settings tests, API typecheck/lint, root `pnpm test`, root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, and `git diff --check`. Deployed API `4c11dc83-388a-448f-8ab9-47163705c0aa`, admin `79bc236f-a4ed-4080-a6b4-04ef58830129`, and storefront `dea43b2d-5861-4e91-9ad2-7a902eb631a0`. Live `pnpm release:check --timeout-ms 30000` passed SEO policy, robots, six sitemap checks, canonical feed, Meta compatibility feed, UCP discovery, and Product JSON-LD smoke. Follow-up: add explicit product condition modeling or omit condition when not proven, improve Store URL/robots/feed diagnostic repair UX, and keep canonical/condition changes tied to dashboard-owned facts rather than fabricated schema/feed values. |
| SEO-029 | P1 | Verified | Product Feed/Schema Condition Truth | Catalog feeds still emitted `<g:condition>new</g:condition>` without a merchant-owned product fact, while Product JSON-LD intentionally omitted condition to avoid fabrication. Google Merchant guidance says condition is optional for new products but required for used/refurbished products, so the platform needs explicit condition support without silently defaulting existing/unknown products. | Added nullable `products.product_condition`, strict create/update validation for `new`/`refurbished`/`used`, admin product-form control defaulting new saves to `New`, feed projection/API/OpenAPI/SDK support, feed XML emission only from a saved condition, and Product/ProductGroup Offer `itemCondition` only from a saved condition. Existing production rows remain `NULL`, so live feeds do not fabricate `new` until a merchant saves an explicit condition. Verification: focused shared/core/API/admin/storefront tests, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`401` files / `2829` tests), `pnpm build`, env/dist-secret/migration guards, SDK regeneration, API deploy `041e85ca-6568-4f72-9664-7b0bdf0cfe2d`, admin deploy `bd99f2e3-96d4-4d7d-bd59-78ba09c170ed`, storefront deploy `712faa1f-cc50-4a09-8795-6ddefe927f48`, remote D1 schema/count smoke proving `product_condition` exists and `29` current rows are unset, live OpenAPI `productCondition` smoke, live feed sample with no condition nodes, live sitemap index smoke, `pnpm release:check --timeout-ms 30000`, and `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` (`ops-check-20260707t125056151z-4531e3b8`) pass. |
| SEO-030 | P2 | Verified | Admin/SEO Discovery Repair UX | SEO dashboard proof could still be confusing: path-only Store URL values were described as acceptable beside discovery controls, structured-data readiness could look green when URL-bearing schema could not be proven from an absolute dashboard Store URL, Store URL saves did not refresh live proof, and feed diagnostic samples were names without repair links. | Admin SEO status now warns when URL-bearing structured-data families or return-policy schema are enabled without a full absolute `http(s)` Store URL, Store URL copy separates discovery proof from path-only dashboard preview/sidebar navigation, Store URL saves invalidate the SEO live-proof query, and feed diagnostic samples link directly to `/admin/products/{id}/edit` when an ID is available while ID-less samples remain text. Verification: delegated worker patch plus lead review, focused admin tests (`3` files / `25` tests), admin typecheck, admin lint, admin build, `git diff --check`, admin deploy `206012ef-42c2-43f6-afa7-fd46b326e917` at `100%`, `pnpm release:check --timeout-ms 30000`, and `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` (`ops-check-20260707t130514426z-bc8a666f`) pass. No screenshot smoke was captured because the available browser/Playwright tooling was not callable in this thread. |
| SEO-031 | P2 | Verified | Storefront/Feed Taxonomy Truth | Product feeds could emit `Health & Beauty > Health Care` as the Google/Facebook category for every unmapped merchant category, creating false catalog facts for apparel, food, drinks, and other Bangladesh merchant catalogs. Google product category is optional when no exact taxonomy is known, while merchant `product_type` can still carry the store's own category name. | Removed the Health & Beauty default taxonomy fallback; Google/Facebook taxonomy fields now emit only from explicit mappings, and unmapped categories keep only merchant-owned `product_type` when a category name exists. Verification: current Google Merchant product-data guidance review, focused feed route tests (`1` file / `26` tests), storefront typecheck, storefront lint, storefront build, `git diff --check`, storefront deploy `03ddc585-7dfd-496e-8e43-4fa6aeba09c6` with build `src-63697dd7b417f5dc`, live canonical and compatibility feed smokes proving `10` sampled items have merchant `product_type`, zero Google/Facebook taxonomy nodes, and no `Health & Beauty` fallback, `pnpm release:check --timeout-ms 30000`, `pnpm check:dist-secrets`, and `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` (`ops-check-20260707t131214531z-425bb662`) pass. |
| SEO-032 | P2 | Verified | Admin/SEO Discovery Control Clarity | The UCP/feed/sitemap control audit found two remaining merchant-confusing SEO readiness cases: return-policy schema could look independently on even though runtime emits it only through OnlineStore/Product schema prerequisites, and the robots status could warn about missing Store URL even when sitemap advertising was off or sitemap generation was disabled. | SEO status now warns for robots Store URL only when sitemap generation and sitemap advertising both require a canonical sitemap URL, names the no-sitemap runtime outcome when sitemap generation is off, and explains that return-policy facts emit only through OnlineStore and/or Product offers with normal schema prerequisites. The settings copy now says return-policy facts are saved for OnlineStore/Product schema instead of implying a standalone public script. Verification: delegated worker patch plus lead review, focused admin tests (`2` files / `28` tests), admin typecheck, admin lint, admin build, `pnpm check:dist-secrets`, `git diff --check`, admin deploy `18b40d20-6aa8-48c3-aef3-b50270c37509`, live `pnpm release:check --timeout-ms 30000` covering SEO policy, robots, six sitemaps, canonical/compatibility feeds, UCP profile/search/lookup/product, and Product JSON-LD, and live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` (`ops-check-20260707t144309218z-a44c0d2b`) passed with known `worker:testdash` queue warnings. Browser screenshot smoke was not captured because Playwright/browser tooling was unavailable in this thread. |
| SEO-033 | P2 | Verified | Admin/SEO Feed Sitemap AEO Controls | Follow-up SEO/AEO/AIO audits confirmed the dashboard already has strong controls for sitemap XML, product feed XML, robots, canonical/noindex, Product/ProductGroup/OnlineStore/WebSite/Breadcrumb/CollectionPage JSON-LD, return policy, product feed exclusion, and catalog-only UCP visibility. The remaining release-quality gaps were that `/api/v1/seo` release-check failures could silently fall back to strict defaults, product feed XML could publish with fallback layout/currency/media facts if layout reads failed, and the dashboard did not plainly say UCP catalog discovery is default-on with HTTPS. | `pnpm release:check` now fails closed when the public SEO policy endpoint returns non-2xx, invalid JSON, or an unknown shape, with an explicit `--allow-strict-seo-policy-fallback` escape hatch for non-release diagnostics only. Canonical and compatibility feed XML now return non-cacheable `503` before product reads when storefront layout data is unavailable. The SEO settings dashboard now shows UCP catalog discovery as HTTPS/default-on, read-only catalog search/lookup, with checkout/cart/orders/payments hidden. Verification: current official Google Search/Merchant/UCP docs review; parallel admin/runtime/verification audits; focused release/feed/admin tests (`3` files / `67` tests); script syntax checks; admin and storefront typecheck/lint/build; `pnpm check:dist-secrets`; `git diff --check`; admin deploy `ac16b1f5-5318-49e3-8077-6eedcae987bf`; storefront deploy `0857efaf-9205-4899-b039-3bb3afa38575`; live `pnpm release:check --timeout-ms 30000` proving public SEO policy, robots, `6` sitemap checks, canonical/compatibility feeds, UCP profile/search/lookup/product, and Product JSON-LD; and live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` (`ops-check-20260707t150023913z-a4b8bff9`) passed with known `worker:testdash` queue warnings. Follow-up safe catalog image resolution was completed in `SEO-034`. |
| SEO-034 | P2 | Verified | Storefront/Admin SEO Discovery Truth | Feed XML, UCP catalog media, Product JSON-LD/Open Graph images, aggregate feed diagnostics, and per-product admin SEO readiness used overlapping but not identical image/link rules. `pnpm release:check` also validated sitemap XML shape without proving that the sitemap index omitted child sections disabled by the public SEO policy. | Added `@scalius/shared/catalog-discovery-media` for clean `http(s)` origin and safe primary-image resolution, then applied it to canonical/compatibility feeds, UCP catalog media, Product JSON-LD/Open Graph images, aggregate feed diagnostics, and per-product admin readiness. UCP now drops products without safe catalog media, admin readiness requires an actual primary product image instead of a non-primary fallback, Product SEO images no longer use UI placeholder fallback, and release-check fails when `/sitemap.xml` advertises a disabled child sitemap section. Verification: official Google Search/Merchant guidance rechecked on 2026-07-07; focused tests (`8` files / `100` tests); affected shared/core/API/admin/storefront typecheck and lint; API/admin/storefront builds; `pnpm check:env`; `pnpm check:dist-secrets`; `git diff --check`; API deploy `427cc654-6740-433f-9259-9412fb78c25b`; admin deploy `df90aae2-adbf-485a-8647-4c9f4088ed23`; storefront deploy `7504f26a-006b-41a9-97cf-c909933b4990`; live `pnpm release:check --timeout-ms 30000`; and live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` (`ops-check-20260707t152503040z-48ff45cb`) pass with known `worker:testdash` queue-producer warnings. |
| SEO-035 | P2 | Verified | Storefront/Discovery Origin Cache Headers | The stricter release check found that cached `/sitemap-products.xml?page=1` responses were public, but a fresh origin response could become `no-store` when the storefront exact-generation lookup was temporarily unavailable and middleware took the `BYPASS_GENERATION` path. Successful sitemap/feed/robots/UCP discovery assets must keep public cache headers even when the internal edge cache layer is bypassed. | The storefront middleware now applies the public discovery/browser cache policy to any fresh successful public XML/text response in the generation-bypass branch, strips accidental cookies from public discovery responses, and keeps unsuccessful/private responses `no-store`. Verification: focused cache/discovery/UCP/release tests (`6` files / `63` tests), storefront typecheck, storefront lint, storefront build, `pnpm check:env`, `pnpm check:dist-secrets`, storefront deploy `6789c022-fb99-4a78-8114-0392046cb7bf`, live `/sitemap-products.xml?page=1` `Cache-Control: no-cache` probe `20/20` public HIT responses plus `12/12` forced MISS probe responses all returning `public, max-age=3600, stale-while-revalidate=86400`, and live `pnpm release:check --timeout-ms 30000` passing public SEO policy, robots, `6` sitemap checks, canonical/compatibility feeds, UCP profile/search/lookup/product, and Product JSON-LD. |
| SEO-036 | P1 | Verified | Admin/SEO Discovery Freshness | Storefront URL saves were still invalidating only the layout cache group even though the URL controls absolute canonical links, robots sitemap URLs, sitemap/feed XML links, homepage schema URLs, and dashboard discovery proof. That could leave crawler-facing XML/schema on the old origin until TTL or unrelated product/SEO invalidation. | Storefront URL saves now invalidate `homepage`, `layout`, and `discovery`, warm `/`, robots, all sitemap child XML, canonical Google feed, and Meta compatibility feed, and `ADMIN_PATH_TO_GROUPS` maps `/api/v1/admin/settings/storefront-url` to the same groups. The OpenAPI document route now returns `Cache-Control: no-store`, and `ops:check`/`release:check` fail when the live OpenAPI contract drops `/api/v1/admin/analytics/health`, preventing stale API contract proof from silently passing. Verification: focused cache/settings/API release tests, API typecheck/lint/build, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, API deploy `264fd086-ef30-4f65-a798-00f7de7c1d08`, and final live `pnpm release:check --timeout-ms 30000` plus `pnpm ops:check --queues --samples 4 --timeout-ms 30000 --json` after edge propagation. |
| SEO-037 | P2 | Verified | Admin/SEO Structured Data Preview | The SEO settings page could show broad structured-data readiness without clearly explaining which schema families would actually emit and which prerequisites attach return-policy facts to OnlineStore/Product schema rather than a standalone public script. | The SEO discovery status model and card now show compact row-level previews for OnlineStore, WebSite SearchAction, MerchantReturnPolicy, Product pages, and Category/Collection pages, with return-policy messaging tied to OnlineStore/Product prerequisites. Verification: focused admin SEO status/card tests, admin typecheck/lint/build, admin deploy `31f5e1f3-f46b-4a17-b1c6-6bc9fbe6f56c`, and final live release/ops smokes. |
| SEO-038 | P2 | Verified | Storefront/ProductGroup JSON-LD | Product detail reads omitted merchant-defined option labels/schema mappings, so ProductGroup JSON-LD on product pages could fall back to Size/Color even when feeds/UCP used custom axes such as Weight/Style. | Product detail projection now includes and normalizes option labels/schema mappings without bloating normal listing reads; product-page ProductGroup JSON-LD uses one normalized option metadata map. Verification: focused product/storefront boundary tests, root typecheck/lint/test (`405` files / `2879` tests)/build, env/dist-secret/diff guards, storefront deploy `5ecc648e-4362-46a0-a398-079db52e3e2f`, live `pnpm release:check --timeout-ms 30000`, and live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` (`ops-check-20260707t194041192z-06679e48`) pass. |
| UCP-001 | P2 | Verified | Storefront/UCP Catalog Discovery | The platform had no agent-readable commerce discovery surface after SEO/AEO/AIO was hardened, but exposing checkout/payment/order capabilities before a UCP session/security model would weaken existing receipt-proof and payment safeguards. | Added a read-only storefront `/.well-known/ucp` profile that requires an HTTPS storefront origin and advertises only `dev.ucp.shopping.catalog.search` plus `dev.ucp.shopping.catalog.lookup`. `/ucp/catalog/search`, `/ucp/catalog/lookup`, and `/ucp/catalog/product` require a syntactically valid HTTPS `UCP-Agent`, return UCP metadata/messages, map prices to ISO minor units from layout currency, expose merchant option labels, canonical URLs, images, GTINs, SKU availability, and lookup input correlation from the dedicated feed/SKU projection. The API feed projection now safely accepts category/search/price/ID filters for UCP without adding feed-only fields to normal listing reads, and lookup IDs can resolve product IDs, handles/slugs, variant IDs, or SKUs. Verification: official UCP catalog/profile guidance review; parallel read-only SEO/UCP/dashboard-control audits; SDK regeneration (`280` OpenAPI paths); focused storefront UCP mapper/route/feed-boundary tests (`3` files / `10` tests); focused API/core feed boundary tests; root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`397` files / `2774` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm audit --audit-level moderate`, `git diff --check`; deploys to API `f4cc956c-a003-4e4b-8b91-08288853b7e7` and storefront `a890b667-819f-4385-9c5d-ce380180a77d`; live UCP profile/search/lookup/product smokes proving only catalog capabilities, no payment handlers/checkout advertisement, `mojo` search with BDT minor-unit variant price, exact variant lookup input correlation, and product detail with four variants; live API feed filter smoke; `pnpm release:check --timeout-ms 30000`; and `pnpm ops:check --queues --samples 1 --timeout-ms 30000` (`ops-check-20260707t100438943z-5ba45eb2`) pass. Future UCP checkout/cart/order/payment support must use D1-backed sessions and idempotency, avoid receipt proof URLs, and verify signing/profile/payment behavior before advertising those capabilities. |
| UCP-002 | P2 | Verified | Release/UCP Smoke Gate | `pnpm release:check` did not prove the newly shipped UCP discovery surface, so a future release could accidentally advertise checkout/payment capabilities or break catalog search/lookup while normal SEO feed checks still passed. | Release check now always verifies `/.well-known/ucp` on live checks, requires an HTTPS storefront URL, JSON profile, same-origin `/ucp` REST endpoint, catalog search/lookup capabilities only, and no checkout/cart/order/payment capability advertisement. When discovery exposes a safe product candidate, release check performs read-only UCP catalog search and lookup with a valid `UCP-Agent` and asserts exact input correlation; empty catalogs remain valid profile-only passes. Verification: `pnpm exec vitest run scripts/release-check.test.mjs --passWithNoTests` (`21` tests), `node --check scripts/release-check.mjs scripts/release-check.test.mjs`, `git diff --check`, and live `pnpm release:check --timeout-ms 30000` proving catalog search/lookup for `gid://scalius/product-variant/var_default_prod_PBS6FCzizGpV6IzVhRTw8`. |
| UCP-003 | P1 | Verified | Storefront/UCP Feed Truth | UCP lookup/product routes could fall back to broad product-detail reads when the dedicated feed projection returned no row, so feed-excluded or discovery-unready products could remain visible to agents even when XML feeds and dashboard diagnostics excluded them. | UCP search, lookup, and product now depend only on the dedicated feed/SKU projection, return `503 temporarily_unavailable` when that projection cannot be read, defensively drop rows marked `excludeFromProductFeed`, and no longer call product-detail fallback reads for lookup/product. Verification: focused storefront UCP tests plus route tests, focused UCP/cart/post-sale regression suite (`10` files / `64` tests), storefront typecheck, root gates, full deploy to API `b62e892d-2500-4a6a-9061-d1f76ce9a41e` and storefront `8062ecd7-70d2-4ef6-91c0-f9cdfec0dcf0`, live release check UCP profile/search/lookup smoke, and live ops check pass. |
| UCP-004 | P2 | Verified | Storefront/UCP Spec Compliance | Follow-up UCP audit found catalog descriptors were too loose, error messages lacked required severity, batch lookup silently truncated IDs over the local max, and product detail did not expose effective selections or option availability signals for configurable products. | UCP service/capability descriptors now match the current 2026-04-08 REST examples, error messages include severity, over-limit lookup returns `400 request_too_large`, product not-found is a non-cacheable HTTP 200 application error, variant-ID product requests ignore conflicting selected options, duplicate selected names fail fast, and product detail returns `selected` plus option-value `exists`/`available` signals. Verification: current UCP 2026-04-08 overview/catalog REST/search/lookup guidance review; focused UCP catalog/route/release tests (`6` files / `70` tests with the SEO proof suite); storefront/admin typecheck+lint; root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`400` files / `2813` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm audit --audit-level moderate`, Drizzle/migration/peer/frozen-install/diff guards; storefront deploy `dbac8672-2550-4a93-865d-6ae06ce3db89`; live `pnpm release:check --timeout-ms 30000` proving HTTPS UCP profile plus catalog search/lookup/product for `gid://scalius/product-variant/var_default_prod_PBS6FCzizGpV6IzVhRTw8`; and live `pnpm ops:check --queues --samples 1 --timeout-ms 30000` (`ops-check-20260707t115105436z-5d2c1dc5`) pass. |
| UCP-005 | P2 | Verified | Admin/UCP Catalog Readiness QA | UCP is a public discovery surface whenever the storefront origin is HTTPS, but the SEO Discovery QA card did not make that catalog-only agent surface visible to merchants. The live proof also risked treating UCP like a product feed and hiding it when the feed toggle was off, even though runtime UCP would still publish. | SEO Discovery QA now shows a UCP catalog readiness row with `/.well-known/ucp`, catalog search/lookup badges, and explicit "no checkout/payment" copy. The live proof probes the UCP profile independently of the feed toggle, skips only when Store URL is not HTTPS, requests JSON, validates `ucp.version`, a HTTPS `dev.ucp.shopping` REST service, catalog search/lookup capabilities, and warns if cart/checkout/order/payment/payment handler capabilities appear. Verification: current UCP latest spec review; read-only dashboard/runtime-control audit; delegated worker patch plus lead review; focused admin tests (`2` files / `20` tests); admin typecheck, admin lint, admin build, `pnpm check:dist-secrets`, `git diff --check`; admin deploy `23fcbbb2-535e-4c2c-80b0-c684bf7c3f8f` at `100%`; live `pnpm release:check --timeout-ms 30000`; and live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` (`ops-check-20260707t132923239z-028f7743`) passed with known `worker:testdash` unexpected-producer queue warnings. |
| UCP-006 | P2 | Verified | Admin/UCP Merchant Policy | UCP catalog discovery is now visible and release-checked, but there was still no merchant policy switch or explicit dashboard policy copy explaining that catalog-only UCP is default-on whenever the Store URL is HTTPS. | Kept catalog-only UCP default-on as a safe discovery surface and added SEO settings copy that names the policy: HTTPS Store URL exposes read-only catalog search/lookup from feed-ready products only, while checkout, cart, orders, and payments stay hidden until a separately verified UCP transaction model exists. Product feed exclusion/readiness continues to govern catalog contents. Verification is included in `SEO-033`. |
| UCP-007 | P2 | Verified | Release/UCP Known Product Proof | Release-check UCP discovery could still pass when a real product URL was known from sitemap/feed proof but `/ucp/catalog/search` returned no candidates, leaving lookup/product URL correlation unproven. The UCP profile success response also was not held to the same public-cache contract as XML discovery surfaces. | `pnpm release:check` now validates public cacheability for `/.well-known/ucp`; when a known product URL exists, an empty UCP search falls back to that URL and still requires catalog lookup correlation plus product-detail first-variant proof. Storefront UCP route tests also prove unsupported `ucp.version` requests fail before catalog work, and catalog mapper tests prove product URL lookup inputs correlate to the featured variant. Verification: current UCP/latest catalog guidance review, focused tests `pnpm exec vitest run scripts/release-check.test.mjs apps/storefront/src/lib/route-tests/ucp.test.ts apps/storefront/src/lib/ucp/catalog.test.ts --passWithNoTests` (`3` files / `49` tests), combined cache/discovery/UCP release tests (`6` files / `63` tests), `node --check scripts/release-check.mjs scripts/release-check.test.mjs`, storefront deploy `6789c022-fb99-4a78-8114-0392046cb7bf`, and live `pnpm release:check --timeout-ms 30000` proving UCP profile/search/lookup/product for `gid://scalius/product-variant/var_default_prod_PBS6FCzizGpV6IzVhRTw8`. |
| UCP-008 | P2 | Verified | Release/UCP Payment Handler Guard | The UCP profile release guard rejected checkout/cart/order/payment capabilities but did not explicitly fail a future root or `ucp`-scoped `payment_handlers` field, which would imply transaction support before a D1-backed UCP session/signing/payment design exists. | `pnpm release:check` now rejects `payment_handlers` both at the profile root and under `ucp` while preserving the catalog-only search/lookup profile contract. Verification: focused release-check tests, script syntax/build guards through API/admin gates, and final live release/ops smokes proving UCP remains catalog-only. |
| ADMIN-015 | P1 | Verified | Admin/Post-sale UX | Verified admin order detail can blank the whole workspace when payment history query fails because `PaymentCard` uses suspense under the page-level error boundary. | `PaymentCard` now reads payment history with non-suspense `useQuery`, keeps loading/error/retry UI inside the card, and leaves order status, shipment, and item panels usable. Verified with route-boundary/order-prefetch tests, admin/root typecheck/lint/test/build, env/dist/migration/diff guards, admin deploy `c4d49c8b-4d18-46a1-a526-0eaa37c0cdd2`, and live order detail smoke for `1HBUHH` with payment summary, shipment, items, and no console/page errors. |
| CUST-002 | P1 | Verified | Customer/Post-sale UX | Receipt fetch failure redirected customers to `/`, and there was no customer-scoped order detail/timeline page for tracking/payment/refund/notification milestones. | Order-success recovery state, customer-scoped detail/timeline endpoint/page, SDK regeneration, root gates, full deploy, and live read-only smoke verified. |
| AUTH-012 | P1 | Verified | Admin/Auth Settings UX | Auth & Access and storefront customer auth did not model sign-in vs sign-up intent, independent OTP channels, or email collection separately from mandatory phone collection. Legacy `phone` also mislabeled SMS as WhatsApp and hid real `whatsapp_otp`. | Shared customer-auth policy enforces mandatory phone collection, supports independent Email/SMS/WhatsApp OTP channels plus email collection mode, normalizes legacy summaries, uses channel-scoped OTP challenge keys, pins accepted sign-up contact fields on the OTP attempt, and the storefront sends explicit `sign_in`/`sign_up` intent with duplicate/unknown-account UX. Focused shared/core/API/storefront tests, API/admin/core/shared/storefront typechecks, and SDK regeneration pass. |
| AUTH-014 | P1 | Verified | Storefront/Customer Auth UX | Customer account login could reload/flash indefinitely because AuthModal emitted `customer-login` during passive session hydration and `/account` responded with `window.location.reload()`; signup profile setup also used native city/zone selects instead of searchable delivery-location UX. | AuthModal now emits login events only after active OTP/profile completion, `/account` refreshes in place, profile setup uses shared `LocationSelector`; focused tests/typechecks/lint, deploy, live `/account` modal/OTP/account reload smoke, and Worker-tail session/order reads pass. |
| ORDER-020 | P1 | Verified | Storefront/Checkout Orders | Public order creation could make buyers wait on queued ingest polling before payment handoff, making the last checkout/payment step feel very slow under cold cache or queue latency. | `/api/v1/orders` now commits the D1 order synchronously before returning `201`, keeps post-commit COD/notification work in `waitUntil()`, and leaves the storefront 202 polling branch as legacy compatibility only; focused tests/typechecks/lint/SDK regeneration, API/storefront deploy, live OpenAPI `201`, and live tail order-to-SSLCommerz session smoke pass. |
| ORDER-021 | P1 | Verified | Admin/Orders/SKU Validation | Read-only audit found admin order create/update can accept an untracked variant ID without proving it belongs to the submitted product or that the parent product/variant is active and non-deleted; untracked SKUs then skip reservation and can bypass `reserveStockBatch()` sellability guards. | `resolveAdminOrderItemInventory()` now validates every manual order line against a concrete live SKU joined to its parent product before create/update inventory work starts, returns trusted `inventoryTracked` only after product/SKU ownership and active/deleted checks, and reports structured `itemIssues` for missing, deleted, mismatched, or inactive rows. Focused SKU/create-order fail-fast tests, update-order atomicity fixture coverage, root typecheck/lint/test/build/env/dist/diff gates, API deploy `e467e230-2fe7-42a8-b04a-45869224015e`, live `/api/v1/health`, OpenAPI/admin-orders, unauth admin fail-closed, and deployed bundle marker smokes pass. |
| CHECKOUT-010 | P1 | Verified | Admin/Storefront Checkout Settings | Parallel customer-flow audit verified partial-payment settings could publish COD-only/no-gateway checkout, gateway allowlists could silently fall back to COD, stale session gateway snapshots could survive payment settings changes, and low-value carts could still send invalid deposit requests. | Shared checkout-flow validator, public checkout config flow filtering, explicit payment allowlist fail-closed behavior, admin checkout-flow preview/query-key isolation, gateway-disable guards, stale gateway snapshot intersection, and storefront full-vs-deposit resolver. Verified with focused checkout/payment/settings tests, root typecheck/lint/test/build/env/migration/dist-secret gates, API deploy `3d279336-3c87-476e-b44a-4d28b7177572`, admin deploy `5adfc6e7-8b8c-45bb-9115-4a8179a60be9`, storefront deploy `070035f3-46ef-4024-836b-5f4619a98d2a`, live checkout config showing partial payment with only online gateways, live cart/checkout HTTP smoke, and deployed storefront/admin asset checks. |
| CHECKOUT-011 | P1 | Verified | Checkout/Hosted Payments UX | Hosted payment failure/cancel/timeout/session-setup flows could strand reserved incomplete orders without obvious buyer or operator recovery. | Buyer and operator recovery are now verified: SSLCommerz/Polar fail/cancel receipt recovery, provider-deadline handling, in-flight payment-session UX, authenticated retry/pay-balance, hosted cart consumption, empty-cart recovery marker, hash-backed receipt proof authority, gateway rotation, and admin operator recovery-link issuance. `POST /api/v1/admin/orders/{id}/payment-recovery-link` is `orders.edit` gated, SSLCommerz/Polar-only, verifies incomplete unpaid/failed evidence, no paid amount, no active shipment/session lease, no unsafe payment rows, and failed local evidence before minting one D1 hash-backed private receipt URL; it performs no provider calls or queue work. Verification: parallel audits/workers, SDK regeneration (`275` paths / `373` operations), focused tests (`8` files / `34` tests), root typecheck/lint/test (`337` files / `2287` tests)/build/env/migration/dist-secret/diff guards, API deploy `d3ae5e52-d5ae-4af6-8fd0-723ca9a211b2`, admin deploy `dfe9d8ce-f8dc-4246-9b33-b7a72dbd4100`, live API health/readyz/OpenAPI/401 smokes, dashboard redirect smoke, and deployed admin asset marker smoke. |
| CHECKOUT-012 | P1 | Verified | Admin/API Checkout Settings | Checkout modes could publish with no compatible payment method: `gateways_only` without an online gateway and `guest_cod_only` without COD. | Shared checkout-flow validator now checks mode-compatible payment methods; admin/API save routes reject broken modes; focused settings/payment tests, root gates, deploy, and live checkout-config smoke pass. |
| CHECKOUT-013 | P1 | Verified | Storefront/API Delivery | Storefront order creation accepts stale or forged city/zone/area IDs instead of validating active delivery-location hierarchy. | Existing `CHECKOUT-017` path already fresh-validates active, non-deleted city→zone→area type/parent invariants before checkout policy/rate limits/attempt claim/KV/commit and stores D1 names instead of caller labels; this slice also made prevalidated cart/delivery fast-path objects carry hidden runtime proofs so forged plain objects fail closed. Focused core/API tests, root gates, local API doctor, API deploy `63534405-85ac-4dae-8162-c3497fcd6642`, live health/OpenAPI/admin-auth/checkout-config smokes, and deployed bundle marker checks pass. |
| CHECKOUT-014 | P2 | Verified | Admin/Storefront Checkout UX | Checkout health ignored missing active shipping methods and usable active city-to-zone delivery hierarchy, and delivery-location/shipping destructive actions could silently remove the last usable checkout chain. | Shared checkout-readiness helper, public checkout fail-closed config, admin readiness endpoint/panel, API guards for last active shipping method and city→zone chain removal, SDK regeneration, full root gates, full deploy, and live checkout/cart/OpenAPI smokes verified. |
| CHECKOUT-015 | P1 | Verified | Checkout/Phone Policy | Allowed-country phone restrictions were enforced in browser pickers but not at checkout/customer-auth API boundaries, despite admin copy saying checkout and customer registration are restricted. | Shared include/exclude validator now backs checkout and customer-auth policy reads; API/customer-auth/profile paths reject disallowed countries before OTP/order mutation. Focused/root gates, API deploy `421f8b05-0413-491e-894c-9b500319e1a2`, live customer-auth rejection, and live storefront checkout-proxy rejection pass. |
| CACHE-017 | P1 | Verified | Storefront/Cache | Cold SSR storefront pages could cache backend failure/empty fallback HTML as successful 200 responses on home/search/category pages; product sitemap/feed routes could also publish empty/partial XML when product reads failed. | Required `/`, `/search`, and `/categories/{slug}` data failures now return non-cacheable `503` unavailable responses; `getAllProducts()` returns `null` on SDK errors/malformed envelopes instead of fabricated empty lists; product sitemap, sitemap index, and Meta/Facebook feed routes return `503 no-store` on failed/partial product reads while preserving legitimate empty catalogs. Verification: focused storefront tests (`6` files / `16` tests), storefront/root typecheck with zero Astro hints, storefront/root lint, root test (`276` files / `1770` tests), root build, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, storefront deploy `26b642a8-f7d7-42ab-a103-369080228121`, and live smokes for `/`, `/search`, `/categories/drinks`, `/sitemap.xml`, `/sitemap-products.xml?page=1`, and `/api/facebook-feed.xml?limit=1`. |
| CACHE-018 | P2 | Verified | API/Storefront Cache | Homepage and CMS page render endpoints were invalidation-grouped but explicitly `no-store`, so cold/new-POP renders bypassed API KV before storefront HTML cache warmed. | `/api/v1/storefront/homepage` now uses `api:storefront:homepage:` API KV caching, `/api/v1/storefront/pages/slug/{slug}` now uses `api:storefront:page:` API KV caching, and the storefront CMS page client no longer sends a forced `Cache-Control: no-cache` request header. Added direct route tests for homepage/page HIT behavior, 404 no-write behavior, and category/collection invalidation coverage around homepage data. Verification: focused API cache suite (`67` files / `557` tests), affected API/storefront typecheck/lint, root typecheck/lint/test (`298` files / `1964` tests), root build, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, API deploy `c89f5766-49a1-4919-be00-fe84240fc39e`, storefront deploy `80c68c87-8878-432f-8a30-199951a5ddd1`, live homepage API `HIT` with public revalidation headers, live CMS page render `MISS` then `HIT` after KV write settling, and live storefront `/` + `/home` HTTP smokes. |
| PERF-017 | P1 | Verified | Storefront/Homepage Collections | Category-backed homepage collections could load broad product sets and then slice in memory, making cold/new-POP homepage renders too sensitive to catalog/category size. | Collection assembly now executes bounded per-category product reads with deterministic ordering and keeps the per-collection merge/slice behavior, avoiding unbounded category fetches for homepage data. Focused core collection tests, root gates, storefront deploy/warm-path smokes, and live ops check listed in `AUTH-050` pass. |
| AUTH-015 | P1 | Verified | Storefront/Checkout Auth | Direct create-order and stale hosted-payment requests could bypass browser-only guest/auth, checkout-mode, payment allowlist, partial-payment, and cart-shape restrictions. | API order creation now fresh-checks guest checkout, current checkout mode, active payment methods, partial-payment flow, valid customer session/phone match, and item quantity bounds before rate limiting or order mutation; storefront forwards the HttpOnly customer session server-side only; payment-session routes reuse fresh checkout-flow settings before gateway work and reject full payment when deposit mode is required. Focused tests, deploy, and live smokes pass. |
| AUTH-016 | P1 | Verified | Customer/Auth Providers | Verified SMS OTP could present as sent even when no SMS provider was configured or readable, and WhatsApp OTP policies could be saved before provider readiness. | Customer auth now preflights SMS readiness before OTP challenge mutation; auth settings reject SMS/WhatsApp policies without ready provider state; WhatsApp provider reads/saves treat obvious dummy token/phone/template values as not configured; admin Auth & Access surfaces exact readiness blockers. Focused core/API/admin tests, SDK regeneration, root gates, deploy, and live OpenAPI/admin/storefront smokes pass. |
| AUTH-017 | P1 | Verified | Storefront/Customer Auth Cookies | Customer-auth proxy could collapse multiple `Set-Cookie` headers from OTP verification, causing login to appear successful while header/cart/account still behave as guest. | API now has focused coverage proving OTP verification emits both `cs_tok` and `cs_auth`; storefront proxy extracts cookies with `getSetCookie()` or a combined-header splitter, strips backend `Domain`, rewrites `SameSite=None` to `Lax`, and appends each cookie separately. Focused/root gates, API/storefront deploy, and live customer-auth proxy smokes pass. |
| AUTH-018 | P1 | Verified | Storefront/Customer Profile | New customers can skip profile completion permanently after OTP creates a session cookie, `/me` does not return saved address/location data for cart/account hydration, and `customer-login` events omit the delivery profile just collected. | D1 profile-completion flags, canonical `/me` profile projection, AuthModal profile-setup resume, full `customer-login` payloads, and cart/account hydration are implemented, tested, migrated, and deployed. API `94c479af-a4a4-4b97-a942-149520cb0b03`; storefront `d7fbc578-2c27-4cae-9825-80ea55610125`. |
| AUTH-019 | P1 | Verified | Customer/Auth OTP Integrity | Customer OTP verification could reinterpret the method/contact at verify time instead of binding to the contact accepted when the OTP was issued. | OTP challenge rows pin normalized request `method` and `identifier`; verify rejects invalid email/phone identifiers and mismatched stored method/contact before account mutation. Focused/root gates, API deploy, and live tampered-verify smoke pass. |
| AUTH-020 | P1 | Verified | Customer/Auth Identity | Email sign-in uses non-unique checkout-provided customer email as an account key, and soft-deleted customers/sessions are not filtered from customer auth paths. | Customer OTP verification now resolves sign-in against active customers only, requires exactly one active email match, rejects ambiguous active-email matches without creating a session, gives support-restore guidance for soft-deleted email/phone matches, blocks soft-deleted phone reuse on sign-up, and keeps customer session/profile reads filtered by `deletedAt IS NULL`. Storefront auth UI no longer offers misleading sign-in/sign-up alternate CTAs for ambiguous/deleted-support errors. Verification: focused core customer-auth tests (`27`), focused storefront auth UI tests (`7`), root typecheck/lint/test (`264` files / `1695` tests), root build, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, API deploy `1ba588dc-aaf5-43dc-b61a-66946e614477`, storefront deploy `83f4d42a-cdfd-4025-9b78-9ec119fa34c0`, live API health `200`, live storefront `/` `200` with build `src-76f85a42300cd5d4`, unauthenticated `/api/customer-auth/me` no-store JSON, and deployed AuthModal asset marker for the fail-closed ambiguous/deleted-account handling. |
| AUTH-021 | P1 | Verified | Customer/Auth Identity | Independent audits found the original checkout concern was partly stale: public guest checkout no longer creates customer rows, failed checkout attempts do not create profiles, and authenticated checkout attaches `orders.customerId` only from a live matching customer session. Remaining identity hardening is explicit account-proof state plus a future receipt-token-backed guest-order claim model. | First slice adds nullable `customers.account_claimed_at`, `phone_verified_at`, `email_verified_at`, and `last_authenticated_at`, updates them only after OTP proof, keeps collected secondary contacts unverified, and batches sign-up customer/session persistence. Guest orders remain receipt-token-only until a dedicated claim endpoint exists. Verification: focused customer-auth tests, core/database/API/admin/root typecheck/lint/test/build gates, remote migration `0070`, remote `PRAGMA table_info(customers)` proving all four account-proof columns, API deploy `69a6a796-85a8-445b-a999-86da0f47a11b`, and live API health/readyz smokes. |
| AUTH-022 | P1 | Verified | Storefront/Checkout Identity | Authenticated checkout could resolve the order owner from submitted phone when guest checkout was enabled, so stale/mistyped phone data could attach COD/full/partial orders to another customer or make them disappear from the signed-in account. | API checkout policy now resolves customer sessions even when guest checkout is enabled, rejects signed-in phone mismatches, returns `CUSTOMER_SESSION_STALE` for expired forwarded customer sessions, and passes authoritative session `customerId` into the order builder. Storefront checkout proxy clears stale customer cookies on that code. `createStorefrontOrder()` no longer looks up customers by submitted phone; true guests build `existingCustomer: null`, and commit fresh-reads active customers by session id before attaching `orders.customerId`. Focused API/core/storefront tests, affected and root typecheck/lint/test/build, env/dist/migration/diff guards, API deploy `38d80904-2f64-4830-b155-adb94a4d6c48`, storefront deploy `380a1160-4b7c-4a18-ae9c-8dc5cd546160`, and live API/storefront smokes pass. |
| AUTH-023 | P1 | Verified | Admin/Auth Provider Readiness | Email OTP can be enabled in Auth & Access without proving the selected transactional email provider/sender is usable, so customers may request OTPs that cannot be delivered. | Shared Email readiness helper now requires saved sender plus Cloudflare `EMAIL` binding or decryptable Resend key; admin saves and buyer OTP sends fail closed before writes/challenge mutation; Auth & Access surfaces Email OTP readiness beside SMS/WhatsApp. Focused/root gates, SDK regeneration, deploys, and live smokes pass. |
| AUTH-024 | P1 | Verified | Storefront/Auth UX | AuthModal is `client:idle`, so early `open-auth-modal` events from cart/account actions can be lost before hydration and make login/signup feel broken. | Layout now installs a tiny inline `open-auth-modal` buffer before the idle AuthModal island, and AuthModal consumes/clears `window.__scaliusAuthModalOpenPending` after registering its hydrated listener. Focused/root gates, storefront deploy `763bffdf-00f2-4add-b38c-df6e0cd76c42`, and live storefront marker smoke pass. |
| CHECKOUT-023 | P2 | Verified | Storefront/Checkout API UX | Storefront checkout order proxy collapses backend create-order failures into `400`, losing `401`, `409`, `429`, and `503` semantics that the customer UI could use for re-auth, duplicate-submit, throttle, or temporary-unavailable states. | Storefront create-order proxy preserves backend status, `errorCode`, and `details`; `CheckoutOrderError` carries status/error code through COD/Stripe/SSLCommerz/Polar handlers; checkout UI uses buyer-safe fallback copy for stale auth, duplicate/changed checkout keys, throttling, and temporary checkout unavailability while keeping specific backend copy. Verification: focused checkout proxy/helper/handler tests (`5` files / `32` tests), storefront typecheck/lint, root typecheck/lint/test (`270` files / `1756` tests), root build, `check:env`, `check:dist-secrets`, `git diff --check`, storefront deploy `dff6501e-4c02-4283-a95a-2ced57cf042c`, live `/health`, `/cart`, `/checkout`, and cross-origin create-order 403 smokes. |
| ADMIN-016 | P1 | Verified | Admin/Post-sale UX | Verified admin order detail lacked complete card-local resilience and operator visibility for payment/refund/shipment/notification partial failures; one failing secondary read could hide unrelated order work. | Payment history is card-local, refund attempts/recovery state are visible on admin order detail/payment history, conflicting order actions lock with operator copy while recovery is active, order detail has a card-local notification delivery panel with durable outbox/receipt visibility plus safe retry for failed/pending parent rows, and shipment/provider recovery is exposed through a sanitized `shipmentRecovery` summary on order list/detail. Admin desktop/mobile order rows and detail panels show shipment recovery badges/notices, route recovery rows to detail, block edit/status/delete/refresh/bulk delete/bulk ship/manual fulfillment/provider shipment creation while an active shipment lock exists, allow retry copy for failed provider rows that are no longer locked, and now expose an RBAC-gated `Repair shipment` action for `reconcile_required` provider-success/local-finalization failures. The repair endpoint reconciles order status, inventory, shipment status, cache invalidation, and buyer status notification from persisted shipment evidence without calling the provider again, then clears only the matching claim. |
| ADMIN-017 | P2 | Verified | Admin/Orders Delivery Locations | Admin manual order create/update resolved delivery-location names from non-deleted rows but could still fall back to caller-provided names and did not enforce active city→zone→area parent/type invariants. | `delivery-location-validation.ts` now shares the active city→zone→area resolver between storefront preflight and admin manual order create/update; admin order writes store canonical D1 names, ignore forged submitted labels, and reject inactive/deleted/wrong-type/cross-parent IDs before customer/order/inventory writes. Focused/root gates, API deploy `1f327db4-a571-476f-8a92-5d02989d8643`, and live health/readyz/admin-auth smokes pass. |
| ADMIN-018 | P1 | Verified | Admin/Payment Recovery UX | Merchants needed a dedicated server-backed recovery queue/export and recovery-safe bulk/list behavior so hosted-payment recovery did not stay hidden behind order detail. | `GET /api/v1/admin/orders` supports `paymentRecovery=recoverable|awaiting_payment|processing|needs_attention`, while dedicated `/payment-recovery` and `/payment-recovery/export` endpoints expose a sanitized server-backed queue/export mapped to `orders.view`. Admin orders render recovery badges, route recovery rows to order detail, use server-backed recovery CSV export, block bulk shipment for selected recovery rows, and abandoned-checkout archived hosted-payment records link to the source order with RBAC-gated delete controls. Verification: focused RBAC/API/admin/source tests, SDK regeneration, root typecheck/lint/test/build/env/dist/migration/diff gates, API deploy `958019be-d29b-43f4-8ff4-7ce103ab2ae3`, admin deploy `d255be55-2747-464a-b997-f6216282b304`, live OpenAPI `271` paths / `369` operations, live direct/proxied recovery/export reads, and canonical `/admin/orders?paymentRecovery=recoverable` page smoke. |
| ADMIN-019 | P1 | Verified | Admin/Orders Atomicity | Admin order edit can update the `orders` scalar row before item replacement succeeds, leaving committed order totals/customer/status fields with old items if delete/reinsert fails. | `updateOrder()` now builds one guarded D1 `safeBatch()` for optional new-customer insert, the CAS order update, guarded replacement item inserts, and old-item deletion. Replacement writes require the immediately preceding successful order edit through SQLite `changes() = 1` plus committed order-version existence, old item deletion targets the previous item IDs after inserts, CAS misses still surface the existing conflict, and inventory compensation remains intact. Focused/root gates, API deploy `bd4ff09f-be8f-4a52-aa49-99740d8448a7`, and live health/readyz/admin-auth smokes pass. |
| ADMIN-020 | P1 | Verified | Orders/Status Inventory Consistency | Several status/refund/shipment flows commit visible order status before inventory reconciliation, so merchants/buyers can see shipped/cancelled/returned while stock counters or `inventoryAction` remain stale after a side-effect failure. | Carrier shipment status sync now marks the shipment `reconcile_required` if the order-status CAS wins but inventory reconciliation fails, and a successful same-status retry clears the marker back to the carrier status after repairing `inventoryAction`. Generic admin status changes, payment return processing, and COD collection/return now use guarded rollback or retryable recovery semantics when inventory/evidence work fails before `inventoryAction` changes. Focused/root gates, API deploys `45adc4bf-444a-431d-bd85-d00e923d4bab`, `4a571118-13ba-42c8-8c33-d1e63dbf1af3`, `1aaa87ea-3646-403d-92fa-c79f9e096a83`, `1306c5d2-24d9-4dca-a43a-d41db7717d8d`, and `08274388-bb9e-4d9d-aea7-d76267b54aa2`, live health/readyz/admin-auth smokes, and live OpenAPI status/shipment/return/COD route smokes pass. |
| ADMIN-021 | P2 | Verified | Admin/Orders RBAC UX | Order list/detail UI rendered status/create/delete/restore/bulk-ship/shipment/COD/notification retry actions for users with only `orders.view`, relying on API 403s after click. | `useOrderActionPermissions()` now maps API order permissions once for list/detail UI, view-only users keep inspect/export/history but lose mutation affordances, and mutation handlers fail closed if stale UI props leak a click. Verification: focused permission/list/detail tests (`3` files / `14` tests), root typecheck/lint/test (`301` files / `1982` tests), admin build, dist-secret and diff checks, authenticated local Chrome orders smoke with no console errors, admin deploy `c2a6d70d-042f-47e1-9744-1839ed4a2579`, live API health `200`, live login `200`, anonymous orders guard `307`, and authenticated demo orders route `200`. |
| ADMIN-022 | P2 | Verified | Admin/Orders State UX | Admin status-transition UI duplicated stale transition lists and omitted backend-valid paths such as `confirmed -> delivered` and `shipped -> confirmed`. | Canonical order/payment/fulfillment transition maps now live in `@scalius/shared/order-state`; core wraps that shared map for API validation and admin re-exports the same order helper through `orderview/types.ts`. Verified transitions include `confirmed -> delivered` and `shipped -> confirmed`. Direct admin status writes and full order edits now normalize status casing/whitespace before persistence, inventory reconciliation, and notification dedupe, and unknown statuses fail before DB work. Verification: shared/core/admin/api typecheck/lint, focused order-state/order-fulfillment/admin-boundary tests, root typecheck/lint/test/build/env/dist/diff gates, API deploy `01a0e619-7c27-4a2f-b362-5a89029d35a1`, admin deploy `219a8763-3c1d-482a-a774-4cf60b16c730`, live API health/readyz, live admin redirect smoke, and live unauthenticated status mutation `401` in ~40ms. |
| ADMIN-023 | P2 | Verified | Admin/Orders Bulk Shipping | Bulk ship UI looped one shipment request per selected order despite an existing aggregate bulk API. | Admin orders now call `POST /api/v1/admin/orders/bulk-ship` once per selected batch through a typed server function/mutation, keep existing RBAC/payment/refund/shipment blockers, clear selection only for successful per-order results, keep failures selected, and show compact per-order failure details in the lazy bulk dialog. The route no longer imports or loops `createOrderShipment` for bulk work and does not trust bulk `shipment` wrapper shape for local status state. Verification: focused admin tests (`2` files / `20` tests), admin typecheck/lint, root typecheck/lint/test (`337` files / `2290` tests)/build/env/migration/dist-secret/diff guards, admin deploy `679cb167-fcec-4e20-9f6c-82b6a737be1a`, live admin route guard, live API `/bulk-ship` unauthenticated `401`, live OpenAPI route smoke, and deployed asset markers for the result surface with no old selected-order loop. |
| ADMIN-024 | P1 | Verified | Admin/Orders Filters | Admin order query types include `paymentStatus`, `paymentMethod`, and `fulfillmentStatus`, but params/API/service dropped them, so merchants could not filter paid-unshipped, COD pending collection, or gateway-specific queues. | Added first-class URL/search, toolbar, server-function, OpenAPI, and core SQL filters; rendered fulfillment badges in desktop/mobile rows; regenerated SDK; added measured D1 indexes `0058`/`0059` for combo and single-filter queues. Verification: focused boundary tests, root tests (`254` files / `1609` tests), affected typecheck/lint/build, env/dist/migration/diff guards, local desktop/mobile Chrome smokes with authenticated API fetch, remote migrations, API deploy `2ba4a6fe-96b8-4d6f-b079-4d2a22174177`, admin deploy `0f0e9d64-c0fe-4408-8ec0-9d24d363cee0`, live OpenAPI smoke, remote D1 `EXPLAIN QUERY PLAN` proving all five new indexes, and live desktop/mobile dashboard smokes with no console errors, no failed requests after settled auth, and no horizontal overflow. |
| ADMIN-025 | P2 | Verified | Admin/Product Edit Hydration | Local Chrome product-edit smoke emitted a React hydration mismatch around form input empty `style` attributes and Radix Switch bubble-input style normalization after SSR. | Clean local and live product-edit browser smokes did not reproduce an `Input`/`Switch` wrapper drift, but dnd-kit sortable wrappers did serialize empty `{ transform, transition }` style objects. Sortable inline style generation is now centralized through `getSortableStyle()`, which omits empty idle styles across admin sortable surfaces and preserves drag-time transform/transition/opacity only when needed. Verified with helper unit tests (`3` tests), admin typecheck/lint/build, env/dist-secret checks, `git diff --check`, local product-edit browser console smoke, admin deploy `19bf07f5-8905-47b4-923e-27e71c4465e3`, and live product-edit console smoke for `prod_PBS6FCzizGpV6IzVhRTw8` with zero errors/warnings/request failures. |
| ADMIN-026 | P2 | Verified | Admin/Account Settings UX | Profile edit mode could hide or strand save/cancel actions because media/photo controls shared the same action row, making username/display-name updates feel broken. | Profile edit actions now live in a persistent top action cluster beside the display-name field, while the media row is photo-only; save/cancel stay reachable during editing, cancel restores the saved user data, saves use the API-returned user when available, and route-context refreshes do not overwrite an in-progress edit. Verification: focused happy-dom `ProfileHeader` interaction tests, account settings source-boundary test, admin/root gates, admin deploy `c4fbe55b-daee-400f-9450-988d9c90e6f1`, local and live account-settings browser smokes proving save/cancel/photo controls remain visible, cancel restores view state, no horizontal overflow, and zero console warnings/errors; screenshots `/tmp/scalius-local-profile-edit-controls-lead.png` and `/tmp/scalius-live-profile-edit-controls-lead.png`. |
| ADMIN-027 | P2 | Verified | Admin/Resume Navigation UX | Idle-tab resume and route changes could feel stuck because default React Query retries multiplied bounded admin read timeouts, order-detail loaders waited on optional panels, and nested scroll restoration had two competing owners. | Admin query retries are now opt-in, order detail gates only on the primary order while shipments/payments/providers/currency warm in the background, and nested admin scroll uses TanStack Router for normal top reset plus a history-traversal-only restore supplement. Verification: focused admin query/route-boundary/order-prefetch tests (`3` files / `36` tests), admin typecheck/lint, `git diff --check`, and local Chrome smoke proving `/admin/products -> /admin/categories -> Back` resets categories to top and restores products scroll from `264` to `264`. |
| ADMIN-028 | P2 | Verified | Admin/Product Description UX | Product edit description flickered because the deferred rich-text field rendered an HTML preview and then auto-mounted the heavy Tiptap editor after idle/viewport preload, swapping the surface even when the merchant had not interacted with it. | `DeferredTiptapEditor` now still preloads the Tiptap chunk on idle/intersection but mounts the editor only on direct focus or pointer interaction, preserving the no-extra-edit-button UX without a load-time swap. Verification: admin typecheck/lint/build/root gates, admin deploy `6dee4b50-2f7d-4bc9-b73d-a145ab9e6c45`, and live Chrome product-edit smoke proving the description preview remains stable after idle with `0` `.ProseMirror` editors and mounts exactly one editor after click. |
| ADMIN-030 | P2 | Verified | Admin/Product Rich Text Flicker | Follow-up product-edit smoke found rich-text activation could still visibly jump because the deferred preview shell was shorter than the compact Tiptap toolbar/editor surface. Earlier first-interaction loading skeleton and one-frame placeholder flickers were already fixed. | `DeferredTiptapEditor` waits for `loadTiptapEditorModule()` before mounting, keeps Tiptap out of route chunks, and now reserves the final compact/non-compact editor footprint in preview/loading shells so first focus swaps controls without changing page height. Verification: focused route-boundary test from the prior slice, admin typecheck/lint/build, local Chrome product-edit smoke for `prod-test-2` showing preview/editor height `237px` and tab panel `261px` before/after, screenshot `/tmp/scalius-product-description-editor-stable.png`, admin deploy `8ff3def6-dae4-4507-a1e9-f7ab01ed9762`, and live product-edit smoke for `prod_PBS6FCzizGpV6IzVhRTw8` with `0 -> 1` `.ProseMirror`, `22` toolbar buttons, unchanged heights, and no console errors. |
| ADMIN-031 | P2 | Verified | Admin/Rich Content Media Flicker | Live cold-cache product-edit measurement for the screenshot product showed the editor shell was stable, but the transformed rich-content image briefly sampled at `0x0` during Cloudflare image redirect/canonical CDN resolution because the stored HTML had no intrinsic dimensions. | Shared `sanitizeHtml()` now derives missing image `width`/`height` attributes from Cloudflare `/cdn-cgi/image/...width=...,height=.../` transform URLs while preserving merchant-provided dimensions and avoiding invented dimensions for generic images. Verification: shared sanitizer tests/typecheck, admin typecheck/build, dist-secret and diff checks, admin deploy `a7eb1418-df97-47de-9c4e-9842c0c954fb`, and live product-edit Chrome smoke for `prod_abe_OILKCt75z47ppI1Sd` showing `width="600"`, `height="600"`, `0` `.ProseMirror` editors, `70/70` image samples at `283x283`, no zero-size collapse, and zero console warnings/errors. |
| ADMIN-032 | P2 | Verified | Admin/Product Rich Text Activation | Post-deploy smoke found the product description no longer flickered on page load, but first focus could still grow the compact editor because the preview viewport was taller than the intended compact Tiptap scroll area after image content loaded. | Compact deferred rich-text preview/loading shells and mounted Tiptap now share a 200px scroll viewport while preserving lazy Tiptap loading and direct focus-to-edit. Verification: focused admin route-boundary test (`36` tests), admin typecheck/lint/build, root typecheck/lint/test (`305` files / `2010` tests), env/dist/migration/diff guards, admin deploy `1d267373-ee44-4204-b2df-feb13ae121c5`, live product-edit smoke for `prod_abe_OILKCt75z47ppI1Sd` showing six stable preview samples at shell `237px`/panel `261px` with `0` `.ProseMirror`, then mounted editor wrapper `236px`/panel `260px`, 200px scroll area, one `.ProseMirror`, reserved `600x600` image attrs, zero console warnings/errors, and screenshot `/tmp/scalius-product-editor-postdeploy.png`. |
| ADMIN-033 | P2 | Verified | Admin/Product Rich Text Activation | Follow-up cold-cache measurement confirmed page-load preview is stable, but first lazy editor activation could scroll the page because Tiptap auto-focused the end of a long description and scrolled that caret into view. | Activation autofocus now uses Tiptap's `scrollIntoView: false` option so direct click-to-edit keeps focus without moving the page viewport. Verification: focused route-boundary test (`37` tests), admin typecheck/lint, diff check, admin deploy `43e720f7-9ee8-451a-9334-cf251263b202`, and live product-edit measurement for `prod_abe_OILKCt75z47ppI1Sd` with cache disabled showing load shell fixed at `237px`, `0` zero-size images, activation viewport fixed at `200px`, one `.ProseMirror`, and `scrollY` stable at `0` before and after activation. |
| ADMIN-034 | P2 | Verified | Admin/Product Rich Text Toolbar | Follow-up screenshot showed the product description body could become editable while the toolbar still looked like a placeholder row, because the deferred wrapper depended on visibility/idle scheduling before mounting the real editor. That made the field look broken even when content eventually loaded. | `DeferredTiptapEditor` still lazy-splits the heavy Tiptap module, but now requests the real editor immediately after field hydration and only uses direct interaction to add autofocus. The idle/IntersectionObserver gate was removed and guarded in source tests. Verification: focused route-boundary test (`37` tests), admin typecheck/lint/build, dist-secret/diff guards, local `/admin/products/new` browser smoke with `.ProseMirror` `contenteditable=true`, `22` toolbar buttons, `0` skeleton toolbar wrappers, successful typing into the editor, and zero browser console errors; admin deploy `dee7fbb9-a42e-4056-adc7-f6abde4ebf72`; live `/admin/products/new` browser smoke with `contenteditable=true`, `22` toolbar buttons, `0` skeleton toolbar wrappers, and zero console errors. |
| ADMIN-035 | P2 | Verified | Admin/Product Rich Text Toolbar | User screenshot on 2026-06-30 proved the product description toolbar could still show the placeholder skeleton while the body rendered, so the deferred-preview path was too clever for the primary merchant editing control. | Product descriptions now lazy-load the real `TiptapEditor` directly behind a plain fixed-height fallback instead of `DeferredTiptapEditor`; deferred rich previews remain available for lower-priority CMS/category/footer/additional-info fields. Verification: focused route-boundary test (`38` tests), admin typecheck/lint/build, root deploy-admin typecheck/build/dist-secret guards, admin deploy `c48d0d71-e16d-4df4-bf42-392b92909d52`, local browser smokes for `/admin/products/new` plus `/admin/products/prod_K0rvSum2CUbhHtpNL0EyL/edit`, and live `/admin/products/new?richTextFix=admin035` smoke showing one `contenteditable` `.ProseMirror`, `22` toolbar buttons, `0` skeleton toolbar wrappers, hidden media trigger not visible, and successful non-saving typing into the editor. |
| ADMIN-036 | P2 | Verified | Admin/Product Rich Text Toolbar | User screenshot on 2026-06-30 matched the old blank `TiptapToolbarSkeleton` toolbar, and the real `TiptapEditor` still had a tiny post-mount fallback that could paint a fake toolbar while `useEditor()` initialized or stale chunks were involved. | The mounted `TiptapEditor` no longer imports or renders `TiptapToolbarSkeleton`; primary product descriptions still lazy-load the real editor chunk through Suspense, but once the editor shell is visible it either has the real toolbar or no transient fake toolbar. Verification: focused route-boundary test (`38` tests), admin typecheck/lint/build, `pnpm check:dist-secrets`, `git diff --check`, local browser product-edit smoke showing one contenteditable editor, zero `bg-muted/35` blank toolbar spans, zero skeleton toolbar wrappers, and the real toolbar; admin deploy `cc15534e-1f2c-4129-983d-aa9e387482b0`; live `/admin/products/new?richTextFix=admin036` smoke showing one contenteditable editor, zero blank toolbar spans, zero skeleton toolbar wrappers, real toolbar rendered, and no `dashboard.scalius.com` console warnings/errors. |
| ADMIN-037 | P3 | Verified | Admin/Product Rich Text Toolbar | Follow-up inspection showed the product description editor was visually healthy, but icon-only toolbar controls were anonymous in the DOM, making browser/a11y verification weaker and leaving room for future false positives around "blank" controls. | `ToolbarButton` now derives `aria-label` from the existing tooltip copy, and the direct link/image/video popover buttons have explicit labels. Verification: focused route-boundary test (`38` tests), admin typecheck, admin lint, admin build, local and live product-description smokes showing one contenteditable `.ProseMirror`, `22` toolbar SVGs/buttons, zero skeleton toolbar roots, and admin deploy `cb4fe7c9-fc78-4824-a42e-40c5b55c4376`. |
| ADMIN-038 | P2 | Verified | Admin/Product Rich Text Init | Product descriptions were visually healthy once Tiptap finished mounting, but the primary editor could still paint a faint/empty initialization gap while `useEditor()` returned `null`, making the field look broken on cold load or slow chunks. | Product descriptions now import the real `TiptapEditor` in the product-form chunk instead of lazying the primary control, and `TiptapEditor` renders a disabled icon toolbar plus sanitized content/placeholder preview until the real editor instance is ready. Lower-priority rich fields may still use `DeferredTiptapEditor`. Verification: focused route-boundary test (`38` tests), admin typecheck/lint/build, `git diff --check`, `pnpm check:dist-secrets`, local Chrome new/edit smokes, admin deploy `c551240b-eae3-44c4-9918-9f29bc21f52c`, and live new/edit smokes showing one `contenteditable` `.ProseMirror`, `17` verified toolbar action buttons, zero skeleton roots/blank spans, saved content preserved, and zero console errors. |
| ADMIN-039 | P2 | Verified | Admin/Product Rich Text Load Flicker | Product edit descriptions could still subtly restyle during cold initialization because the primary `TiptapEditor` placeholder body used the public `.rich-content` preview styles before `useEditor()` mounted the real `.ProseMirror` document. | The primary editor initialization body now sanitizes saved HTML into a static `.ProseMirror` shell with the same compact editor typography/viewport as the mounted editor; empty initialization uses the same placeholder pseudo-element shape. The toolbar and direct product editor path are unchanged. Verification: focused route-boundary test, admin/root gates, admin deploy `c4fbe55b-daee-400f-9450-988d9c90e6f1`, local `/admin/products/prod-test-2/edit` smoke with one `200px` `.ProseMirror`, toolbar present, zero console warnings/errors, and live `/admin/products/prod_abe_OILKCt75z47ppI1Sd/edit` smoke with one editor, toolbar present, rich saved content preserved, and zero console warnings/errors; screenshots `/tmp/scalius-local-product-editor-flicker-lead.png` and `/tmp/scalius-live-product-editor-flicker-lead.png`. |
| ADMIN-042 | P2 | Verified | Admin/Collection Edit Product Labels | Collection edit could render with late or missing product lookup labels for saved `config.productIds` / `featuredProductId`, risking raw `prod_...` IDs leaking into the merchant-facing form while labels loaded. | Collection edit keeps canonical product IDs in form state but renders a neutral loading label until product records are known; the featured-product trigger uses the same fallback. Regression coverage proves edit mode hides raw product IDs before labels arrive, shows product names after lookup data arrives, and submits canonical string IDs. Verification: focused collection tests (`3` files / `11` tests), `pnpm build`, `pnpm check:dist-secrets`, `git diff --check`, admin deploy `a35880e8-7e3d-4a50-b4dc-d2967a5dc568`, live `pnpm release:check --timeout-ms 30000`, live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` (`ops-check-20260708t001328598z-3cb76d4f`), and live browser smoke on `/admin/collections/mnLNrND_Ci0ki_4deGpTf/edit` with save visible, no fatal render text, no visible `prod_...` labels, no horizontal overflow, and zero browser warning/error logs. |
| ADMIN-040 | P2 | Verified | Admin/Collections Edit Runtime | Category-only collections with `productIds: []` could crash the edit page because the product-label lookup query was disabled but the route still read `productLookup.products`. Direct `JSON.parse(collection.config)` calls also left collection reads fragile across admin/public/AI-context surfaces. | Collection config parsing/stringifying is centralized in `@scalius/core/modules/collections/collection-config`, writes persist the canonical shape, public collection and AI-context reads use the same normalizer, and admin edit treats disabled product lookup data as an empty list. Verification: core collection-config tests, affected typecheck/lint/build, `pnpm check:dist-secrets`, `git diff --check`, live D1 config-shape audit proving `5` valid canonical rows and no legacy `specificProductIds`, local category-only collection edit smoke with zero console errors, API deploy `82500a2b-06ba-4ff3-ac46-0b12b8af1039`, admin deploy `f92d2d34-fcb1-4bd0-811e-34edaf26cd28`, and live `/admin/collections/mnLNrND_Ci0ki_4deGpTf/edit` smoke with zero console errors. |
| ADMIN-041 | P2 | Verified | Admin/Collections Lookup Shape Hardening | A user-reported collection edit console error referenced an older missing deployed edit chunk (`edit-DQkWilGU.js`) and `undefined.products`; live D1 showed the five collection rows were already canonical, but lookup query boundaries still trusted API payload shape. | Product and collection by-id query options now normalize malformed/undefined lookup payloads to `{ products: [] }` / `{ collections: [] }`, collection option queries always return array-shaped categories/products, and collection config normalization tolerates older `products` arrays containing ids or product objects before persisting canonical `productIds`. Verification: focused lookup/config tests (`2` files / `13` tests), admin/core/API affected typecheck/lint/build, root typecheck/lint/test (`361` files / `2424` tests)/build, env/migration/dist-secret/diff guards, remote D1 audit proving `5` valid configs, `5` current `productIds`, and zero `products`/`specificProductIds` legacy keys, API deploy `d0f8ed10-1759-4b9d-a8ac-571d2232d520`, admin deploy `67180a31-16d3-465d-8721-3d23e6d9a0f0`, live API health/readyz, and live demo-auth HTTP smoke returning `200` for `/admin/collections/mnLNrND_Ci0ki_4deGpTf/edit` with deployed assets `index-B5TkyBtE.js` and `collection-form-Doa2uYVQ.js`. |
| ADMIN-029 | P2 | Verified | Admin/Order Detail Hydration | Fresh live order-detail smoke for `DW8W05` still emitted React minified error `#418` from the deployed admin bundle after the notification fix, indicating a separate SSR/client markup mismatch on the order page. | Root cause was verified as order-detail SSR/client drift from locale-dependent amount/date formatting, render-time relative shipment timestamps, and optional payment/COD/notification/provider reads racing the dehydrated first render. Order detail now uses deterministic Bangladesh-time formatters, removes render-time relative dates and hydration suppressions from the order render path, and gates optional panels through `useHydrated()` so SSR and the first client render match. Verification: focused order route-graph tests, focused order-prefetch tests, admin typecheck/lint, local fresh-tab browser smoke for `/admin/orders/SNJU2N?hydration=timestamp-filtered` with no new console errors, root typecheck/lint/test/build/env/dist/diff guards, admin deploy `abb3cb5f-30e0-4e18-9c03-9f2648ffa028`, and live fresh-tab `https://dashboard.scalius.com/admin/orders/DW8W05?hydration=admin029` smoke with `0` new console errors and visible COD/notification panels. |
| NOTIF-006 | P1 | Verified | Orders/Notifications | Verified direct refund/return endpoints and some post-sale state changes do not consistently enqueue customer-facing notification receipts, leaving buyers unaware of refund/return/payment-status progress. | Direct admin return/refund paths now emit explicit notification facts only after committed mutations: `processReturn()` returns previous/new status plus version for `order_returned`; `processRefund()` returns `order_refunded` for full refunds with `refund:${orderId}:${refundGroupId}:full`, `order_partially_refunded` for partial refunds with `refund:${orderId}:${refundGroupId}:partial`, and no fact for no-op repair results; API routes enqueue durable outbox rows without exposing internal notification facts. Balance-paid sub-slice adds explicit `payment_balance_paid` customer/admin event settings, customer email/SMS/WhatsApp templates, admin-push labeling, order-level dedupe `payment_balance_paid:{orderId}`, and confirmed Stripe/SSLCommerz/Polar `paymentType=balance` queue routing so remaining-payment receipts no longer collide with `order_created` dedupe. Customer timeline sub-slice centralizes account notification receipt projection, labels `SMS`/`WhatsApp` cleanly, and asserts that `payment_balance_paid`, `order_refunded`, and `order_partially_refunded` receipts become customer account timeline events. Polar external-refund sub-slice makes `processPolarWebhookRefund()` return a private full/partial refund notification fact only after local processing succeeds, uses stable `polar-refund:*` dedupe keys for idempotent full and cumulative partial webhooks, and has the payment queue consumer enqueue the fact through the durable outbox. Reconciler sub-slice makes `finalizeAcceptedRefundAttemptIds()` return ledger-derived full/partial refund notification facts and has scheduled maintenance record them through the same durable outbox after local reconciliation commits. Refund state-message sub-slice adds `refund_processing` and `refund_failed` customer/admin setting events, email/SMS/WhatsApp labels/templates, group-deduped reconciler facts for provider-pending and provider-rejected attempts, and keeps expired pre-provider `pending` attempts silent because no refund reached a gateway. Direct partial-failure sub-slice adds `PartialRefundProcessedError` so split direct refunds and auto-refunded returns carry committed finalizer facts, affected order IDs, optional `order_returned` status changes, and group-deduped remainder state facts to the API route; routes invalidate affected availability caches and enqueue those facts before returning the operator-facing failure. Stripe external-refund sub-slice keeps `charge.refunded` queue handling audit-only, marks the durable webhook row for manual reconciliation with charge/refund evidence, and has scheduled reconciliation import succeeded provider refunds into deterministic local refund rows before finalizing and emitting buyer notification facts. Admin delivery-state sub-slice exposes durable outbox rows plus channel delivery receipts on order detail, gates retry to failed/pending parent rows through the existing outbox enqueue helper, and binds retry to `orders.edit` while read-only history uses `orders.view`. Manual fulfillment sub-slice returns a private `statusChange` fact only when a final own-courier/manual shipment actually changes buyer-visible order status from `confirmed` to `shipped`; the API records that fact through the durable notification outbox and strips it from the admin response, while fulfillment-status-only edits do not reuse `order_completed`. Explicit terminal resend sub-slice adds `POST /api/v1/admin/orders/{id}/notifications/{outboxId}/resend`, gated by `orders.edit`, requiring a merchant/client `resendRequestId`, and creating a fresh `manual_resend:{sourceOutboxId}:{resendRequestId}` outbox parent only for `sent` notifications; retry remains limited to failed/pending/dead-lettered rows. Verification: focused core/API notification/refund tests, focused customer timeline test, focused Polar refund/queue-consumer tests, focused refund finalizer/reconciler/scheduled tests, touched core/API/admin typecheck/lint, root typecheck/lint/test (`293` files / `1924` tests)/build, env/dist/migration/diff guards, API deploy `8e7a2cae-dcde-47b4-b6f7-9fd6e01cd8b3`, admin deploy `89deee07-e59c-4b48-b87e-43ab078457d7`, live API health/readyz, dashboard auth redirect smoke, and deployed admin asset marker pass. Earlier balance-paid deploy proof: API `95778456-7abd-4240-82bb-f60ffdf0dd87`, admin `857ce642-a2c8-4803-875e-e6bd82a935f1`. Timeline assertion proof: `pnpm --filter @scalius/core test -- src/modules/customers/customers.service.test.ts`, `pnpm --filter @scalius/core typecheck`, and `pnpm --filter @scalius/core lint`. Polar external-refund proof: `pnpm --filter @scalius/core test -- src/modules/payments/polar.test.ts` and `pnpm --filter @scalius/api test -- src/queue-consumer.test.ts`. Reconciler proof: `pnpm --filter @scalius/core test -- src/modules/payments/refund-service-finalization.test.ts src/modules/payments/refund-reconciliation.test.ts` and `pnpm --filter @scalius/api test -- src/scheduled-maintenance.test.ts`. Refund state-message proof: focused core tests (`notifications.service`, `settings.service`, `refund-reconciliation`), focused API tests (`order-notification-queue`, `scheduled-maintenance`), core/API/admin typecheck+lint, root test (`293` files / `1931` tests), root typecheck/lint/build, env/dist/migration/diff guards, API deploy `5c936447-9651-407b-88d8-c01c7b401a4a`, admin deploy `b7c1d0d6-7664-4104-9349-8022765a9abd`, live API health/readyz, dashboard auth redirect smoke, and deployed `NotificationChannelsBuilder` asset marker containing `refund_processing`/`refund_failed`. Direct partial-failure proof: `pnpm --filter @scalius/core test -- src/modules/payments/refund-service-allocation.test.ts`, `pnpm --filter @scalius/api test -- src/routes/admin/orders-refund-notifications.test.ts`, core/API typecheck and lint, root typecheck/lint/test (`293` files / `1933` tests)/build, env/dist/migration/diff guards, API deploy `c22d34d8-0531-48e2-978d-8cb11a3d357c`, and live API health/readyz. Stripe external-refund proof: `pnpm --filter @scalius/core test -- src/modules/payments/refund-reconciliation.test.ts`, `pnpm --filter @scalius/api test -- src/routes/webhooks/stripe.test.ts src/queue-consumer.test.ts src/scheduled-maintenance.test.ts`, core/API typecheck+lint, root typecheck/lint/test (`293` files / `1937` tests)/build, env/dist/migration/diff guards, API deploy `7f323eef-ce65-44a8-924c-0955a92e739f`, and live API health/readyz. Admin delivery-state proof: `pnpm --filter @scalius/core test -- src/modules/notifications/order-notification-outbox.test.ts src/auth/rbac/route-permissions.test.ts`, `pnpm --filter @scalius/api test -- src/routes/admin/orders-notifications.test.ts`, root-config admin mutation test, and core/API/admin typecheck+lint. Manual fulfillment proof: `pnpm --filter @scalius/core test -- src/modules/orders/orders.fulfillment.test.ts`, `pnpm --filter @scalius/api test -- src/routes/admin/orders-manual-fulfillment-notifications.test.ts`, core/API typecheck+lint, root typecheck/lint/test (`324` files / `2176` tests)/build, env/dist/migration/dist-secret/diff guards. Terminal resend proof: parallel backend/admin workers, SDK regeneration to 274 paths / 372 operations, focused core/API/RBAC/admin tests, root typecheck/lint/test (`335` files / `2275` tests)/build/env/dist/migration/diff guards, API deploy `2f1ebd52-a812-4440-b76f-77cfebb82604`, admin deploy `a66337e7-6b8f-4f5f-8b72-69f5fa3ee190`, live OpenAPI route proof, unauthenticated resend `401`, unauthenticated order page login redirect, and live admin asset markers for `Send again` plus resend toasts. Stale queued-row alerting stays tracked under `ARCH-002`. Parallel audit on this slice found no current path that emits buyer notifications before a committed local state change. |
| NOTIF-007 | P1 | Verified | Admin/Notification Settings UX | Customer SMS order notifications can be saved without SMS provider readiness, and customer Push is offered even though customer push delivery is not implemented. | Customer notification channels are now limited to implemented delivery paths (`email`, `sms`, `whatsapp`): core rejects customer Push before persistence, strips legacy saved Push on reads, rejects SMS saves until `getSmsProviderReadiness()` is configured, and the admin notification matrix removes customer Push while showing SMS/WhatsApp blockers. API `GET /notification-channels` exposes SMS readiness fields. Focused core/API/admin tests, SDK regeneration, root typecheck/lint/test/build, env/dist/migration/diff guards, API deploy `c749838b-5e9b-4598-b5f6-2bfa0dfe6eb8`, admin deploy `1a1bc84a-2f17-4cc6-9e36-82d00be8e588`, live OpenAPI marker, dashboard login/redirect smoke, and deployed admin asset marker pass. |
| NOTIF-008 | P1 | Verified | Orders/Notifications | Live order `DW8W05` showed dummy/bad SMS.net.bd credentials causing hundreds of order-notification attempts, while old duplicate Queue retries bypassed the outbox backoff and surfaced noisy `delivery_receipt_busy` errors in admin order detail. | SMS provider results now classify merchant-actionable credential/config/account/recipient/balance failures as non-retryable skipped receipts, while transient HTTP/provider/network responses remain retryable. Queue consumer durable `order.notification` failures now mark the parent outbox failed and ACK the Queue message so D1 `nextAttemptAt` and scheduled outbox flushing own retries. Processing claims now honor parent outbox `nextAttemptAt` for failed/pending rows, and the order Notifications card translates common setup failures into merchant-actionable copy while preserving raw values in titles. Verification: focused core/API notification/outbox/queue/retryability tests, core/API/admin/root typecheck/lint/test/build gates, migration/env/dist/diff guards, API deploy `69a6a796-85a8-445b-a999-86da0f47a11b`, admin deploy `6dee4b50-2f7d-4bc9-b73d-a145ab9e6c45`, live D1 cleanup moving existing SMS.net.bd authorization failures to skipped receipts and parent outboxes to sent where terminal, live `DW8W05` notification-card smoke showing merchant-actionable skipped email/SMS/WhatsApp copy with no `delivery_receipt_busy`, and live Notifications/Auth settings smokes. |
| NOTIF-009 | P1 | Verified | Orders/Notifications Cost Guard | Follow-up audit found more merchant setup failures still looked retryable: missing email provider setup, missing/not-ready SMS provider setup, status-only provider auth failures such as Resend `401`, and Firebase service-account/auth failures could continue queue/D1/provider churn or leave push receipts processing after FCM threw. | Notification dispatch now has a central non-retryable setup-failure classifier. Missing email/SMS provider setup, decrypt/credential/auth/token/sender/account/balance blockers, status-only provider setup responses in the `400`/`401`/`402`/`403`/`404`/`405`/`422` family, and bad Firebase credentials become skipped delivery receipts until settings change; transient timeout/rate-limit/5xx/network failures remain retryable. FCM exceptions after receipt claims now mark every claimed push receipt skipped or failed immediately. Admin order detail groups duplicate channel/provider receipts, hides parent raw errors once receipt rows explain the outcome, and shows terminal parent rows as `Delivery settled`, skipped receipts as `Not sent`/`Stopped after N attempts`, and setup failures as merchant-actionable copy instead of raw provider payloads. Verification: focused notification/outbox/provider-selection tests, focused admin display/boundary tests, affected core/API/admin typecheck+lint, root typecheck/lint/test/build/env/dist/migration/diff gates, local admin order-detail smoke proving grouped terminal notification rows with no browser warnings/errors, remote D1 proof for live `DW8W05` showing two terminal sent parent outboxes and grouped receipt data (`10` FCM accepted receipts, SMS.net.bd skipped after `56` historical attempts, email/WhatsApp setup skips), API deploy `b8ab2f35-c840-4936-b7ac-8b64320332b2`, admin deploy `3801b15a-8781-439e-bdc5-bb881625724a`, live API health/readyz, live `DW8W05` order-card Chrome smoke with `Delivery settled`, `10 admin devices`, `Stopped after 56 attempts`, no `delivery_receipt_busy`, no raw `error=405`, and zero console warnings/errors, plus live `/admin/settings/notifications` smoke with no error boundary or raw dump. |
| NOTIF-010 | P2 | Verified | Admin/Notifications UX | Follow-up audit found that some status-only setup failures could still render raw text, Firebase setup failures before push token fanout were not persisted as receipt history, and the sidebar Notifications route only exposed Firebase settings while the channel matrix lived under General settings. | Resend/HTTP/Firebase setup strings now render as merchant-actionable copy, pre-fanout Firebase setup failures record a synthetic skipped FCM receipt, and `/admin/settings/notifications` renders the customer/admin channel matrix beside Firebase provider setup. Verification: focused core notification service test, focused admin notification display tests, affected typecheck/lint gates, root typecheck/lint/test/build/env/dist/migration/diff gates, API deploy `de00d975-7c60-47c6-beed-b1ef2edc14d3`, admin deploy `304a0136-87a9-494b-9319-5338f4a73f7c`, live API health/readyz, live Notifications settings smoke with channel matrix/Firebase provider and zero console warnings/errors, and live `DW8W05` order smoke showing merchant copy for dummy SMS credentials with no raw `error=405`, `delivery_receipt_busy`, `Resend API error`, `invalid_grant`, `HTTP 400`, or `HTTP 403`. |
| NOTIF-011 | P1 | Verified | Orders/Notifications Cost Guard | Audit of the cost guard found one remaining path where `getActiveSmsProvider()` or WhatsApp credential resolution could throw a decrypt/setup error before a channel receipt existed, causing the parent outbox to treat merchant setup as retryable. | Thrown SMS/WhatsApp setup/decrypt/config failures now create terminal setup-scoped skipped receipts (`sms-setup:*`, `whatsapp-setup:*`) when the shared non-retryable classifier recognizes them; transient thrown exceptions still remain retryable. Verification: focused core notification service tests (`75` files / `494` tests in package run), focused admin display/API queue tests (`3` files / `45` tests), root typecheck/lint/test (`305` files / `2010` tests), root build/env/dist/migration/diff guards, API deploy `2dcc73ba-7e0c-4cc0-b11d-0744378035a2`, admin deploy `1d267373-ee44-4204-b2df-feb13ae121c5`, live API health/readyz, live `/admin/settings/notifications` smoke with channel matrix/Firebase provider and zero console warnings/errors, live `DW8W05` smoke showing merchant copy, `Delivery settled`, `Stopped after 56 attempts`, no raw `error=405`, no `delivery_receipt_busy`, no long JSON/stack dump, zero console warnings/errors, and screenshot `/tmp/scalius-order-notifications-postdeploy.png`. |
| NOTIF-012 | P1 | Verified | Orders/Notifications Cost Guard | Live `DW8W05` inspection showed historical SMS.net.bd bad-credential churn and the settings surface still allowed admin Push to remain enabled without proving Firebase service-account readiness. Unknown provider failures also had no hard terminal attempt cap. | Delivery receipts now infer non-retryable status-only setup failures, cap retryable channel attempts with terminal `delivery_attempt_limit_reached`, translate that state in admin order history, and the admin notification settings API/UI reject/disable Push until Firebase credentials are readable from encrypted settings or `FIREBASE_SERVICE_ACCOUNT_CRED_JSON`. The order detail route now gates optional shipment/provider query data until hydration so cached client data cannot trigger a React hydration mismatch. Verification: focused core notification/Firebase tests, focused API notification-channel tests, focused admin display/route-boundary/order-prefetch tests, affected core/API/admin typecheck+lint/build gates, root typecheck/lint/test (`306` files / `2030` tests), dist-secret/diff guards, API deploy `63a77612-1fe3-484a-847d-7d66a37ec609`, admin deploy `f70e0cc9-64c8-43f2-9250-c8e3cd8b4fa4`, live API health/readyz, fresh live `/admin/settings/notifications` smoke with customer/admin channel sections and zero console warnings/errors, fresh live `DW8W05` smoke showing `Delivery settled`, `missing-email`, merchant copy for rejected SMS credentials, `Stopped after 56 attempts`, `10 admin devices`, no raw `error=405`, no `delivery_receipt_busy`, and zero console warnings/errors, plus fresh product description editor smoke showing stable lazy shell, clean Tiptap activation, and screenshots `/tmp/scalius-notifications-notif012-final.png`, `/tmp/scalius-order-dw8w05-notif012-final.png`, `/tmp/scalius-product-editor-notif012-final-before.png`, `/tmp/scalius-product-editor-notif012-final-after.png`. |
| NOTIF-013 | P1 | Verified | Orders/Notifications Cost Guard | Follow-up live `DW8W05` review confirmed historical bad SMS.net.bd credentials had already been made terminal per receipt, but future orders would still make one doomed external provider call per active provider until settings changed. | Added D1-backed provider-health pause markers in `settings.notification_provider_health`: merchant-actionable credential/config/account failures block future order-notification external calls for the same channel/provider until the relevant settings surface is saved. Email, SMS, WhatsApp, and FCM order-notification dispatch consult the marker before provider work; SMS/Email/WhatsApp/Firebase settings saves clear the matching channel pause; admin order history translates paused sends into merchant-actionable copy. Verification: focused core notification/provider-health tests (`2` files / `31` tests), focused API notification/settings tests (`2` files / `30` tests), admin display test through root Vitest config (`4` tests), core/API/admin typecheck+lint, API deploy `adef1b7e-c14b-4252-b86f-3ab4c5a3fbb2`, admin deploy `f5325adb-234a-427d-85ff-d75d4e1065b4`, live API health/readyz, live `DW8W05` smoke showing `Delivery settled`, `Stopped after 56 attempts`, `10 admin devices`, no raw `error=405`, no `delivery_receipt_busy`, and live Notifications settings smoke with customer/admin channel sections, Firebase provider settings, no stack/raw dump, and no app console/page errors. Remaining follow-up: separate OTP delivery cost-guard audit. |
| NOTIF-022 | P1 | Verified | Orders/Notifications Cost Guard | Missing/placeholder provider setup could still fan out one failure per token/recipient before the provider-health pause engaged, and compact logs could still expose email/phone fragments in setup-failure details. | Provider breaker detection now recognizes missing/not-ready/placeholder/mismatched credential states, masks email and phone text before persisting or logging reasons, blocks missing SMS readiness before provider resolution, skips FCM fanout on credential setup failures with one provider pause, and uses the configured channel provider for non-retryable fallback blocks. Verification: focused core notification provider-health/service tests, API queue-consumer/settings notification tests, core/API typecheck and lint. |
| NOTIF-017 | P1 | Verified | Orders/Notifications Cost Guard | Notification settings could still look green when `notification_provider_health` had paused the active SMS/WhatsApp/FCM provider, and capped historical failures that still looked like bad setup did not always pause future provider calls. | Notification settings now treat provider-health pauses as readiness blockers for SMS, WhatsApp, and Admin Push; customer/admin channel saves fail closed while paused; WhatsApp template saves can clear a paused WhatsApp channel before validation; capped retryable receipts write a provider pause when final evidence classifies as setup failure; order notification history labels skipped setup failures as `Paused` only for raw setup/provider-block evidence, while generic capped transients remain `Stopped after N attempts`. The settings UI preserves persisted disabled channel selections so credential repair resumes the merchant's intended channels instead of silently unchecking them. Verification: focused core settings/notification/provider-health tests (`76` files / `516` tests), focused API notification-channel tests (`68` files / `578` tests), focused admin order-display/route-boundary tests, root typecheck/lint/test (`309` files / `2057` tests), root build/env/dist/migration/diff guards, SDK regeneration, local API/admin browser smokes for notifications and product rich-text preload/activation, API deploy `f8768aaf-3a13-4697-9486-3bf3037535b2`, admin deploy `d2782073-33ab-4313-8578-74f0407d81eb`, live API health/readyz, live notification settings API/browser smoke showing WhatsApp readiness copy and no raw `error=405`, live `DW8W05` API/browser smoke showing historical SMS.net.bd `405 Authorization required` data is hidden behind merchant-facing `Paused` display, no raw provider strings, no `delivery_receipt_busy`, and zero console/page errors. Product rich-text load flicker was also rechecked: idle/viewport preload fetches Tiptap without auto-mounting, direct click mounts exactly one editor, and route-boundary coverage guards that split. |
| NOTIF-018 | P2 | Verified | Admin/Notifications UX | Live `DW8W05` and notification-settings review showed the old preserve-disabled-channel behavior was too clever: stale WhatsApp/SMS selections could stay visually selected even when provider readiness made the channel impossible, confusing merchants and causing save attempts to fight backend validation. | Simplification slice: `NotificationChannelsBuilder` now clears readiness-locked SMS/WhatsApp selections on load and immediately before save, while the API keeps rejecting stale clients that try to enable unready providers. Live D1 proof showed `DW8W05` parent notification outboxes are terminal `sent` with `last_error = null`, receipt history is terminal (`10` FCM accepted receipts, missing-email skipped, WhatsApp credentials skipped, SMS.net.bd skipped after historical `56` attempts), and there is no current retry storm. Verification: admin typecheck/lint/build, root-config route-boundary test (`37` tests), shared sanitizer regression/typecheck/lint proving the rich-content flicker fix remains intact, `git diff --check`, `pnpm check:dist-secrets`, live remote D1 inspection, admin deploy `be3b7c3e-73ca-42ff-b453-3e13c590f4d5`, live dashboard route redirecting to auth, and live deployed `NotificationChannelsBuilder-CXrEl0TG.js` asset containing the SMS/WhatsApp readiness gates plus sanitized save path. |
| NOTIF-019 | P1 | Verified | Orders/Notifications Cost Guard | Live `DW8W05` inspection on 2026-06-30 showed the latest SMS.net.bd `405 Authorization required` terminal receipt was newer than the saved SMS settings, but `settings.notification_provider_health` was empty; future orders could therefore make one doomed provider call before recreating the pause marker. | Provider-health lookup now recovers email/SMS/FCM pauses from terminal delivery receipts when setup-failure evidence is newer than the relevant provider settings; runtime sends backfill the provider-health marker before recording a blocked skipped receipt, and settings saves remain the resume action because older receipt evidence is ignored after newer settings `updated_at`. Migration `0072_notification_receipt_provider_status_index` adds `(channel, provider, status, updated_at)` for cheap recovery scans. Verification: focused core notification/provider-health tests (`2` files / `36` tests), core/database/API/root typecheck, root lint, root test (`310` files / `2067` tests), root build, env/dist-secret/diff gates, database migration metadata, local and remote migration `0072`, API deploy `988d385a-4c7c-4967-9882-f496f47ccf5d`, live API health `200`, live readyz `200 ready`, remote D1 index-list proof, remote `EXPLAIN QUERY PLAN` proof using `order_notification_delivery_receipts_provider_status_updated_idx`, and live D1 timestamp proof showing SMS settings older than the latest SMS.net.bd setup failure. |
| NOTIF-020 | P1 | Verified | Orders/Notifications Cost Guard | Follow-up notification/settings audit found the order-notifications dead-letter queue was configured without a consumer, parent outbox rows had no hard terminal cap, manual retry did not re-open retryable child receipts, customer Email notifications could be enabled before email provider readiness, disabled Admin Push/locked customer channels could remain selected, and WhatsApp receipt-recovered provider pauses did not watch the WhatsApp settings category. | Order notification parent outboxes now cap durable retries at `8` and move to `dead_lettered`; `order-notifications-dlq` is consumed in production/local Wrangler configs and archives outbox-backed DLQ messages without provider work; manual retry reopens only retryable child receipts while preserving accepted/delivered/skipped terminal receipts; admin order history can display/retry `dead_lettered` parent rows with safe copy; customer Email/SMS/WhatsApp settings fail closed on provider readiness and provider-health pauses; settings UI sanitizes readiness-locked customer/admin selections before save; provider-health receipt recovery now includes WhatsApp settings; raw provider pause details no longer render in settings blockers. Verification: focused core notification/settings tests (`77` files / `539` tests), focused API notification-channel/queue tests (`70` files / `589` tests), focused admin route/display tests (`43` tests), core/API/admin/api-client typecheck, core/API/admin lint, API/admin build, SDK regeneration from live local API (`269` documented routes), root deploy typecheck/build/dist-secret checks, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, API deploy `ef80b48e-218e-4f68-83c6-1ca7b4afd52f` with `Consumer for order-notifications-dlq`, admin deploy `cd1aa4c9-87e4-438c-b55b-9ed9a7db65dc`, live API health `200`, live readyz `200 ready`, `wrangler queues info order-notifications-dlq` showing `Consumers: worker:scalius-api`, live `/admin/settings/notifications` Chrome smoke showing customer/admin channel matrices with safe SMS/WhatsApp readiness copy and zero console warnings/errors, and live `DW8W05` Chrome smoke showing merchant-facing notification history (`Dead Lettered`, `Paused`, `Delivery settled`) with no raw `error=405`, decrypt-key text, or `delivery_receipt_busy`. |
| NOTIF-021 | P2 | Verified | Admin/Notifications UX | Follow-up audit found two remaining merchant-experience gaps: obvious placeholder SMS credentials could still pass structural readiness until the first provider call, and repeated setup-failure receipts could fragment the order notification card by raw provider status/last-error text. | Shared SMS placeholder detection now lives in the core SMS resolver/save path for values such as `dummy`, `example`, `changeme`, `your-token-here`, and simple numeric junk, so settings, OTP, and order-notification readiness all fail closed without paid provider calls. Admin order notification receipt grouping now keys setup failures by normalized issue class and hides duplicate setup-detail lines, preferring the explicit paused-provider message when present. Dead-lettered parent rows show `Retry stopped` instead of alarming historical attempt counts as the primary label. Verification: parallel read-only notification/RTE audits, focused core SMS/customer-auth tests (`77` files / `541` tests), focused admin notification-display test (`7` tests), root typecheck/lint/test (`314` files / `2106` tests), `pnpm check:env`, `pnpm check:dist-secrets`, API deploy `5bf6979f-46e6-4c6d-bd2f-b0fd86aa5149`, final admin deploy `3dae5d75-dbfc-4e1d-a24d-35fbf9b13df5`, live API health/readyz, live `/admin/settings/notifications` Chrome smoke showing customer/admin matrices and safe SMS pause copy with zero console warnings/errors, and live `DW8W05` order smoke showing `Retry stopped`, `Paused`, `Delivery settled`, no `275 attempts` primary label, no raw `error=405`, no decrypt-key text, and no `delivery_receipt_busy`. |
| NOTIF-014 | P1 | Verified | Customer/Auth OTP Cost Guard | Parallel audit found `auth.send_otp` discarded provider retryability, so dummy/bad email/SMS/WhatsApp credentials could repeatedly call providers through Queue retries even though the failure required merchant settings changes. OTP success logs also exposed raw recipients. | OTP provider failures now classify merchant-actionable setup/account/credential/authorization/sender/balance/recipient/template failures as terminal skipped `auth_otp_delivery_receipts`, write the shared D1-backed `notification_provider_health` marker for proven bad provider setup, skip future provider calls while the marker exists until settings save clears it, and log delivery keys/identifier hashes instead of raw recipients or OTP codes. Email provider success logs also mask recipients. Verification: focused API queue-consumer tests (`68` files / `570` tests), focused core email/provider-health tests (`76` files / `510` tests), API/core typecheck+lint, `git diff --check`, API deploy `f624209c-7702-49ee-965b-83d1d45659db`, and live API health/readyz. Live OTP send was intentionally not run to avoid touching customer/provider channels. |
| NOTIF-015 | P1 | Verified | Customer/Auth OTP DLQ/Backoff | `auth-otp-dlq` was configured as a dead-letter queue, but the API worker did not consume it; the queue consumer also retried OTP busy/not-due receipts with a fixed 30-second delay instead of the D1 receipt backoff. | `auth-otp-dlq` is now a real API consumer in production/local Wrangler configs. DLQ messages create/update the D1 OTP delivery receipt as skipped with redacted evidence and never call email/SMS/WhatsApp providers. Busy receipts return a retry hint from `nextAttemptAt`/claim lease state, and retryable OTP failures use the shared bounded receipt backoff. Verification: focused API queue-consumer tests (`68` files / `572` tests), focused core OTP receipt tests (`76` files / `511` tests), API/core typecheck+lint, `pnpm check:env`, `git diff --check`, API deploy `fceba577-c220-435c-871b-4eae2e836105`, live API health/readyz, deploy trigger proof showing `Consumer for auth-otp-dlq`, and `wrangler queues info auth-otp-dlq` showing `Consumers: worker:scalius-api`. |
| NOTIF-016 | P2 | Verified | Customer/Auth OTP Provider-Accepted Idempotency | If an OTP provider accepts a send but the accepted receipt write fails, the next queue retry can blindly send again for providers without upstream idempotency, especially Cloudflare Email, WhatsApp, and SMS providers other than GenNet. | Accepted/skipped OTP receipt writes now get short in-invocation retries. If an accepted provider response still cannot be persisted to D1, the queue consumer stores a short-lived non-PII KV recovery hint with delivery key/channel/provider acceptance metadata, retries the queue message, and future normal or DLQ attempts repair the D1 receipt as accepted before any provider call. Verification: focused API queue-consumer tests (`68` files / `575` tests), focused core OTP receipt tests (`76` files / `511` tests), API/core typecheck+lint, `pnpm check:env`, `git diff --check`, API deploy `daf5bb83-fdfd-4f85-bb60-26fb2a26be08`, live API health/readyz, deploy trigger proof showing `Consumer for auth-otp-dlq`, and `wrangler queues info auth-otp-dlq` showing `Consumers: worker:scalius-api`. |
| CUST-003 | P1 | Verified | Customer/Post-sale UX | Account/session reads can stall or collapse network/API failures into logged-out/permanent-loading states instead of retryable errors. | Customer-auth browser helpers now use bounded same-origin reads/writes, distinguish unauthenticated `401` from timeout/`429`/`5xx`/malformed-response `unavailable` states, `/account` and `/account/orders/{id}` render retryable account/order errors, and cart submit-time auth gating verifies `/me` before opening auth. Focused/root gates, storefront deploy, and live page/asset smokes pass. |
| CUST-004 | P1 | Verified | Customer/Post-sale UX | Account order due/spent summaries derived from `totalAmount - paidAmount`, so refunded/cancelled/returned orders could look unpaid and capped lists undercounted lifetime spend. | Customer account history now returns server-computed customer-visible `balanceDue`, aggregate account `summary`, and `pagination.hasMore`; cancelled/refunded/returned/partially-refunded/failed-payment orders project zero buyer due without changing payment-state ledger math; `/account` renders server facts instead of capped client reductions. Focused core/API/storefront tests, SDK regeneration from local API OpenAPI, root typecheck/lint/test/build, env/dist/migration/diff guards, API deploy `166f2a87-bad4-4926-8cfc-fc811113da84`, storefront deploy `1a3982b1-9c53-44e6-8f1f-72297b1e7893`, live API health/OpenAPI schema smoke, live unauthenticated orders `401` private no-store smoke, and deployed account asset marker smoke pass. |
| CUST-005 | P1 | Verified | Customer/Post-sale Payments | Customers had no storefront path to retry failed online payments or pay remaining partial-payment balances even though backend balance sessions existed. | Authenticated customer-owned payment sessions now use strict empty-body `/customer-auth/orders/{id}/payment-session`, customer-account attempt proofs, policy-backed `paymentRecovery`, private account redirects, and account-only Stripe/hosted recovery UI without receipt-token or checkout-success side effects. Focused/root gates, API/storefront deploys, and live fail-closed smokes pass. |
| CUST-006 | P2 | Verified | Customer/Post-sale History | Customer order history silently capped at 50 orders and timeline rendered the current status on the original “Order placed” event. | Customer order history now uses keyset cursor pagination with `nextCursor`, account summary remains uncapped, and order-detail timelines keep immutable `Order placed` plus separate current-status events. Focused core/API/storefront tests, SDK regeneration, root typecheck/lint/test/build/env/dist/migration/diff gates, API deploy `05293795-7142-4785-94d0-504637af502d`, storefront deploy `02b6b129-bbf3-4db8-a831-f75dd6961873`, live OpenAPI/401/account/asset smokes pass. |
| CUST-007 | P1 | Verified | Customer/Post-sale Self-Service | Customers needed an auditable path to request pre-shipment cancellation, return, or refund without letting the buyer UI directly mutate payment, shipment, inventory, COD, or order status state. The older account profile delivery-location ID concern has been rechecked and is already fixed. | Account-owned request creation remains implemented through migration `0073_order_support_requests`, customer order detail actions, and scoped `POST /api/v1/customer-auth/orders/{id}/support-requests`. Receipt-token guest request creation is now implemented through migration `0074_order_support_requests_nullable_customer`, live receipt detail support history/actions, `POST /api/v1/orders/receipt/{id}/support-requests`, and storefront `/api/order-support/receipt-request`; true guest rows keep nullable `customer_id`, actor type `guest_receipt`, and no account ownership side effect. Admin resolution remains through `PUT /api/v1/admin/orders/{id}/support-requests/{requestId}/status` with explicit transitions, guarded D1 updates, terminal `active_key` clearing, audit events, `orders.edit` RBAC, OpenAPI/SDK coverage, and resolution UI that separates operational refund/shipment/payment/order changes. Request lifecycle notifications use the existing order notification outbox with safe type/status labels only. Verification: focused API receipt tests, focused storefront source/proxy tests, focused core support-request tests (`77` files / `535` tests), migration metadata, SDK regeneration (`269` live OpenAPI paths), root typecheck/lint/test (`313` files / `2091` tests), root build, env/dist/diff guards, local Chrome true-guest receipt submit returning `201` with zero forms and D1 proof of nullable request/event `customer_id`, remote migration `0074`, remote D1 nullable-column proof, API deploy `0b38b421-1179-41cc-990d-c29a7ce6c8d1`, storefront deploy `10c86fe7-2bd4-41b8-8968-bce3ffe01797`, admin deploy `cb4fe7c9-fc78-4824-a42e-40c5b55c4376`, live API health/readyz `200`, live OpenAPI receipt-support route/schema smoke, live storefront health `200`, deployed storefront proxy invalid-input `400` no-store smoke, and admin redirect smoke. |
| CUST-008 | P2 | Verified | Customer/Post-sale UX | Guest buyers were not clearly guided into receipt-token tracking vs account-owned history from success, and refund/payment history lacked sanitized reason/provider reference/settlement context for customer and support views. | Customer order detail exposes buyer-safe refund attempts, active refund progress copy, and refund timeline events without provider secrets or raw internal statuses. Order success explains that guest orders are tracked by the private receipt link, adds copy-link UX plus a sign-in CTA for future account-owned orders, and sends authenticated buyers to `/account/orders/{id}` without pretending guest receipts are account-owned. Account order cards surface payment-failed, balance-due, and refund/return context with a direct timeline link. Admin refund operations now expose support-safe reason, internal refund reference, source/refund payment rows, source transaction id, provider refund/correlation ids, provider status, allocation/probe counts, last safe error, and settled/failed/probe timestamps; order detail keeps refund recovery context as a fallback when the secondary payment-history panel fails. Focused/root gates, SDK regeneration from live local API OpenAPI, API deploy `419afc81-23bd-4230-8f0a-e8266e184994`, admin deploy `5727361b-d4b4-4a66-a1a8-8077e1096719`, live API health/readyz/OpenAPI field smoke, dashboard redirect smoke, and deployed admin asset marker pass. |
| PERF-013 | P1 | Verified | Storefront/Product Listings | Category/search pagination links preserve only `q` and `page`, so page navigation can silently drop sort, price, boolean, and attribute filters. | Category/search pagination and sort navigation now build from canonical `currentFilters` via `buildProductListHref()` / `buildProductListPaginationHref()`, preserving validated filters while dropping tracking/default/noisy params. Focused/root gates, local/live smokes, and storefront deploy `15697a36-d928-4dd2-9574-262289704136` pass. |
| PERF-014 | P2 | Verified | Storefront/Cart Runtime | Cart page/runtime listeners could survive Astro page transitions, duplicate abandoned-checkout saves, rerender totals twice, and miss `document`-scoped cart update events while stale cart repair UI could sit below the checkout form on mobile. | Cart runtime initialization is now idempotent with abortable listener ownership, a single cart-store subscription, single abandoned-checkout debounce wiring, and `document`-scoped `cart-updated` handling. Cart page navigation aborts page-owned listeners, clears only checkout transfer data while preserving the active checkout id, and renders stale cart repair feedback before checkout actions on mobile. Focused cart/session tests, root tests/gates, local Astro dev browser smokes, generated Worker smoke, and generated Wrangler dry-run pass. |
| PERF-015 | P2 | Verified | Storefront/Auth Runtime | Global AuthModal performed unconditional checkout-config and session hydration after load, duplicating cart/account fetches and creating visible “checking account” pauses. | AuthModal now starts from page-injected checkout config when available, fetches auth policy only on first auth action when no injected config exists, reads `/me` only when the readable `cs_auth=1` mirror proves a possible session, clears that mirror after an authoritative unauthenticated session read, and dedupes concurrent customer-session reads. Cart SSR injects its already-fetched checkout config into `window.__CHECKOUT_CONFIG__`, so opening auth from cart does not issue another config request. Verification: focused storefront auth tests, storefront typecheck/lint/build, dist-secret and diff checks, local Chrome smoke, storefront deploy `a344e39c-a6fb-4328-b856-7b5ef7fd6a19`, and live browser smoke proving guest idle home/cart made `0` checkout-config and `0` `/me` reads, cart-open made `0` extra config reads, home-open made exactly `1` on-demand config read, a stale `cs_auth=1` mirror made one `/me` read then cleared, and all live checks had zero console/page/request failures. |
| PERF-016 | P2 | Verified | Admin/Rich Text UX | Product edit rich description could still subtly restyle on cold route load because `RichContent` imported its CSS as a route/component side effect. | `rich-content.css` now imports from the root admin stylesheet instead of the `RichContent` component, so product description preview CSS is part of the first page stylesheet while Tiptap remains lazy. The deferred editor fallback no longer imports/renders fake toolbar icons; it reserves toolbar height with a neutral loading rail so a slow Tiptap chunk cannot look like broken disabled controls. Verification: admin typecheck/lint/build, root gates, production asset scan showing no separate `rich-content` CSS chunk, admin deploy `304a0136-87a9-494b-9319-5338f4a73f7c`, local product-edit smoke, and live `prod_abe_OILKCt75z47ppI1Sd` product-edit smoke showing `.rich-content` rendered, image dimensions reserved, no separate rich-content stylesheet, and zero console warnings/errors. Follow-up screenshot investigation on 2026-06-30 verified local and live product edit/new routes mount real ProseMirror toolbar controls with zero fallback blocks; focused route-boundary test, admin typecheck/lint/build, `git diff --check`, and `pnpm check:dist-secrets` passed. Admin deploy `e519a08e-6b22-415e-9dd2-709f17a14515`; deployed smoke on `prod_abe_OILKCt75z47ppI1Sd` showed real toolbar labels, ProseMirror content, and zero fallback blocks/rails. |
| DEL-003 | P1 | Verified | Delivery/Webhooks | Verified delivery webhook/provider selection can match inactive or stale provider rows in some paths, which risks accepting callbacks against the wrong merchant configuration. | Delivery webhook auth now requires exactly one active provider row for the courier type, returns the verified provider id, and fails closed for zero or duplicate active rows. Pathao/Steadfast webhook shipment lookups are scoped by verified provider id + provider type + external identifier, and admin shipment create/status-poll paths reject inactive provider ids before provider API calls or local writes. Verified with focused delivery/webhook tests (`4` files / `25` tests), API/core typecheck and lint, API dry-run build, env/dist/migration guards, API deploy `a13a84ee-c0e4-4608-b00f-d0436e086e50`, and live health/readyz smokes. |
| DEL-004 | P1 | Verified | Admin/Delivery Provider UX | Pathao location mappings are required for shipment creation but not editable in the location form, and delivery provider activation needed a hard readiness gate. | Provider create and update-as-create now default to inactive drafts; active saves require complete Pathao/Steadfast required fields through shared core readiness rules after masked-secret restore and before DB writes/cache invalidation. Admin provider status uses the same blocker checklist, disables activation/active saves until fields are complete, links Pathao setup to Delivery Locations, and the location form exposes first-class positive-integer Pathao ID fields for city/zone/area instead of raw JSON. Pathao shipment creation now preflights required positive-integer city/zone mappings before provider token work or local placeholder writes; area remains optional. Verification: focused core/API/admin tests, touched workspace typecheck/lint, API/admin builds, env/dist/migration/diff guards, API deploys `552e6b15-e5ea-40b8-b924-03dfa2138dc0` and `fbacffcb-8751-4a2d-abdb-f0ac0dae9f2b`, admin deploys `37b5e6b0-0f64-4e6e-aa7a-caab305eb30a` and `06c065c8-6ade-4942-986b-06e841a34286`, live API health/readyz, and live admin route smoke (`307` to `/auth/login`). |
| META-001 | P1 | Verified | Analytics/Meta CAPI | Meta CAPI needed strict trusted-source handling, durable backend Purchase delivery, private matching, non-hot-retry setup failures, and browser Pixel/CAPI parity visibility. | Trusted-source, external-id hashing, backend Purchase CAPI outbox, purchase-only private matching, and provider setup skip behavior are deployed. Pixel parity is now a warning-only admin diagnostic on `GET /api/v1/admin/settings/meta-conversions`: it compares the saved CAPI Pixel ID with active snippets containing numeric `fbq('init', ...)`, ignores storefront `window.fbq` stubs/placeholders/noscript images/track-only calls, warns for invalid/missing/mismatched/multiple/unreadable browser Pixel states, and returns `unavailable` without failing settings reads if the diagnostic query fails. Verification: official Meta CAPI dedup guidance checked 2026-06-29; focused core parser tests and API settings-route tests; root typecheck/lint/test (`306` files / `2021` tests)/build/env/dist/migration/diff gates; generated SDK/OpenAPI updated and live OpenAPI still has 266 routes with `pixelParity`; API deploys `38fce4a7-8692-4f5d-bba9-ebbfd017daec`, `de00d975-7c60-47c6-beed-b1ef2edc14d3`, `dbe9c84f-8409-4015-88c8-ff2449c85a0e`, and `49d3fab2-3a37-4961-a523-97a9896a65e0`; storefront deploy `733e85c3-601f-449d-a040-05138f6b6495`; admin deploy `88b5f537-e2a8-4a3a-b99c-23997709b07c`; live API health/readyz; live dashboard Meta CAPI smoke showed the current dummy `demo@scalius.com` Pixel ID as invalid with zero console warnings/errors. |
| PAY-016 | P1 | Verified | Payments/Refunds | Refunds validated against aggregate `orders.paidAmount` but dispatched only the latest succeeded gateway payment, so split deposit/balance orders could be locally fully refunded while one provider charge remained unrefunded. | Refunds now allocate newest-first across all eligible captured payment rows, create one `order_payments` refund row per allocation with `sourcePaymentId`/group/idempotency metadata, send explicit per-capture Stripe amounts, use deterministic SSLCommerz refund references, support mixed-gateway allocations, fail closed on insufficient gateway override balance, and update order payment state only for provider-successful allocations behind the refund-claim order version. Focused/wide payment tests, root gates, API deploy `e596020b-0594-418d-a20d-b2f10a5d9d21`, and live health/readyz pass. |
| PAY-017 | P1 | Verified | Payments/Refund State | Admin refunds and Polar refund webhooks updated paid/payment status without consistently recomputing `orders.balanceDue`, producing stale partial-payment/customer due states. | `payment-state.ts` now centralizes order payment arithmetic. Payment confirmation, COD collection, admin manual order create/edit, admin refunds, and Polar refund webhooks recompute `paidAmount`/`balanceDue`/`paymentStatus`; refund rollback restores prior `balanceDue`; stale stored COD balances fall back to computed outstanding balance. Focused/root tests, typecheck/lint/build/platform guards, API deploy `88d2e4d8-13b8-48c8-931c-04b39285cf12`, and live health/readyz/OpenAPI smokes pass. |
| PAY-022 | P2 | Verified | Payments/Polar Refund State | Polar refund webhooks receive cumulative `amountRefunded`, but partial refund handling applied the ratio to current `orders.paidAmount`, so repeated cumulative partial events could compound instead of converging. | Polar refund queue messages now preserve `polarCheckoutId`; `processPolarWebhookRefund()` resolves the matching captured Polar source payment, treats provider refund totals as cumulative against that source amount, inserts only newly observed local refund-payment deltas, derives whole-order paid/balance/status from the payment ledger, retries while an earlier source refund row is still pending, and treats a fully refunded Polar deposit as a partial order refund when other captured payments remain. Verification: focused core Polar tests (`76` files / `519` tests), focused API queue-consumer tests (`68` files / `578` tests), core/API typecheck+lint, root typecheck/lint/test (`309` files / `2060` tests), root build, env/dist/migration/diff guards, API deploy `0cb35740-cb0f-4f07-815f-9789433279f3`, and live API health/readyz. Live Polar refund mutation was intentionally not run because it would touch real provider/order state. |
| PAY-018 | P1 | Verified | Payments/Gateway Readiness | Gateway readiness can treat undecryptable encrypted credentials as configured because graceful decrypt fallback returns non-empty ciphertext. | Stripe/SSLCommerz/Polar settings now strict-read secrets, expose `credentialErrors`, exclude wrong-key gateways from active checkout methods, block public sessions before provider calls, and keep disabled-but-readable webhook validation working for late callbacks. Focused payment/webhook/settings tests, root gates, API deploy `90329719-a87b-4b38-8d03-0119b39ffd40`, and live OpenAPI/checkout-config smokes pass. |
| PAY-019 | P1 | Verified | Payments/Checkout Latency | Online payment checkout still did browser order creation followed by a separate browser payment-session creation, adding avoidable round trips after the authoritative order commit. | Storefront create-order now accepts an online-only `initialPaymentSession` request, creates the first Stripe/SSLCommerz/Polar session server-side only after the backend order commit succeeds, sends only `orderId` plus receipt proof to gateway-session endpoints, and returns either `initialPaymentSession` or `initialPaymentSessionError` with the committed order response. Browser handlers consume the attached session, avoid duplicate provider retries on attached errors, SSLCommerz/Polar fallback payloads no longer send client currency/payment details, and pay-click duplicate cart freshness validation was removed in favor of authoritative order-create validation. Verified with focused storefront checkout tests (`3` files / `19` tests), storefront typecheck/lint, root typecheck, root lint, root test (`268` files / `1736` tests), root build, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata check, `git diff --check`, storefront deploy `9d3461e6-9e74-47e5-840b-a2578a9bc88e`, and live storefront `/health`, `/`, `/cart`, and `/checkout` smokes. |
| PAY-021 | P1 | Verified | Payments/Checkout Latency | The fused initial-session default from `PAY-019` still kept provider latency inside the normal browser order-submit response and made external gateway slowness feel like slow order creation. | Browser `createOrder()` now sends only the committed order payload by default; Stripe/SSLCommerz/Polar handlers create sessions after order commit through gateway-specific same-origin proxies with only `orderId` plus receipt proof; the explicit `initialPaymentSession: true` proxy branch remains opt-in and covered. API payment-session routes now await `markPaymentSessionAttemptCreated()` before responding, then schedule only the best-effort `orders.paymentIntentId` recovery hint with guarded `executionCtx.waitUntil()` when available. Verification: focused storefront checkout tests (`3` files / `27` tests), focused API payment tests (`56` files / `473` tests), API/storefront typecheck/lint, root typecheck/lint/test (`276` files / `1777` tests), root build, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata, `git diff --check`, API deploy `5e0edbc3-61e7-41b5-ad5a-10d44a625837`, storefront deploy `374c9de0-0eb7-442a-b2f6-3dca54482031`, live API health/readyz, live Stripe missing-receipt `400`, live checkout config, live storefront `/health`/`/cart`/`/checkout`, and deployed checkout asset marker showing no default `initialPaymentSession` request body. |
| CACHE-019 | P1 | Verified | API/Storefront Cache | Product and discount writes can rewarm category pages from stale category-products API KV because product/discount invalidation excludes the `categories` API group while storefront category HTML is purged/warmed. | The products invalidation group now includes `api:categories:`, so product and discount catalog writes bump/delete the category-products API fence before scheduling storefront category/search HTML purge/warm. Focused/root gates, API deploy `9c52d40a-d0f4-417a-a06d-d1bf12ff9576`, and live category-products/API/storefront smokes pass. |
| CACHE-020 | P1 | Verified | Storefront/Cache | Storefront L2/KV cache generation context could silently fall back to default generation when AsyncLocalStorage was absent, and `Set-Cookie` responses could be missed by runtimes exposing cookies through `getSetCookie()`, risking private or stale content entering cache. | `withEdgeCache()` now uses the fallback cache context outside ALS, public response cacheability rejects all detectable `Set-Cookie` shapes, and product detail pages return non-cacheable `503` when required layout data is unavailable instead of rendering partial successful HTML. Verification: focused storefront cache/page-data tests, storefront typecheck and lint. |
| CACHE-021 | P1 | Verified | Storefront/Public Read Resilience | Live verification after the SEO/admin probe work found `/health` returning immediately while public cached routes (`/`, `/search`, `/robots.txt`, and `/api/product-feed.xml?limit=5`) hung with zero bytes until client cancellation; Wrangler tail showed `outcome: canceled` with ~1-2ms CPU, pointing to a stalled async dependency rather than hot CPU. | Storefront public API reads now give the service binding a short hard timeout, then fall back to HTTPS API only for safe public GET/HEAD reads. Fallback is blocked for authenticated calls, non-idempotent methods, sensitive/proof/session/token headers, and private API families such as auth/customer/checkout/orders/payments/refunds/webhooks/scanner/setup, so receipt/status/order/payment proof paths are not replayed. Focused storefront route/client/order tests, root typecheck/lint/test (`384` files / `2654` tests), root build, env/dist-secret, and diff checks pass. Storefront deploy `2a652614-70c7-4c61-8c9c-effe087a8f69` warmed `/` and `/search` in ~2s; live curl smokes returned `/health` `200` in 0.04s, `/` and `/search` cache hits in under 0.11s, and `/robots.txt`, `/sitemap.xml`, `/api/product-feed.xml?limit=5`, `/api/facebook-feed.xml?limit=5` `200` with public cache headers in under 0.82s. Admin SEO live-proof smoke against admin deploy `06840e26-70cf-4d1a-81d0-f7dced744ef5` showed `Live proof complete`, robots `1` Sitemap line, sitemap `5` locs, both feeds `5` items/images/availability values, and zero console/page errors. |
| CACHE-022 | P1 | Verified | API/Admin Cache Ops | Admin global clear-all bypassed the durable storefront purge queue and header access exposed a high-impact cache action to cache-view-only users; pending storefront purge/warm DLQ state was not visible in the cache settings UI. Cloudflare Workers Cache also needed an explicit release decision before adding another cache layer. | API clear-all now invalidates API cache then routes every storefront group through `triggerStorefrontPurgeForGroups()` with queue-first delivery and sanitized direct fallback. Admin cache settings now shows a bounded storefront queue recovery panel with replay/ignore actions, and the header cache action requires `settings.cache.manage`. Workers Cache remains intentionally disabled for release-critical paths until host/tenant keying, middleware bypass, response headers, and purge semantics are designed. API deploy `ea298d04-aec8-4c37-8f36-a4ddd63e33b0`, admin deploy `067cfbb1-2a06-4efc-a4b4-72ab8257ee74`, live ops queue smoke and `pnpm release:check --timeout-ms 30000` pass. |
| CACHE-023 | P2 | Verified | API/Admin/Storefront Cache | Stock-only product availability changes could leave normal storefront product-list cache families stale, and release smoke coverage did not explicitly prove public cache headers/private checkout/purge method behavior. Admin cache recovery actions were also too easy to trigger from the cache settings page. | `all_products_` and `category_products_` now have exact generation families and product availability invalidation purges product detail, variants, all-product lists, category-product lists, feed, and sitemap without bumping the whole storefront HTML cache. `pnpm release:check` now includes read-only storefront cache-header smokes for home, search, checkout, product feed, and purge GET. Admin cache settings hides mutation actions from cache-view-only users, renames ignore to `Mark resolved` with confirmation, and makes the mapping panel keyboard/ARIA accessible. Verification: focused API/storefront/admin tests, API/storefront typechecks, `git diff --check`, API deploy `8385676f-7148-43b7-a659-3dedc36f892f`, storefront deploy `0b150bd2-bec0-4a76-a981-098cf8bfa0d5`, and live `pnpm release:check --timeout-ms 30000` pass. |
| OPS-007 | P1 | Verified | Storefront/Astro Deploy | The storefront still documented and deployed through a source `src/worker.ts` custom entrypoint, which bypasses Astro 7's generated Worker bundle and can fail Wrangler deploy/dev with unresolved virtual Astro modules. | Storefront is upgraded to Astro 7 / `@astrojs/cloudflare` 14 / `@astrojs/react` 6 / Vite 8. The custom source Worker entrypoint is removed, source `wrangler.jsonc` remains adapter input only, deploy uses `apps/storefront/dist/server/wrangler.json` generated by `astro build`, and targeted deploys verify latest Worker deployment plus live `/health`. Storefront typecheck/build, root gates, `wrangler deploy --config dist/server/wrangler.json --dry-run`, local Astro dev smoke, generated Worker smoke, and live post-deploy health pass. |
| DEL-005 | P1 | Verified | Admin/Shipment Status Sync | Admin shipment “status check” could update shipment status without updating buyer-visible order status, invalidating caches, or notifying customers, while the adjacent refresh route performed the full sync. | Shared API shipment-status sync helper now backs order-scoped `/status`, order-scoped `/refresh`, and standalone `/shipments/{id}/check-status`, including provider poll, `lastChecked`, order/inventory reconciliation, availability cache invalidation, and notification enqueue. The standalone check response now returns a full shipment status payload with boolean `orderStatusUpdate` instead of a stale object/string contract. Verification: parallel backend/admin caller audits, focused helper/route-boundary/order-notification/delivery tests (`6` files / `33` tests), API/core/api-client/admin typecheck and lint, API/admin builds, SDK regeneration from local API OpenAPI (`261` paths), env/dist/migration/diff guards, API deploy `1b26c9ed-5ce2-4a0c-9461-9384ca74294b`, live API health/readyz, and live OpenAPI marker for `summary = "Check shipment status from provider and sync order"` plus `orderStatusUpdate.type = boolean`. |
| DEL-006 | P2 | Verified | Admin/Bulk Shipping UX | Bulk shipment actions could still feel under-modeled because provider readiness was binary active/inactive, not an operator-visible tested/configured/tested-active state with action-time blockers. | Delivery providers now persist live-test attempt/success/failure timestamps plus a credential/config HMAC fingerprint. Provider list/get responses expose `draft`, `configured`, `tested`, `active`, or `blocked` readiness with `canCreateShipment`; active-but-untested or changed-credential providers fail closed for single and bulk shipment creation before local placeholders, order claims, or courier calls. Admin provider lists/details, single shipment forms, and bulk shipment dialogs show the same compact readiness state and blocker copy. Verification: focused core/API/admin readiness tests, admin/core/API typecheck and lint, database migration metadata, local D1 migration, root typecheck/lint/test/build, env/dist-secret/diff guards, API deploy `a750ed55-fce8-42e0-bd13-91528fbc7def` with remote migration `0077_delivery_provider_readiness.sql`, admin deploy `55f4e750-d3df-4430-b329-b3b5f72d5a95`, live API health/readyz, unauth admin/API guards, and authenticated live delivery-provider page smoke showing existing providers as `Blocked` with no console errors. |
| AUTH-013 | P2 | Won't Fix | Customer/Auth UX | Forced multi-proof customer login/signup is explicitly rejected for the Bangladesh SMB release: enabled OTP channels remain alternatives, not cumulative proofs. Mandatory phone collection plus one verified OTP channel gives enough customer-account proof for current checkout/order-history risk, while requiring two or more OTP channels would add provider dependency, recovery complexity, merchant-setting ambiguity, and avoidable conversion loss. | Decision scout reviewed customer-auth policy/docs/code/tests; no backend or frontend build is recommended. Future high-risk step-up verification should be tracked separately as risk-based account/order recovery, not as a merchant setting that blocks ordinary login/signup. |
| ARCH-001 | P1 | Verified | Checkout/Cloudflare Architecture | D1 `checkout_attempts` owns public checkout submit idempotency, reserved order/receipt IDs, and committed response replay; D1 `order_receipts` owns hash-backed public receipt proof validation with `checkout_attempts` only as the self-heal fallback and KV only as a repairable hint. | Remaining work was stale: `PAY-010` verified scheduled stale hosted-payment cleanup; `CHECKOUT-011` and payment-session policy verified failed unpaid hosted-payment recovery plus guarded gateway rotation; `PAY-014`/migration `0063` verified D1 hosted-session single-flight. `CheckoutCoordinator` Durable Object is explicitly rejected for now because D1 unique indexes, leases, CAS, and focused tests cover the coordination surface, and no measured checkout contention justifies another Durable Object. |
| CONTRACT-004 | P1 | Verified | API/OpenAPI | Original drift verified: generated spec advertised `bearerAuth` but had no operation-level security requirements, and `409`/`503` response coverage was sparse despite runtime `ConflictError`/`ServiceUnavailableError` paths. | The finalized OpenAPI contract now has operation-level security metadata for protected service/admin/cache/orders/customer-account paths and route-level response docs for the known customer OTP, payment settings, admin user, order/refund/return/COD/fulfillment/shipment, product/variant, checkout-language, catalog/content, settings/ops/cache, metadata/access, inventory, shipment, SMS/email/AI/Meta, and customer delete runtime failures. The broad admin mutation sweep added 43 response-status deltas with zero removed statuses and no added/removed operations; the regenerated API client remains at `273` paths / `371` operations. Verification: focused contract tests (`7` files / `24` tests), API/API-client typecheck+lint, SDK regeneration, root typecheck/lint/test (`335` files / `2269` tests)/build, env/migration/dist-secret/diff guards, API deploy `9515a2ab-b9f6-4f17-948b-bf69df6ca49f` with Worker startup `87ms`, four live `/readyz` samples ready, live OpenAPI smoke proving representative `409`/`503`/`404` statuses and no unsupported shipment-GET `409`, and live no-cookie admin search `401` with `X-Request-Id`. |
| OPS-004 | P1 | Verified | Platform/CI | Verified quality-gate drift: `.github` was ignored/untracked while a local CI workflow existed, and it ran only install/lint/typecheck/build/test without platform guards. | `.github` is no longer ignored, and the tracked CI quality gate now runs frozen install, dependency audit, `check:env`, migration metadata, `pnpm generate:sdk`, generated-client freshness diff, lint, typecheck, tests, build, API deploy dry-run, dist-secret scan, working-tree cleanliness, and whitespace checks. `pnpm generate:sdk` was repaired by removing the static Cloudflare Agents runtime import from the widget-generation admin route and lazy-loading it only at request time; a source-boundary test prevents regressing that Node/OpenAPI boundary. Verification: `pnpm generate:sdk`, generated-client diff guard, focused API boundary/OpenAPI tests, CI YAML parse, `pnpm audit --audit-level moderate`, `pnpm check:env`, migration metadata guard, `pnpm run deploy:api -- --dry-run`, `pnpm lint`, root `pnpm test` (`323` files / `2174` tests), root `pnpm build`, `pnpm check:dist-secrets`, API deploy `d3ec5396-3d6a-4606-8fe0-fdf7c5080191` from code commit `e4abb2b1`, live health `200`, live readyz retry `200`/`200`/`200`, live OpenAPI `269` paths / `367` operations, and protected widget-generation status smoke `401` pass. |
| CACHE-016 | P1 | Verified | API/Storefront Cache | Verified purge/rewarm durability gap: API invalidation scheduled storefront HTTP purge safely, but missed/failed purge or warm work was not durably replayed and could leave long-lived Cache API entries stale. | Cloudflare-native `storefront-cache` now retries normalized `storefront.cache_purge` intents, queue-driven purges call `/api/purge-cache` with `warm:false`, and successful purges enqueue `storefront.cache_warm` so warm retries never repeat purge/version bumps. Terminal `storefront-cache-dlq` messages are consumed by `scalius-api`, archived in D1 `storefront_cache_queue_failures`, and admin cache APIs can list, replay by re-enqueueing the original payload, or ignore them. Verification: focused cache/queue route tests, regenerated SDK (`266` paths / `364` operations), root typecheck/lint/test (`294` files / `1944` tests)/build/env/dist/migration/drizzle/audit/peer/diff gates, remote migration `0067_storefront_cache_queue_failures`, API deploy `4a2cdddb-d3de-4c9a-8094-5fb407a9ad18`, live API health/readyz, live OpenAPI route/count smoke, remote D1 table proof, unauthenticated cache-DLQ `401`, and Wrangler queue metadata showing `storefront-cache-dlq` consumed by `scalius-api`. D1 outbox remains a future architecture choice only if queue-send-after-commit loss is measured as material. |
| ARCH-004 | P2 | Verified | Checkout/Queue Simplification | Normal checkout now commits synchronously through D1, but the legacy `ORDER_INGEST_QUEUE` binding/consumer/tests/docs remained, preserving an extra async order-ingest surface after the migration window. | Retired the order-ingest surface from Scalius API: removed Worker producer/consumer bindings, queue dispatcher routing, core async ingest handler/tests, stale Env/readiness checks, and current docs. `CreateStorefrontOrderResult` now carries `commitPayload`/`StorefrontOrderCommitPayload` instead of queue-shaped naming, while the historical `checkout-ingest:v1` reservation key stays stable for crash-retry safety. Verification: focused API/core tests, root typecheck/lint/test/build, env/dist/migration/diff guards, API deploy `5955fc55-61eb-4657-8426-aa9192c70923`, live health/readyz, Wrangler dry-run/deploy binding proof with only payment-events/order-notifications/auth-otp queues, and explicit remote removal of the stale `scalius-api` order-ingest consumer. Cloudflare still has a physical `order-ingest` queue with old `testdash` as its only producer and zero consumers; this repo no longer owns or consumes it. |
| PAY-008 | P1 | Verified | Payments/Webhooks | Cloudflare payment reliability research verified `webhook_events` can remain terminal `queued` after queue/DLQ/manual-reconciliation outcomes because payment queue messages did not carry webhook event IDs and consumers did not close the source event row. | Queue messages now carry `webhookEventId`, the consumer marks `processed`/`manual_reconciliation`/terminal `failed`, scheduled maintenance fails stale six-hour queued payment events through an indexed bounded sweep, the `payment-events-dlq` consumer archives compact D1 evidence without replaying payment side effects, and admin order payment history surfaces sanitized webhook issues. |
| PAY-009 | P1 | Verified | Payments/Refunds | Cloudflare payment reliability research verified ambiguous provider refund failures can be locally rolled back even if the provider accepted the refund, allowing duplicate refund attempts on retry. | Fail-closed slices now keep ambiguous online-provider failures pending, add D1 `refund_attempts` as the durable per-allocation operation ledger, block new refunds while active attempts exist, mark provider-successful/local-unreconciled attempts as `reconcile_required`, run a bounded scheduled reconciler that probes Stripe/SSLCommerz/Polar or locally finalizes accepted attempts before releasing claims, expose sanitized admin/customer recovery visibility, and add explicit admin manual refund-attempt recovery through `POST /api/v1/admin/orders/{id}/refund-attempts/{attemptId}/reconcile`. Manual recovery honors the same D1 claim lease as scheduled recovery, can force-check provider-visible active attempts before the next scheduled probe, refuses active leases, and does not prematurely fail fresh `pending` attempts before they are due. Admin payment detail now polls while active refund recovery exists, offers refund-capable operators a compact `Check now` action per active attempt, refreshes order/list/payment queries, and keeps failed attempts retryable through the normal `Issue Refund` path after the active lock clears. Verification: focused core/API/admin/RBAC tests, SDK regeneration (`272` OpenAPI paths), root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`327` files / `2223` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, API deploy `0428a210-b70a-4265-8383-607fd0ba91cd`, admin deploy `fa6562c0-ec23-46cd-9d28-ce31f9740d1f`, live API health/readyz, live OpenAPI route smoke, and dashboard unauthenticated redirect smoke pass. |
| PAY-010 | P1 | Verified | Payments/Inventory | Failed or abandoned online-payment orders can remain `incomplete` with reserved inventory because failed payment events do not cancel/release and expiry only sweeps orphan reservations. | Scheduled stale hosted-payment cleanup now claims, releases, archives, invalidates, and is deployed. |
| PAY-011 | P1 | Verified | Payments/Notifications | Online orders no longer enqueue normal `order_created` customer notifications before payment is confirmed, and `/order-success` no longer finalizes unpaid/incomplete external-payment orders as success. | Receipt DTO includes non-PII payment state; storefront order-success gates copy, cart clearing, and purchase tracking on paid/partial confirmation; incomplete order-created notifications are deferred until payment queue confirmation; focused/root gates, deploy, and live smoke pass. |
| PAY-012 | P1 | Verified | Admin/Payments Settings | Stripe can be marked configured/customer-visible without a publishable key, so online-only checkout may expose a broken Stripe option. | Stripe checkout readiness now requires enabled gateway settings plus secret key, webhook secret, and publishable key; public checkout/config allowlists and registry publication fail closed; admin payment-method saves and Stripe enabled saves reject missing effective fields; admin Payment Gateways exposes usable/blocked states and reasons. Verified with focused core/API tests, SDK regeneration, root gates, deploy, and live smokes. |
| PAY-013 | P1 | Verified | Payments/Checkout Idempotency | Payment session creation for Stripe, SSLCommerz, and Polar is now durably claimed before provider calls, so retries/double-clicks replay or surface an accepted in-flight state instead of creating duplicate external sessions/attempts. | D1 `payment_session_attempts` claim/replay table, provider idempotency keys where supported, deterministic SSLCommerz `tran_id` suffixes, focused/root gates, API deploy, and live D1/OpenAPI/health smoke. |
| PAY-014 | P1 | Verified | Payments/Session Reliability | Hosted payment session provider calls used to have no backend-side deadline, and deposit payment-plan writes used to happen best-effort after provider acceptance, so slow providers could leave five-minute processing conflicts and deposit IPNs could become unprocessable if the plan insert failed. | Deposit payment plans are persisted before provider calls, provider calls run behind bounded deadlines, timeout-shaped results mark attempts failed and return retryable `503`s, live processing attempts return `202 processing`, and D1 now enforces one live provider call per order/gateway/payment type while volatile retry metadata no longer fragments replay. Focused/root gates, SDK regeneration, remote migration `0063`, API deploy, and live health/OpenAPI/D1 smokes pass. |
| PAY-015 | P1 | Verified | Payments/Polar Idempotency | Polar checkout creation is D1-attempt-idempotent for normal retries, but provider recovery needed a crash/timeout-safe path after Polar accepts checkout and before `markPaymentSessionAttemptCreated()` stores the checkout ID. | Polar checkouts now store the D1 payment-session attempt key in metadata, send a stable `externalCustomerId` when available, use Polar `returnUrl`, and reclaimed attempts (`attempts > 1`) query recent open Polar checkouts by product/customer filter plus exact amount/currency/order/payment metadata before creating another provider session. Recovery lookup failures fail closed instead of risking duplicates. Verification: official/installed Polar SDK shape check, focused core/API payment tests, local admin browser rich-editor smoke, root `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, and `git diff --check`. |
| PAY-020 | P0 | Verified | Payments/Partial Payment | Balance payment sessions can be opened while the deposit plan is still pending because session policy accepts any non-cancelled/non-completed plan and payment confirmation only adds incoming amount to `paidAmount`. | Session policy now allows `balance` only after `paymentStatus = partial`, `paidAmount > 0`, and `payment_plans.status = deposit_paid`; confirmed payment processing validates full/deposit/balance amount and plan state before claims and includes payment-plan CAS in the batch success contract. Focused payment-session/process-payment tests and affected typechecks pass. |
| ARCH-002 | P2 | Verified | Notifications/Queues | Order notification outbox cron did not re-enqueue stale `queued` rows, so a lost Queue message after a queued mark could leave notifications stuck. | Scheduled notification outbox flushing now includes `queued` rows older than one hour, replays them through the existing outbox enqueue claim, preserves same-`outboxId` channel receipt idempotency, logs an alertable `staleQueued` count, and adds `order_notification_outbox_queued_idx` for cheap D1 sweeps. Focused core/API tests, migration metadata, and affected typecheck/lint pass. |
| ARCH-003 | P2 | Verified | Checkout/Idempotency | Checkout submit lacked a client/D1 idempotency key, so double-clicks or network retries could create multiple real orders when stock existed. | Storefront now sends a stable `checkoutRequestId`; D1 `checkout_attempts` claims the submit before order writes, reserves the canonical order/receipt IDs, replays committed same-payload retries, returns pollable `202` for active duplicates, rejects changed payloads with `409`, and backs receipt/status recovery when KV is missing. |
| CHECKOUT-016 | P1 | Verified | Storefront/Checkout Auth | COD-only authenticated checkout could fail when guest checkout was disabled because the server-rendered COD form path did not forward the HttpOnly customer session to API order creation, and the browser submit gate trusted only the readable `cs_auth` mirror cookie. | Shared `cs_tok` cookie parsing, COD server POST session forwarding, guest-disabled submit verification through `/api/customer-auth/me`, and regression tests for forwarding/source wiring. Verified with focused checkout-auth tests, storefront typecheck/lint, root typecheck/lint/test/build/platform guards, storefront deploy `055a7069-64df-4406-8b2d-9cf1af2e7c1c`, live `/cart` and `/api/customer-auth/me` smokes, and deployed cart JS marker checks. |
| CHECKOUT-017 | P1 | Verified | Storefront/Checkout Locations | Public order creation accepted stale/forged inactive city/zone/area IDs or fell back to submitted display names, while storefront selectors only expose active delivery data. | `createStorefrontOrder()` now fresh-validates active, non-deleted city→zone→area type/parent invariants, stores D1 location names instead of caller-provided names, and route coverage proves location validation failures return 400 before checkout status/receipt KV writes or commit. Verified with focused core/API tests, core/API typecheck/lint, root typecheck/lint/test/build/platform guards, API deploy `0117b18c-4d70-4181-afff-0bf1cc8b71b1`, live API health/locations/checkout-config smokes, and live storefront cart smoke. |
| CHECKOUT-018 | P2 | Verified | Admin/Checkout UX | Partial payment admin copy still implies COD usefulness even though current policy makes partial payment online-only and can force full online payment for carts at or below the deposit amount. | Admin checkout-flow copy now names partial payment as online advance payment, says COD is hidden while enabled, explains low-cart full-online behavior, and adjusts guest-disabled COD-only copy to authenticated COD-only. Verified with affected/root gates, admin deploy, and live browser smoke. |
| CHECKOUT-019 | P1 | Verified | Checkout/Idempotency | Committed checkout replays could be blocked by newer checkout policy or rate limits because policy/rate-limit checks ran before D1 checkout-attempt claim/replay. | `POST /orders` now uses read-only `resolveExistingCheckoutAttempt()` before mutable policy/rate gates so committed same-payload attempts replay `201` and active duplicates return `202`; missing/failed/stale attempts still pass policy/rate before D1 claim creation/reclaim, with post-claim race handling retained. |
| CHECKOUT-020 | P1 | Verified | Admin/Payment Settings UX | Payment gateway settings conflate provider readiness/enabled state with storefront allowlist state, so partial saves or lazy credential loading can show misleading enabled/live/sandbox badges. | `GET /payment-methods` now returns raw merchant preferences plus effective active/default methods and per-gateway provider/visibility/readiness fields; SSLCommerz/Polar enabled saves and payment-method preference saves preflight checkout readiness, Polar requires webhook secret, and preference writes use one `safeBatch()`. Admin UI separates provider enabled from Show at checkout and renders visible/hidden/blocked states. Verified with focused/root gates, API/admin deploy, live OpenAPI, and browser smoke. |
| CHECKOUT-021 | P2 | Verified | Storefront/Checkout Transfer | SessionStorage failures during cart-to-checkout transfer silently redirected customers back to cart because checkout transfer data was missing; quick-buy had the same silent empty-cart risk when `quickBuyData` could not be stored. | Checkout transfer writes now require read-back-verified `scalius_checkout_data` before leaving cart, treat the gateway snapshot as optional because checkout already falls back to fresh config, show a persistent cart-side storage error instead of navigating on blocked/quota-full storage, and render an explicit `/checkout` recovery message/action for missing or unreadable transfer data. Quick-buy storage failure now redirects to cart with a server-rendered actionable error instead of an unexplained empty cart. Verification: focused storefront tests (`4` files / `30` tests), storefront typecheck/lint/build, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`321` files / `2168` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, storefront deploy `c57ac117-1e6a-45a0-a06b-91e6ffad4715`, live storefront `/health`, live `/checkout` and `/cart?quickBuyStorage=blocked` HTTP smokes, live cart error-content smoke, and deployed JS marker checks for checkout recovery plus cart transfer guard logic. |
| CHECKOUT-022 | P1 | Verified | Storefront/Checkout Catalog Freshness | Final order creation filters deleted products but not inactive products, so stale carts or direct API calls can still order unpublished products despite the inactive-product error text. | `createStorefrontOrder()` now filters products by `isActive = true`, keeps an explicit active-row guard, and rejects submitted variants that belong to a different product before checkout-status/receipt KV or order commit. Verified with focused core/API tests, root gates, API deploy, and live read-only smokes. |
| CHECKOUT-024 | P0 | Verified | Storefront/Checkout Payments | Failed checkout/payment-session/order errors can leave the pay button permanently disabled because the error path restores button text before `isProcessing` is reset. | Checkout error branches now hide overlays and reset `isProcessing` before restoring button text; focused storefront checkout handler tests and affected typecheck pass. |
| CHECKOUT-025 | P0 | Verified | Storefront/Checkout Payments | Full/deposit choice is derived from stale browser/sessionStorage totals after the server commits an authoritative order, so price/shipping/discount drift can create an order then fail session creation. | Storefront create-order proxy/helper now returns authoritative `totalAmount`/`paymentMethod`, and browser payment-session calls omit `paymentType`/`depositAmount` so the API derives mode from the committed order. Focused payment-session and storefront handler tests cover server-derived deposit/full fallback. |
| CHECKOUT-026 | P1 | Verified | Storefront/Checkout Payments | Online payment session failures after order creation do not preserve `orderId`/`receiptToken` for retry, stranding buyers outside the receipt recovery flow. | SSLCommerz/Polar session failures after order creation redirect to receipt-token recovery with payment context; Stripe card/session failures stay on checkout with the idempotent order proof preserved for retry instead of clearing checkout state or redirecting to a non-retryable hosted recovery page. |
| CHECKOUT-027 | P1 | Verified | Storefront/Checkout Gateways | Stale gateway snapshots can intersect to zero usable methods and still render checkout without a clear recovery path instead of falling back to fresh methods or showing settings-changed guidance. | Checkout now discards an empty stale transferred gateway intersection and falls back to fresh public checkout config, preserving normal method rendering after merchant payment-setting changes. |
| CHECKOUT-028 | P1 | Verified | Admin/Payment Settings UX | Admin default payment method could be chosen from raw enabled methods even when checkout flow hides it, while storefront did not apply the public active default and asked buyers to select manually. | Admin/API flow-filter `activeMethods`/`activeDefaultMethod`, reject hidden defaults on save, limit the dashboard default selector to flow-visible enabled methods, public checkout config exposes only a rendered/provider-ready `activeDefaultMethod`, and storefront preselects it. Focused/root gates, full deploy, and live checkout config KV HIT verified. |
| CHECKOUT-029 | P1 | Verified | Admin/Payment Settings UX | Payment Gateways initializes local state to COD defaults; if the settings read fails, the UI can still render and saving may overwrite real payment settings with defaults. | Payment Gateways now clears the payment-method payload on load failure, renders a locked retry state instead of the checkout visibility editor, and refuses visibility saves while settings are loading or missing. API tests characterize COD-only as a valid intentional server payload, so the UI owns preventing fallback/default saves. Focused admin/API tests, root gates, build/env/dist/migration guards, admin deploy `08ffaf8f-abe0-41c4-9ee9-e67ee9e8b3e4`, live login/redirect smoke, and built asset marker probe pass. |
| CHECKOUT-040 | P2 | Verified | Admin/Checkout Flow UX | Checkout Flow preview skipped payment-method compatibility checks when the payment-method query failed, so the readiness panel could look healthier than reality and saves were left to backend rejection. | Checkout Flow now separates payment readiness pending vs unavailable, shows an amber retry blocker when payment methods cannot be loaded, marks Payment flow unknown, and disables checkout-flow saves until the payment-method payload is available. Focused admin boundary test, admin typecheck/lint/build, dist/diff guards, admin deploy `88288347-159e-4704-bd9b-90f7e26e0c1f`, live login/redirect smoke, and deployed asset marker probe pass. |
| CHECKOUT-041 | P1 | Verified | Admin/API Checkout Settings | Standard checkout could still be saved with neither COD nor a configured online gateway, leaving customers with a valid-looking checkout mode but zero usable payment methods. | Shared checkout-flow validation now rejects `checkoutMode = "all"` unless COD or at least one online gateway is usable, and the admin preview policy mirrors the same blocker through a pure helper covered by focused tests. Verification: focused admin checkout-flow policy tests, core checkout-flow tests, API payment/settings tests, admin/core/API typecheck and lint. |
| CHECKOUT-050 | P1 | Verified | Storefront/Checkout Orders | SSLCommerz checkout could pass policy/rate-limit/attempt setup before proving strict credential readability and provider amount bounds for the committed order total, making broken credentials or out-of-range totals look like late checkout/payment failures. | Public order creation now checks active payment methods with strict `CREDENTIAL_ENCRYPTION_KEY` provider reads and rejects SSLCommerz totals outside provider BDT limits before rate limits, checkout-attempt claims, KV hints, inventory/order mutation, or provider setup. Focused API order/payment-session tests, root gates, API deploy `71d37e84-446a-4c90-9dbb-b1f22dba66b7`, and live health/readyz/OpenAPI/ops smokes pass. |
| CHECKOUT-051 | P1 | Verified | Storefront/Checkout Recovery Tests | Checkout status/receipt KV recovery tests were stale around `waitUntil()` and hashed proof keys, so regressions could reintroduce raw checkout proof persistence/logging or make post-commit side effects untested. | The order-create suite now awaits scheduled `waitUntil()` work, asserts status/receipt KV keys are HMAC-derived, and proves raw `chk_*` receipt proof does not appear in KV keys, receipt values, or logs. Focused API order-create tests, root gates, deploys, and live ops check listed in `AUTH-050` pass. |
| CHECKOUT-052 | P1 | Verified | Storefront/Checkout Status Proof | Duplicate/in-flight checkout recovery returned the `chk_` receipt proof as the polling token for `/orders/status/{token}`, putting a bearer receipt proof in browser URLs while the first request finished. | Checkout attempts now derive a separate `cst_` status token from the D1 request key; `/orders/status` rejects `chk_` receipt proofs before KV/D1 lookup, 202 responses return `statusToken` only, and the storefront poller uses `cst_` without inventing receipt-token fallbacks. Verification: focused API order-create/status tests, storefront order helper tests, SDK regeneration, root gates, full deploy, live `GET /api/v1/orders/status/chk_status` returns `400 VALIDATION_ERROR`, and live OpenAPI confirms `202` create-order responses include `statusToken` but no `checkoutToken`/`receiptToken`. |
| CHECKOUT-042 | P2 | Verified | Storefront/Checkout Proxy Runtime | Same-origin checkout order/payment-session proxies still used the authenticated API helper path for public proof/origin-guarded routes, adding avoidable token work and auth-failure surface to the hot buyer submit path. | Storefront checkout order, status, receipt, cart-validation, Stripe, SSLCommerz, and Polar calls now use explicit public/no-store backend calls while preserving customer session forwarding only where needed. Focused storefront checkout/order proxy tests, public API payment/order boundary tests, root gates, storefront deploy `95dd055c-6890-43e7-81d9-0056838a44aa` / version `4d46c4fe-6187-4469-bfb9-1b9c2f3ac53b`, live sitemap/search smokes, and live invalid checkout/payment-session proxy smokes prove fail-closed `400`/`404` responses without service-token/auth failures. |
| CHECKOUT-043 | P1 | Verified | Storefront/Stale Cart Repair | A final reservation failure could create a claimed checkout attempt and redirect the buyer to cart repair, but after the buyer removed/reduced/refreshed the stale line the browser reused the same `checkoutId`, making retries fail the duplicate-attempt guard instead of letting the repaired cart submit. | Storefront cart repair state now rotates the checkout ID whenever blocking server cart issues are applied or repaired, while normal cart navigation preserves the active checkout ID. Remove, reduce, and refresh issue actions rotate before mutation only while the cart is blocked by server issues, so repaired carts get a fresh idempotency key and failed claimed attempts are not reused. Verification: focused cart checkout-id regression tests, focused UCP/cart/post-sale regression suite (`10` files / `64` tests), storefront typecheck, local post-sale smoke, root gates, storefront deploy `8062ecd7-70d2-4ef6-91c0-f9cdfec0dcf0`, live release check, and ops check pass. |
| CHECKOUT-044 | P1 | Verified | API/Storefront Checkout Status | Checkout status polling still exposed receipt proof in status JSON/KV hints during duplicate/in-flight order recovery, even though receipt proof is meant to stay out of URL-shaped polling and durable hints. | `/orders/status/{token}` now rejects non-`cst_` status tokens before KV/D1 reads, checkout-status KV stores only metadata, legacy KV rows are sanitized before response, and the storefront privately replays the idempotent order request to recover receipt proof for the same-origin receipt-cookie handoff. Verification: focused API/storefront order tests, root typecheck/lint/test (`405` files / `2879` tests)/build, env/dist-secret/diff guards, API deploy `0ec46374-d6ff-4d7e-86c8-40252f03274d`, storefront deploy `5ecc648e-4362-46a0-a398-079db52e3e2f`, live `GET /api/v1/orders/status/chk_live_probe` returned `400 VALIDATION_ERROR`, live `pnpm release:check --timeout-ms 30000`, and live ops check `ops-check-20260707t194041192z-06679e48` pass. |
| CHECKOUT-045 | P2 | Verified | Admin/Checkout Settings | Checkout Flow partial-payment preview and helper copy allowed any positive advance amount, while SSLCommerz-backed checkout rejects amounts outside BDT 10-500,000. | Admin checkout preview, inline warning, input min/max, and copy now share the SSLCommerz amount limits, warning on BDT 5 and BDT 500001 before save while allowing BDT 10. Verification: focused admin policy tests, admin typecheck/lint/build through root gates, root test/build, env/dist-secret/diff guards, admin deploy `1337f458-2d62-40c3-a9f3-4d9091d2902e`, live `pnpm release:check --timeout-ms 30000`, and live ops check `ops-check-20260707t194041192z-06679e48` pass. |
| CHECKOUT-030 | P1 | Verified | Storefront/Cart Freshness | Returning carts rendered stale localStorage item snapshots as purchasable when one, many, or all products/variants were out of stock, inactive, deleted, or price-changed; online checkout failed late after the buyer clicked pay. | Batched `validateStorefrontCartItems()` plus `/orders/cart-validation` and storefront `/api/checkout/validate-cart` now validate cart load/changes and cart-to-checkout transfer, return per-line availability/stock/price/variant issues, render remove/reduce/refresh repair actions, block checkout while affected lines remain, and passed focused/root gates plus live stale-line smoke. |
| CHECKOUT-031 | P1 | Verified | API/Checkout Error UX | Product/variant/stock checkout failures were first-error-only and could expose internal product/variant IDs such as `Product {id} not found` or `Insufficient stock for variant {id}`. | Product validation and late inventory reservation now return structured customer-safe `details.itemIssues`; the storefront create-order proxy preserves `details`, checkout/cart error handling reads all item issues, and focused/root/live validation smokes pass. |
| CHECKOUT-032 | P2 | Verified | Storefront/Quick Buy | `/buy/{slug}` could create a cart item for an existing product/variant without checking live stock, so a buy-now customer could land in checkout with an immediately failing item. | `/buy/{slug}` now calls the storefront cart-validation helper before writing quick-buy session data or analytics, rejects invalid quantities, maps stale stock/price/product/variant issues back to product-detail repair copy, and keeps final API reservation as the authority. Focused/root gates, storefront deploy `43fc8f81-2590-416f-b8cc-005d73881ee9`, and live quick-buy/invalid-quantity/proxy-origin smokes pass. |
| CHECKOUT-033 | P1 | Verified | Storefront/Checkout UX | `/checkout` trusted the transferred cart snapshot until final order creation; if stock, price, variant, or publication state changed while the buyer was already on checkout, the API blocked the order but the buyer saw a top-level payment/submit error instead of per-line repair actions. | Checkout now validates the transferred cart snapshot on page load and again immediately before payment/order submit. Stale or unreadable validation writes a one-shot `scalius_cart_repair_state` payload and redirects to `/cart?checkoutIssues=1`; the cart consumes that state, renders per-line repair actions, and then revalidates with the backend as authority. Late create-order races also preserve `details.itemIssues` through gateway handlers, and `POST /orders` accepts `cartKey` so duplicate lines map deterministically. Verified with focused/root tests, root typecheck/lint/build/env/dist/migration guards, API deploy `4a0bd12e-907a-4671-87e5-5311e05c39a1`, storefront deploy `8e6ebdc1-6f97-4eed-a9d2-b7ba13d91e75`, live API health/OpenAPI `cartKey` smoke, and live storefront checkout/cart asset probes. |
| CHECKOUT-034 | P1 | Verified | Storefront/Product Inventory | Storefront product detail synthesized a fake `default` variant with huge stock for products without persisted variants, but cart validation normalized `default` to no variant and rejected checkout, so the product could look buyable but could not be purchased. | Product detail now marks `product.hasVariants === false` as unavailable, disables Add to Cart/Buy Now, and the product controller blocks any click path with a buyer-safe unavailable toast. `/buy/{slug}` also redirects variantless products back to product detail instead of writing quick-buy cart data. Core validation still rejects no-variant/no-inventory buyer lines until product-level inventory exists. Verified with focused product/checkout/core tests, root gates, API deploy `4a0bd12e-907a-4671-87e5-5311e05c39a1`, storefront deploy `8e6ebdc1-6f97-4eed-a9d2-b7ba13d91e75`, and live product-controller asset probe. |
| CHECKOUT-035 | P1 | Verified | Storefront/Cart Validation | Cart validation failure clears issues and returns `true`, and the COD server path can refetch/recalculate current prices without explicit buyer acceptance if validation is bypassed or transiently fails. | Storefront cart validation now keeps a blocking global freshness error when `/api/checkout/validate-cart` fails or returns an unusable response, invalidates in-flight validation on cart mutation, and keeps final order creation guarded by `validateStorefrontCartItems()`. Focused/root gates pass; storefront deploy `546fe109-7220-4743-8b18-ad7ee90694f6`; live cart asset marker and storefront proxy stale-item smoke pass. |
| CHECKOUT-036 | P2 | Verified | Storefront/Cart Repair UX | Core cart validation could emit a `select_variant` repair action when a product requires a concrete variant, but the cart row UI did not implement that action and fell back to “Remove item.” | Cart issue actions now render `select_variant` as a `Choose option` link to the product detail page when the local cart line has a slug, while preserving remove fallback for slugless legacy lines. The action renderer is covered by storefront tests and live cart asset probes. |
| CHECKOUT-037 | P1 | Verified | Storefront/Checkout Delivery Freshness | Cart/checkout item freshness was preflighted, but active shipping method and city/zone/area freshness were only rechecked during final order creation, so stale checkout tabs could fail late with generic payment/submit errors. | Shared `validateStorefrontDeliveryPreflight()` now validates active city/zone/area hierarchy and active shipping method before checkout policy, rate limits, checkout-attempt claims, KV writes, or order commit; `/orders/cart-validation` can also preflight delivery once city/zone are present. Focused/root gates, SDK regeneration, API deploy `4a3c53bb-5266-4164-a441-f1e1dc044bcb`, storefront deploy `c6a617c7-67ae-4669-b6a7-3d88a9c2cb43`, and live OpenAPI/cart-validation smokes pass. |
| CHECKOUT-038 | P1 | Verified | Storefront/Cart Delivery Totals | Cart validation returned current `freeDelivery`/delivery eligibility, but the storefront ignored that data and kept local item snapshots for totals, so server shipping could differ from what the buyer saw. | Storefront cart validation now reconciles validated `freeDelivery` state back into local cart lines before totals/checkout transfer, clears shipping-sensitive discounts on eligibility drift, and covers true-to-false plus false-to-true changes in storefront tests. Focused/root gates and storefront deploy `c6a617c7-67ae-4669-b6a7-3d88a9c2cb43` pass. |
| CHECKOUT-039 | P2 | Verified | Storefront/Cart Repair UX | Stale carts with multiple/all deleted, unpublished, variant-required, price-changed, or out-of-stock lines are structurally validated, but buyers must repair each line one by one and COD-only late server failures can lose the rich per-line repair state. | Structured COD failure `itemIssues` now seed the cart repair UI, bulk remove/reduce/refresh actions are rendered for affected lines, and original cart keys are preserved for COD repair. Focused/root gates, local desktop/mobile screenshots, local D1-backed cart-validation smoke, storefront deploy `1b0f4231-c971-4aaa-8f35-4530c1afdeff`, live `/cart` 200, and deployed asset marker probes pass. |
| DISCOUNT-001 | P1 | Verified | Checkout/Discount Concurrency | Discount `maxUses` and one-per-customer checks are advisory before order writes; commit-time redemption must be D1-authoritative and buyer-friendly under races. | Existing `maxUses` trigger remains the global cap authority; migration `0050_discount_customer_redemptions` adds immutable per-phone redemption claims and replaces the one-per-customer trigger; synchronous checkout and legacy queue ingest map trigger aborts to clean `ValidationError`s after releasing reserved stock. Verified with focused core/API tests, local/remote D1 schema proof, root tests, API deploy `39af927b-019c-4f19-8bc9-ec56d73d8c8e`, and live health/OpenAPI/deployment smokes. |
| INV-001 | P1 | Verified | Storefront/Inventory | Variantless purchasable products bypassed stock reservation because checkout sent `variantId: null` and core used `inventoryAction: "none"`. | Storefront cart/order validation now rejects buyer lines without a concrete active variant for products that have variants, rejects no-variant/no-stock products until product-level inventory exists, and focused checkout validation/reservation tests cover the fail-closed behavior. |
| INV-002 | P1 | Verified | Storefront/Inventory Cache | Synchronous public order creation now invalidates affected product availability caches after the order commit, so product detail/search/listing availability cannot remain stale after checkout stock reservation. | API order route schedules targeted `invalidateProductAvailabilityCaches()` after successful commit and catches purge failures; helper tests cover exact product/search/API/storefront cache keys; focused/root gates, API deploy, and live smokes pass. |
| INV-003 | P2 | Verified | Checkout/Inventory Rollback | Storefront checkout and admin manual-order create rollback after failed order-write batches no longer use best-effort serial `releaseMultiple()`. `commitStorefrontOrderPayload()` uses `releaseReservedStockBatch()` with `checkout-rollback:v1`; `createOrder()` uses the same strict helper with `admin-order-create-rollback:v1`. | Rollback release merges entries, verifies reservation movement quantity, writes a deterministic `released` movement claim plus variant counter update in one D1 `safeBatch()`, retries CAS/transient failures with fresh stock-version reads, treats exact duplicate release claims and already-fully-released reservations as idempotent success, and fails checkout/admin create closed with `503` if cleanup cannot be proven. Verification: focused core inventory/order commit/admin create tests, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`321` files / `2162` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, API deploy `3a023022-ad7a-410f-9351-730b7e2efbdb` (Worker startup `147ms`), and live API health/readyz/OpenAPI smokes (`269` paths / `367` operations, `POST /api/v1/orders` and `POST /api/v1/admin/orders` present). |
| INV-004 | P1 | Verified | Checkout/Inventory Race | Product/variant validation filtered deleted/inactive rows before order build, but `reserveStockBatch()` later reloaded variants by ID without rechecking `deletedAt` or parent product active/deleted state. | `reserveStock()` and `reserveStockBatch()` now join parent products for reservation-time sellability reads, require active/non-deleted product and non-deleted variant rows, guard variant counter updates, map late failures to structured item issues, and pass focused/root tests. |
| INV-005 | P1 | Verified | Product/Inventory Architecture | Simple products with no option variants were modeled as no-inventory products, so demo/simple products could be shown as unavailable after the fake `default` variant was removed. Product-level inventory would split the checkout/order/inventory authority and reintroduce variantless bypass risk. | SKU-first rule implemented: every sellable product has a real `product_variants` row, simple products use one hidden/default no-option SKU, optioned products require explicit selected SKUs, SKU-less or ambiguous no-option SKU sets fail closed, and `order_items.inventory_tracked` snapshots whether stock movement applies. Migration `0055_default_sku_inventory_tracking` backfills untracked default SKUs for products without variants and marks old variantless order items untracked. Verified with SDK regeneration, migration metadata, root typecheck/lint/test/build/env/dist/diff gates, remote D1 proof (`products_without_active_sku = 0`, `active_default_skus = 15`, `historical_variantless_untracked = 36`), API deploy `c930b862-fe56-439f-a6b6-fff9310f98c9`, admin deploy `68fefc75-d84a-44ab-8e38-991b49a4cc19`, storefront deploy `e10a01cc-4e21-4ba0-af89-a75ee8095c51`, live API/OpenAPI/cart smokes, and live default-SKU product page plus `/buy/{slug}` smokes. |
| INV-006 | P1 | Verified | Product/Admin/Storefront SKU UX | SKU-first architecture was mostly enforced, but COD-only cart submit still recomputed stock for variantless/default lines, admin manual orders could add “main product” variantless items, and the product variant UI treated the hidden simple SKU like a normal stock row. | COD-only submit now uses cart validation and validated SKU IDs/prices/delivery, admin manual orders require explicit SKU selection, hidden simple SKUs show as protected/no-stock-limit instead of out-of-stock, and merchant-created variants require at least one customer option. Focused/root gates, full deploy, live D1 invariant proof, and live default-SKU product `/buy` smoke pass. |
| INV-007 | P1 | Verified | Product/Inventory Stock Ledger | Product variant edit, bulk edit, scanner stock-set, and manual inventory adjustment used to be able to update physical stock outside a strict movement claim. Variant duplication also copied on-hand stock into a new SKU. | Existing-SKU stock edits now split `stock` out of ordinary metadata writes. Movement insert and variant stock/`stockVersion` update share the same D1 batch and `stockVersion` precondition; failed claims retry or conflict instead of silently losing the audit row. Variant duplication now starts stock/reserved/preorder at zero. Focused core tests cover single edit, bulk edit, duplicate, stock-set, clamped adjustment deltas, and manual inventory adjustment; product routes still invalidate product catalog caches after variant saves. |
| INV-008 | P2 | Verified | Product/Admin Variant UX | Simple products previously exposed their protected default SKU inside the generic Product Variants table, making an intentional no-stock-limit SKU look like a broken variant row. | Admin now classifies SKU topology into simple/optioned/ambiguous modes: one protected no-option default SKU renders a Simple Product SKU panel, optioned products hide that protected SKU from customer-option tools but reserve its SKU for import/bulk conflict checks, non-default/no-option rows fail closed, option rows require size or color in schema/UI/import/bulk generation, default SKUs cannot be deleted/bulk-edited through generic option tools, simple SKU price/discount stays authoritative from the product Pricing card, dirty simple-SKU edits save before first-option setup, first option setup shows the protected SKU and pre-fills price/stock/weight while forcing a new SKU, import/bulk failures keep merchant input open, active products cannot delete the final customer option and silently fall back to a hidden default SKU, and admin edit/detail surfaces describe untracked simple/default SKUs as `No stock limit` rather than `Always available`, `Not tracked`, or red zero availability. Verified with focused variant/auth tests (`4` files / `42` tests), admin/core/API typechecks, admin/core lint, admin build, env/dist-secret checks, no-JS login smoke, mobile overflow check, local Chrome screenshots under `/var/folders/03/rwfhbyc170j01rb7tz5c33qm0000gn/T/scalius-product-variant-ux-20260622`, `git diff --check`, API deploy `2f5d9cad-7849-412e-a52b-9f419ece1a45`, admin deploy `0096d1fd-78b1-4b0b-8e79-b5aad52fa951`, live API health `200`, and live dashboard login HTML smoke. Remaining work is tracked as follow-on UX/product scope, not a blocker for this slice: an explicit optioned->simple conversion workflow and a cart repair flow that replaces stale hidden-default lines after selecting a new option. |
| INV-009 | P1 | Verified | Product/Storefront Simple-SKU Drift | SKU-first architecture was verified, but legacy/demo products could still drift into buyer-visible unavailability when they had zero active SKUs or one old no-option SKU left as tracked zero-stock, and storefront product detail/quick-buy duplicated buyer-variant selection. | Shared storefront buyer-SKU resolver now rejects fake `default` placeholders, resolves exactly-one no-option SKU as a simple product, ignores hidden defaults for optioned products, and fails closed for ambiguous no-option sets. Product detail, JSON-LD, quick-buy, and client add-to-cart use the same availability rules; untracked simple SKUs no longer fail client-side stock validation. Migration `0057_simple_sku_legacy_repair` inserts missing hidden simple SKUs and normalizes zero-stock/no-movement legacy simple SKUs to untracked. Verified with focused tests, migration metadata, local SQLite repair smoke, root typecheck/lint/test/build/env/dist/diff gates, full deploy (API `c88bc6dc-101a-4470-95ea-fdb7064109c6`, admin `ee8518dd-d27a-4164-80a8-92c54eb01188`, storefront `22bdbcfd-b567-410c-8973-2636e26de2c4`), live API/OpenAPI smokes, remote D1 invariants (`products_without_active_sku = 0`, `ambiguous_no_option_products = 0`, `tracked_zero_simple_no_movement = 0`), and live `test-products` product page plus `/buy/test-products?qty=1` smokes showing the persisted default SKU as purchasable. |
| INV-010 | P1 | Verified | Admin/Product Variant Stock-Limit UX | Option SKU edit paths still treated stock as the only inventory control: first option setup could inherit `No stock limit` from the old simple SKU, row edit/spreadsheet edit could not turn tracking back on/off, CSV import/export dropped `trackInventory`, and bulk generator preview showed only a stock number. | `trackInventory` is now explicit across row edit, spreadsheet edit, first-option defaults, bulk generator, preview, and CSV; ignored bulk stock edits are normalized when tracking is off; over-reserved availability clamps to sold out. Focused/root gates, local and live browser smokes, admin deploy `ecac9094-7bd7-40ae-8212-6fd0d9c00784`, and live asset markers pass. |
| INV-011 | P2 | Verified | Admin/Product Variant UX | Product option rows hid edit behind a kebab menu, `VariantManager` was eager on product edit first paint, mobile variants fell back to a cramped table, and merchant-facing SKU/stock labels still said `SIMPLE SKU`, `NO LIMIT`, `Current simple SKU`, or `Stock: not tracked`. | Variant rows now expose direct edit actions with accessible labels, mobile renders compact variant cards, the product edit route lazy-loads `VariantManager` behind a `Suspense` fallback, and copy is normalized to `Product SKU`, `No options`, `No size`, `No color`, and `No stock limit`. Verification: focused variant/route boundary tests (`36` tests), admin typecheck/lint/build, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`260` files / `1660` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, old-label source/dist scan, local `pnpm dev:doctor:admin` after warmup, local browser screenshots for simple desktop/mobile product edit, optioned desktop/mobile variants, and simple product view under `/tmp/scalius-product-variant-inv011-20260622/`, admin deploy `d3d79472-ad60-4d59-9213-f3bc6eca0add`, and live dashboard auth/login smoke (`/auth/login` 200, protected product edit redirects to login without session). |
| INV-012 | P2 | Verified | Storefront/Product Catalog SKU Invariants | Storefront listing/detail/search, global search, filterable attributes, collections/homepage, shortcode products, and quick-buy flows could drift from the SKU-first rule if they only checked active product rows or treated empty/non-default no-option variants as purchasable. | Public catalog eligibility now uses a shared buyer-resolvable SKU predicate; active products require either real option SKUs or one protected default simple SKU. Admin restore repairs active SKU-less restores, selection validation fails closed for zero variants, shortcode and quick-buy use the same buyer-visible resolver, and non-default no-option rows are not treated as simple products. Verification: focused SKU/public-boundary tests (`7` files / `21` tests), quick-buy route test (`8` tests), core/storefront/root typecheck, core/storefront/root lint, root tests (`263` files / `1670` tests), production build, env/dist/migration guards, remote D1 invariant smoke (`invalid_active_public_products = 0`), API deploy `d999ec93-bbd9-4e30-ad5f-996e7db4019c`, storefront deploy `2f5d5bc7-bd89-4603-803f-fd7973eee3c6`, live API health/products/detail smokes, and live storefront home/search/product/quick-buy smokes. |
| INV-013 | P1 | Verified | Product/Admin/Storefront SKU Semantics | Legacy/default simple SKUs can carry customer-looking option labels such as `Default`, so merchants see a confusing variant row and public `hasVariants` can mean “has a protected default SKU” instead of “has customer choices.” | `is_default` is now the protected simple-SKU authority: default SKU option labels are normalized in admin, variant APIs, storefront DTOs, and migration `0062`; public `hasVariants` is true only for non-default customer-option SKUs; cart validation rejects sole non-default no-option SKUs. Verified with focused admin/core/API tests, root typecheck/lint/test/build/env/dist/migration/diff gates, local API smokes for simple and optioned products, and browser/headless screenshots under `/tmp/scalius-product-setup-20260622` proving admin `Option 1` image mapping, simple SKU no-option/no-stock-limit UX, purchasable simple storefront, and tracked-zero option storefront unavailable state. |
| INV-014 | P2 | Verified | Admin/Product Variant Editor UX | The option add/edit row was visually noisy and confusing: barcode fields competed with primary SKU/option/pricing edits, the action cell could clip at normal admin widths, and the mobile fallback risked becoming a squeezed table instead of a usable editor. | Reworked add/edit into a compact spreadsheet-style row on desktop with 32px cell controls, icon-only save/cancel actions, secondary barcode fields in a popover, generic Option 1/2 copy with size/weight/color/style examples, and a separate compact mobile card. Benchmarked against Shopify bulk/variant editing, Square item variations, Airtable grid editing, and Shopify bulk variant APIs; generated a UI mockup reference with the image generator; verified local desktop edit/add, barcode popover, mobile edit/add screenshots, no horizontal overflow, focused variant boundary test, admin typecheck, and admin lint. |
| INV-015 | P1 | Verified | Admin/Product Variant Grid UX | The prior compact row still looked like a mini form squeezed into a table: stock tracking and stock quantity shared one cell, barcode competed with SKU, the table had no dedicated stock-limit column, bulk edit used a different column contract, and form-contained variant action buttons could accidentally submit the product editor. | Desktop option rows now use a stable SKU grid with a dedicated `Stock limit` column, `On Hand` as quantity only, read-only `Available`, borderless cell-like row inputs, barcode/details in the actions popover, aligned bulk-edit columns, and explicit `type="button"` on variant-module controls. Research cross-checked Shopify variant/bulk-editor rows, Square item variations, WooCommerce variation properties, and Airtable dense grid behavior; image generation produced a spreadsheet-style reference mockup. Verified with local desktop/mobile screenshots under `/tmp/scalius-variant-grid-ui-20260623`, no product-page console/page errors, focused admin typecheck/lint, `VariantManager-boundaries.test.ts` (`9` tests), root typecheck/lint/test (`268` files / `1737` tests), build, env/dist/migration/diff guards, admin deploy `610943ab-4e7f-471e-940b-8d82b43839d4`, and live dashboard login/protected-route smokes. |
| INV-016 | P2 | Verified | Admin/Product Variant Grid Polish | The desktop option editor could still feel wider than the product form, and placing barcode controls in the far-right action rail made the spreadsheet row feel like a crowded mini-form again. | Removed the desktop inline barcode action from the row action rail, compacted the SKU/option/stock/discount/update column budget to avoid normal desktop page-level horizontal overflow, kept save/cancel visible in a sticky action column, and preserved generic `Option 1`/`Option 2` examples. Rechecked Shopify bulk editing, Shopify variant editing, WooCommerce variable-product variation controls, and Square item-option variation docs; generated image references and retained only the dense-grid/option-value lessons. Verification: focused variant boundary test (`9` tests), admin typecheck, admin lint, local Chrome screenshots under `/tmp/scalius-variant-ui-research-20260623`, no console/page errors, and desktop overflow probe `innerWidth = scrollWidth = 1440`. |
| INV-017 | P2 | Verified | Admin/Product Option Editor Compactness | Follow-up UI pass made the product option toolbar/table/form more compact and added spreadsheet-mode draft row creation, but the source-boundary tests still encoded older labels/widths and the compact table briefly drifted toward size/color-only semantics. | Preserved the compact editor while restoring generic `Option 1`/`Option 2` labels with size/weight and color/style examples, kept protected simple SKUs labeled `Product SKU`, kept no-limit inventory copy as `No stock limit`, removed stale TypeScript/lint noise, and updated boundary tests to guard the new compact grid contract. Verification: `git diff --check`, focused variant tests (`2` files / `11` tests), admin typecheck, and admin lint. |
| INV-018 | P1 | Verified | Checkout/Stale Cart SKU Authority | Public catalog eligibility ignored literal legacy `product_variants.id = "default"`, but checkout cart validation could still treat such a row as the only simple SKU and let a stale/local cart buy a product the storefront would hide. | `validateStorefrontCartItems()` now excludes literal `default` variant ids in the D1 read and in-memory SKU set before resolving simple products, so cart page repair, quick-buy, COD, online checkout, and direct API submit fail closed consistently. Verified with focused cart/order tests (`7` files / `88` tests), core/storefront/API typecheck and lint, API dry-run build, env/dist/migration guards, API deploy `4a2a3cd9-d1fd-4e5f-87c0-8680c5c0b024`, live health/readyz, and remote D1 proof (`legacy_default_sku_rows = 0`). |
| INV-019 | P2 | Verified | Admin/Product Variant Topology | Soft-deleted SKUs could still affect the admin variant manager's simple/optioned/ambiguous mode, making a product look optioned or malformed after deleted rows should have been invisible to merchants. | Variant mode resolution now filters soft-deleted rows before simple/default/optioned decisions, hidden simple-SKU reservation, option matrix rows, stats, and footer counts while keeping malformed active SKU data visible. Verification: focused variant-mode and VariantManager boundary tests, admin typecheck and lint. |
| INV-020 | P1 | Verified | Product/SKU Domain | Variant delete/bulk-delete could hard-delete SKUs with reservations, order references, or inventory movement history; optioned products could mix Option 1-only, Option 2-only, and both-option SKU shapes that the storefront cannot resolve consistently. | Variant delete now rejects reserved stock and non-terminal order references, soft-deletes terminal order or inventory-movement-backed SKUs with a zero-reservation guard, and hard-deletes only unused SKUs. Create, update, bulk-create, bulk-update, and product-save boundaries now enforce one option-axis shape per optioned product while preserving the protected simple default SKU invariant. Verification: focused core product run, root typecheck/lint/test (`381` files / `2619` tests), build/env/dist/migration/drizzle/audit/peer/frozen-install/diff gates, full deploy API `d4da4b57-a37c-4c0b-b777-f7f0f3185331`, admin `fbe06d25-5b34-447d-ab3a-07091d646877`, storefront `8bb81458-f570-4e6c-8c2f-956f13e65f99`, and live ops check `ops-check-20260706t041347670z-23eecc06` pass. |
| INV-021 | P1 | Verified | Product/Permanent Delete History | Product permanent delete could hard-delete variants that already had inventory movement history, orphaning or erasing stock audit evidence. | Single and bulk permanent product delete now inspect all child SKU ids first, block the destructive delete when any child SKU has inventory movements, and clear low-stock alerts before deleting movement-free variants. Verification: focused product admin delete tests, root typecheck/lint/test/build/security gates, deploy API `5f4fe9a5-672b-4a35-a43d-c2c12ff345d9`, admin `16c0aa27-835f-409c-b431-cc70c6bb2800`, storefront `9e2cfb69-b7c8-4cf4-bdcb-e507b86145da`, ops `60612cda-df46-497a-82ad-5853e7c14416`, and live ops check `ops-check-20260706t181040901z-b360a3d8` pass. |
| PERF-010 | P2 | Verified | Storefront/Product Listings | Cold category/search listings were under-indexed for actual storefront newest and attribute-filter shapes. | Migration `0075_storefront_listing_indexes` adds `products_public_newest_idx(is_active, deleted_at, created_at DESC)`, `products_public_category_newest_idx(category_id, is_active, deleted_at, created_at DESC)`, and `product_attribute_values_attr_value_product_idx(attribute_id, value, product_id)`, with `PRAGMA optimize`. The attribute filter helper now uses an inner join and skips `GROUP BY`/`HAVING` for the common single-filter path; multi-attribute filters still use the grouped intersection path. Verification: focused database/core/storefront tests, local migration and D1 query-plan proof, root typecheck/lint/test/build/env/dist-secret/diff gates (`318` files / `2139` tests), remote migration/deploy, API deploy `f9cdb388-9e46-4e52-890e-0db1752dcea4` with Worker startup `124ms`, remote D1 index-list proof, remote plans showing global newest uses `products_public_newest_idx` with no temp sort, category newest uses `products_public_category_newest_idx` with no temp sort, and `brand=Apple` single-filter lookup uses `product_attributes_slug_idx` plus covering `product_attribute_values_attr_value_product_idx` with no temp group. Live smokes pass for health, `/readyz`, default products, `brand=Apple` products, storefront `/search?sortBy=newest`, and storefront `/search?brand=Apple`. |
| PERF-011 | P2 | Verified | Storefront/Search | Command-palette search sent unnormalized queries, did not abort stale requests, product-only lookup used the broader `/search` route instead of `/products/search`, and backend search/listing misses needed explicit cache-key/cost policy. | Storefront command-palette search normalizes semantic whitespace before fetch/navigation, aborts superseded/closed requests, ignores stale responses, shared `search()` normalizes before SDK calls, and `searchProductsForForm()` uses `/api/v1/products/search` with real pagination/variants. Backend policy is now explicit: public search/listing cache keys normalize `q`/`search` and numeric params, cached global-search hits intentionally bypass the miss-only limiter, invalid raw query shapes bypass cache before validation, valid cache misses are rate-limited before DB/FTS work, punctuation-only FTS input returns empty instead of broad catalog reads, public search limit is capped at `50`, product/category list limits are capped at `100`, and public page numbers are capped at `1000`. Verification: focused storefront search/product/client URL tests; focused API/cache/core tests including `search.test.ts`, `public-search-query.test.ts`, route-boundary tests, and product storefront boundary tests; SDK regeneration; local API smokes for normalized search, punctuation-only search/listing, invalid empty params, bounded limits/pages, category invalid-search, and attribute-filter invalid-search; root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`320` files / `2154` tests), `pnpm build`, env/dist-secret/migration/diff guards; API deploy `a410bbc6-0eb7-4b69-8cd5-3e8b2d7208c1` with Worker startup `113ms`; live API health/readyz, invalid global search, empty/excessive limit `400`s, public page cap `400`s, normalized global search, whitespace-only `/products/search` blank-search behavior, invalid product/category listing empties, attributes invalid-search empty filters, and OpenAPI limit/page/400 smoke pass. |
| PERF-012 | P1 | Verified | Checkout/Runtime Performance | Hot checkout/payment paths had avoidable serial work and stale client authority: order-session totals could drift from server totals before payment mode selection, cache prefix sweeps could hold payment processing, and checkout/payment paths lacked measured load proof. | First slice batches D1 reservation variant-state reads; `ARCH-004` then retired the legacy order-ingest consumer from Scalius API and removed that serial queue surface entirely. Checkout-latency slice makes `commitStorefrontOrderPayload()` D1-only, keeps order commit plus checkout-attempt finalization awaited, and moves `checkout_status:*` plus `order_receipt:*` KV recovery hints behind `waitUntil` after D1 authority is established; COD-only storefront success schedules abandoned-checkout cleanup with `Astro.locals.cfContext.waitUntil` when available. Latest self-heal/dedupe slice makes `/orders/status/:token` D1 fallbacks that prove committed/failed state repair `checkout_status:*` and completed `order_receipt:*` hints through the same guarded background writer, so transient KV misses do not cause repeated polling reads. It also short-circuits exact duplicate fresh `checkout_attempts` and `payment_session_attempts` processing rows from the first selected row instead of issuing a reclaim update and second state read. Target-gateway settings slice splits raw merchant allowlist/checkout-flow checks from provider readiness and fresh-reads only the selected Stripe/SSLCommerz/Polar credentials during payment-session creation, so unrelated gateways no longer add decrypt/cache/provider-read work or unrelated failure surface to a single buyer payment attempt. Customer account recovery slice makes order-detail `paymentRecovery` reuse the customer-owned order header instead of reloading the same order, keeps private recovery fields out of the public passthrough response, and routes `/customer-auth/orders/{id}/payment-session` through one request-local `createCustomerAccountPaymentSession()` dispatcher so order/gateway/policy/settings are resolved once before provider work. Receipt/public-order boundary slice makes receipt pages pass their already loaded order state into support-action resolution, filters deleted orders in the receipt order read, serializes receipt DTOs explicitly so internal `customerId`/`fulfillmentStatus` fields do not leak, repairs `order_receipt:*` KV only for committed checkout-attempt fallbacks, and removes the app-level bearer middleware from public `/orders/*` proof/origin-guarded routes after live smoke proved valid receipt links were incorrectly returning `401`. Final side-effect/logging slice makes Stripe, SSLCommerz, and Polar session creation prove selected gateway readiness before inserting a deposit payment plan, so dummy/bad credentials fail closed without payment-plan or payment-session-attempt writes; expected typed readiness failures now log as compact one-line `[api-error]` warnings instead of stack traces while unknown crashes still keep full error objects. Verification: parallel PERF-012 subagent audits, focused core/API/admin/storefront tests from prior slices, affected core/API/admin typecheck+lint, root gates from prior slices, local COD load proof through `scripts/dev-postsale.mjs` (`50/50` `201`, concurrency `5`, p95 `181.5ms`, zero failures), focused payment-session/api-response test suite (`72` files / `608` tests), API typecheck/lint, script syntax/focused tests (`22` tests), root typecheck/lint/test/build (`316` files / `2133` tests), env/dist-secret/migration/diff guards, local `payment-readiness` smoke proving unconfigured Stripe/SSLCommerz/Polar return `503` with `0` `payment_plans` and `0` `payment_session_attempts` plus one-line warnings, API deploy `da0da14c-9c9e-4f00-9595-1f1a26fa07b6`, and live read-only API health/readyz/checkout-config/OpenAPI smokes (`269` paths). Prior deploys: API `4cddbd1f-24af-4376-a580-a970edf2deca`, target-gateway API `a0dcce84-09ba-4faa-bc6f-07d7936c341b`, customer-account recovery API `f77eec45-ac5d-40d7-9d44-d049c73e7bfd`, receipt/public-order boundary API `ad652836-3aea-420b-940d-77db538f71c0`. |
| NOTIF-005 | P1 | Verified | Orders/Notifications | Live tail on 2026-06-20 showed order notification outbox retries stuck on admin FCM tokens returning `messaging/unknown-error` with `Device unregistered`/`NotRegistered`, then subsequent retries hit `delivery_receipt_busy`. Current invalid-token cleanup only recognized exact registration-token codes. | Centralized FCM permanent-token classification for exact registration-token codes plus Firebase stale-device variants (`Device unregistered`, `NotRegistered`, token-not-registered wording), marks them as skipped delivery receipts, deactivates tokens only after the receipt is terminal, preserves transient unknown/server failures as retryable, and covers the queue boundary where skipped admin-push receipts let the parent outbox become sent. Verified with focused tests/typechecks/lints, API deploy `56cd3a5d-cac8-4dad-9dad-7271bb02e04d`, live health, scheduled outbox replay, and live queue tail showing 8 stale FCM tokens deactivated with the queue message completing OK. |
| OPS-006 | P2 | Verified | Platform/Testability | Verified post-sale smoke gap: local setup had no reproducible customer/order fixture or OTP test harness, and live disposable order creation was unsafe without a staging/test-store policy. | Added `scripts/dev-postsale.mjs` plus `pnpm dev:post-sale:*` aliases for local-only fixture seed, COD cart/order/idempotent-replay/receipt/support smoke, bounded disposable COD load smoke, OTP readiness smoke, and online gateway readiness fail-closed smoke. The shared local mutation guard rejects production and non-local API targets; the playbook now defines production as read-only-only and keeps mutating staging out of tooling until a real test-store policy exists. Verification: focused script tests, real local checkout smoke (`WQK23C`, replayed same order id, receipt/support passed with support request `osr_rVKU_axdEFbCTOPk`), local load smoke (`50/50` `201`, concurrency `5`, p95 `181.5ms`, zero failures), local OTP smoke (`503` fail-closed with `0` pending challenges for dummy/unconfigured delivery), and local payment-readiness smoke proving unconfigured Stripe/SSLCommerz/Polar return `503` with `0` `payment_plans` and `0` `payment_session_attempts` while readiness errors log as one-line warnings instead of stack traces. Email readiness copy was normalized to generic delivery wording so customer-auth warnings no longer say "order notifications" when the blocker affects OTP/email delivery too. Latest API deploy `da0da14c-9c9e-4f00-9595-1f1a26fa07b6`; live read-only smokes passed for API health, `/readyz` (`ready`, D1 `1217ms`), checkout config (`success: true`, `activeDefaultMethod: "cod"`, `checkoutMode: "all"`, partial payment off), and OpenAPI route smoke (`269` paths). |
| OPS-005 | P2 | In Progress | Platform/Observability | Verified operational-readiness gap: health endpoints are shallow and production observability is mostly console logs instead of deep readiness, request correlation, alertable counters, and runbooks. | Deep API readiness slice adds non-cacheable `/api/v1/readyz` with bounded read-only D1/KV/R2 probes, Queue/DO binding checks, and required runtime-var checks. HTTP correlation slice adds global safe `X-Request-Id` preservation/generation, exposes it through CORS, threads request id plus Cloudflare `CF-Ray` into structured `[api-ops]` error logs, logs degraded readiness as `api.readyz.degraded` with required-check statuses only, and documents request-id use in the API/playbook docs. Operational runbook slice adds `audit/OPERATIONAL_RUNBOOK.md` plus API/audit/playbook links covering alert signals, request/queue/cron correlation, safe read-only live smokes, queue/DLQ checks, and deploy/rollback investigation. Cloudflare-native ops monitor slice adds deployed Worker `scalius-ops-monitor` with no public route, `/readyz` sampling, queue/DLQ `Queue.metrics()` checks through bindings, KV streak/cooldown state, structured redacted logs, and no external API tokens. Email alert-channel slice adds Cloudflare Email Service `send_email` binding `ALERT_EMAIL`, empty safe defaults for `ALERT_EMAIL_FROM` / `ALERT_EMAIL_TO`, compact redacted plain-text delivery, and distinct `alertCount`, `routedAlertCount`, and `deliveryFailureCount` run fields so logs-only alerts are not mistaken for routed delivery. Verification: focused monitor/tooling tests (`30` tests), ops-monitor tests (`11`), ops-monitor typecheck/lint/build, root typecheck during `pnpm deploy:ops-monitor`, env/dist-secret/diff guards, KV namespace `OPS_MONITOR_STATE` `6e5d1736490f4e7e82d7a27b1b173355`, ops-monitor deploy `cc15af3e-23b5-4033-b9cb-1fbc3709ce41` at `100%`, cron `*/2 * * * *`, live Wrangler tail run `ops-monitor-20260705t072832017z-580df9e3` on version `cc15af3e-23b5-4033-b9cb-1fbc3709ce41` with all eight queues `0`, `/readyz` healthy, `alertCount:0`, `routedAlertCount:0`, and `deliveryFailureCount:0`, and live `pnpm ops:check --queues --samples 1 --timeout-ms 20000` request id `ops-check-20260705t072704712z-b77aa6d2` passing `/readyz` and all eight queue wiring checks. Fresh 2026-07-08 evidence: latest ops-monitor deployment `ab6d5333-ac17-441d-987f-c6fb7340189d` serves version `ed91cdf7-3aaa-4012-b655-c8071f60c4fc` at `100%`; dry-run binding proof still shows `ALERT_EMAIL` unrestricted with empty `ALERT_EMAIL_FROM` / `ALERT_EMAIL_TO`; live tail run `ops-monitor-20260707t210227309z-b1fd6197` on that version reported `/readyz` ok, all eight queues/DLQs backlog `0`, `issueCount:0`, `alertCount:0`, `routedAlertCount:0`, and `deliveryFailureCount:0`; live ops checks `ops-check-20260707t210503184z-848d6340` and `ops-check-20260707t233356288z-61735425` passed required queue wiring with only known logs-only Email and `worker:testdash` warnings. Remaining scope before closure: configure verified Cloudflare Email Service sender/destination, tighten binding restrictions to the verified ops inbox if possible, deploy, and capture safe monitor-generated test-notification evidence with `routedAlertCount:1` and `deliveryFailureCount:0`. |
| OPS-008 | P1 | Verified | API/D1 Background Budget | Live `/readyz` and queue evidence had shown background D1/dependency pressure could make deep readiness noisy or compete with buyer/admin paths, and the scheduled ops monitor itself could previously hang on a stalled `/readyz` fetch or `Queue.metrics()` read. | Background-budget work is now code-owned and verified: D1 readiness has a larger bounded budget than KV/R2, cache/queue fan-out and DLQ archival are capped, cleanup scans are indexed, dashboard heavy reads are deferred/evidence-backed, API startup/import graphs are lazy, post-deploy readiness sampling and `pnpm ops:check` are repo-owned, and `scalius-ops-monitor` now has configurable `READYZ_TIMEOUT_MS` / `QUEUE_METRICS_TIMEOUT_MS`, aborts hung `/readyz`, times out each queue metrics read, and logs compact readyz/queue summaries without provider payloads. Verification: focused ops/core/API tests, root typecheck/lint/test (`375` files / `2541` tests)/build/env/migration/dist-secret guards, API deploy `aede6cdc-321a-478c-8b58-d57d8a472f47` with startup `114ms` and four ready samples, ops deploy `3e480e19-7164-4bf9-b907-6fe3babf9734` at `100%`, live `ops:check` request `ops-check-20260706t010530976z-2b6f123d` (`4/4` ready, OpenAPI `277` paths), live queue/provider wiring check `ops-check-20260706t010530990z-23b21d63`, and remote deployment proof. Remaining alert-channel sender/destination setup and safe test-notification evidence stay under `OPS-005`, not this D1/background-budget line. |
| REL-001 | P1 | Verified | Release/Verification | The stable-release checklist named branch, API, dashboard, storefront, and discovery requirements, but no single read-only command mapped the current tracker/docs/live surfaces into repeatable release evidence. | Added `pnpm release:check` as a compact read-only release smoke: it fails open P0/P1 tracker rows, verifies required docs, reuses API ops health/readyz/OpenAPI/deployment proof, checks dashboard `/admin` proves an auth gate, probes storefront `/health`, `/`, `/search`, validates robots plus all six sitemap/feed endpoints from the stable checklist, rejects product sitemap `priority`/`changefreq`, and opens a feed-discovered storefront product URL without hardcoded catalog slugs. It intentionally does not run heavy gates or mutating buyer/admin smokes. Verification: `node --check scripts/release-check.mjs`, focused release/ops script tests (`2` files / `21` tests), script ESLint, `pnpm release:check --skip-live --json`, live `pnpm release:check --timeout-ms 20000` with API OpenAPI `278` paths and deployment `358bede2-013f-40d3-a102-18e02047b71e`, live `pnpm release:check --skip-wrangler --timeout-ms 20000 --json` proving fallback mode, and `git diff --check`. |
| NOTIF-003 | P1 | Verified | Orders/Notifications | Order notifications could be lost after committed order/status/COD/shipment writes if `ORDER_NOTIFICATIONS_QUEUE.send()` failed or the Worker crashed before enqueue. | Durable D1 `order_notification_outbox`, same-batch `order_created` outbox insert, queue consumer sent/failure marks, cron replay, root tests, remote migration, API deploy `fd852673-f9a0-49f2-b011-0f9122f4a892`, and live D1 schema/API smoke. |
| NOTIF-004 | P1 | Verified | Orders/Notifications | Retrying a durable order notification could duplicate already-accepted customer/admin channel sends or mark the parent outbox sent after a swallowed channel failure. | D1 `order_notification_delivery_receipts`, receipt claims around email/SMS/WhatsApp-skip/FCM targets, Resend `Idempotency-Key`, deterministic GenNet `csms_id`, FCM message-name receipts, focused notification/provider/queue tests, migration metadata check, and core/API/database typechecks. |

## Secondary Queue

| ID | Severity | Status | Owner | Summary | Primary Verification |
| --- | --- | --- | --- | --- | --- |
| ADMIN-001 | P2 | Verified | Admin/API | Split the legacy admin server-function barrel and remove file-level `@ts-nocheck`. | Legacy barrel removed; admin/API/api-client typechecks pass and SDK regenerated. |
| ADMIN-002 | P2 | Verified | Admin/RBAC | Align admin shell role guard with API RBAC. | `admin-access` tests plus admin/API auth typecheck tests pass. |
| ADMIN-003 | P2 | Verified | Admin/Routes | Add `loaderDeps` for URL-search-driven list loaders. | Admin typecheck and root tests pass after loader-deps sweep. |
| ADMIN-004 | P2 | Verified | Admin/API | Reduce drift between server-function transport and browser proxy. | Same endpoint behavior compared through both paths. |
| ADMIN-005 | P2 | Verified | Admin/Navigation | Dynamic navigation product preview called an unregistered API route. | Route exists, counts via storefront filtering, exact RBAC and SDK regenerated. |
| ADMIN-006 | P2 | Verified | Admin/RBAC | Permission override UI sent the wrong payload shape. | Typed RBAC server functions and focused admin checks pass. |
| ADMIN-007 | P2 | Verified | Admin/RBAC | Account settings nested provider used all permission definitions. | Account page uses parent admin route permission context; admin checks pass. |
| ADMIN-008 | P2 | Verified | Admin/Auth | Admin invite UI hid email delivery failure. | Typed create-user response branches on `emailFailed`. |
| STORE-002 | P2 | Verified | Storefront/API | Browser `/api/v1` fallback is not a real proxy. | Missing `PUBLIC_API_URL` fails loudly; no browser `/api/v1` fallback remains. |
| STORE-003 | P2 | Verified | Storefront/Checkout | Redirect gateways clear cart before payment completion. | Historical 2026-06-13 fix preserved redirect-gateway cart/session state until protected success; superseded for SSLCommerz/Polar by `CHECKOUT-011`, where durable hosted order commits consume stale cart data and recovery continues through receipt/order paths plus the empty-cart recovery marker. |
| STORE-004 | P2 | Verified | Storefront/Cart | Location prefill targets nonexistent selects. | Location prefill uses the React component event API; resolver tests and storefront typecheck pass. |
| CONTENT-001 | P2 | Verified | Content/Storefront | `publishedAt` is not enforced on public pages/sitemap. | Future pages are hidden by a shared public visibility predicate; core test passes. |
| CONTENT-002 | P2 | Verified | SEO/Storefront | Cart is in sitemap and lacks noindex. | Sitemap excludes cart; cart has noindex; focused storefront tests pass. |
| NOTIF-001 | P2 | Verified | Notifications | Queue notification type union is narrower than service/settings. | Shared type used in queue/service/settings/admin UI. |
| NOTIF-002 | P2 | Verified | Notifications/Settings | SMS notification path may lack credential encryption key. | SMS notification provider resolution receives the credential key; focused test passes. |
| CONF-001 | P2 | Verified | API/Settings | Credential encryption helper prefers `JWT_SECRET`. | Credential key is preferred; JWT remains legacy fallback only; tests pass. |
| CONTRACT-001 | P2 | Verified | Storefront/API | Storefront calls stale `/discounts/usage` endpoint. | Stale endpoint call removed; order creation/queue tests pass. |
| CONTRACT-002 | P2 | Verified | Storefront/API | Local order payload type drifts from generated SDK. | `CreateOrderPayload` aliases generated `OrderPostRequest`; storefront/api-client typechecks pass. |
| CONTRACT-003 | P2 | Verified | API/SDK | Timestamp schemas generate weak `unknown` SDK unions. | Regenerated SDK has precise `string | number | null` timestamp fields and no weak timestamp `unknown` unions. |
| DB-001 | P2 | Verified | Database | Migration snapshot metadata appears incomplete. | Metadata/journal guard with manual snapshot-gap allowlist passes alongside Drizzle check. |
| PLAT-001 | P2 | Verified | Platform | Cloudflare Env declarations are duplicated and drifting. | `pnpm check:env` plus focused app typechecks pass. |
| DEV-001 | P2 | Verified | Platform/Docs | Local dev docs/scripts/cleanup are inconsistent. | Setup output, README, examples, cleanup verified. |
| DEV-002 | P2 | Verified | Platform/Lint | Root lint gives false confidence. | `pnpm lint` runs seven real code workspace lint tasks. |
| DEV-003 | P2 | Verified | Platform/Local Dev | Dev helper safety hardening for reset, option values, shell exit codes, and env repair. | Script syntax checks, focused Vitest, option smoke tests, and `pnpm dev:doctor`. |
| DEV-004 | P2 | Verified | Platform/Local Dev | Local dev/deploy helpers assumed `pnpm` was on PATH, so agent shells with Node/Corepack but no pnpm PATH entry failed before migrations or startup. | Dev/root helpers now resolve the active pnpm/Corepack shim, normalize Corepack `pnpm.mjs` to the sibling `pnpm` executable for Turbo child processes, `dev.sh` passes it to child scripts, `deploy.mjs` propagates it through `SCALIUS_PNPM_BIN`/`PATH`, root Turbo gates use `scripts/turbo-run.mjs`, `dev:doctor` reports the resolved executable, and Astro 7 background storefront dev is log-followed/stopped by the wrapper. Verification: script syntax checks, focused local-dev tests (`4` files / `29` tests), PATH-stripped dry-run, real local D1 migration with Node on PATH but no pnpm on PATH, `pnpm dev:storefront` local smoke returning storefront `/` 200 and `/health` ok, root lint/typecheck/build/deploy dry-runs, and root deploy dry-run (`pnpm run deploy -- --dry-run`) passing typecheck/build/dist-secret gates. |
| DEV-005 | P3 | Verified | Storefront/Deps | Astro storefront dev logs showed Vite's `optimizeDeps.esbuildOptions` deprecation warning from `@astrojs/cloudflare@14.0.0`'s environment hook, not repo `astro.config.mjs`. | Upgraded the storefront Cloudflare adapter to `@astrojs/cloudflare@14.1.1`; the installed adapter path uses `optimizeDeps.rolldownOptions` and no repo config override was added. Verification: `pnpm --filter @scalius/storefront add -D @astrojs/cloudflare@^14.1.1`, storefront typecheck/build, local `astro dev` startup and browser smoke with no `optimizeDeps.esbuildOptions`/deprecation warning, root gates, storefront deploy `ae0188c6-23a9-4622-86e1-602431f286dd`, deploy health/warmers, and live storefront browser smoke with rendered homepage, no horizontal overflow, and zero console warnings/errors; screenshots `/tmp/scalius-local-storefront-astro-adapter-lead.png` and `/tmp/scalius-live-storefront-astro-adapter-lead.png`. |
| DEV-006 | P1 | Verified | Storefront/Astro Dev SSR | Local `astro dev` could return blank account pages after Vite 8 dependency optimization with React invalid-hook errors from shared React islands (`lucide-react`, `sonner`, `AuthModal`, `CartFlyout`, `CommandPalette`), while generated `astro preview` rendered the same route cleanly. | `astro.config.mjs` now dedupes the full React singleton entrypoint set, including `react-dom/server`, through one shared list. A source-boundary guard protects the invariant and documents why React must not be excluded from the Cloudflare dev SSR optimizer, because that externalizes `react` through `require()` in the worker-like runner. Verified with local `astro dev` account-order smoke, generated preview account-order smoke, storefront/root gates, storefront deploy `cd0cc66c-b926-443f-af74-de4bdd7dcc9a`, and live health/account/home/search smokes. |
| BUILD-001 | P2 | Verified | Platform/Turbo | Turbo build inputs omit public assets/helper scripts. | Storefront build dry-run includes `public/flags/BD.svg`, scripts, configs, and root flag-copy input. |
| BUILD-002 | P2 | Verified | Platform/Assets | Flag prebuild relies on transitive dependency. | `node scripts/copy-flags.mjs` copies 265 SVGs and fails if required flags are missing. |
| TEST-002 | P2 | Verified | Storefront/Tests | Storefront focused tests blocked by missing `happy-dom`. | Focused storefront test starts and runs. |
| ORDER-009 | P2 | Verified | Admin/Orders/Inventory | Full order edits can commit item changes before inventory deltas are safely applied. | Inventory delta failures abort without losing old item context. |
| ORDER-010 | P2 | Verified | Orders/Queue | Order-ingest fallback can double-reserve after uncertain rollback. | Fallback reuses held reservations and retries only after confirmed release. |
| STORE-005 | P2 | Verified | Storefront/Checkout | Inline checkout config serialization allowed stored XSS through gateway config values. | Script-safe serializer and inert DOM-parser test with `</script>` payload pass. |
| STORE-006 | P2 | Verified | Storefront/Cart | Empty-cart language strings rendered through `innerHTML`. | Empty-cart renderer uses text nodes; happy-dom malicious language test passes. |
| PRIV-004 | P2 | Verified | Storefront/Tracking | Raw checkout PII persisted in `sessionStorage` and enriched broad Meta CAPI events. | Broad Meta CAPI events ignore session PII; checkout cleanup clears legacy PII keys; external gateway redirects clear raw checkout transfer state. |
| CACHE-001 | P2 | Verified | API/Settings/Cache | Payment settings saves did not invalidate checkout config/storefront checkout caches, and public checkout config ignored the aggregate enabled-method allowlist. | Payment settings route tests assert checkout invalidation and purge; checkout config service tests assert payment-method allowlist behavior. |
| BUILD-005 | P2 | Verified | Platform/Turbo | Turbo build cache ignored app-local env inputs and build-time env vars. | Turbo dry-run global cache inputs include app `.env*` files and declared build env keys; `scripts/turbo-config.test.mjs` passes. |
| SDK-001 | P2 | Verified | API Client | Generated SDK runtime `@hey-api/client-fetch` was deprecated/outdated. | `@hey-api/openapi-ts` is updated to 0.98.2, deprecated runtime package is removed, SDK regenerated, and API-client/admin/storefront/root typechecks pass. |
| MEDIA-001 | P2 | Verified | Content/Media | Live storefront homepage referenced missing CDN object `zLPBsNbtJCMxTkfPAPHcr.png`, causing a blocked/blank image. | Broken image removed from product gallery via admin API; replacement primary image returns image content and live homepage no longer references the 404 object. |
| CACHE-002 | P1 | Verified | Storefront/Cache | KV version lookup failure can fall back to version `1` and resurrect stale HTML/L2 cache entries after purge. | Cache-version unit tests, storefront typecheck/build, deploy, and live storefront smoke. |
| CACHE-003 | P1 | Verified | API/Storefront Cache | Non-widget admin settings/content/reference writes missed storefront purge groups or used stale groups. | Route tests for shipping/location/language/navigation/site/hero create/update/delete/read behavior plus local/live smoke. |
| CACHE-004 | P2 | Verified | API/Storefront Cache | Widget writes still purged all widget-related storefront groups instead of deriving the affected placement scopes. | Target-aware widget invalidation tests cover homepage/page/product/category placements, exact prefix purges, and inactive widgets. |
| CACHE-008 | P2 | Verified | API/Delivery Cache | Delivery-provider create/update/delete routes are mapped to the checkout cache group but did not invalidate API/storefront checkout caches. | Focused route tests prove checkout invalidation runs for provider create/update/delete/upsert; current code schedules the storefront purge after awaited API KV invalidation. |
| AUTH-004 | P1 | Verified | Admin/Auth | Admin guards can read stale Better Auth session cookie cache after custom 2FA session flag updates. | Admin/API session reads disable Better Auth cookie-cache reuse; local and live dashboard login/orders smoke pass. |
| AUTH-008 | P2 | Verified | Core/Auth | Revoke sessions on password reset and key the Better Auth factory cache by auth secret plus runtime origins. | Core auth tests, deploy, and admin/API auth smoke. |
| SEC-005 | P1 | Verified | Admin/Auth | TanStack Start server functions were missing explicit CSRF middleware. | Admin typecheck/build, local sign-in/orders smoke, deploy, and live dashboard smoke. |
| PERF-001 | P2 | Verified | Admin/Performance | Admin shell and scanner load browser-only/expensive work too early. | Admin build chunk scan, local dashboard/scanner checks, deploy, and live dashboard browser smoke. |
| PERF-002 | P2 | Verified | Admin/Dashboard | Dashboard first load performs avoidable serial API/DB work and mounts Recharts for all-zero data. | Dashboard data tests, admin/API typechecks, local/live dashboard smoke. |
| PERF-003 | P2 | In Progress | Admin/Performance | Dashboard activity is split from first paint/indexed, order-detail payment/COD data is prefetched, checkout inactive-tab prefetching is trimmed, hot shell query options are split, list navigation avoids blocking cold client loads, mutation freshness invalidates dashboard/product aggregates, dashboard first-paint motion runtime is removed, route components import mutation hooks from domain modules instead of the broad compatibility barrel, orders interaction UI lazy-loads on demand, shared DataTable drag-and-drop code is isolated behind the sortable lazy path, discount edit loads only the active discount-type form, media/widget helpers lazy-load on user actions, rich-text form fields render sanitized previews, lazy-split Tiptap out of route bundles, and immediately request the real editor after field hydration so visible toolbars do not depend on idle/IntersectionObserver scheduling, product form and variant interaction code lazy-load only after needed, product edit keeps the option manager off SSR until hydration, product-option CSV tools lazy-load on demand, navigation-builder reorder DnD is explicit/on-demand, General Settings Header/Footer hidden social/navigation subtabs lazy-load on demand, collection/discount pickers resolve selected IDs through targeted lookups, admin shell user-menu/mobile Sheet interaction code is deferred, hero-slider drag-and-drop/media-page coupling is deferred behind explicit lazy boundaries, shared DataTable sort/action/page-size menus are deferred behind first interaction, categories/customers/pages delete confirmations lazy-load on first delete interaction, hot admin routes are guarded against eager command-palette UI, and the broad `api.queries.ts` barrel has been deleted. Broader route/component bundle slimming remains. | Dashboard/order-detail/checkout/auth focused tests, mutation/query import-boundary scans, `pnpm check:admin-perf`, admin build chunk scans and lazy-boundary scans, discount/media/widget/rich-editor/product-form/settings/collection-picker/hero-slider/data-table/list-delete-dialog smokes, product edit SSR/hydration smokes, root gates, local/live browser/dashboard/list/settings/storefront smoke, remote D1 index check, live split endpoint checks, and live dashboard/hero-slider chunk checks. |
| PERF-004 | P2 | Verified | Admin/Storefront Performance | Admin data tables forced refetch on every remount, media route blocked on unused React Query prefetches, admin query cache GC was short for real editing sessions, product pages eagerly requested hidden 1400px zoom assets, and storefront Cache API metadata advertised unsupported SWR directives. | Root warning-free lint/typecheck/test/build/safety gates, deploy, and live admin/storefront browser smoke. |
| PERF-005 | P2 | Verified | Admin/Dashboard Performance | Dashboard first-paint components imported `motion/react` for simple stat-card, gradient, and word-flip animations. | Motion removed from dashboard first-paint source, dashboard dist scan has no motion runtime markers, admin checks/root gates, deploy, and local/live dashboard smoke. |
| ADMIN-010 | P2 | Verified | Admin/Hydration | Product list SSR could hydrate against a different query-cache shape because category/stats prefetches were fire-and-forget while route UI consumed their results; localized date cells could also differ across SSR/client. | Product loader now awaits first-render query data and shared date cells suppress known timestamp text drift; root gates, admin deploy, and live `/admin/products` console sweep pass. |
| ADMIN-011 | P2 | Verified | Admin/Delivery Cache | Delivery-provider save/delete UI updated local state but did not invalidate `queryKeys.settings.deliveryProviders()`, leaving stale provider data in other admin consumers. | Provider save/delete now invalidates `queryKeys.settings.deliveryProviders()` after local state updates; admin typecheck/lint pass. |
| ADMIN-012 | P2 | Verified | Admin/Order Detail | Order detail redirects away from valid orders if delivery-provider warmup fails, even though the page can render without providers. | Provider prefetch is optional/logging, order detail renders with an empty provider fallback, focused prefetch test passes, and local/live order-detail smoke passed. |
| ADMIN-013 | P2 | Verified | Admin/Performance | `api.mutations.ts` statically imported every mutation domain, keeping the broad mutation barrel in route-reachable graphs. | Mutation hooks are split into `src/lib/api-mutations/*`, route/component imports use domain modules, `api.mutations.ts` is a compatibility re-export barrel, export parity is preserved, root gates pass, final deploy is API `fbd26e4f-7598-48f4-b9c4-157bc3439bbd` / admin `5c93cbc7-c3e6-4a03-80e0-38f5ff5eafb5` / storefront `586a5c96-0965-48d7-8920-0cdccb759f55`, and local/live route import + browser smoke checks are clean. |
| ADMIN-014 | P2 | Verified | Admin/RBAC | Account settings still wrapped itself in a nested permission provider and team controls advertised actions that API RBAC could reject. | Nested provider removed, account settings consumes parent `/admin` permission context, team/invite/delete/permission controls are permission-gated, RBAC mutations refresh admin route context, and focused/static/root/local/live checks pass. |
| STORE-008 | P2 | Verified | Storefront/Widgets | Stored/generated widget HTML linked to nonexistent `/collections` and `/collections/all` routes on the storefront homepage. | Shared widget-rendering test, root gates, storefront deploy, and live storefront route smoke prove links rewrite to `/search` with no bad collection links. |
| CACHE-015 | P2 | Verified | API/Storefront Cache | Some post-commit non-catalog writes still awaited storefront purge directly, so a downstream purge failure could turn a committed write into a false 500. | Pages, widgets, checkout/settings/reference routes now schedule storefront purges after awaited API KV invalidation; focused cache tests, root gates, local/live smoke, and deploy pass. |
| PERF-006 | P2 | Verified | Admin/Performance | Root/admin route graph pulled Zod through `list-helpers` for route error boundaries and hot dashboard/settings paths still imported the broad query barrel. | `RouteErrorComponent` split to `route-error.tsx`, hot dashboard/settings/cache/delivery/shipping/fraud/hero-slider query options moved under `api-query-options/*`, focused route-graph guard and manifest scan pass; root gates, local dev smoke, full deploy, and live dashboard/settings/storefront smoke pass. |
| PERF-007 | P2 | Verified | Admin/Performance | Primary admin list routes and managers still depended on the broad `api.queries.ts` barrel for small domain queries. | Narrow domain query-option modules, route-graph guard, route chunk scan, root gates, local list-route smoke, full deploy, and live dashboard/storefront/list-route smoke pass. |
| PERF-008 | P2 | Verified | Admin/Performance | Remaining detail/create/special admin routes still depended on the broad `api.queries.ts` barrel, direct customer form submits missed dashboard aggregate invalidation, and deferred rich-text previews rendered saved HTML as odd plain text. | Broad query barrel deleted; all route-facing query options live in `api-query-options/*`; customer form invalidates dashboard aggregates; rich-text previews render sanitized `.rich-content` HTML while Tiptap remains lazy; root gates, audit, local/live browser smoke, and admin deploy `f4bf38a1-f285-4eb5-94e8-68fc60d5e7dd` pass. |
| PERF-009 | P2 | Verified | Admin/Performance | Notification channel settings imported the broad core notifications barrel into a client route chunk, pulling backend/outbox/Drizzle code into the dashboard bundle. | Narrow `notification-types` import, route-graph guard, client chunk marker scan, root gates, admin deploy, and local/live settings smoke pass. |
| STORE-009 | P2 | Verified | Storefront/Performance | Category/search dynamic-filter cold paths waited for layout before product-list fetches even after attributes validated the filters. | Dynamic filter paths now await only attribute metadata before validation, start the product/category product fetch immediately after canonicalization, and join layout/widgets afterward. Focused boundary/query tests, root gates, local smokes, storefront deploy `bfdc996a-8c29-4b67-aa9a-e85f91a095fb`, and live `/search?brand=Apple` plus `/categories/laptop?brand=Apple` smokes pass. |

## Cleanup Queue

| ID | Severity | Status | Owner | Summary | Primary Verification |
| --- | --- | --- | --- | --- | --- |
| DOC-001 | P3 | Verified | Docs | Package README counts/dependency claims are stale. | Docs remove volatile counts or generate them. |
| DOC-002 | P3 | Verified | Docs | README drift after the latest API/RBAC/cache/order changes: OpenAPI counts, queue name, state transitions, collection helpers, customer session mirroring, manager exclusions, product gaps, storefront homepage cache notes, and admin route-context refresh rules. | Docs updated from current code, AGENTS updated for the route-context helper, and stale-phrase scans pass. |
| CLEAN-001 | P3 | Verified | Admin/Cleanup | `.DS_Store` exists under admin route directory. | File removed; root `.gitignore` already ignores `.DS_Store`. |
| CACHE-009 | P3 | Verified | Admin/Cache UI | Cache UI said API-only groups do not affect cached HTML, but prefix purges can bump the shared storefront version and move L2 keys; the real distinction is direct HTML impact versus data-prefix invalidation. | Cache UI now labels groups `Warms HTML` versus `Data prefixes`, avoids invalid badge-in-paragraph hydration markup, and local/live `/admin/settings/cache` browser smoke is console-clean. |
| CACHE-010 | P3 | Verified | Admin/Cache UI | Cache settings route warmed cache stats/groups/last-cleared through TanStack Query, but `CacheManager` ignored warmed query caches and refetched the same data into local state. | `CacheManager` consumes `cache*QueryOptions()` through React Query, manual refresh uses query refetch, and local/live browser request capture shows no duplicate cache server-function/API fanout after hydration. |
| CACHE-011 | P3 | Verified | API/Admin Cache UI | Clear-all cache action locally marked every group cleared, but the API did not persist `sc:_last_cleared:*` timestamps for all groups. | `POST /api/v1/cache/clear` now persists all group timestamps, cache clear actions live in `api-mutations/cache`, and `apps/api/src/routes/cache.test.ts` plus local action smoke cover group/all clears. |
| UI-001 | P3 | Verified | Admin/Dashboard | Empty disposable dashboard emits Recharts zero-size warning. | Empty daily activity renders a fixed-height empty state instead of mounting Recharts. |

## Recently Verified Baseline

- Last full root baseline from the SEO discovery proof and UCP spec-compliance slice passed focused SEO/UCP/release tests (`6` files / `70` tests), storefront/admin typecheck+lint, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`400` files / `2813` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm audit --audit-level moderate`, `pnpm --filter @scalius/database check:migrations`, `pnpm exec drizzle-kit check --config packages/database/drizzle.config.ts`, `pnpm peers check`, `pnpm install --frozen-lockfile`, and `git diff --check`.
- Database migration metadata guard currently reports 84 SQL files / 84 journal entries / 32 snapshots / 52 allowed manual snapshot gaps. `pnpm db:generate` still prints the repo's known Drizzle parent-snapshot collision warning for `0045`/`0047`, but exits successfully; use the metadata guard plus `drizzle-kit check` as the truth until the historical snapshots are normalized.
- Latest affected deploys for this hardening slice: API `041e85ca-6568-4f72-9664-7b0bdf0cfe2d`, admin `ac16b1f5-5318-49e3-8077-6eedcae987bf`, storefront `0857efaf-9205-4899-b039-3bb3afa38575`, and ops monitor `ad8b6be1-4c37-4e84-9755-19b2d9de53e8`.
- Latest live ops check passed: `ops-check-20260707t150023913z-a4b8bff9` with `/readyz` `200`, OpenAPI `280` paths, API deployment `041e85ca-6568-4f72-9664-7b0bdf0cfe2d` at `100%`, and known warnings for unexpected `worker:testdash` producers on `payment-events`, `order-notifications`, and `auth-otp`.
- Latest live SEO/feed/UCP smoke passed after admin deploy `ac16b1f5-5318-49e3-8077-6eedcae987bf` and storefront deploy `0857efaf-9205-4899-b039-3bb3afa38575`: `pnpm release:check --timeout-ms 30000` verified tracker/docs, API health/readyz/OpenAPI (`280` paths), dashboard `/admin` auth redirect, storefront `/health`/`/`/`/search`, strict public SEO policy, homepage global JSON-LD, robots, `6` sitemap checks, canonical and compatibility feeds with `5` items each, catalog-only UCP discovery/search/lookup/product, and `/products/test-productss` Product JSON-LD.
- Current branch is ahead of `origin/mono-repo`; run `git status -sb` before release and make release candidates from current HEAD or a pushed branch.
- Latest affected deploy for the customer auth cookie-domain hardening slice is API `70acbcd1-5ef0-468b-97a1-71b0ff500c60` and storefront `ba54c128-ed53-4f1c-871d-dd48364b30c3`.
- Latest affected deploy for the customer OTP challenge privacy slice is API `195d32b4-7848-43e3-9a7e-8f1a00f76180`.
- Local dev smoke used API/admin/storefront dev servers: `pnpm dev:doctor --require-running` passed with only the Node 24 versus `.nvmrc` Node 22 warning; local browser rendered `/admin/products`, `/admin/products/new`, `/admin/orders`, `/admin/categories`, `/admin/customers`, `/admin/pages`, `/admin/collections`, `/admin/discounts`, `/admin/inventory`, `/admin/attributes`, `/admin/abandoned-checkouts`, and storefront `/` without error shells or console errors.
- Live dashboard smoke covered unauthenticated `/admin` redirecting to `/auth/login`, demo sign-in returning 200 with a token, authenticated list routes returning 200 after canonical redirects, and live browser rendering the moved list routes with no error shell or captured console errors.
- Live storefront `/` returned 200 in HTTP and browser smoke; narrow error checks found no `Something Went Wrong`, unexpected-error, or application-error text, and no captured production storefront console errors.
- Dependency freshness check: `pnpm audit --audit-level moderate` passes after the transitive `js-yaml` 4.2.0 override plus 2026-06-20 floors for `undici >=8.5.0` and `dompurify >=3.4.11`. The old storefront Vite 7 hold was removed on 2026-06-23 by `OPS-007`; storefront now runs Astro 7, `@astrojs/cloudflare` 14, `@astrojs/react` 6, and Vite 8 with storefront/root gates plus generated Worker dry-run/browser smokes.
- Storefront focused Vitest: passes after adding the missing `happy-dom` test dependency.

## Remediation Notes

2026-07-08:

- `MCP-018` adds a verified safe Admin MCP dashboard-summary tool without making `apps/agent` a dashboard backend. `admin_dashboard_summary` is read-only, strict empty-input, API-service-binding-only over no-PII `GET /api/v1/admin/dashboard/metrics-summary`, and compact to aggregate stats plus explicit privacy/mutation limits. The API route returns only `getDashboardSummaryStats(db)` and never calls recent-order, full-dashboard, or activity queries. The tool omits recent orders, order IDs, customer PII/contact data, lifetime revenue, daily activity, payment evidence, provider payloads, upstream error bodies, unknown upstream fields, and all mutation authority. Release-check requires the tool in authenticated Admin MCP `tools/list` and calls it with `{}`. Verification: focused API dashboard tests (`760`), focused agent tests (`62`), focused release-check tests (`56`), API/agent/API-client typechecks, API/agent lints/builds, env/dist-secret/release-skip/diff gates, deploy dry-runs, API deploy `38c96afb-976a-4ad5-8cd7-b5c6e7b7c3ce`, agent deploy `45261550-7d7e-43b1-a7c6-6d503a37f7e8`, live authenticated release check proving `admin_dashboard_summary`, and ops check `ops-check-20260708t074650650z-88fc72f2`.
- `MCP-017` adds a verified safe Admin MCP inventory lookup without making `apps/agent` an inventory backend. `admin_inventory_lookup` is read-only, API-service-binding-only over `GET /api/v1/admin/inventory` with fixed `section=variants`, bounded `query`, `page`, `limit`, `status`, `sort`, and `order`, and compact tracked-variant stock fields/stats only. It omits movements, alerts, barcodes, prices, version fields, adjustment authority, scanner lookup, order IDs, notes, upstream error bodies, and all mutation authority. Release-check requires the tool in authenticated Admin MCP `tools/list` and calls it with demo-safe `{ query: "test", limit: 1, page: 1 }`. Verification: focused agent tests (`58`), focused release-check tests (`55`), agent typecheck/lint/build, env/dist-secret/release-skip/diff gates, deploy dry-run, agent deploy `aa0e8ea0-b95d-40bf-bde7-14606b4bf536`, live authenticated release check proving `admin_navigation_context`, `admin_category_search`, `admin_collection_search`, `admin_page_search`, `admin_media_search`, `admin_product_search`, `admin_order_search`, and `admin_inventory_lookup`, and ops check `ops-check-20260708t072511786z-dd175635`.
- `MCP-016` adds a verified safe Admin MCP media-search tool without making `apps/agent` a media backend. `admin_media_search` is read-only, bounded to latest/search filters such as `query`, `page`, `limit`, `folderId`, and `mimeType`, and API-service-binding-only behind the dashboard cookie preflight. It returns compact media file metadata with safe URLs only and omits upload/delete/move/folder mutation authority, storage internals, unsafe URLs, deleted/unknown fields, upstream error bodies, and all mutation authority. Release-check requires the tool in authenticated Admin MCP `tools/list` and calls it with demo-safe `{ query: "test", limit: 1, page: 1 }`. Verification: focused agent tests (`52`), focused release-check tests (`54`), agent typecheck/lint/build, env/dist-secret/release-skip/diff gates, deploy dry-run, agent deploy `dd1b5455-08d6-4dda-a40c-dc0bd83795e6`, live authenticated release check proving `admin_navigation_context`, `admin_category_search`, `admin_collection_search`, `admin_page_search`, `admin_media_search`, `admin_product_search`, and `admin_order_search`, and ops check `ops-check-20260708t071043411z-c3bb1975`.
- `MCP-015` adds a safe Admin MCP CMS page-search tool without making `apps/agent` a page backend. `admin_page_search` is read-only, bounded to `query`, `page`, and `limit`, and API-service-binding-only behind the dashboard cookie preflight. It returns compact page identifiers plus publish/discovery/layout flags, keeps canonical paths only when they are valid non-reserved one-segment CMS page paths, and omits HTML content, meta copy, images, deleted rows/fields, unknown upstream fields, upstream error bodies, and all mutation authority. Release-check now requires the tool in authenticated Admin MCP `tools/list` and calls it with demo-safe `{ query: "test", limit: 1, page: 1 }`, requiring no-store, no MCP tool error, and at least one content block. Verification: focused agent tests (`47`), focused release-check tests (`53`), agent typecheck/lint/build, env/dist-secret/release-skip/diff gates, deploy dry-run, agent deploy `811aa425-7265-4fd5-9b17-842cdfbe98a8`, live authenticated release check proving `admin_navigation_context`, `admin_category_search`, `admin_collection_search`, `admin_page_search`, `admin_product_search`, and `admin_order_search`, and ops check `ops-check-20260708t063120208z-f051790e`.
- `MCP-014` adds a safe Admin MCP collection-search tool without making `apps/agent` a collection backend. `admin_collection_search` is read-only, bounded to `query`, `page`, and `limit`, and API-service-binding-only behind the dashboard cookie preflight. It returns compact collection id/name/product-count/discovery metadata, keeps canonical paths only when they exactly match `/collections/<collectionId>`, and omits slugs, descriptions, meta fields, image facts, product records/lists, deleted rows/fields, unknown upstream fields, upstream error bodies, and all mutation authority. Release-check now requires the tool in authenticated Admin MCP `tools/list` and calls it with demo-safe `{ query: "test", limit: 1, page: 1 }`, requiring no-store, no MCP tool error, and at least one content block. Verification: focused agent tests (`43`), focused release-check tests (`52`), agent typecheck/lint/build, env/dist-secret/release-skip/diff gates, agent deploy `bcde5820-5453-4b18-a96a-c272b2378186`, live authenticated release check proving `admin_navigation_context`, `admin_category_search`, `admin_collection_search`, `admin_product_search`, and `admin_order_search`, and ops check `ops-check-20260708t061520383z-35aa791e`.
- `MCP-013` adds a safe Admin MCP category-search tool without making `apps/agent` a category backend. `admin_category_search` is read-only, bounded to `query`, `page`, and `limit`, and API-service-binding-only behind the dashboard cookie preflight. It returns compact category identifiers/product-count/discovery metadata and omits descriptions, meta fields, image facts, deleted rows/fields, unknown upstream fields, upstream error bodies, and all mutation authority. Release-check now requires the tool in authenticated Admin MCP `tools/list` and calls it with demo-safe `{ query: "test", limit: 1, page: 1 }`, requiring no-store, no MCP tool error, and at least one content block. Verification: focused agent tests (`39`), focused release-check tests (`51`), agent typecheck/lint/build, env/dist-secret/release-skip/diff gates, agent deploy `7c8f089a-8c1e-4dba-a6a0-93f048cd2787`, live authenticated release check proving `admin_navigation_context`, `admin_category_search`, `admin_product_search`, and `admin_order_search`, and ops check `ops-check-20260708t055740392z-b572bc27`.
- `MCP-011` adds a safe Admin MCP order-search tool without making `apps/agent` an order backend. `admin_order_search` is read-only, bounded to `query`, `page`, and `limit`, and API-service-binding-only behind the dashboard cookie preflight. It returns compact order identifiers/status/timestamp/amount/count summaries and omits raw customer name/phone/email, secrets, provider payloads, receipt/recovery/session tokens, items/item lines, addresses, tracking data, raw payment evidence, unknown upstream fields, and upstream error bodies. Release-check now requires the tool in authenticated Admin MCP `tools/list` and calls it with demo-safe query `DW8W05`. Verification: focused agent tests (`31`), focused release-check tests (`49`), agent typecheck/lint/build, env/dist-secret/release-skip/diff gates, agent deploy `40d0d03d-8392-46fc-b4fa-680172850c87`, live authenticated release check proving `admin_navigation_context`, `admin_product_search`, and `admin_order_search`, and ops check `ops-check-20260708t053007095z-b76c39db`.
- `MCP-012` adds public Storefront MCP category discovery without widening UCP or commerce mutation authority. `catalog_categories` is public, read-only, API-service-binding-only, and limited to compact category facts; it sends no cookies/auth and exposes no private customer, order, payment, checkout, session, receipt, recovery, support, provider, or admin data. Release-check now expects `catalog_categories` in public agent MCP `tools/list` and calls it with bounded `{ limit: 1 }`, requiring no-store, non-error, non-empty structured content. UCP capability checks remain catalog-only search/lookup/product and must not advertise `catalog_categories`. Verification: focused agent tests (`35`), focused release-check tests (`50`), agent typecheck/lint/build, env/dist-secret/release-skip/diff gates, agent deploy `c9ce8d8d-cf25-4fe6-85d9-85963e64551f`, live release check proving `catalog_profile`, `catalog_categories`, and `cart_validate`, and ops check `ops-check-20260708t054320788z-c901cd42`.
- `MCP-010` adds the first bounded Admin MCP domain read tool without making `apps/agent` a second admin backend. `admin_product_search` accepts only `query`, `page`, and `limit`, calls the existing products list route through the API service binding with the dashboard cookie and fixed `updatedAt desc` ordering, and compacts output to identifiers/status/category/count/timestamp fields only. It intentionally omits descriptions, prices, discounts, SKUs, primary images, deleted fields, stock, barcodes, unknown upstream fields, and upstream error bodies. Release-check now requires the tool in authenticated Admin MCP `tools/list` and calls it with a bounded query. Verification: explorer review, focused agent tests (`27`), focused release-check tests (`48`), agent typecheck/lint/build, env/dist-secret/diff gates, agent deploy `8cb5b558-7aac-47c7-8491-22037391d4d0`, live authenticated release check, and ops check `ops-check-20260708t051556118z-dd5375f0`.
- `AI-002` closes the assistant model-profile prerequisite without widening MCP authority. The existing AI settings domain now owns canonical `adminChat`, `storefrontChat`, `widgetGeneration`, `imageGeneration`, and `voice` profiles; only `widgetGeneration` inherits the current widget provider default, future assistant profiles stay disabled with empty models until explicitly saved, and `resolveAiModelProfile()` fails closed unless profile enabled, provider enabled, strict credential readiness, and model allowlist membership all pass. The admin settings route/UI can read and save profiles but rejects profile payload secrets. Verification: core AI settings tests (`95` files / `780` tests), API AI settings tests (`92` files / `759` tests), admin Widget AI boundary test (`1` file / `3` tests), core/API/admin/API-client typechecks, API/admin lints/builds, `pnpm generate:sdk`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm check:admin-perf`, `pnpm release:check --skip-live --json`, `git diff --check`, API deploy `e6e387ef-0daa-4434-a4ef-df6809893d53`, admin deploy `a816af7e-a7b0-41fb-9b39-0af5efa8de13`, live authenticated `pnpm release:check --timeout-ms 30000`, and live ops check `ops-check-20260708t050035076z-3432426b` pass with only known OPS-005/OPS-010 warnings.
- `MCP-005` and `MCP-006` are verified and deployed in `scalius-agent` version `bcb16c5a-02ec-4f16-95bf-ce14f95cea69`. Admin MCP now has two read-only tools, `admin_session_context` and `admin_navigation_context`, both behind the existing cookie-only API RBAC preflight. Storefront MCP now allows exactly the four catalog tools plus `cart_validate`; `cart_validate` accepts only a bounded snapshot, sends no cookies/auth/private buyer values, and returns stale-cart repair semantics without cart mutation. Release/deploy gates now execute both `catalog_profile` and `cart_validate` against the deployed Worker and still reject checkout/order/payment/customer/recovery and cart-mutation terms while UCP remains catalog-only. Verification: `pnpm --filter @scalius/agent test` (`21` tests), agent typecheck/lint/build, focused release/deploy script tests (`54` tests), `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm release:check --skip-live --json`, `git diff --check`, `pnpm run deploy:agent`, manual live `cart_validate` smoke returning `PRODUCT_UNAVAILABLE/remove`, and live `pnpm release:check --timeout-ms 30000` with only known OPS-005/OPS-010 warnings/actions.
- `MCP-002` deploy-gate hardening now makes `pnpm run deploy:agent` prove deployed tool execution as well as release candidates. `verifyAgentDeploy()` reuses the shared agent smoke with `catalogToolSmoke: true`, passes the configured storefront origin, and logs the `catalog_profile` endpoint/capability count after the deployed Worker returns a valid catalog-only UCP profile. This blocks an agent deploy that can list catalog tools but cannot execute `catalog_profile` or returns checkout/cart/order/payment/customer/recovery capabilities. Verification: `node --check scripts/deploy.mjs`, `node --check scripts/deploy.test.mjs`, focused `pnpm exec vitest run scripts/deploy.test.mjs scripts/release-check.test.mjs --passWithNoTests` (`2` files / `48` tests), `git diff --check`, and direct live `verifyAgentDeploy({ storefrontUrl: "https://storefront.scalius.com", timeoutMs: 30000 })` showing `catalog_profile call ok` for `https://storefront.scalius.com/ucp`.
- `MCP-002` release-gate hardening now proves deployed MCP tool execution, not only MCP discovery. `pnpm release:check` keeps the exact catalog-only `tools/list` gate and now calls the deployed agent's read-only `catalog_profile` tool, requires non-error structured content, and validates the returned UCP profile with the same HTTPS/storefront-origin/catalog-only rules used for storefront discovery. This keeps stable limited to the stateless public catalog Worker and blocks accidental checkout/cart/order/payment/customer/recovery capabilities from being introduced through the agent. Verification: `node --check scripts/release-check.mjs`, `node --check scripts/release-check.test.mjs`, focused `pnpm exec vitest run scripts/release-check.test.mjs scripts/deploy.test.mjs --passWithNoTests` (`2` files / `47` tests), `git diff --check`, live `pnpm release:check --timeout-ms 30000` showing `PASS agent MCP ... catalog_profile call ok`, and live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` (`ops-check-20260708t003815458z-ce036a6a`) passing with only known OPS-005/OPS-010 warnings.
- `MCP-003` adds the first release-safe Admin MCP foundation without making the agent Worker a second admin API. `apps/admin-v2` now exposes `/api/assistant/mcp` as a same-origin dashboard proxy to `apps/agent` `/mcp/admin`; the proxy rejects cross-origin cookie requests, strips `Authorization`, forwards only cookie/MCP protocol headers, and marks responses `no-store`. The agent route checks the dashboard cookie through the API service binding at `/api/v1/admin/rbac/my-permissions`, relies on existing admin auth/onboarding/2FA/RBAC middleware, reuses the guarded permission context for the read-only `admin_session_context` tool, and has no direct D1/KV/R2/queue/provider-secret bindings. Verification: focused agent/admin proxy/release/deploy tests, agent/admin typechecks/lints/builds, env/dist/diff gates, agent deploy `38a75356-f7ba-4896-afc9-67429aa7b86d`, admin deploy `fe3c357d-8cb4-4fd8-be09-ad1e4ecc3b4a`, live `pnpm release:check --timeout-ms 30000`, and an authenticated live demo-admin MCP initialize/tools-list/tools-call probe returning only `admin_session_context` with `no-store` on all MCP responses.
- `OPS-005` / `OPS-010` stable-release owner handoff is documented after fresh read-only Cloudflare checks and parallel audits. There are still no code-owned P0/P1 release blockers, but the stable tag should wait for two P2 operational acceptances unless the owner explicitly accepts the risk: routed Cloudflare Email alert proof for `scalius-ops-monitor` and cleanup of the stale external `worker:testdash` queue producers. Current evidence: Wrangler `4.107.1` can list the enabled `scalius.com` Email Routing zone but the active token still lacks `email_routing:write` / `email_sending:write`, so Email Sending/Routing detail checks fail with Cloudflare auth error `10000`; live ops-monitor has `ALERT_EMAIL` bound but empty `ALERT_EMAIL_FROM` / `ALERT_EMAIL_TO` and an unrestricted binding; `worker:testdash` still has live queue bindings for `payment-events`, `order-notifications`, `auth-otp`, and legacy `order-ingest`, while `https://store.wrygo.com/` and `/health` return `200`, so deleting or allowlisting it would hide real drift. The verification playbook now records the safe owner path: refresh Wrangler Email scopes, verify non-personal ops sender/destination aliases, configure restricted bindings, deploy ops-monitor, capture one redacted routed alert proof, and migrate or recover `testdash` before removing its queue producers. Verification: `pnpm --dir apps/ops-monitor exec wrangler whoami`, read-only `wrangler email routing/sending` checks, `pnpm --dir apps/api exec wrangler queues info payment-events/order-notifications/auth-otp/order-ingest`, `wrangler versions view --name testdash`, `curl -fsSI https://store.wrygo.com/`, `curl -fsSI https://store.wrygo.com/health`, `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json`, parallel OPS-005/OPS-010 audits, and `git diff --check`.
- `OPS-012`, `AUTH-053`, and `ADMIN-030` are verified and deployed. The release gate now carries ops-check warnings/actions forward so the compact release smoke cannot hide the logs-only ops Email configuration; Auth & Access now surfaces SMS readiness fetch failures and blocks SMS OTP policy saves while readiness is unknown; product edit is back on the deferred rich-text boundary and variant-management labels now follow the merchant's saved option axes instead of assuming every catalog is Size/Color. Verification: `pnpm exec vitest run scripts/release-check.test.mjs apps/admin-v2/src/routes/admin/products/$productId/-edit-boundaries.test.ts apps/admin-v2/src/components/admin/product-form/variants/VariantManager-boundaries.test.ts apps/admin-v2/src/lib/route-graph-boundaries.test.ts apps/admin-v2/src/components/admin/settings/sensitive-settings-load-error-boundaries.test.ts --reporter=dot` (`5` files / `104` tests), `node --check scripts/release-check.mjs`, `node --check scripts/release-check.test.mjs`, `git diff --check`, admin typecheck/lint/build, admin deploy `765d1478-88b2-428b-a727-a6455a8a6458`, `pnpm release:check --timeout-ms 30000`, `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` (`ops-check-20260707t223230238z-2a2ffd63`), and `pnpm check:dist-secrets` pass. Remaining stable-release acceptances stay `OPS-005` routed Email alert proof and `OPS-010` stale external `worker:testdash` producer cleanup.

2026-07-05:

- `PRIV-003` is verified and deployed. Guest receipt proof now stays out of browser-visible URLs: storefront checkout/COD/gateway flows set a per-order host-only `HttpOnly` receipt cookie, `/order-success` ignores and strips legacy `token`/`receipt_token`/`receiptToken` query params, receipt API reads require `X-Receipt-Token`, same-origin payment/support proxies forward proof from the cookie, SSLCommerz/Polar callback redirects and provider URLs are proof-free, and admin payment-recovery copy links return clean `existing_browser_receipt` URLs without `receiptToken`/`tokenHash`. Receipt KV hints now use `order_receipt:{sha256(token)}` instead of raw `chk_` keys. Verification: parallel API/storefront workers plus integration review, SDK regeneration, focused API/admin/storefront/core tests, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`354` files / `2398` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, local dev API/storefront receipt smokes, API deploy `72f559fd-dbf9-40ac-9e29-eea0a9a50ce6`, storefront deploy `7f448241-8ef3-46aa-abe3-dd7277568cb9`, admin deploy `3cc22699-379c-452c-8d77-d2147bb80826`, live API `/health`/`readyz`, live receipt query proof `404`, live tokenized `/order-success` redirecting to clean `/order-success?orderId=...`, live rendered recovery page with zero proof markers, live OpenAPI receipt header proof, and live admin recovery schema with `existing_browser_receipt` and no token fields. Remaining post-sale product scope: `POSTSALE-021` buyer-verified cross-browser recovery, because merchant-sendable recovery must not reintroduce a renamed bearer URL.
- `POSTSALE-021` is verified and deployed. Buyer cross-browser hosted-payment recovery now uses a proofless `/payment-recovery?orderId=...` URL plus an order-owned D1 `order_payment_recovery_challenges` table (`0078`) with hashed identifiers/codes, cooldowns, attempts, expiry, queue handoff cleanup, and bounded scheduled cleanup. Admin recovery copy now returns a buyer-verification link with `accessMode: "buyer_verified_receipt"` and no raw receipt token/hash. Public `POST /orders/payment-recovery/send-otp` returns generic no-store success and never exposes contact/proof; protected `POST /orders/payment-recovery/verify-otp` is service-JWT-only for the storefront server handoff. Storefront `/payment-recovery` is POST-only, verifies buyer contact by SMS/WhatsApp/email, then the same-origin proxy sets the existing per-order HttpOnly receipt cookie and returns a clean `/order-success?...` redirect without putting proof in browser JSON or URLs. Order-success missing-cookie hosted-payment fallback now links to recovery and still renders no receipt PII. Purpose-specific OTP delivery copy uses `auth.send_otp` with `purpose: "order_payment_recovery"`. Verification: three parallel audit reports in `audit/parallel/POSTSALE-021-*.md`, focused core/API/admin/storefront tests, `pnpm --filter @scalius/api typecheck`, `pnpm --filter @scalius/storefront typecheck`, `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm generate:sdk`, `pnpm --filter @scalius/database check:migrations`, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`358` files / `2412` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, local D1 migration, local browser screenshot `/tmp/scalius-payment-recovery-local.png`, local recovery/missing-cookie HTTP smokes, remote D1 migration `0078`, full deploy API `af710575-999a-456c-ac1f-2bcf1925a9ae`, admin `08a844fc-39fb-4b10-9058-4f4111458ca9`, storefront `a0577540-d33b-4954-93af-614a060ed55e`, ops-monitor `5113b0ba-feef-45ee-8c99-95aed1cd82c6`, deploy-script `/health` and four `/readyz` samples, live `/readyz` `200`, live recovery page `200`, live missing-cookie order-success recovery state `400`, live API send-OTP generic `200`, live storefront proxy send-code same-origin `200`, direct verify `401`, and live proof-marker scans across recovery HTML, order-success HTML, and JSON responses showing no `chk_`, `receiptToken`, or `tokenHash`. Tooling note: `pnpm db:generate` was attempted before the manual migration but Drizzle reported the existing `0045`/`0047` snapshot parent collision and emitted no migration; manual migration metadata is allowlisted and `check:migrations` passes.
- `PERF-003` discount type selector slice removes the decorative `motion/react` dependency from `/admin/discounts/new` type selection. `DiscountTypeSelector` now uses Tailwind hover/active/focus-visible transitions while keeping button semantics and route-owned lazy form loading. Verification: focused route-graph boundary test (`43` tests), admin typecheck/lint/build, root typecheck/lint/test/build (`346` files / `2368` tests), env/dist-secret/migration checks, source+dist marker scan for `motion/react`/`whileHover`/`whileTap`, `git diff --check`, local browser smoke with all three type choices, admin deploy `e4423ba3-e32c-48b4-a098-15835af3d4ed`, and live browser smoke with all three type choices pass. Screenshots: `/tmp/scalius-local-discount-type-selector.png`, `/tmp/scalius-live-discount-type-selector.png`.
- `PERF-003` scanner-token auth-barrel hygiene slice removes an avoidable static admin router edge into the broad `@scalius/core/auth` barrel. `@scalius/core` now exposes `@scalius/core/auth/scanner-token-claims`, and `apps/admin-v2/src/routes/api/scanner-token.tsx` imports only the D1 scanner-token claim helpers it needs while keeping RBAC permissions on the existing narrow permissions subpath. A route-graph boundary test guards against reintroducing the broad auth barrel in scanner-token routing. Verification: `pnpm exec vitest run apps/admin-v2/src/routes/api/-scanner-token.test.tsx apps/admin-v2/src/lib/route-graph-boundaries.test.ts packages/core/src/auth/scanner-token-claims.test.ts` (`53` tests), admin/core typecheck and lint, `pnpm --filter @scalius/admin-v2 build`, router chunk scan proving scanner-token now imports `scanner-token-claims-*.js`, `git diff --check`, and admin deploy `9976cddb-bef7-42a7-9075-905c54d47fc4` pass. Live smoke: `/api/scanner-token` no-cookie `401`, invalid token `401`, no-cookie POST `401`, unauthenticated `/admin` `307`, demo sign-in `200`, and authenticated `/admin` `200`. This is not full auth-route isolation: `/api/auth/*` and scanner-token session lookup still dynamically import `auth.server`, which should remain a separate auth architecture slice if pursued.
- `PERF-003` nested admin scroll-restoration slice improves back/forward navigation stability without broad router changes. `useAdminNestedScrollRestoration()` now keeps its pop-restore retry bounded but allows delayed route content height to settle for up to `30` animation frames instead of `8`; this avoids restoring to `0` on heavier pages whose main scroll container grows after data/components render. Added a happy-dom regression proving normal forward route navigation is left to TanStack Router while browser Back restores `#admin-main-scroll` after delayed content growth. Verification: `pnpm exec vitest run apps/admin-v2/src/lib/admin-scroll-restoration.test.tsx apps/admin-v2/src/lib/route-graph-boundaries.test.ts` (`40` tests), `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm --filter @scalius/admin-v2 build`, `git diff --check`, and admin deploy `13fca387-6d1b-4400-b3f9-4abd1e671c35` pass. Live smoke: unauthenticated `/admin` returned `307` to `/auth/login`, `/auth/login` returned `200`, demo sign-in returned `200`, authenticated `/admin` returned `200`, and browser probe showed `/admin/products` nested scroll restored from `593.5` after navigating to Orders and using browser Back. Follow-up analysis: do not blindly re-lazy the primary product description editor until a browser smoke proves no toolbar/skeleton/flicker regression; full `/api/auth/*` extraction remains architecture-sensitive even after the scanner-token barrel hygiene slice above.
- `PERF-003` data-table barrel slice keeps admin list route entries off the broad `components/admin/data-table` barrel. Products, orders, customers, categories, collections, discounts, pages, attributes, and widget list/trash routes now import `DataTable`, `DataTableToolbar`, and `useServerTable` from their leaf modules, leaving route chunks out of unrelated data-table helper exports. A route-graph boundary test guards against reintroducing exact admin-route barrel imports. Verification: focused `pnpm exec vitest run apps/admin-v2/src/lib/route-graph-boundaries.test.ts` (`39` tests), `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm --filter @scalius/admin-v2 build`, exact barrel import scan returning no admin-route matches, `git diff --check`, and admin deploy `60735e68-2e6e-43bd-88c8-e35b4518243c` pass. Live smoke: unauthenticated `/admin` returned `307` to `/auth/login`, `/auth/login` returned `200`, demo sign-in returned `200` for `demo@scalius.com` without 2FA, and authenticated `/admin` returned `200` with dashboard/orders/products content. PERF-003 remains open for the next isolated slices: product-form primary description Tiptap splitting, broader auth route graph architecture, dashboard chart weight, PDF export weight, and list column/action code splitting.
- `PERF-003` order-detail secondary-panel slice keeps payment/shipment/status/items immediate but moves lower-priority support-request and notification panels behind lazy boundaries. `OrderSupportRequestsCard` now loads only when an order actually has support requests, `OrderNotificationsCard` lazy-loads behind a stable card fallback, and source-boundary coverage prevents reintroducing static imports while explicitly keeping `PaymentCard`/`ShipmentCard` direct. Parallel audits found no code-owned OPS-005 closure before Cloudflare Email setup and warned not to relazy the primary product description editor; the remaining rich-text issue is a separate local-ref media picker fix. Verification so far: focused route/permission/prefetch tests (`55`), admin typecheck, admin lint, admin build, and dist marker scan proving the `_orderId` chunk no longer contains support dialog copy (`Resolve customer request`, `Customer request`, `Support request`) or notification receipt copy (`recorded attempt`, `Delivery settled`, `Stopped after`) while separate `OrderSupportRequestsCard-*` and `OrderNotificationsCard-*` chunks exist. Server `_orderId` chunk is now about `131.82 kB` after previously appearing around `155 kB` in the same deploy-era build output. Parallel note: `audit/parallel/PERF-003-order-detail-panels.md`.
- Admin edit-route lookup hardening fixes the `Cannot read properties of undefined (reading 'products')` class of crashes from collection/discount edit pages when secondary product/collection label lookups are empty, disabled, or still loading. Shared `productsByIdsQueryOptions()` and `collectionsByIdsQueryOptions()` now expose shaped empty `placeholderData`, return shaped empty payloads for empty IDs, and the discount edit route no longer type-asserts possibly undefined lookup data before reading `.products`/`.collections`. This is a UI data-contract fix, not a database migration; legacy/manual collections with no product IDs should open normally. Verification so far: focused lookup and route-boundary tests (`51`), admin typecheck, and admin lint.
- `PERF-003` shared DataTable interaction-menu slice keeps the default list-table helper path off Radix dropdown primitives. Sortable column headers, generic row-action menus, and pagination page-size menus now render compact trigger buttons first and lazy-load `DataTableColumnHeaderMenu`, `DataTableRowActionsMenu`, or `DataTablePaginationPageSizeMenu` only after click/keyboard open. `DataTable-boundaries.test.ts` guards the source boundary across `DataTable`, `DataTableColumnHeader`, `DataTableRowActions`, `DataTablePagination`, and `columns/column-factories`. Verification: focused `pnpm exec vitest run apps/admin-v2/src/components/admin/data-table/DataTable-boundaries.test.ts apps/admin-v2/src/lib/route-graph-boundaries.test.ts` (`42` tests), `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm --filter @scalius/admin-v2 build`, root deploy typecheck, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, source scan proving dropdown imports live only in the lazy menu files, production chunk scan proving server `column-factories-*.js` no longer imports `dropdown-menu` while the lazy menu chunks do, local Chrome `/admin/products` smoke opening sortable header/action/page-size menus with no console/page errors, admin deploy `3e6c3194-9ded-4a82-97eb-fca10a7bde1d`, and live authenticated `/admin/products` smoke proving `DataTableColumnHeaderMenu-DN9o51go.js`, `DataTableRowActionsMenu-Df-za5Rz.js`, and `DataTablePaginationPageSizeMenu-BBZ2plxp.js` load after interaction and all menus open. Client `column-factories-*.js` still lists the menu chunks and `dropdown-menu` in Vite's dynamic-import dependency map, and route-local dropdowns on `/admin/orders` and `/admin/discounts` still intentionally preload `dropdown-menu`; those are separate future slices, not shared DataTable coupling.
- `OPS-005` docs-only monitoring-contract slice adds `audit/ops-monitoring-contract.md` and links it from the operational runbook and verification playbook. The contract now points future work at the intended Cloudflare-native scheduled ops monitor: no public route, `/readyz` sampling, queue/DLQ `Queue.metrics()` via bindings, KV streak/cooldown state, structured redacted logs, and optional external alert routing after deploy. Verification: `git diff --check`, `pnpm check:env`, live `pnpm ops:check --json` request id `ops-check-20260705t044211831z-4e68f7d2` with `4/4` ready samples, OpenAPI `275` paths, API version `a750ed55-fce8-42e0-bd13-91528fbc7def` at `100%`, and live `pnpm ops:check --queues --samples 1 --timeout-ms 20000` request id `ops-check-20260705t044211847z-6b4c6ae0` passing all eight normal/DLQ queues. No ops-monitor deploy evidence existed for that slice, so live deploy, Wrangler tail/cron evidence, and alert-channel verification remain open.
- `OPS-005` scheduled ops-monitor slice is deployed. New workspace `@scalius/ops-monitor` runs Worker `scalius-ops-monitor` every two minutes with no public route, KV `OPS_MONITOR_STATE` (`6e5d1736490f4e7e82d7a27b1b173355`), `/readyz` sampling, and all eight queue/DLQ bindings read through `Queue.metrics()`. Verification: focused monitor tests (`8`), focused script/tooling tests (`27`), ops-monitor typecheck/lint/build, root typecheck during `pnpm deploy:ops-monitor`, root lint/build, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, deploy version `43ffb22b-8fae-438f-87c2-d45a0c36dbab` at `100%`, cron `*/2 * * * *`, Wrangler tail run `ops-monitor-20260705t070632025z-5ffb1c4f` with one transient `order-notifications-dlq` backlog `396`, next run `ops-monitor-20260705t070832006z-732a2cc6` with all queues `0`, and live `pnpm ops:check --queues --samples 1 --timeout-ms 20000` request id `ops-check-20260705t070903510z-0fa85148` passing `/readyz` plus all queue provider wiring. OPS-005 stay open for routed alert-channel verification and safe test-notification evidence.
- `OPS-005` Cloudflare Email alert-channel code path is deployed to ops monitor version `cc15af3e-23b5-4033-b9cb-1fbc3709ce41`. The Worker now has Cloudflare Email Service `send_email` binding `ALERT_EMAIL`, source-safe empty `ALERT_EMAIL_FROM` / `ALERT_EMAIL_TO`, redacted plain-text alert delivery when verified sender/destination config exists, redacted `ops_monitor.alert_delivery_failed` logs on delivery failure, and separate `alertCount`, `routedAlertCount`, and `deliveryFailureCount` fields in scheduled run logs. Missing email config remains logs-only by design. Verification: Cloudflare Email Service docs reviewed; focused ops-monitor tests (`11`), focused tooling tests (`30`), ops-monitor typecheck/lint/build, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, `pnpm deploy:ops-monitor`, deploy id `a4d51077-ebad-4cb5-995d-b11d1c5e48f3` at `100%`, cron `*/2 * * * *`, dry-run/deploy binding output showing `env.ALERT_EMAIL (unrestricted)`, live tail run `ops-monitor-20260705t072832017z-580df9e3` on version `cc15af3e-23b5-4033-b9cb-1fbc3709ce41` with healthy `/readyz`, all eight queues `0`, `alertCount:0`, `routedAlertCount:0`, and `deliveryFailureCount:0`, plus live `pnpm ops:check --queues --samples 1 --timeout-ms 20000` request id `ops-check-20260705t072704712z-b77aa6d2`. OPS-005 still require verified Email Service sender/destination config, preferably restricted binding addresses, and safe routed test-notification evidence before closure.
- `OPS-005` routed Email proof attempt on 2026-07-05 is externally blocked, not code-blocked. Read-only Wrangler checks confirmed the current OAuth token can list the `scalius.com` Email Routing zone, but `email routing addresses list`, `email routing settings scalius.com`, and `email sending settings scalius.com` fail with Cloudflare auth error `10000` because the token is missing `email_routing:write` and `email_sending:write`; official Email Service docs also require destination-address verification by email. Next action is account setup: refresh Wrangler scopes with `wrangler login`, create/verify sender and destination aliases, optionally restrict the `ALERT_EMAIL` binding to those aliases, then run one redacted routed alert proof.
- `OPS-005` routed Email proof follow-up on 2026-07-05 remains blocked with current live configuration. `pnpm --dir apps/ops-monitor exec wrangler versions view cc15af3e-23b5-4033-b9cb-1fbc3709ce41 --json` shows the deployed version still has `ALERT_EMAIL_FROM` and `ALERT_EMAIL_TO` as empty plain-text bindings, so `loadAlertEmailConfig()` cannot route a notification even though the `ALERT_EMAIL` `send_email` binding exists. The binding is still unrestricted, and the same read-only Email Service checks still fail with Cloudflare auth error `10000` for missing `email_routing:write` / `email_sending:write`; `wrangler email routing list` can only prove the `scalius.com` Email Routing zone is enabled. No test notification was sent. Owner action: run `wrangler login` to refresh Email scopes, create/verify sender and destination aliases, set `ALERT_EMAIL_FROM` / `ALERT_EMAIL_TO`, restrict the `ALERT_EMAIL` binding to those aliases if supported for the account, deploy with `pnpm deploy:ops-monitor`, then capture one redacted routed alert proof.
- `OPS-005` OPS-EMAIL follow-up on 2026-07-05T09:50Z rechecked the remaining Email Service scope without deploying. Wrangler `4.104.0` local schema and current Cloudflare docs confirm `send_email` can be restricted with `destination_address`, `allowed_destination_addresses`, or `allowed_sender_addresses`, but the live `scalius-ops-monitor` version `cc15af3e-23b5-4033-b9cb-1fbc3709ce41` still has empty `ALERT_EMAIL_FROM` / `ALERT_EMAIL_TO` and an unrestricted `ALERT_EMAIL` binding. Current OAuth scopes still miss `email_routing:write` and `email_sending:write`; `email routing list` shows `scalius.com` enabled, while `email routing settings scalius.com`, `email routing addresses list`, `email sending list scalius.com`, and `email sending settings scalius.com` fail with Cloudflare auth error `10000`. Verification: ops-monitor test (`11`), typecheck, lint, dry-run build, live `pnpm ops:check --queues --samples 1 --timeout-ms 20000` request id `ops-check-20260705t094857347z-c9c9fc22`, and live tail run `ops-monitor-20260705t095032010z-05785057` with all queues `0`, `status:"ok"`, `alertCount:0`, `routedAlertCount:0`, and `deliveryFailureCount:0`. No notification was sent. Owner action remains: `pnpm --dir apps/ops-monitor exec wrangler login`, create/verify the destination address, confirm sender/domain readiness, configure verified aliases plus binding restrictions, deploy with `pnpm deploy:ops-monitor`, and capture one monitor-routed alert proof.
- `OPS-005` / `OPS-010` ops-smoke hardening on 2026-07-08 keeps both items honest in release checks without pretending external setup is fixed. `pnpm ops:check` now reads `apps/ops-monitor/wrangler.jsonc` and surfaces the ops-monitor alert-channel contract: Worker name, observability, `*/2 * * * *` cron, `OPS_MONITOR_STATE` KV, `ALERT_EMAIL` send binding, configured sender/recipients, and binding restrictions. Logs-only Email setup remains warning-level with explicit required actions, while missing repo-owned monitor config fails. Queue metadata now treats unexpected producers such as old `worker:testdash` as warning-level cleanup debt but unexpected consumers as fail-level routing risk. OPS-005 still requires refreshed Cloudflare Email scopes, verified aliases, deployed config, and one redacted routed alert proof; OPS-010 still requires migrating or recovering `testdash` before removing stale producer warnings. Verification: focused `scripts/ops-check.test.mjs` (`18` tests), `node --check scripts/ops-check.mjs`, `git diff --check`, local `pnpm ops:check --skip-wrangler --samples 1 --timeout-ms 20000 --json` request id `ops-check-20260707t220628622z-df7ac72d`, live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` request id `ops-check-20260707t220637976z-a8768708` passing with only the expected logs-only Email and `worker:testdash` producer warnings, live `pnpm release:check --timeout-ms 30000` passing 410 tracker rows/no open P0/P1 blockers/API 4-of-4 ready/discovery/UCP, and no Worker deploy because only repo tooling/docs changed.
- `OPS-005` routed-proof hardening on 2026-07-08 adds a scheduled-only `OPS_MONITOR_STATE` one-shot marker at `ops-monitor:v1:control:routed-alert-proof`. The ops monitor consumes the marker before sending, routes one synthetic proof issue through the existing Cloudflare Email path, keeps public `fetch` closed as `404 no-store`, rejects unsafe/expired/oversized markers, and writes a redacted 24h result key under `ops-monitor:v1:control:routed-alert-proof:result:<nonce>` with `sent`, `failed`, or `log_only` status. This replaces the older temporary-bad-`READYZ_URL` proof method. Verification: ops-monitor tests (`21`), ops-monitor typecheck/lint/build, all-workspace typecheck during deploy, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, ops-monitor deploy version `485b6d3f-0b8d-4daf-b623-0ba1f3c62968`, live tail run `ops-monitor-20260708t010230572z-83258db4` with status `ok`, all eight queues `0`, `alertCount:0`, `routedAlertCount:0`, and `deliveryFailureCount:0`, live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` request id `ops-check-20260708t010007493z-251f45e5`, and live `pnpm release:check --timeout-ms 30000` pass with only expected logs-only Email and `worker:testdash` warnings. OPS-005 still requires verified Cloudflare Email sender/destination config, binding restrictions where supported, and one live scheduled proof with result `sent`, `routedAlertCount:1`, and `deliveryFailureCount:0`.
- Admin checkout-flow readiness copy drift was fixed while running the full root suite: `CheckoutFlowSettings` now shows the same fail-closed payment-readiness message as `checkout-flow-policy`, matching the boundary test and merchant-facing save lock copy. Verification: focused route-boundary test (`41` tests), admin lint/typecheck, root test (`343` files / `2361` tests), admin deploy `9f9ea53b-40a3-47dc-9385-2f604e4a519e`, live `/admin` `307 /auth/login`, live `/auth/login` `200`, and deployed `CheckoutFlowSettings-BJV1D0hG.js` contains the corrected readiness text.
- `NOTIF-006` manual fulfillment slice is verified and deployed. Final own-courier/manual fulfillment now returns a private status-change fact only when it actually moves the buyer-visible order status from `confirmed` to `shipped`; the API route records that through the existing durable order notification outbox and strips the private fact from the admin response. Fulfillment-status-only edits remain internal and do not send `order_completed`, because that event is tied to the buyer-visible `completed` order status. Verification: focused core fulfillment tests, focused API manual-fulfillment notification tests, core/API typecheck and lint, root typecheck/lint/test (`324` files / `2176` tests)/build, `pnpm check:env`, migration metadata guard, `pnpm check:dist-secrets`, `git diff --check`, API deploy `3668d510-8571-4845-b4fa-06d0a6ec8050`, live health `200`, live OpenAPI `269` paths / `367` operations with the manual fulfillment route present, no-cookie manual fulfillment `401`, and live readyz retries `200`/`200`/`200` pass. The first post-deploy readyz probe degraded on KV timeout and is recorded under `OPS-008`.
- `CHECKOUT-011` fresh parallel audit found a P1 duplicate-order risk: hosted SSLCommerz/Polar flows could commit an online order, clear checkout session state on redirect/recovery, and leave the local cart able to generate a fresh `checkoutId`, bypassing backend same-key replay and creating a second incomplete order for the same cart.
- `CHECKOUT-011` hosted cart-consumption slice is verified and deployed. SSLCommerz and Polar now set `clearCartOnRedirect` after a durable order commit for both external gateway redirects and receipt recovery redirects, so stale local cart data cannot create a second incomplete order with a fresh checkout id after the first order exists. Verification: focused hosted-payment handler/checkout cleanup/cart init tests (`3` files / `33` tests), storefront typecheck/lint, root typecheck/lint/test (`324` files / `2176` tests)/build, `pnpm check:env`, migration metadata guard, `pnpm check:dist-secrets`, and `git diff --check` pass. Storefront deploy `4b20d387-6bb0-41fa-8ded-1eadb4fa11d2` is live with `/health`, `/cart`, and `/checkout` returning `200`; the deployed checkout asset `/_astro/checkout.astro_astro_type_script_index_0_lang.DfrQ0UG4.js` contains `clearCartOnRedirect` plus SSLCommerz/Polar markers. The later empty-cart recovery-marker slice below closes the manual-back-navigation follow-up.
- `CHECKOUT-011` empty-cart recovery-marker slice is verified and deployed. SSLCommerz/Polar handlers now return a private `hostedPaymentRecoveryUrl` for both successful gateway handoff and post-commit recovery redirects; checkout stores only a same-origin receipt URL, gateway, and timestamp in `scalius_hosted_payment_recovery` before consuming the cart. The cart empty state reads the short-lived marker and shows `View payment status`, then clears it on click or when the customer has a non-empty cart, so the buyer gets a receipt path without stale cart resurrection. Verification: focused hosted-payment handler/session-state/cart empty-state/cart init/checkout redirect tests (`5` files / `52` tests), storefront typecheck/lint, root typecheck/lint/test (`324` files / `2179` tests)/build, `pnpm check:env`, migration metadata guard, `pnpm check:dist-secrets`, and `git diff --check` pass. Storefront deploy `bb1d42d5-1540-4348-9240-b6051e5b02a4` is live with `/health`, `/cart`, and `/checkout` returning `200`; deployed cart/checkout/session-state assets contain `View payment status`, hosted recovery metadata, and `scalius_hosted_payment_recovery` markers.
- `ADMIN-018` order-list payment-recovery visibility/export slice is verified and deployed. `GET /api/v1/admin/orders` accepts `paymentRecovery=recoverable|awaiting_payment|processing|needs_attention`, and dedicated `GET /api/v1/admin/orders/payment-recovery` plus `/payment-recovery/export` provide a server-backed queue/export surface mapped to `orders.view`. Responses expose only sanitized recovery summaries derived from hosted order state plus `payment_session_attempts` status/lease rows; attempt keys, request hashes, response payloads, claim IDs, receipt tokens, and provider payloads stay hidden. Admin orders show compact recovery badges, route recovery rows to order detail instead of edit, use the server-backed CSV export when a recovery filter is active, block bulk shipment for selected recovery rows, and abandoned-checkout archived hosted-payment records link back to the source order while delete controls remain RBAC-gated. Live verification also caught a production admin-read timeout caused by cookie-bearing API auth calling Better Auth's heavier session handler; `adminAuthMiddleware` now locally verifies signed Better Auth cookies with `BETTER_AUTH_SECRET` before D1/RBAC, wraps the D1 session lookup in transient read retry, and raw/tampered cookies fail as fast `401`s instead of hanging. Verification: parallel backend/UI/docs audits, focused RBAC/API/admin/source tests, `pnpm generate:sdk`, API/admin/core/api-client typecheck/lint, root typecheck/lint/test/build/env/dist/migration/diff gates, local dev admin/API smokes, API deploy `958019be-d29b-43f4-8ff4-7ce103ab2ae3`, admin deploy `d255be55-2747-464a-b997-f6216282b304`, live health/readyz, live OpenAPI `271` paths / `369` operations with recovery endpoints, live invalid-cookie `401` in ~53ms, live direct admin orders/recovery/export reads returning `200` without timeout, dashboard proxy orders/recovery/export reads around 0.22-0.36s, and canonical `/admin/orders?paymentRecovery=recoverable` page follow to `200` in ~0.48s.
- `ADMIN-016` shipment-lock follow-up is deployed to admin `6466e8cd-5ee2-4084-8f83-541d0cb20302`. Order-detail shipment history now disables provider status refresh with explicit operator copy while `activeRefundOperation` is present, matching the backend refund-operation mutation guard before the operator clicks into a rejected status sync. `ADMIN-018` was also corrected from stale `In Progress` tracker status to `Verified` because the server-backed recovery queue/export slice is already live. Verification: admin typecheck/lint, root Vitest source-boundary checks (`manual-fulfillment-boundaries`, `order-detail-permission-boundaries`), admin build, dist-secret/diff guards, deploy with Worker startup `18ms`, live `/admin` redirect smoke, and deployed asset marker checks for `refreshDisabledReason` plus shipment refund-lock copy.
- `ADMIN-022` is verified and deployed. The duplicated admin status-transition table was removed in favor of `@scalius/shared/order-state`, with core API validation and admin UI choices consuming the same canonical map. The fix also closes the direct-API casing edge case found during review: `updateOrderStatus()` and full order edits normalize order statuses before writes, inventory reconciliation, and notification dedupe, while unknown statuses fail before DB work. Verification included root typecheck/lint/test/build/env/dist/diff gates, `557` focused core order tests, `2,192` full Vitest tests, API deploy `01a0e619-7c27-4a2f-b362-5a89029d35a1`, admin deploy `219a8763-3c1d-482a-a774-4cf60b16c730`, live health/readyz, admin redirect smoke, and fast unauthenticated status mutation rejection.
- `CONTRACT-004` is verified and deployed. Parallel workers audited catalog/content, ops/settings/cache, metadata/access, and residual admin mutation routes, then the integration cleanup kept the sweep additive: 43 live OpenAPI response-status additions, zero removed statuses, and no operation/path churn (`273` paths / `371` operations). Added focused contract tests for the new route groups, regenerated the API client, and deployed API `9515a2ab-b9f6-4f17-948b-bf69df6ca49f`. Verification: focused OpenAPI contract tests (`7` files / `24` tests), API/API-client typecheck+lint, root typecheck/lint/test (`335` files / `2269` tests), root build, `pnpm check:env`, migration metadata guard, `pnpm check:dist-secrets`, `git diff --check`, deploy dry-run, live deploy with Worker startup `87ms`, four `/readyz` samples ready, served OpenAPI status smoke, and live no-cookie admin search `401` with request id.

2026-07-04:

- `OPS-004` is verified and deployed. `.github` is now tracked again and CI exercises the same platform gates expected before a release: dependency audit, SDK regeneration/freshness, env binding guard, migration metadata, lint, typecheck, tests, build, API deploy dry-run, dist-secret scan, tree cleanliness, and whitespace. The SDK generator no longer depends on a live API server and no longer trips over Cloudflare-only `agents`/`cloudflare:workers` imports because `widget-generation-runs` lazy-loads the Agents runtime only inside request handlers. Verification: `pnpm generate:sdk`, generated-client diff guard, focused API boundary/OpenAPI tests, CI YAML parse, `pnpm audit --audit-level moderate`, `pnpm check:env`, migration metadata guard, `pnpm run deploy:api -- --dry-run`, `pnpm lint`, root `pnpm test` (`323` files / `2174` tests), root `pnpm build`, `pnpm check:dist-secrets`, API deploy `d3ec5396-3d6a-4606-8fe0-fdf7c5080191` from code commit `e4abb2b1`, live health `200`, live readyz retry `200`/`200`/`200`, live OpenAPI `269` paths / `367` operations, and protected widget-generation status smoke `401` pass. The first post-deploy readyz probe degraded on KV/R2 timeouts at the 1.5s probe budget, then recovered on three immediate retries; keep that as an operational-readiness signal if it repeats.

- `CHECKOUT-021` is verified and deployed. Cart-to-checkout transfer now uses a small checkout-session helper that must persist and read back `scalius_checkout_data` before navigating to `/checkout`; the transferred gateway snapshot remains optional and is cleared/fresh-config-fallback when unavailable. If browser storage is blocked or quota-full, the cart restores the submit button and shows a persistent top-of-cart error instead of sending the buyer into a `/checkout` bounce. Direct or stale `/checkout` visits now render an explicit recovery message with a working Return to cart action, and quick-buy storage failure now lands on `/cart?quickBuyStorage=blocked` with a server-rendered explanation instead of an unexplained empty cart. Verification: focused storefront tests (`4` files / `30` tests), storefront typecheck/lint/build, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`321` files / `2168` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard (`76` SQL files / `76` journal entries), `git diff --check`, storefront deploy/version `c57ac117-1e6a-45a0-a06b-91e6ffad4715` with 15ms startup, live `/health`, `/checkout`, and `/cart?quickBuyStorage=blocked` smokes, live cart storage-error content proof, and deployed chunk marker checks for checkout recovery, transfer writes, and the shared session-state helper.

2026-06-28:

- `PRIV-002` is verified and deployed. `customer_auth_otp_challenges` no longer stores raw primary identifiers in `otp_key` or `identifier`, and no longer stores raw `contact_email`/`phone` columns. Migration `0069_customer_auth_otp_privacy` intentionally drops and recreates this short-lived table, invalidating in-flight OTPs so buyers re-request codes instead of carrying a legacy raw-column path. Runtime challenge keys are now `cust_otp:{channel}:{identifierHmac}`, identifier lookup uses a keyed HMAC plus buyer-safe mask, code verification still uses an HMAC of `otpKey:code`, and sign-up-only secondary contacts are stored as `enc:` AES-GCM values with `CREDENTIAL_ENCRYPTION_KEY`. Verification: Hume/Maxwell read-only audits, focused customer-auth challenge/service/API route tests (`3` files / `60` tests), affected core/API/database typechecks and lints, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`295` files / `1957` tests), root `pnpm build`, `pnpm check:env`, migration metadata guard (`70` SQL files / `70` journal entries), `pnpm check:dist-secrets`, `git diff --check`, local D1 migration/table-info smoke, remote D1 migration/table-info smoke proving no raw contact columns, API deploy `195d32b4-7848-43e3-9a7e-8f1a00f76180`, live API health/readyz, and live invalid OTP verify `400` through the deployed opaque lookup path.

- `AUTH-032` is verified and deployed. Customer auth cookie scoping no longer derives `Domain=.last-two-labels` from `STOREFRONT_URL` or `window.location.hostname`, so public-suffix domains like `shop.example.co.uk` and sibling subdomains are not accidentally over-broadened. The API sets host-only `cs_tok`/`cs_auth` cookies by default; `CUSTOMER_AUTH_COOKIE_DOMAIN` is the only supported opt-in for an explicit parent domain, with normalization rejecting localhost, IPs, single-label hosts, URLs, and malformed domains. Storefront logout/client cleanup now clears only the host-only readable mirror while the server clears the HttpOnly token. Verification: focused customer-auth service tests, focused API customer-auth cache tests, source scan for removed hostname/root-domain heuristics, affected core/API/storefront typechecks and lints, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`295` files / `1956` tests), root `pnpm build`, `pnpm check:env`, migration metadata guard, `pnpm check:dist-secrets`, `git diff --check`, API deploy `70acbcd1-5ef0-468b-97a1-71b0ff500c60`, storefront deploy `ba54c128-ed53-4f1c-871d-dd48364b30c3`, live API health/readyz and storefront health smokes, live API logout cookie smoke showing no `Domain=`, and live same-origin storefront logout smoke returning `200` with host-only clearing cookies.

2026-06-23:

- `OPS-007` is verified. Storefront is upgraded to Astro 7, `@astrojs/cloudflare` 14, `@astrojs/react` 6, and Vite 8 after checking the official Astro 7 release/upgrade guidance for the Vite 8/Rolldown stack, the Rust `.astro` compiler, Satteri Markdown defaults, stable route caching, and removed legacy experimental flags. The stale custom `apps/storefront/src/worker.ts` entrypoint was deleted because Astro 7's Cloudflare adapter generates the production Worker and deploy config under `dist/server/`; `scripts/deploy.mjs` now deploys `apps/storefront/dist/server/wrangler.json` after build. Verification: storefront typecheck/build, root tests/typecheck/lint/build, env/dist-secret/diff guards, `wrangler deploy --config dist/server/wrangler.json --dry-run`, `pnpm run deploy:storefront -- --dry-run`, local Astro dev desktop/mobile cart browser smokes, and generated Worker cart smoke pass.

- `PERF-014` is verified. Cart runtime initialization is now idempotent across repeated `initCartFunctionality()` and Astro page transitions: the previous store subscription is unsubscribed, DOM listeners are owned by an `AbortController`, abandoned-checkout timers are cleared before rewire, duplicate discount/checkout listeners are prevented, and `document`-scoped `cart-updated` events refresh checkout state correctly. Cart page-owned listeners are also abortable, stale cart repair content renders before checkout actions on mobile, and leaving the cart clears only checkout transfer/session payloads while preserving the active abandoned-checkout id used for recovery. Verification: focused cart/session tests, root gates, local Astro dev desktop/mobile cart browser smokes, and generated Worker cart smoke pass.

2026-06-22:

- `INV-010` is verified and deployed. Product option inventory control now uses one explicit SKU-level stock-limit rule across row edit, spreadsheet edit, first-option setup, bulk generation, preview, CSV import/export, and read-only display. First option setup always starts stock-tracked even when the old simple SKU was `No stock limit`; row/spreadsheet/bulk edit can switch to `No stock limit` without keeping a misleading stock input; bulk update normalization drops ignored stock edits when tracking is disabled; CSV round-trips a `Track Stock` column with strict yes/no parsing; over-reserved tracked SKUs clamp availability to `0` and show sold out instead of negative/low stock. Verification: read-only parallel audits for product/variant UX, stale-cart impact, and admin-order SKU validation; focused variant tests (`4` files / `19` tests); admin typecheck/lint/build; root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`259` files / `1646` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`; local dev doctor; local authenticated browser DOM smokes for option row edit, spreadsheet edit, bulk generator preview, and simple-SKU first-option setup; screenshot capture failed in the in-app browser with `Page.captureScreenshot` timeout, so this slice used DOM/runtime assertions; admin deploy `ecac9094-7bd7-40ae-8212-6fd0d9c00784` / deployment `fa5cdb9f-dbe6-48dd-8c6e-ff3849ce6f1b`; live login form POST smoke; live asset marker smokes; remote D1 product-fixture proof; and live authenticated product-editor row-edit plus bulk-generator no-save smokes.

- `INV-008` copy/resilience follow-up keeps the SKU-first model but removes merchant-hostile stock language from admin simple-SKU surfaces. Simple/default untracked SKUs now render `No stock limit` in the dedicated simple SKU panel, first-option transition summary, generic option table fallback, stats chip, and read-only product detail; product detail no longer computes red zero availability for `trackInventory === false`. The storefront variant DOM fallback parser now preserves `data-variant-track-inventory="false"` so rare JSON-hydration failure paths do not turn untracked stock-zero SKUs into unavailable variants. Verification: parallel explorer audits for rich-text and SKU/variant UX; focused admin/source tests (`VariantManager-boundaries`, `ProductView-boundaries`); focused storefront `variant-state-machine` test; admin/storefront typecheck/lint/build; root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`259` files / `1642` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, local D1 simple-SKU proof, local admin doctor, and authenticated browser DOM checks for `/admin/products/new`, `/admin/products/prod_K0rvSum2CUbhHtpNL0EyL/edit#variant-section`, and `/admin/products/prod_K0rvSum2CUbhHtpNL0EyL`. In-app and OS screenshot capture both failed in this environment (`Page.captureScreenshot` timeout / `could not create image from display`), so this slice used DOM/runtime assertions instead of image artifacts.

- `AUTH-028` is verified and deployed. Invited admin creation now creates an unshared random bootstrap password only to satisfy Better Auth account creation, never emails or returns it, sends a reset/setup link through Better Auth, and marks the user with `mustChangePassword` plus `mustEnrollTwoFactor`. API admin middleware live-reads those flags before RBAC and blocks all admin endpoints except password setup and 2FA enrollment until both gates are cleared. The admin dashboard direct-D1 guard redirects flagged users to `/auth/forgot-password` or `/auth/setup-2fa`, the team UI shows `Password setup` / `2FA setup` badges, and the generated SDK includes onboarding fields. Verification: focused invite/auth/guard tests (`6` files / `96` tests), root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`254` files / `1620` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, local browser invite smoke at `/tmp/scalius-auth-invite-onboarding-smoke.png`, remote migration `0060_admin_onboarding_flags`, API deploy `76e564cd-fa47-4b20-9a10-3156a67d09d2`, admin deploy `a1458364-4568-4728-934a-381745934456`, live API health `200`, live OpenAPI onboarding-field proof, remote D1 `user.must_change_password` / `must_enroll_two_factor` proof, live unauthenticated admin-users `401`, live admin login no-JS POST/disabled-control smoke, and deployed account bundle markers with no `temporary password` copy.

- Auth-form URL safety remains a standing regression surface after repeated user-reported browser login attempts encoded credentials into the URL, including the 2026-06-22 reminder that a real browser once leaked login form values into the address bar. `AUTH-033`, `AUTH-034`, and `AUTH-035` are already verified for no-JS/pre-hydration form safety, but future auth/account/checkout form work must keep source-boundary tests and live/local no-JS smokes that prove credential, OTP, email, phone, password, token, discount, and recovery values cannot enter URLs or browser history.

- `STORE-011` is verified and deployed. `Layout.astro` remains the only storefront source that renders a primary `<main>` landmark; Layout-owned cart, checkout, search, product detail, category detail, and collection detail pages now render neutral containers inside it, and category/search analytics selectors bind to the stable product-list data attribute instead of a page-owned `main`. Verification: source-boundary test `apps/storefront/src/lib/layout-landmarks-source.test.ts`, focused storefront tests, root typecheck/lint/test/build/env/dist/migration guards, local rendered HTML landmark counts, local screenshots at `/tmp/scalius-store011-*.png`, storefront deploy `efdd9a69-d8fe-4fbb-be31-e5d47619b4cf`, and live HTML smokes showing exactly one `<main>` on `/cart`, `/checkout`, `/search`, `/account`, `/products/test-productss`, `/categories/drinks`, and `/collections/9dHdb5uI8o7t3kKdIyUr8`.

- `STORE-009` is verified and deployed. Category/search URLs with dynamic attribute filters no longer wait for layout before starting product-list reads: the pages still start layout and filter metadata immediately, await only the attribute metadata for canonical filter validation, then start `getAllProducts()` / `getProductsByCategory()` and join layout afterward. Category widgets remain chained from the category-products response in both normal and dynamic paths. `product-list-query` tests now explicitly prove built-in filters stay on the cheap path while attribute-like and unknown render params require metadata validation. Verification: read-only subagent audit, focused storefront tests (`apps/storefront/src/lib/page-data-boundaries.test.ts` and `product-list-query.test.ts`, `13` tests), storefront typecheck/lint/build, root typecheck/lint/test (`255` files / `1629` tests), `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, local `/search`, `/categories/clothing`, and canonical redirect smokes, storefront deploy `bfdc996a-8c29-4b67-aa9a-e85f91a095fb`, live attribute metadata checks, and live rendered dynamic-filter smokes for `/search?brand=Apple` and `/categories/laptop?brand=Apple`.

- `PERF-013` is verified and deployed. Category/search pagination URLs and sort-dropdown navigation now rebuild from the resolved canonical product-list `currentFilters` instead of mutating raw `Astro.url`/`window.location`, so validated query, sort, price, boolean, and dynamic attribute filters survive navigation while tracking/default/invalid params are stripped. Source-boundary tests prevent reintroducing raw URL pagination, and helper tests cover canonical page/sort changes. Verification: parallel subagent audits, focused storefront tests (`15` tests), storefront typecheck/lint/build, root typecheck/lint/test (`255` files / `1631` tests), root build, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, local HTTP/Chrome smokes with screenshot `/tmp/scalius-perf013-local-search.png`, storefront deploy `15697a36-d928-4dd2-9574-262289704136`, live rendered pagination href smoke (`/search?page=2&sortBy=price-asc` from a noisy URL), live dynamic-filter canonical-state smokes, live noisy-param redirect smoke, and live Chrome sort navigation screenshot `/tmp/scalius-perf013-live-search-sort.png`.

- `AUTH-018` is verified and deployed. Customer sign-up now sets durable `customers.profile_completion_required_at` until required profile fields are complete; `profile_completed_at` is backfilled only for complete legacy rows. `/verify-otp`, `/me`, and `/profile` return the canonical D1 customer profile with address, city/zone/area IDs, resolved labels, `profileComplete`, and `needsProfileCompletion`; `updateCustomerProfile()` validates active delivery-location hierarchy and ignores forged display labels. AuthModal resumes `profile_setup` after passive session hydration when a customer still needs completion, blocks silent dismiss with a save-or-sign-out choice, and emits `customer-login` only with the full canonical profile after active login/profile completion. Cart/account hydration now receives profile/location hints without overwriting already-entered checkout fields. Verification: parallel subagent audits for backend/session, storefront UX, and checkout/account impact; focused customer-auth tests (`4` files / `51` tests); root `pnpm typecheck`; warning-free root `pnpm lint`; root `pnpm test` (`254` files / `1625` tests); root `pnpm build`; `pnpm check:env`; `pnpm check:dist-secrets`; migration metadata guard; `git diff --check`; SDK regeneration from local OpenAPI (`262` local paths, generated client with profile fields); local API health/`/customer-auth/me`/OpenAPI smokes; local Chrome cart/account screenshots at `/tmp/scalius-auth018-cart-local.png` and `/tmp/scalius-auth018-account-local.png`; local browser `/account` auth-modal open smoke with no console warnings/errors; remote D1 migration `0061_customer_profile_completion`; API deploy `94c479af-a4a4-4b97-a942-149520cb0b03`; storefront deploy `d7fbc578-2c27-4cae-9825-80ea55610125`; live API health/unauthenticated `/customer-auth/me`/OpenAPI profile-field smokes; remote D1 `PRAGMA table_info(customers)` proof for both profile columns; live storefront `/`, `/cart`, `/api/customer-auth/me`, and deployed `AuthModal.X-Gbk3wA.js` marker smokes.

- `AUTH-031` is verified and deployed. Customer OTP send rate limiting moved from KV get-then-put to guarded D1 rows in `customer_auth_otp_rate_limits`, with scheduled bounded cleanup for expired windows. `/customer-auth/send-otp` now prefers `CF-Connecting-IP`, only parses `X-Forwarded-For` when the runtime is explicitly loopback/local, hashes IP buckets, and sends missing/untrusted client identity into a shared fail-closed bucket. Verification: focused customer-auth rate-limit/service/API/scheduled tests (`4` files / `39` tests), root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`240` files / `1555` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, local and remote D1 migration `0056_customer_auth_otp_rate_limits`, remote D1 table-info proof, API deploy `6e8f93f6-3ae7-4e65-9a00-7051999c97ea`, live API health `200`, live OpenAPI customer-auth route smoke, and deployment-list proof at `100%`.

- `INV-007` is verified and deployed. Existing SKU stock changes from product variant edit and spreadsheet bulk edit now split stock out from ordinary metadata writes and batch the movement claim with the guarded variant update, so combined edits cannot save metadata while losing the stock movement. `setStock()`, scanner `adjustStock()`, and manual `adjustInventory()` also insert the `inventory_movements` claim and update stock/`stockVersion` in one D1 batch guarded by the same `stockVersion` precondition; clamped reductions record the actual movement delta. Variant duplication now copies merchandising fields only and starts the new SKU at zero physical/reserved/preorder stock. Verification: focused core stock-ledger tests (`3` files), core suite (`61` files / `349` tests), affected core/API typechecks, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`244` files / `1566` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, API deploy `563e2914-6325-4813-a905-f50378fdd324`, live `/api/v1/health` `200`, live `/api/v1/openapi.json` `200`, and OpenAPI route proof for variant update plus bulk-update routes.

- `INV-006` is verified and deployed. The long-term product rule is still SKU-first: products are merchandising containers, `product_variants` rows are the only sellable/inventory identities, simple products have one hidden/default no-option SKU, and merchant-created option SKUs must expose at least one customer option. This slice closed the remaining UX/enforcement leaks around that model: COD-only cart submit now calls cart validation and uses validated SKU IDs/prices/delivery details, including discount validation against the same server-validated item snapshot; admin manual orders no longer allow “main product” variantless items; the product variant table protects hidden simple SKUs and now labels their untracked inventory as `No stock limit`; duplicate/delete/bulk-select skip the protected default; and create/update/bulk variant services reject ambiguous no-option merchant SKUs. Remaining long-term work is tracked separately in `INV-007` for stock-ledger/movement discipline and `INV-008` for guided simple/optioned transition UX. Verification: focused storefront/core tests (`2` files / `6` tests), affected admin/storefront/core typechecks and lints, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`241` files / `1560` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, API deploy `e696b984-7955-4597-b060-e335e912a86a`, admin deploy `eddb40db-81da-4134-ada3-12a186994349`, storefront deploy `5c555e0d-1b8c-4f95-bb6d-6b232f84e0e2`, live API health `200`, remote D1 proof (`products_without_active_sku = 0`, `ambiguous_no_option_products = 0`), live default-SKU product page `In Stock` smoke, and live `/buy/{slug}` smoke embedding the real `var_default_*` SKU.

- `CHECKOUT-032` is verified and deployed. Quick-buy now resolves the candidate buyer SKU, validates current product/variant/stock/price/free-delivery state through `/orders/cart-validation`, and only then writes `quickBuyData` or fires quick-buy analytics. Invalid quantities fail closed before validation, stale product/variant/stock/price failures redirect back to product detail with buyer-safe repair copy, and the checkout validation proxy now uses the shared unsafe-cookie Origin guard. Verification: focused storefront tests (`2` files / `12` tests), storefront typecheck/lint, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`239` files / `1546` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard (`56` SQL files / `56` journal entries), `git diff --check`, storefront deploy `43fc8f81-2590-416f-b8cc-005d73881ee9`, live `/buy/{slug}` smoke writing a real `var_default_*` SKU, live invalid-quantity redirect/product notice smoke, and live `/api/checkout/validate-cart` cross-origin cookie `403` smoke.

2026-06-21:

- `INV-005` is verified and deployed. Product inventory now follows a SKU-first rule: products are merchandising containers, every sellable product has a real `product_variants` SKU row, simple products use one hidden/default no-option SKU, optioned products require explicit selected SKUs, and ambiguous/SKU-less products fail closed. Migration `0055_default_sku_inventory_tracking` added `product_variants.is_default`, `product_variants.track_inventory`, `order_items.inventory_tracked`, backfilled untracked default SKUs for simple products, and preserved old variantless orders as untracked. Storefront product pages and `/buy/{slug}` use buyer-facing variants so hidden defaults do not leak after a product gains options; cart validation rejects stale hidden-default carts with a `select_variant` repair. Verification: focused/root tests (`238` files / `1538` tests), root typecheck/lint/build/env/dist/diff gates, SDK regeneration (`261` paths), local and remote D1 migration proof, API deploy `c930b862-fe56-439f-a6b6-fff9310f98c9`, admin deploy `68fefc75-d84a-44ab-8e38-991b49a4cc19`, storefront deploy `e10a01cc-4e21-4ba0-af89-a75ee8095c51`, live API/OpenAPI/cart smokes, remote D1 proof (`products_without_active_sku = 0`, `active_default_skus = 15`, `historical_variantless_untracked = 36`), and live default-SKU product page plus `/buy/{slug}` smokes showing the real default SKU is purchasable.

- `SEC-014` is verified and deployed. Admin/cache API routes no longer accept JWT Bearer fallback, removing the second admin credential plane that could drift from Better Auth revocation, ban/deleted state, and 2FA session truth. The only non-dashboard exception remains scanner session cookies, which are still restricted to exact scanner workflow endpoints and do not inherit the minting admin's RBAC. API docs and core auth docs now describe JWT auth as non-admin service-token infrastructure only. Verification: SEC-014 explorer audit, focused `admin-auth` middleware tests (`21` tests), affected API typecheck/lint, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`238` files / `1527` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard (`55` SQL files / `55` journal entries), `git diff --check`, API deploy `2ead5ab0-d7fe-4af1-91c2-989d9d8c022e`, live API health `200`, and live `/api/v1/admin/products` no-cookie/Bearer-only `401` smokes with no `X-New-Token` header.
- `CHECKOUT-037` and `CHECKOUT-038` are verified and deployed. Returning carts still get item-level repair issues for one, many, or all stale products/variants/stock/price changes; now delivery freshness is also fail-closed before policy/rate/checkout-attempt/KV/order side effects. `/orders/cart-validation` accepts optional `city`, `zone`, `area`, and `shippingMethodId` and returns an optional `delivery` preflight result; the storefront proxy forwards those fields once selected. Final order creation reuses the same prevalidated delivery result instead of re-reading later, while direct callers still fail closed through the shared helper. The cart client reconciles validated `freeDelivery` back into local cart state before totals and checkout transfer, and clears shipping-sensitive discounts when delivery eligibility changes. Verification: focused core order tests (`1` file / `14` tests), focused API order/cart-validation tests (`1` file / `26` tests), focused storefront cart tests (`1` file / `5` tests), SDK regeneration (`261` documented routes), root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`238` files / `1532` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard (`55` SQL files / `55` journal entries), `git diff --check`, API deploy `4a3c53bb-5266-4164-a441-f1e1dc044bcb`, storefront deploy `c6a617c7-67ae-4669-b6a7-3d88a9c2cb43`, live API health `200`, live OpenAPI smoke proving cart-validation has delivery inputs/output, live `/cart` deployed asset marker, and live storefront stale-line validation smoke returning `PRODUCT_UNAVAILABLE` with action `remove`.
- `SEC-013` is verified and deployed. The lightweight admin route guard still avoids importing the full Better Auth server stack on hot route entry, but it now verifies Better Auth's HMAC-signed session cookie format (`token.base64(hmac-sha256(token, BETTER_AUTH_SECRET))`) before any D1 session lookup. Raw leaked tokens, tampered signatures, wrong-secret signatures, and missing `BETTER_AUTH_SECRET` all fail closed as unauthenticated. Verification: focused admin session/auth route tests, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`238` files / `1526` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard (`55` SQL files / `55` journal entries), `git diff --check`, admin deploy `fa94e771-e058-487b-ad2e-1b2c0890df55`, and live `/admin` smokes proving both missing-cookie and forged raw-cookie requests redirect to `/auth/login`.
- `AUTH-023` is verified and deployed. Email OTP readiness is now a shared core helper requiring a saved sender plus Cloudflare `EMAIL` binding or decryptable Resend key; admin auth saves and customer OTP sends use the same fail-closed check before writes/challenge mutation, and Auth & Access shows Email OTP readiness beside SMS/WhatsApp readiness. New read-only auth/cart audits were folded into the queue as `SEC-013`, `SEC-014`, `AUTH-031`, `PRIV-002`, `AUTH-032`, `CHECKOUT-037`, and `CHECKOUT-038`; existing stale-cart product/stock/deleted-item handling was verified as already covered by cart validation/final order validation. Verification: focused shared/core/API tests, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`238` files / `1524` tests), root `pnpm build`, SDK regeneration (`261` documented routes), `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, API deploy `2acb5519-8cb2-48c3-b91b-1e1619d7cadf`, admin deploy `d0a0a014-f7ed-4745-954d-52e11108f583`, storefront deploy `a4f99975-88eb-4bdc-957f-575a3dd93ad2`, live API health/OpenAPI/checkout-config smokes, live storefront marker smoke, and deployed admin AuthSettingsBuilder asset marker smoke.
- `AUTH-029` and `AUTH-030` are verified and deployed. Storefront customer sessions moved from raw-token KV records to D1 `customer_sessions` rows keyed by HMAC token hash; `/customer-auth/me`, account order/detail/payment recovery, profile updates, logout, and guest-disabled checkout authentication now use the D1 authority. Session reads join live `customers` rows and fail closed for expired, revoked, missing, or soft-deleted customers. Admin customer soft/bulk/permanent deletes revoke/delete affected session rows, and scheduled maintenance prunes expired/old revoked customer sessions in bounded batches. Verification: read-only auth-session subagent audit, focused auth/API/session tests (`6` files / `59` tests), affected typechecks/lints, migration metadata guard, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`235` files / `1502` tests), root `pnpm build`, local migration/table-info smoke for `0054_customer_sessions`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, API deploy `5bc3ebee-044b-4b37-b715-54ee189a2f4f`, remote D1 table/index smoke, live API health `200`, and live `/customer-auth/me` no-cookie/fake-cookie private no-store smokes returning `{ authenticated: false }`.
- `AUTH-027` and `CHECKOUT-035` are verified and deployed. Customer `sendOtp()` no longer performs pre-send account-existence checks, so sign-in/sign-up registration state is only disclosed after a valid OTP is consumed by `verifyOtp()`; send responses are generic and always backed by a real D1 OTP challenge/cooldown when transport readiness passes. The storefront OTP step now offers a safe alternate-intent action after post-proof unknown-account or duplicate-account errors. Returning/stale cart validation now fails closed when the freshness check itself cannot be read, preserves a blocking top-of-cart retry message, and invalidates in-flight validation when cart contents change; final order creation still re-runs `validateStorefrontCartItems()` before policy/rate gates or D1 checkout claims. Verification: read-only subagent audit, focused auth/cart/order tests (`6` files / `63` tests), affected typechecks/lints, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`234` files / `1494` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, API deploy `c725f94a-8853-44f8-aa05-8a7f4211f9cf`, storefront deploy `546fe109-7220-4743-8b18-ad7ee90694f6`, live API health `200`, live `/cart` markup smoke, live deployed AuthModal/cart asset marker smokes, and live storefront proxy `/api/checkout/validate-cart` stale-item smoke returning a structured `PRODUCT_UNAVAILABLE` issue.

- `AUTH-026` is verified and deployed. First-admin bootstrap no longer uses Cloudflare KV for setup locking or setup throttling; `/api/v1/setup` now rate-limits through D1 `admin_setup_rate_limits`, claims the singleton D1 `admin_setup_claims` row before calling Better Auth, reclaims failed/stale claims through guarded update predicates, and completes admin promotion plus claim completion through one guarded D1 `safeBatch()` finish. Verification: focused core admin-setup tests (`6` tests), focused API auth-management tests (`22` tests), affected typechecks/lints, migration metadata guard, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`233` files / `1482` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, remote D1 migration `0052_admin_setup_coordination`, API deploy `7977cc51-d8be-4395-a578-d13e6d50ee65`, live API health `200`, live `/api/v1/setup` `adminExists=true`, live setup POST `403`, and remote D1 table proof for `admin_setup_claims` plus `admin_setup_rate_limits`.

- `SEC-011` is verified and deployed. Scanner QR token minting still requires authenticated, 2FA-complete inventory-capable admins, but the QR exchange no longer uses KV read/session-write/token-delete as the single-use authority. Tokens are hashed into D1 `scanner_token_claims`; exchange performs a guarded `consumed_at IS NULL AND expires_at > now` update before writing the KV scanner session with `claimTokenHash`; racing exchanges now produce one winning scanner session and losing `401`s. Scheduled API maintenance prunes expired/unneeded claims. Verification: focused shared scanner-auth test (`3` tests), focused core scanner-token-claims test (`4` tests), focused admin scanner-token plus API scheduled-maintenance tests (`11` tests), affected workspace typechecks/lints, migration metadata guard, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`234` files / `1489` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, remote D1 migration `0053_scanner_token_claims`, API deploy `50999170-909e-47cc-9bb4-128784c987b2`, admin deploy `3de21275-ea4b-4489-88c8-40104e31d64d`, live API health `200`, remote D1 table/index proof for `scanner_token_claims`, live unauthenticated scanner token mint `401`, and live invalid token exchange `401`.

- `SEC-012` is verified and deployed. Credentialed API CORS and cookie-origin guards no longer grant localhost/loopback origins in production by default. `getCorsOriginContext()` now adds loopback wildcard ports only when a first-party runtime URL (`PUBLIC_API_BASE_URL`, `BETTER_AUTH_URL`, or `STOREFRONT_URL`) is itself loopback; explicit exact localhost origins in `CREDENTIAL_CORS_ALLOWED_ORIGINS` remain opt-in without enabling every local port. Verification used current Cloudflare Workers best-practices docs and `@cloudflare/workers-types@4.20260620.1`, focused shared CORS tests (`8` tests), API cookie-origin middleware suite (`54` files / `437` tests through the API test script), affected typechecks/lints, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`234` files / `1493` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, API deploy `62a419af-1fd7-4613-9fbf-bf1a86554328`, live API health `200`, live storefront preflight `204` with `Access-Control-Allow-Origin: https://storefront.scalius.com`, live localhost preflight `204` with no `Access-Control-Allow-Origin`, and live localhost-origin cookie write `403`.

- Parallel 2026-06-21 auth/cart audits were verified against current source and folded into the queue. New or updated security rows: `AUTH-027` customer account enumeration before rate limits, `SEC-012` production localhost credentialed-CORS/cookie-origin allowance, `AUTH-028` admin temp-password invites without forced reset/2FA, `AUTH-029` soft-deleted customer sessions, `AUTH-030` raw customer session tokens in KV, and `CONF-002` provider credential decrypt fail-open behavior. New cart/checkout UX rows: `CHECKOUT-033` checkout-page stale-cart revalidation, `CHECKOUT-034` fake default variantless product mismatch, and `CHECKOUT-035` submit-time validation fail-open/COD silent repricing risk; `CHECKOUT-032` remains open for buy-now preflight.

- `SEC-010` is verified and deployed. Credentialed API CORS no longer reads merchant/admin CSP domains or CDN domains from KV/runtime settings; `getCorsOriginContext()` now allows local dev, first-party runtime origins, and explicit exact URL origins from credentialed-CORS env vars only, while filtering wildcard env entries. Unsafe browser requests that carry cookies are now rejected when `Origin` is not first-party/exact-allowed: the API guard covers customer-auth, cache, orders, and admin routes; admin same-origin file-route proxies guard admin API, auth, and scanner-token writes; storefront customer-auth/logout/checkout/payment-session proxies guard buyer writes. The admin Security UI now labels these domains as CSP-only so merchants do not think they are configuring credentialed API access. Verification: current Cloudflare Workers best-practices and latest `@cloudflare/workers-types` were retrieved before the Worker-facing change; focused shared CORS/origin tests (`2` files / `10` tests), focused API cookie-origin middleware test (`1` file / `4` tests), focused admin proxy/scanner tests (`2` files / `9` tests), focused storefront logout/create-order proxy tests (`2` files / `2` tests), affected workspace typechecks/lints, root `pnpm typecheck`, warning-free root `pnpm lint`, root `pnpm test` (`232` files / `1473` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, API deploy `593039cd-a099-40a0-b685-c590d4bb634a`, admin deploy `5c592963-ac3d-490d-aab3-3edf4fccf719`, storefront deploy `fa402d5a-63da-4f27-9ffd-575e5be8bbe2`, live API health `200`, live first-party storefront preflight `204` with `Access-Control-Allow-Origin: https://storefront.scalius.com`, live third-party preflight `204` with no `Access-Control-Allow-Origin`, live API customer-auth cross-origin cookie write `403`, live admin proxy cross-origin cookie write `403`, and live storefront logout/checkout cross-origin cookie writes `403`.

2026-06-20:

- `SEC-007` is verified and deployed. Admin `GET /api/v1/admin/abandoned-checkouts` no longer deletes old abandoned-checkout rows, archives stale hosted-payment orders, or prunes empty rows while serving a list read. The stale hosted-payment archive remains in scheduled maintenance with the existing product-availability cache invalidation, and old/empty abandoned-checkout row pruning moved into bounded core helper `cleanupStaleAbandonedCheckouts()` called by the 15-minute API cron. This removes the hidden mutation from an `ORDERS_VIEW` route and avoids the previous GET path's missing cache-invalidation side effect. Verification: current Cloudflare Workers best-practices/latest Workers types reviewed before the Worker-facing change; focused Vitest for the cleanup helper, scheduler wiring, and GET source boundary (`3` files / `7` tests); core/API typechecks and lints; root `pnpm typecheck`; warning-free root `pnpm lint`; root `pnpm test` (`225` files / `1444` tests); root `pnpm build`; `pnpm check:env`; `pnpm check:dist-secrets`; `git diff --check`; API deploy `cae463cd-6cf3-4add-af00-5b53347d5119`; live API health `200`; live OpenAPI summary for `/api/v1/admin/abandoned-checkouts` now `List abandoned checkouts`; and unauthenticated live admin list read returns `401`.

- `AUTH-025` is verified and deployed. Customer OTP verification authority moved from read-modify-write KV JSON to D1 `customer_auth_otp_challenges`: `sendOtp()` now stores only an HMAC-SHA256 code hash plus channel/intent/pinned-contact metadata, `/send-otp` deletes the exact challenge on queue handoff failure, `verifyOtp()` uses guarded D1 updates to atomically consume correct codes or increment/lock wrong-code attempts, and scheduled maintenance deletes expired/stale challenge rows in bounded batches. We intentionally do not restore a used OTP after account-policy errors, because restoring correct-code state was one of the race paths. Verification: focused customer-auth challenge/service tests (`2` files / `19` tests), focused API route/scheduled tests (`2` files / `15` tests), core/API/database typechecks and core/API lints, migration metadata guard, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`227` files / `1454` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, local D1 migration/table-info smoke, remote D1 migration/table-info smoke, API deploy `2a7b6ee4-55e3-411e-8edb-c3aa099ba531`, live API health `200`, live OpenAPI smoke (`260` paths with customer send/verify), live invalid-verify `400 VALIDATION_ERROR`, and deployment-list proof at `100%`.

- `CHECKOUT-030`, `CHECKOUT-031`, `INV-001`, and `INV-004` are verified and deployed. Returning carts now run through shared `validateStorefrontCartItems()` and protected `POST /api/v1/orders/cart-validation`; the storefront `/api/checkout/validate-cart` proxy validates cart load/changes plus cart-to-checkout transfer, renders per-line remove/reduce/refresh actions, and blocks checkout while affected lines remain. Public order creation validates stale carts before checkout policy, rate limits, checkout-attempt claims, KV receipt/status writes, payment setup, or inventory mutation, while late reservation failures are mapped to the same customer-safe `details.itemIssues` shape. Variantless buyer lines now fail closed for variant-managed products, no-variant/no-stock products stay unavailable until product-level inventory exists, and reservation-time reads recheck parent product active/deleted state plus variant deletion before stock counters move. Verification: focused core order/ingest/inventory tests (`3` files / `19` tests), focused API order route tests (`1` file / `23` tests), focused storefront cart/checkout tests (`3` files / `8` tests), focused root inventory/queue unit tests (`2` files / `17` tests), regenerated SDK/OpenAPI (`261` paths), core/API/storefront/api-client typechecks, core/API/storefront lints, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`228` files / `1460` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, API deploy `7260ef72-1317-4405-b7e8-782ff50bca3f`, storefront deploy `6d03746d-e628-4b81-9692-cb555a4163a3`, live API health `200`, live OpenAPI smoke (`261` paths with `/api/v1/orders/cart-validation`), live `/cart` `200`, and live stale-line cart-validation smoke returning `PRODUCT_UNAVAILABLE` with action `remove`.

- `SEC-009` is verified and deployed. Transactional email fallback no longer prints full HTML/text bodies when Cloudflare Email/Resend are unavailable, closing the OTP/admin-2FA/reset-link/temp-password log leak found by the auth-security audit. The active email integration and deprecated universal Resend adapter now log only masked from/to metadata, content lengths, provider preference, and `contentLogged: false`, while returning the existing non-delivery signal so customer OTP queue jobs remain retryable/fail-closed. Verification: focused email/queue tests (`3` files / `23` tests) including a secret non-leak assertion and source-boundary guard; core/API typechecks and lints; root `pnpm typecheck`; warning-free root `pnpm lint`; root `pnpm test` (`226` files / `1445` tests); root `pnpm build`; `pnpm check:env`; `pnpm check:dist-secrets`; `git diff --check`; source scan for removed body-log fallback; API deploy `185b1a67-b614-4db6-a193-dc58fc2cd0c1`; admin deploy `3be4718f-2feb-4ae0-9933-77c70224d9d1`; live API health `200`; live admin login `200`; live unauthenticated `/admin` `307`; and deployment-list proof showing both versions at `100%`.

- Parallel 2026-06-20 auth/cart audits were verified against code and folded into the queue. New security rows: `SEC-009` sensitive email fallback logging, `SEC-010` CSP-to-credentialed-CORS coupling, `AUTH-026` first-admin setup atomicity, `SEC-011` scanner QR single-use races, and `AUTH-027` customer account enumeration. The stale-cart audit opened or reconfirmed `CHECKOUT-030`, `CHECKOUT-031`, `CHECKOUT-032`, `INV-001`, and `INV-004`; the cart-validation slice later verified `CHECKOUT-030`, `CHECKOUT-031`, `INV-001`, and `INV-004`, while `CHECKOUT-032` remains open for buy-now preflight.

- `SEC-008` is verified and deployed. Credentialed API CORS origin matching no longer builds regexes by replacing `*` inside unescaped strings. `getCorsOriginContext()` now normalizes candidate origins, exact-matches normalized configured origins, supports only real `*.domain` subdomain wildcards and `host:*` development ports, and rejects unsupported wildcard shapes instead of guessing. Verification: current Cloudflare Workers best-practices and latest `@cloudflare/workers-types` were retrieved before the Worker-facing change; focused shared CORS tests (`1` file / `5` tests), shared typecheck and touched-file ESLint, root `pnpm typecheck`, warning-free root `pnpm lint`, root `pnpm test` (`223` files / `1439` tests), root `pnpm build`, `pnpm check:dist-secrets`, `git diff --check`, API deploy `c4615be8-e469-4083-a9d5-a4a4f6bdab83`, live API health `200`, and live preflight smokes proving `https://storefront.scalius.com`/`https://dashboard.scalius.com` are allowed while `https://api.scalius.com.evil.test`, `https://cloud.scalius.com.evil.test`, and `https://badscalius.com` receive no `Access-Control-Allow-Origin`.

- `SEC-006` is verified and deployed. The same-origin admin scanner-token route now mirrors the admin shell/API 2FA posture: a session with `user.twoFactorEnabled === true` must also have `session.twoFactorVerified === true` before RBAC is loaded or any scanner token is written to KV. Scanner token minting still requires `products.view` plus `products.edit`, or super-admin, after 2FA passes. Verification: focused scanner-token test (`1` file / `5` tests), admin typecheck and touched-file ESLint, root `pnpm typecheck`, warning-free root `pnpm lint`, root `pnpm test` (`222` files / `1434` tests), root `pnpm build`, `pnpm check:dist-secrets`, `git diff --check`, admin deploy `30d98bd7-a4a2-4cbb-91a5-6bf9cd172a19`, live unauthenticated `POST /api/scanner-token` returning `401`, live `/admin` redirect smoke, and deployed server-bundle marker check for `isAdminTwoFactorVerified` plus "Two-factor verification required".

- `CUST-003` is verified and deployed. Storefront customer-auth helpers now run bounded same-origin proxy reads/writes, parse malformed envelopes fail-closed, and expose `unavailable` separately from real unauthenticated sessions. `/account` renders a top-level retry state when `/me` cannot be read, account order detail renders retryable protected-order errors for temporary session/detail failures, and cart submit-time guest-disabled checkout verifies `/me` before opening auth while blocking temporary account-read failures with retry copy. Verification: focused storefront customer-auth/source/cart-auth tests (`3` files / `13` tests), storefront typecheck and touched-file ESLint, root `pnpm typecheck`, warning-free root `pnpm lint`, root `pnpm test` (`222` files / `1433` tests), root `pnpm build`, `pnpm check:dist-secrets`, `git diff --check`, storefront deploy `dd7e979d-7cc8-458c-8daf-2c9fc74da65d`, live `/account`, `/account/orders/live-smoke`, `/cart`, `/api/customer-auth/me` smokes, and deployed asset marker checks for account/cart/order-detail retry behavior. A parallel stale-cart audit confirmed final API order creation was fail-closed for inactive/deleted products, missing variants, and insufficient stock, while cart/online-checkout UX was too late and first-error-only; the later cart-validation slice verified `CHECKOUT-030`, `CHECKOUT-031`, `INV-001`, and `INV-004`, leaving `CHECKOUT-032` open for quick-buy stock preflight. A parallel auth-security audit opened `SEC-006`, `SEC-007`, `AUTH-025`, `SEC-008`, and `RBAC-004`, while reconfirming existing `AUTH-020`, `AUTH-021`, and `AUTH-022` remain important identity hardening work.

- Admin Checkout Flow/Auth readiness red-state check: production checkout delivery readiness is healthy by read-only remote D1 and public config evidence (`3` active shipping methods, `1258` active city-zone pairs, `/api/v1/checkout/config` has `checkoutReadiness.ready=true`). The confusing delivery rows were first made amber instead of hard-red when the admin readiness check was unavailable; the exact follow-up root cause was `/api/v1/admin/settings/checkout-readiness` missing from the central RBAC route map, so authenticated settings viewers received fail-closed `403` even though checkout itself was ready. `RBAC-003` maps the endpoint to `settings.general.view` and is deployed in API version `cfd3f7db-c8a8-47dc-9e0d-b461b60ff22d`. A 2026-06-20 recheck after the screenshot reconfirmed the public checkout side is ready, and the admin UI now refetches checkout readiness on mount/focus, shows a loading state while retrying, and surfaces the actual API error if the admin-side read still fails. A later 2026-06-20 screenshot recheck found the live authenticated admin proxy and public checkout config both returning `checkoutReadiness.ready=true`; the remaining weakness was the Checkout Flow client using TanStack server-function transport for a tiny browser-only status read, where non-browser-like/referrer-stripped requests could return `403` and leave the panel in the amber fallback. The Checkout Flow readiness query now uses `/api/v1/admin/settings/checkout-readiness` through the existing dashboard admin proxy in the browser, keeps the server-function path only for server-side execution, and changes the amber copy to "Checkout readiness status could not be refreshed" so merchants do not mistake a status-check failure for missing shipping setup. Admin deploy `8e0ef4c2-2370-4b88-8e6c-a9a9a95c767b`; live smokes verified dashboard proxy readiness `200`, public checkout config readiness `true`, and deployed Checkout Flow copy/asset markers. Auth & Access SMS red state is a real fail-closed provider readiness issue when SMS is selected: stored SMS.net.bd credentials exist but cannot be decrypted by the current credential key, while the live customer auth policy remains email-only. The UI now explains that merchants must re-enter and save the provider API key or restore the original `CREDENTIAL_ENCRYPTION_KEY`. Verification: read-only Wrangler remote D1 queries, live public checkout/shipping/location/config smokes, admin asset smoke, focused admin-auth middleware test, focused admin `tsc --noEmit`, touched-file ESLint, focused checkout-readiness query/proxy Vitest, admin build/deploy, and `git diff --check`.

- `CUST-005` is verified and deployed. Customer order detail now includes a policy-backed `paymentRecovery` preview assembled through the same checkout-flow/gateway-readiness/payment-session policy used for receipt-token checkout recovery. `POST /api/v1/customer-auth/orders/{id}/payment-session` requires an authenticated customer session, scopes the order by `customerId`, accepts only `{}`, never accepts/returns receipt tokens, and creates Stripe/SSLCommerz/Polar sessions through the shared `payment-session-create.ts` helper with customer-account attempt proofs and account return URLs. `/account/orders/{id}` renders `Retry payment`/`Pay balance` only when that preview is eligible, confirms Stripe in place, redirects hosted gateways through validated absolute URLs, returns SSLCommerz/Polar account callbacks to the private order page, and avoids `/order-success`, cart clearing, or purchase-finalization side effects. Verification: focused API/customer-auth/payment tests, focused storefront helper/source tests, payment-session-attempt proof tests, generated SDK refresh (`260` OpenAPI paths), root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`220` files / `1423` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `pnpm audit --audit-level moderate`, `pnpm peers check`, frozen install, `git diff --check`, API deploy `72aa043d-ca67-4369-bdb5-658403c9b2c7`, storefront deploy `5fda867b-7b6b-490a-83af-f5db95e48598`, live API health `200`, live OpenAPI payment-session route smoke, live direct and storefront-proxy unauthenticated payment-session `401` private/no-store smokes, live account-order shell showing recovery markup without order-success markers, and live storefront `/` + `/cart` `200` smokes.

- `PAY-020`, `CHECKOUT-024`, `CHECKOUT-025`, `CHECKOUT-026`, `CHECKOUT-027`, and `CHECKOUT-028` are verified and deployed from the parallel customer-flow audit slice. Payment session policy now derives omitted `paymentType` from the committed order plus current partial-payment settings, allows `balance` only after a deposit-paid plan, persists pending deposit payment plans before Stripe/SSLCommerz/Polar provider calls, and validates full/deposit/balance amounts plus payment-plan CAS during confirmed payment processing. Storefront order creation now carries authoritative `totalAmount`/`paymentMethod` back to checkout, hosted initial session calls send only `orderId` plus receipt proof, SSLCommerz/Polar session setup failures after order commit redirect to receipt-token recovery, Stripe setup failures stay on checkout with idempotent proof preserved, stale transferred gateway snapshots fall back to fresh config when the intersection is empty, and checkout error paths re-enable the pay button correctly. Admin payment-method settings now separate raw merchant preferences from flow-visible defaults, block hidden defaults, disable unusable provider visibility switches, and the public checkout config exposes only a provider-ready/rendered `activeDefaultMethod` which storefront preselects. Verification: focused payment/session/admin/storefront tests, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`216` files / `1401` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `pnpm audit --audit-level moderate`, `pnpm peers check`, frozen install, `git diff --check`, full deploy API `972af027-da35-42c0-bdc6-53ffbb26656f`, admin `e02da88c-da9d-4c5d-bb5c-5c1736f57fd4`, storefront `db3999c9-30a8-4061-90a3-b706edecdb14`, live API health `200`, live dashboard checkout route redirect smoke, live storefront `/` and `/cart` smokes, and live `/api/v1/checkout/config` KV `HIT` with `activeDefaultMethod: "cod"` and gateways `stripe`, `sslcommerz`, `polar`, `cod`. Remaining tracked risks: pollable in-flight payment-session UX (`PAY-014`), sessionStorage transfer fallback (`CHECKOUT-021`), and broader storefront cold-path performance (`PERF-010`/`PERF-012`).
- `CHECKOUT-022` is verified and deployed. `createStorefrontOrder()` now fresh-reads only active, non-deleted products, keeps an explicit returned-row active guard so mocked/incorrect query results still fail closed, and rejects product/variant mismatches before building the order payload. The public order route has regression coverage proving product-unavailable failures do not mint checkout-status/receipt KV and do not call `commitStorefrontOrderPayload()`. No SDK/OpenAPI contract change was needed. Verification: Noether read-only subagent audit of the current checkout path, focused core order-builder test (`1` file / `11` tests), focused API order-route test (`1` file / `20` tests), core/API typechecks and lints, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`214` files / `1376` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, local API health/OpenAPI smoke (`260` dev paths), API deploy `a31d68f0-5a77-443b-bdca-4c46494cbdab`, live API health `200`, live OpenAPI smoke (`259` paths with `/api/v1/orders`), and Wrangler deployment-list proof at `100%`. A local disposable failed-order POST was intentionally blocked by local guest-checkout policy before catalog validation; production order mutation was intentionally avoided.
- `CHECKOUT-018` and `CHECKOUT-020` are verified and deployed. Payment-method settings now expose raw merchant preferences separately from effective checkout-visible methods, per-gateway provider/readiness/visibility fields, and fail-closed SSLCommerz/Polar readiness checks before publishing or enabling a gateway; Polar checkout requires a webhook secret, and payment-method preference writes are one D1 `safeBatch()`. Admin Payment Gateways now separates Provider enabled from Show at checkout, avoids misleading Live/Test/Sandbox badges before credentials are loaded, renders blocked/hidden-by-flow states, and stops auto-saving COD visibility. Checkout Flow copy now describes partial payment as online advance payment, documents COD being hidden, explains low-cart full-online behavior, and labels guest-disabled COD-only mode as authenticated COD-only. Parallel research opened/expanded `AUTH-022`, `AUTH-023`, `NOTIF-006`, `NOTIF-007`, `CUST-008`, `DEL-004`, `CHECKOUT-011`, `CHECKOUT-021`, `CHECKOUT-022`, `PAY-015`, `ARCH-004`, `DISCOUNT-001`, `INV-003`, and `PERF-012` so the unresolved customer-flow risks stay visible. Verification: focused core payment/settings tests (`3` files / `30` tests), focused API payment-settings/cache/session tests (`3` files / `90` tests), affected admin/API/core/api-client typechecks and lints, SDK regeneration from local OpenAPI, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`214` files / `1372` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, API deploy `238ae4cf-59f9-44e4-b82c-d80cb1a49d8b`, admin deploy `82de08eb-5695-4a9b-a278-5cb60f1c2c31`, live API health/OpenAPI schema smoke (`259` paths with the new payment-method fields), deployed admin chunk marker smoke, and live dashboard browser smoke of the Checkout Flow and Payment Gateways tabs with no error shell or captured console errors.
- `CHECKOUT-019` is verified and deployed. Public checkout submit now builds the attempt identity and does a read-only D1 checkout-attempt precheck before guest/auth/payment policy and IP/phone throttles, so lost-response retries can replay committed responses or return active-processing `202` despite later merchant setting or rate-limit changes. New, failed, or stale attempts still run the fresh policy/rate gates before `claimCheckoutAttempt()` creates/reclaims a row, and the route keeps post-claim replay/processing branches for races. Verification: focused core checkout-attempt tests (`7` tests), focused API order-create tests (`19` tests), affected core/API typechecks and lints, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`214` files / `1367` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, API deploy `3d71df1e-dc16-41d4-b984-51139bf9feb9`, live API health `200`, live OpenAPI `/api/v1/orders` contract smoke (`259` paths with `201/202/409/429/503`), and deployment-list proof at `100%`. Production order mutation was intentionally avoided until `OPS-006` provides a staging/disposable-order policy.
- `PAY-010` is verified and deployed. Stale hosted-payment cleanup moved from the admin abandoned-checkout route into `@scalius/core/modules/orders/stale-incomplete-orders` and is now called by scheduled API maintenance every 15 minutes with a 60-minute grace period and `25`-order limit. It targets only Stripe/SSLCommerz/Polar `incomplete` orders with `paymentStatus` `unpaid`/`failed`, `paidAmount <= 0`, no soft delete, no active shipment claim, no pending/succeeded `order_payments`, and no live `payment_session_attempts` processing lease. Each order wins a guarded cancelled claim before inventory release, rolls back to `incomplete` if release fails, finalizes through D1 `safeBatch()`, fences pending payment-plan cancellation on the finalized order version, writes the abandoned-checkout snapshot only after finalization, and invalidates product availability caches for archived order ids. Verification: focused core stale-cleanup tests plus full core package run (`48` files / `269` tests), API scheduled-maintenance tests plus full API package run (`51` files / `393` tests), core/API typechecks and lints, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`213` files / `1351` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, API deploy `91f6bf32-1dfb-4ada-b7de-c46ffccff135`, live API health `200`, live OpenAPI `259` paths, deployment-list proof at `100%`, and remote D1 read-only stale-candidate query returning `0` with `changed_db=false`. This predated the verified `CHECKOUT-011`/`CUST-005` buyer recovery slices.
- `CHECKOUT-011` first buyer-facing recovery slice is verified and deployed; account-owned pay-balance/retry was later closed by `CUST-005`, while in-flight session recovery remains tracked by `PAY-014`. SSLCommerz/Polar callback URLs now preserve `receipt_token`, `payment_type`, and deposit amount; SSLCommerz fail/cancel and Polar cancel redirect to private `/order-success` recovery with `result=failed|cancelled`; `/order-success` renders hosted retry actions for SSLCommerz/Polar payment-issue or gateway fail/cancel returns, keeps cart/session/purchase tracking gated until paid/partial confirmation, and no longer sends random per-click `retryKey` values that bypass durable session replay. Storefront hosted-session proxies no longer auto-retry POSTs. Parallel audits opened/updated `CUST-005`, `PAY-014`, `ADMIN-018`, `CHECKOUT-019`, `CHECKOUT-020`, `CUST-007`, and `PERF-012`. Verification: focused API payment-session tests (`51` tests), storefront retry/side-effect/state tests (`16` tests), SDK regeneration from live local OpenAPI (`259` paths), affected and root typecheck/lint/test/build/safety gates (`214` files / `1360` tests), API deploy `76de3e9a-c8b5-4eaa-b155-6d81ebbaafe4`, storefront deploy `af8ea9b6-b52f-464a-9730-d57b551c531a`, live API health/OpenAPI retry-key smoke, live SSLCommerz/Polar cancel redirect smokes, storefront recovery page smoke with `data-order-finalize="false"`, and deployed dist marker for the retry script.

- `PAY-014` provider-deadline sub-slice is verified and deployed while the parent row remains open for pollable live-claim UX. Stripe, SSLCommerz, and Polar session creation now run under API-side provider deadlines below the storefront proxy timeout; Stripe gets per-request SDK timeout with network retries disabled; SSLCommerz raw fetch and Polar SDK calls receive abort signals; timeout-shaped provider results mark the durable payment-session attempt failed and return retryable `503` instead of generic `500`; storefront payment-session proxies preserve backend status, do not retry Stripe/hosted POSTs, and map proxy aborts to retryable buyer copy. Receipt-page hosted retries now reuse the durable attempt identity by omitting random `retryKey`. Verification: focused API payment-session/deadline tests (`52` files / `417` tests under the API test project), focused storefront retry side-effect test, affected core/API/storefront typechecks and lints, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`217` files / `1406` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, API deploy `017c24fa-2d69-42c8-8ddb-39635a955b8f`, storefront deploy `f908095c-ac04-47e5-9302-e122f10445c5`, live API health `200`, live invalid Stripe intent proof rejection `400`, live storefront `/` and `/cart` `200`, and live `/order-success` recovery page showing `data-order-finalize="false"`, a retry button, and no `retryKey` marker.
- `PAY-014` live-claim processing sub-slice is verified and deployed while the parent row remains open for broader same-order hosted single-flight semantics. Exact duplicate payment-session attempts that collide with a still-live D1 `processing` claim now return `202 Accepted` with `status: "processing"`, `Retry-After: 2`, and `Cache-Control: no-store` before any Stripe/SSLCommerz/Polar provider call. Storefront same-origin proxies preserve that accepted response without inventing gateway URLs or retrying POSTs; hosted checkout sends already-committed orders to receipt recovery, Stripe keeps the buyer on checkout with retryable copy, initial-session fusion records a committed-order session error, and account recovery shows the same processing message. Verification: focused core payment-session-attempt tests, focused API payment-session/customer-auth tests, focused storefront checkout/proxy/account/order-success tests, SDK regeneration from local API OpenAPI (`261` paths / `359` operations), affected workspace typechecks/lints, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`277` files / `1786` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, API deploy `733f28b8-ac76-492c-bdc1-0c96930e2878`, storefront deploy `c0979461-bab9-496c-a9b3-7ccc6bed4795`, live API `/api/v1/health` and `/api/v1/readyz` smokes, live OpenAPI 202-response smoke for all three public gateway-session routes, live invalid Stripe intent validation `400`, live storefront `/health`, `/cart`, and `/checkout` smokes, and live dashboard `/health` smoke.
- `PAY-014` final same-order single-flight slice is verified and deployed. Migration `0063_payment_session_live_singleflight` deduplicates any historical duplicate live processing rows, then adds a partial unique D1 index on `(order_id, gateway, payment_type) WHERE status = 'processing'`. `claimPaymentSessionAttempt()` now returns the normal `202 processing` response for fresh same-order live attempts even when the exact attempt key differs, reclaims stale same-order live attempts by updating the row to the new canonical identity, clears stale competing live rows before reclaiming an old exact failed attempt, and still keeps created replay exact to the proof/return-target context so customer-account recovery cannot reuse receipt-token callback URLs. SSLCommerz/Polar public schemas no longer advertise `retryKey`, and caller retry metadata no longer enters hosted session identity material. Verification: PAY-014/PERF-012/ARCH-001 parallel read-only subagent audits, focused core payment-session-attempt tests (`8` tests), focused API payment/customer-auth tests (`85` tests), SDK regeneration from local API OpenAPI (`261` paths / `359` operations, no `retryKey` in SSLCommerz/Polar session schemas), root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`277` files / `1790` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, remote duplicate-live preflight returning `0`, remote migration `0063`, API deploy `09465ea3-9253-4f69-8327-200f77ead981`, live `/api/v1/health` and `/api/v1/readyz`, live OpenAPI no-`retryKey`/202-response smoke for Stripe/SSLCommerz/Polar (`261` paths / `359` operations), live invalid Stripe proof validation `400`, live remote D1 index proof, and live remote duplicate-live invariant `0`.
- `ARCH-001` hash-backed receipt authority sub-slice is verified and deployed. Migration `0076_order_receipts` adds D1 `order_receipts` with `token_hash` primary key plus order/status-expiry indexes; checkout commit records a hash-backed receipt proof, public receipt/payment-session validation reads D1 first, committed `checkout_attempts` self-heal missing receipt rows, processing attempts remain temporary proof, and KV receipt rows are only repaired hints. Raw receipt tokens are no longer stored in D1 or logged by the D1 repair path. Verification: focused core receipt/checkout-attempt tests (`80` files / `563` tests), focused API receipt/payment/order-create tests (`78` files / `638` tests), affected core/API/database typecheck and lint, database migration metadata guard, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`326` files / `2198` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, remote migration `0076`, API deploy `9ac2b703-9244-41b3-990a-98bc12e3c7e8` / version `fe789204-584b-4a60-9e8b-90c5c89e30bf`, live `/api/v1/health` and `/api/v1/readyz`, remote D1 `PRAGMA table_info(order_receipts)` and `PRAGMA index_list(order_receipts)` proof, live invalid Stripe receipt proof returning `404 Order receipt not found`, and remote `order_receipts` count staying `0` after that read-only negative smoke.
- `ARCH-003` is verified and deployed. Public checkout submit now requires a stable storefront `checkoutRequestId`, claims D1 `checkout_attempts` before order creation, reserves the canonical `orderId` and checkout/receipt token, replays committed same-payload retries, returns a pollable `202` for active duplicate submits, rejects same-key payload drift with `409`, and uses D1 as the fallback authority for order status and receipt-token validation when KV is missing. Storefront cart/COD and hosted-payment checkout now forward the stable request id, and successful checkout clears it so a fresh cart cannot reuse the previous id. Verification: focused core/API/storefront tests, local duplicate-COD submit smoke proving two identical POSTs returned the same order `SNJU2N` and one committed `checkout_attempts` row, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`212` files / `1344` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm peers check`, `pnpm audit --audit-level moderate`, database migration metadata guard, `git diff --check`, remote D1 migration `0049_checkout_attempts`, API deploy `0277a900-f98e-46de-875f-7e7b60ec6182`, storefront deploy `4e487ddd-fd65-4428-8e1b-3a48d130a96d`, live API health `200`, live OpenAPI `POST /api/v1/orders` showing required `checkoutRequestId` plus `409`, live remote D1 `PRAGMA table_info(checkout_attempts)`, and live storefront cart/checkout/session assets showing the `checkoutId` to `checkoutRequestId` path and success cleanup. Live production order mutation remains intentionally avoided until `OPS-006` creates a staging/disposable-order policy.
- Platform dependency hygiene was tightened while verifying `ARCH-003`: `undici` now resolves to `8.5.0` for Cloudflare Miniflare/Wrangler tooling and `dompurify` to `3.4.11` for admin PDF/html export transitive paths, bringing `pnpm audit --audit-level moderate` back to green after fresh advisories.
- `PAY-013` is verified and deployed. Stripe, SSLCommerz, and Polar session creation now builds a deterministic order/gateway/payment-type/amount/currency/receipt-token attempt identity, claims it in D1 before calling any provider, replays stored created responses, rejects active duplicate processing attempts, and reclaims failed or stale processing attempts. Stripe receives the attempt key as the provider idempotency key, SSLCommerz gets a deterministic scoped transaction suffix, Polar now forwards the trusted cancel URL, and provider acceptance is recorded before non-critical local payment-plan/order side effects. Verification: focused API payment-session tests (`51` files / `393` tests in the API package run), focused core payment-session-attempt tests (`46` files / `255` tests in the core package run), root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`211` files / `1337` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, database migration metadata guard, `git diff --check`, remote D1 migration `0048_payment_session_attempts`, API deploy `34c0e021-4528-434c-b275-9dee53723493`, live API health `200`, live OpenAPI `259` paths including `/api/v1/payment/stripe/intent`, `/api/v1/payment/sslcommerz/session`, and `/api/v1/payment/polar/session`, and live remote D1 `PRAGMA table_info(payment_session_attempts)` proof.
- `INV-002` is verified and deployed. Public `POST /api/v1/orders` now schedules targeted product availability cache invalidation immediately after `commitStorefrontOrderPayload()` succeeds, using the committed order id so exact product detail, search/listing, storefront product-variant prefixes, product HTML paths, and shortcode page paths are derived from the persisted order items. The invalidation wrapper catches/logs failures so a committed checkout still returns `201`; commit failures do not purge availability caches. Parallel explorers confirmed this is the right placement and that `PAY-013` should be the next larger checkout slice before retry/pay-balance UX. Verification: focused API order/cache tests (`51` files / `386` tests in the API package run), root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`210` files / `1327` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, database migration metadata guard, `git diff --check`, API-only deploy `90b01f44-0e9f-40c6-98ec-c629316701a6`, live API health `200`, live OpenAPI `259` paths including `/api/v1/orders` and `/api/v1/products/search`, live storefront `/search` HTTP smoke, and Wrangler deployment-list confirmation that the new API version is at 100%.
- `PAY-011` is verified and deployed. Order receipts now expose only non-PII payment state needed by the storefront (`paymentMethod`, `paymentStatus`, `paidAmount`, `balanceDue`); `/order-success` now renders payment-pending/payment-issue states for incomplete or unpaid hosted-payment orders, keeps cart/session state intact there, and only fires purchase analytics/cart clearing after paid or partial-paid confirmation. Stripe handoff no longer clears the cart before redirect. Same-batch order-created outbox rows are skipped for incomplete online orders, and Stripe/SSLCommerz/Polar confirmed payment queue handlers enqueue the durable `order_created` notification only after payment processing succeeds, retrying the queue message if notification enqueue fails. The parallel customer-flow audit also opened `AUTH-021`, `PAY-013`, and `INV-002`; `CHECKOUT-011` remains for retry/pay-balance/cancel-timeout recovery. Verification: focused API/storefront/core tests, affected package typechecks/lints, SDK regeneration from local API OpenAPI (`259` paths), root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`210` files / `1326` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, database migration metadata guard, `git diff --check`, full deploy to API `a07ca567-0740-4372-a395-ab64ae6cd6ab`, admin `4707f429-5771-4f2b-b3b8-8c68bcc13d6a`, storefront `bd47c366-b3a1-452f-abfe-722e91008360`, live API health/OpenAPI payment-field smoke, live storefront `/order-success` bad-token recovery smoke showing `data-order-finalize="false"`, live storefront home smoke, and live dashboard redirect smoke.
- `PAY-012` is verified and deployed. Stripe is now server-readable for legacy/webhook/refund paths but checkout-usable only when enabled with a non-empty secret key, webhook secret, and publishable key. `getActivePaymentMethods()`, the gateway registry, and public checkout config all fail closed before publishing Stripe without the browser key; admin `/settings/payment-methods` returns `usable`/`missingFields`/`blockedReason`; payment-method saves reject selected unusable gateways; Stripe enabled saves validate effective stored/masked/submitted fields before DB writes or cache invalidation; and the Payment Gateways UI shows selected blocked methods with reason text instead of a misleading configured state. Parallel audits then prioritized `CHECKOUT-011`/`PAY-011` for hosted-payment pending success/notification timing and `CUST-003`/`CUST-004`/`CUST-005` for resilient account reads, canonical balance display, and pay-balance recovery. Verification: focused core payment/settings tests, focused API payment-settings tests, affected core/API/admin typechecks and lints, SDK regeneration from live local API OpenAPI (`259` paths), root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`207` files / `1309` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, full deploy to API `d051b91d-1645-4700-835a-8241baadf02a`, admin `f8a1ef0d-6322-41ed-8f6f-65ac06d35296`, storefront `2fc4b4b7-a8cb-4286-bd5b-bcef920e3512`, live API health/OpenAPI schema smoke with `usable`/`missingFields`/`blockedReason`, live checkout config smoke proving published Stripe has a non-empty publishable key, live storefront `/cart` smoke, live dashboard checkout route redirect smoke, and deployed Payment Gateways chunk marker smoke.
- `CHECKOUT-014` is verified and deployed. Checkout readiness now has one shared core helper for active shipping method plus active city→zone hierarchy, public checkout config fails closed with `unavailable`/`checkoutReadiness` when delivery setup is incomplete, the cart submit state stays disabled through SSR and cart updates, the admin Checkout Flow panel shows payment/shipping/location readiness, and API shipping/delivery-location destructive mutations reject changes that would take a currently ready checkout offline. Verification: focused core/API/storefront tests (`35` focused tests), SDK regeneration from local API OpenAPI (`259` paths / `357` operations), affected package typechecks/lints, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`207` files / `1304` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, Drizzle check, `git diff --check`, full deploy to API `61522e22-6ee0-410a-8769-595aea14b262`, admin `957ce280-e374-436d-ab4b-29d21b070dbb`, storefront `d9a2ceda-fee5-4546-a39f-0036ba83eb64`, live checkout config smoke showing `checkoutReadiness.ready=true`, live OpenAPI route-count/readiness-route smoke, live storefront `/cart` metadata smoke, live storefront `/` smoke, and live dashboard checkout route redirect smoke.
- `AUTH-017` and `AUTH-019` are verified and deployed. Storefront customer-auth proxy now preserves every upstream `Set-Cookie` header through `getSetCookie()` or a combined-header splitter, rewrites backend-domain cookies into host-only same-origin cookies, and keeps `SameSite=None` to `Lax` rewriting scoped to the proxy case. Customer OTP challenge state pins the normalized request `method` and `identifier`; verification validates primary email/phone format and rejects mismatched stored method/contact before account mutation. Parallel customer-flow audits also opened/expanded `AUTH-020`, `CHECKOUT-016`, `CHECKOUT-017`, `CHECKOUT-018`, `CUST-005`, `CUST-006`, `PAY-011`, `PAY-012`, `INV-001`, `CACHE-018`, `PERF-010`, and `PERF-011`. Verification: focused auth/proxy tests (`30` tests), root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`204` files / `1288` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, API deploy `0c84bf65-4507-4971-b951-adb5d589d9d5`, storefront deploy `3e031f95-baf4-4280-9234-04b35a8fe320`, live API health/OpenAPI (`258` paths), live storefront `/api/customer-auth/me` private no-store unauthenticated response, live tampered verify rejection through the storefront proxy, and live `/`, `/cart`, `/account` HTTP smokes with no error markers.
- `AUTH-016` and `CHECKOUT-012` are verified and deployed. Customer OTP delivery now fails closed before OTP challenge/cooldown mutation when SMS is selected without an active, decryptable SMS provider; admin Auth & Access saves SMS provider settings before enabling SMS policy, blocks SMS/WhatsApp policies whose provider prerequisites are not ready, surfaces exact readiness issues beside the selected channels, and shows backend rejection messages in settings toasts. Checkout-flow validation now rejects `guest_cod_only` without COD and `gateways_only` without at least one enabled/configured online gateway, and the admin Checkout Flow preview mirrors those blockers. The parallel customer-flow audits also opened `AUTH-017`, `AUTH-018`, `CUST-003`, `CUST-004`, `CHECKOUT-013`, `CHECKOUT-014`, `CHECKOUT-015`, `PAY-010`, `ARCH-002`, and `ARCH-003` for the remaining verified gaps. Verification: generated SDK refreshed from a live local API worker (`258` OpenAPI paths); focused core SMS/customer-auth/checkout-flow tests (`22` tests); focused API settings/payment tests (`33` tests); affected API/core/admin/api-client typechecks/lints; root `pnpm typecheck`; root `pnpm lint`; root `pnpm test` (`203` files / `1278` tests); root `pnpm build`; `pnpm check:env`; `pnpm check:dist-secrets`; migration metadata guard; `git diff --check`; full deploy to API `f96b1130-f75f-4da7-b3d1-90f3991e2f78`, admin `2ffea71d-4a7a-446a-a7a1-ff5e97d708ff`, storefront `f16c3c2d-5278-48a2-a627-6810dd8469c9`; live API health `200`; live OpenAPI SMS readiness schema smoke; live checkout config smoke showing `checkoutMode=all`, partial payment enabled, and only online gateways; dashboard `/admin/settings` redirect smoke; storefront `/`, `/cart`, and `/search` HTTP smokes with no error markers.
- `AUTH-015` is verified and deployed. The API create-order route now enforces merchant checkout intent before rate limiting or order mutation: fresh active payment-method allowlist, checkout-mode usability, partial-payment COD blocking, guest-disabled customer session proof through KV, and signed-in phone/session matching. The storefront create-order proxy reads the HttpOnly `cs_tok` cookie only on the server and forwards it through an internal `X-Customer-Session` header. API schema validation also rejects empty carts, fractional quantities, and quantities above 99. Stripe/SSLCommerz/Polar payment-session routes now fresh-check checkout mode before currency/provider calls and reject caller-selected `full` payment while configured partial-payment deposit is required. Verification: parallel read-only audits by Euclid and Aquinas, SDK regeneration from local API OpenAPI (`258` routes), focused order/payment tests (`52` tests), API/API-client/storefront typechecks, API/storefront lints, `pnpm check:env`, `pnpm check:dist-secrets`, database migration metadata guard, `git diff --check`, API deploy `f3260b7b-1fd8-4e14-beee-1e94a0e2ae88`, storefront deploy `0529a8b8-eee5-4098-8ea7-72390439b89b`, live OpenAPI schema smoke, live `/cart` HTTP smoke, and live invalid empty-cart create-order smoke returning `400` before mutation.
- `AUTH-014` is verified and deployed. Root cause was verified in code and by subagent audit: `AuthModal` dispatched `customer-login` while merely hydrating an existing session, and `/account` handled that event with `window.location.reload()`, causing a reload/flash loop. AuthModal now emits `customer-login` only after active OTP verification or profile completion, `/account` refreshes its session/order state in place, and new-customer profile setup now reuses the searchable shared `LocationSelector` instead of native city/zone selects. Verification: focused `LocationSelector` tests, storefront `astro check`, API/core/api-client typechecks, touched-file ESLint, combined focused order/storefront/core test run, API deploy `6eb06c6e-62ae-45df-b3e5-b338f8dd3961`, storefront deploy `ac727997-4bdc-4f6c-8802-2dfc8f83c522`, live `/account` HTTP/browser smoke with one `<main>` and no console errors, AuthModal interaction smoke, Cloudflare Email OTP delivery to `arob.badhon@gmail.com`, successful live OTP sign-in, authenticated account view showing customer/order details, reload smoke after passive session hydration with no flashing/reload loop, and Worker tails showing `verify-otp`, `/me`, and `/orders` success.
- `ORDER-020` is verified and deployed. Public storefront order creation now writes legacy checkout/receipt KV, commits the D1 order synchronously through `commitStorefrontOrderPayload()`, returns `201` only after the order exists, and moves COD tracking plus durable order-notification enqueue work to `executionCtx.waitUntil()`/post-commit. The storefront helper keeps 202 polling only for legacy async responses, so the normal payment handoff no longer waits up to the old polling window before creating a gateway session. Verification: SDK regeneration from local API OpenAPI (`258` routes), `apps/api/src/routes/orders-create.test.ts`, `packages/core/src/modules/orders/orders.queue.test.ts`, `packages/core/src/modules/orders/orders.storefront.test.ts`, API/core/api-client typechecks, storefront `astro check`, touched-file ESLint, `git diff --check`, API deploy `6eb06c6e-62ae-45df-b3e5-b338f8dd3961`, storefront deploy `ac727997-4bdc-4f6c-8802-2dfc8f83c522`, live API health, live OpenAPI showing `POST /api/v1/orders` success `201`, and live tail smoke showing `POST /api/v1/orders` followed by SSLCommerz session creation without buyer-path `order-ingest` queue polling.
- `NOTIF-005` is verified and deployed. Live deploy-tail evidence while verifying `ORDER-020` showed order `MNFF70` notification retries repeatedly failed on admin FCM push because Firebase returned permanent invalid-device responses as `messaging/unknown-error` (`Device unregistered`, `NotRegistered`), then later attempts saw `delivery_receipt_busy`. The notification service now maps those Firebase stale-token variants to terminal skipped receipts, deactivates the token only after a successful skipped receipt mark, keeps transient FCM errors retryable, and the queue consumer has a regression proving skipped admin-push receipts allow the parent outbox to be marked sent. Verification: focused notification/queue tests (`31` tests), core/API typechecks, core/API lints, commit `4e5e6136`, API deploy `56cd3a5d-cac8-4dad-9dad-7271bb02e04d`, live health `200`, remote D1 due row inspection for `MNFF70`, scheduled outbox flush (`scanned=1, enqueued=1`), and live `ORDER_NOTIFICATIONS_QUEUE` tail showing eight stale FCM tokens classified/deactivated with the message completing OK.
- Cloudflare-native architecture research was delegated and recorded under `docs/research/`: checkout/order coordination, storefront cache freshness, payment reliability, and customer auth/session. Verified conclusions opened `ARCH-001`, `PAY-008`, and `PAY-009`; `CACHE-016` remains the tracked cache durability gap. Near-term direction is D1 as the authoritative ledger for checkout receipts, sessions, webhook/refund/payment attempts, Queues only after durable claims, KV as cache/status hints, Service Bindings for internal Worker control paths, and narrow Durable Objects only for per-checkout/per-entity coordination where measured contention or idempotency requires strict serialization.
- `CUST-002` and `AUTH-012` are verified and deployed. Customer order history now links each order to `/account/orders/{id}`, backed by `GET /api/v1/customer-auth/orders/{id}` scoped to the authenticated `cs_tok` session and returning customer-safe order, item, shipment, payment-plan, COD, notification-receipt, and timeline data without provider secrets/gateway ids/raw receipt internals. `/order-success` now renders a noindex recovery state for missing/expired receipt proof instead of redirecting to `/`, and it only clears checkout/cart state plus fires Meta purchase tracking after a real receipt payload is present. The Auth & Access settings form now normalizes legacy `phone` to SMS, exposes the real `whatsapp_otp` mode, shows WhatsApp/SMS provider configuration only for modes the backend actually uses, and saves SMS credentials for all SMS-backed modes. Verification: focused API customer-auth cache/private-read tests (`51` files / `349` tests in API package run), core/API/admin/storefront/api-client typechecks, warning-free root lint, root `pnpm test` (`198` files / `1193` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, database migration metadata guard, `git diff --check`, SDK regeneration with `258` OpenAPI paths, full deploy to API `286264c3-b7ea-4931-8213-33e3a6d229e7`, admin `6c32011d-506b-454d-93ed-80fd8f44e821`, storefront `e37faf25-7de1-4b39-8dc8-e6fd85370872`, live OpenAPI route smoke, live unauthenticated customer-order-detail `401` with private no-store headers, live order-success recovery HTTP smoke, and live `/account/orders/live-smoke` shell/noindex smoke. Live order mutation/customer fixture smoke remains intentionally deferred to `OPS-006`.
- `CUST-001` is verified and deployed. Authenticated customer order history now includes a capped latest-shipment/provider summary for each order, and the storefront account page distinguishes loading, unauthenticated, empty, and error states instead of rendering read failures as “No orders yet.” `/account` now renders shipment status/tracking actions when available, uses idempotent Astro page initialization, avoids nested `<main>` markup, and `OrderSuccessButtons` reads the public `cs_auth=1` mirror cookie instead of the HttpOnly customer token. Verification: focused customer-auth cache route test (`4` tests), core/API/storefront/api-client typechecks, API/core/storefront lints, SDK regeneration from a live local API, root `pnpm typecheck`, warning-free `pnpm lint`, root `pnpm test` (`198` files / `1191` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata check, `git diff --check`, local API/storefront HTTP and browser smoke, API deploy `2b57cee5-fdbe-4ad8-aaf5-7eae173851aa`, storefront deploy `5c722c8c-5514-4f84-86f1-b1fc3e06e529`, live API health, live OpenAPI `/api/v1/customer-auth/orders` containing `latestShipment`/`trackingId`/`trackingUrl`, and live storefront `/account` desktop/mobile browser smoke with one `<main>`, no horizontal overflow at `390px`, no broken Astro dispatch source, and no captured console warnings/errors. Live mutation/order creation was intentionally not attempted; `OPS-006` tracks the missing reproducible customer/order fixture and staging mutation policy.
- `STORE-010` is verified and deployed. Storefront executable scripts, JSON-LD, `type="application/json"` data islands, and generated quick-buy HTML now serialize JSON through `serializeJsonForInlineScript()`, and admin-configured theme colors are rejected on unsafe save plus sanitized defensively before storage/layout rendering. The admin theme OpenAPI schema now exposes string-record colors and the generated SDK is refreshed. Verification: focused shared/storefront/API tests (`17` tests), shared/storefront/API/core typechecks, API/storefront lints, SDK generation from a live local API, root `pnpm typecheck`, warning-free `pnpm lint`, root `pnpm test` (`198` files / `1190` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata check, source-boundary scan for raw inline JSON sinks, `git diff --check`, local API/storefront HTTP and browser smoke, API deploy `11c603b1-443d-425b-90ac-faefc3a4fbb9`, storefront deploy `9ff70280-a464-46cd-9f87-009afba8d771`, live API health/OpenAPI schema smoke, live storefront `/search`, `/products/fish`, and `/categories/drinks` browser smoke with parseable JSON-LD/data-island scripts, no raw `</script` inside script payloads, no error shells, and no captured console errors. `alpha-audit.md` and `audit/independent-audit.md` were reduced to verified tracker entries and deleted.
- `PERF-009` is verified and deployed. `NotificationChannelsBuilder` now imports `ORDER_NOTIFICATION_TYPES`, labels, and the shared type from `@scalius/core/modules/notifications/notification-types` instead of the broad backend notifications barrel that re-exported service/outbox/receipt code. `route-graph-boundaries.test.ts` now scans admin components and routes for exact broad-barrel imports. Verification: read-only subagent audit, focused route-graph guard (`22` tests), admin TypeScript, admin ESLint, admin build, production client chunk scan showing `NotificationChannelsBuilder-DvkRSoGT.js` is `7,484` bytes with no `drizzle:entityKind`, `order_notification_outbox`, `getDb`, or `@scalius/database`, root `pnpm typecheck`, warning-free `pnpm lint`, root `pnpm test` (`197` files / `1174` tests), `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, local API/admin sign-in/settings/notification-channel HTTP smoke, admin deploy `36d5d85b-5c3c-4fc5-9a74-fc08ec0cc8aa`, live deployed chunk marker smoke, and live demo sign-in/settings/notification-channel smoke.
- `ORDER-017` is verified and deployed. Order status inventory transitions now write deterministic, movement-generation-based `transition:*` movement claims and variant counter CAS updates in one D1 `safeBatch()` for deduct/release/restore, adopting exact duplicate claims as idempotent retry success and rejecting mismatched duplicate claims for manual reconciliation. The claim generation is derived from prior movement history instead of `orders.version`, so unrelated order edits cannot make a crash retry double-apply stock. Re-reserving restored orders now passes a deterministic movement id into `reserveStockBatch()`, and final `orders.inventoryAction` updates are guarded against the pre-transition action while accepting another caller's already-finalized target action. Verification: focused inventory transition tests (`32` tests), batch-reservation regression tests (`44` combined focused tests), core typecheck, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`197` files / `1183` tests), `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, API deploy `ee4c2f9a-98c6-49e6-afe1-643dcbbcbebd`, local API health/docs/OpenAPI/setup smoke, and live API health/docs/OpenAPI/setup smoke.
- `ORDER-015` is verified and deployed. `releaseExpiredReservations()` now sweeps a bounded orphan-reservation batch (`50` default, max `200`) and returns `hasMore` for backlog visibility. Each expired release uses deterministic movement id `expiry_release:{orderId}:{variantId}` and applies the movement claim plus reserved-stock decrement through D1 `safeBatch()`, so overlapping cron runs cannot double-release the same orphan group; stale active orders still remain outside the sweeper and must use explicit order transition logic. The API scheduled handler logs `limit` and `hasMore`. Verification: focused core expiry tests (`6` tests), core/API typechecks, core/API lints, API dry-run build, root `pnpm typecheck`, warning-free `pnpm lint`, root `pnpm test` (`197` files / `1173` tests), `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, local API health/docs/scheduled smoke with `hasMore=false`, API deploy `2c99229c-fc5d-4411-8653-6246cadbefab`, and live API health/docs/setup smoke.
- `ORDER-016` is verified and deployed. `reserveStockBatch()` now inserts strict reservation movement claims and variant counter CAS updates in one D1 `safeBatch()`, with movement inserts gated by pre-read `stockVersion`; deterministic checkout claims use `reservationKey: "checkout-ingest:v1"` and release-count generations so crash/redelivery dedupes active claims without blocking retry after confirmed release. `handleOrderIngestBatch()` now reuses exact active reservation movements before reserving missing entries and fails closed on quantity/stray movement mismatches. `inventory_movements.order_id` is intentionally non-FK-enforced in TypeScript schema to match checkout's pre-order-commit reservation claim. Verification: focused order-queue and batch-reservation tests (`21` tests), expanded order/inventory tests (`51` tests), core/database typechecks, core lint, migration metadata check, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`197` files / `1180` tests), `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, local API setup/docs/OpenAPI/health smoke, API deploy `0ad23d64-3c6d-4fd6-9905-59033ea7c44d`, live API health/docs/OpenAPI/setup smoke, and remote D1 `PRAGMA foreign_key_list(inventory_movements)` returning no enforced FKs.
- Parallel read-only audits opened the next concrete slices: `ORDER-017` inventory transition idempotency under concurrent reconciliation and `STORE-009` category/search dynamic-filter cold-path parallelization.
- `PERF-003` hero-slider slice is verified and deployed. `/admin/settings/hero-sliders` now lazy-loads `SliderTab`, renders plain `SlideRow` editors by default, and loads `SortableSlidesEditor` only after the Reorder action; `@dnd-kit/*`, `SortableSlide`, `SlideOverlay`, and `createPortal` stay in that sortable editor chunk. The media-manager barrel is picker-only and no longer exports `MediaManagerPage`, leaving the standalone media route to import it directly. Verification: focused route-graph guard (`21` tests), admin typecheck/lint/build, direct chunk import scans, root `pnpm typecheck`, warning-free `pnpm lint`, `pnpm test` (`197` files / `1170` tests), `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, `pnpm run deploy:admin -- --dry-run`, local `pnpm dev:admin` hero-slider HTTP smoke, admin deploy `dfd511fe-ec98-4ea5-8bd7-ec81f04799b4`, live authenticated hero-slider route/API smoke, and live SHA-256 matches for `hero-sliders-BUpu0FwN.js`, `SliderTab-D-5iRhRc.js`, `SortableSlidesEditor-BIzW3Pj0.js`, `SlideRow-GvsQFUXr.js`, `media-manager-DcOz5ZHs.js`, `MediaManager-BkVwzaIM.js`, and `MediaManagerPage-B24_PLi-.js`.

2026-06-18:

- `PERF-008` is verified and deployed. The remaining broad admin query barrel was deleted; products, orders, categories, collections, customers, discounts, pages, widgets, analytics, and checkout-language route-facing query options now live in `apps/admin-v2/src/lib/api-query-options/*`, and all admin source is guarded against `api.queries` imports. Customer form create/update invalidates `queryKeys.dashboard.all`. `DeferredTiptapEditor` renders saved content through sanitized `.rich-content` HTML while keeping the real Tiptap/ProseMirror editor out of eager route chunks; the current editor UX requests the lazy editor immediately after field hydration or direct interaction rather than requiring a manual Edit click. The new audit advisory for transitive `js-yaml` through the OpenAPI generator path is resolved with a `js-yaml` 4.2.0 override. Verification: focused route-graph/order-detail tests (`10` tests), broad-barrel source and manifest scans, form chunk scans proving no eager Tiptap internals, admin typecheck/lint/build, root `pnpm typecheck`, warning-free `pnpm lint`, `pnpm test` (`132` files / `827` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, Drizzle check, `pnpm audit --audit-level moderate`, `pnpm peers check`, frozen offline install, `git diff --check`, local `pnpm dev` plus warmed `pnpm dev:doctor --require-running`, local browser dashboard/analytics/create/product-edit-rich-preview/storefront smoke, admin deploy `f4bf38a1-f285-4eb5-94e8-68fc60d5e7dd`, live API setup/storefront/dashboard HTTP checks, live demo sign-in, live product edit rich-preview check, and live dashboard/analytics/create/storefront browser smoke with no console errors. No production data was saved.

2026-06-15:

- `PERF-007` is verified and deployed. Primary admin list/default-form surfaces now import domain-specific query options from `apps/admin-v2/src/lib/api-query-options/` instead of the broad `api.queries.ts` barrel: products, orders, categories, customers, pages, collections, discounts, inventory, attributes, abandoned checkouts, product-new category options, inventory manager, abandoned-checkout manager, and bulk-ship provider options. `route-graph-boundaries.test.ts` protects these files and also ensures narrow query-option modules never import the broad barrel. Verification: focused route-graph guard (`3` tests), broad-barrel import scans, narrow-module scan, admin typecheck/lint/build, protected server manifest scan (`22` entries), root `pnpm typecheck`, warning-free `pnpm lint`, `pnpm test` (`132` files / `824` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, Drizzle check, `pnpm audit --audit-level moderate`, `pnpm peers check`, frozen offline install, `git diff --check`, `pnpm outdated -r` showing only the intentional storefront Vite 7 pin, local `pnpm dev` plus warmed `pnpm dev:doctor --require-running`, local browser list-route/storefront smoke, full deploy to API `293bf435-7db5-4392-ace2-4960b87c1862`, admin `d057ea94-57d5-4334-8f8f-23da6e397d6c`, storefront `7444afdf-b9cf-40e4-850b-fc3563070611`, live API setup/storefront/dashboard HTTP checks, live demo sign-in, live authenticated list-route HTTP checks, and live browser list-route/storefront smoke with no console errors. No production data was saved.
- `CACHE-015` is verified and deployed. Non-catalog committed writes now use the scheduled purge pattern: `invalidateApiAndScheduleStorefrontGroups(groups, c)` awaits API KV invalidation and schedules the storefront purge through `waitUntil`, while widget writes keep exact prefix invalidation through `triggerStorefrontPurgeForPrefixes()` after awaited API pattern invalidation. This covers pages, checkout languages, attributes, analytics/navigation, delivery locations/providers, shipping methods, payment settings, system/site settings, and widget create/update/delete/restore/status paths. Verification: focused cache-invalidation route tests (`11` files / `54` tests), API typecheck/lint, root `pnpm typecheck`, warning-free `pnpm lint`, `pnpm test` (`131` files / `821` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, Drizzle check, `pnpm audit --audit-level moderate`, `pnpm peers check`, frozen install, `git diff --check`, local `pnpm dev` plus warmed `pnpm dev:doctor --require-running`, local API/admin/storefront HTTP smoke, full deploy to API `7f1ad12c-4ae5-49be-8828-d614f1ba139a`, admin `9311b19c-bad9-466a-8fd8-9aa04e758ae7`, storefront `14cef7ec-a259-4a44-84f3-f44459682787`, live API setup/OpenAPI checks, live demo sign-in plus authenticated account-settings smoke, and live storefront `/` smoke. No production data was saved.
- `RBAC-002`, `ADMIN-014`, and `DOC-002` are verified and deployed. API admin-user routes no longer apply legacy handler-local `user.role === "admin"` checks after central RBAC has allowed the request; team-user listing maps to view/manage/manage-roles permissions, Pathao import status/delete paths and widget-generation status paths are explicit in route permissions, account settings consumes the parent `/admin` permission context without a nested provider, team invite/delete/permission controls are gated by their real permissions, and role/user-permission mutations refresh the cached admin route context through `refreshAdminRouteContext(router)`. Documentation now records the new route-context refresh rule plus the current OpenAPI/migration/docs drift corrections. Verification: focused account-context static test, focused API auth-management/admin-auth tests, admin/API typechecks and lints, root `pnpm typecheck`, warning-free `pnpm lint`, `pnpm test` (`131` files / `818` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, Drizzle check, `pnpm audit --audit-level moderate`, `pnpm peers check`, frozen install, `git diff --check`, local dev doctor/browser account/settings and storefront smoke, full deploy to API `89c80fd0-420e-444d-9bdb-2b13b26b8477`, admin `8738e994-5178-4716-bec8-9a7f8bd906d9`, storefront `db87cc85-0e61-42c4-811e-69ff30230bfb`, live API setup/OpenAPI checks, live unauthenticated dashboard redirect check, live authenticated account/team browser smoke, and live storefront smoke. No production data was saved.
- `CACHE-009`, `CACHE-010`, and `CACHE-011` are verified and deployed. The admin cache UI now distinguishes directly HTML-affecting `Warms HTML` groups from `Data prefixes` groups, removes invalid badge nesting that caused hydration errors, consumes the route-warmed TanStack Query cache instead of refetching cache server functions on mount, and routes clear actions through cache mutation hooks that update/invalidate the shared query keys. The API clear-all route now persists `sc:_last_cleared:*` for every invalidation group, and storefront purge warming uses `url.origin` so local/staging ports are preserved. Verification: `apps/api/src/routes/cache.test.ts`, `apps/storefront/src/pages/api/purge-cache.test.ts`, affected API/admin/storefront typechecks and lints, root `pnpm typecheck`, warning-free `pnpm lint`, `pnpm test` (`128` files / `802` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata check, `pnpm audit --audit-level moderate`, `pnpm peers check`, `git diff --check`, local `pnpm dev` plus warmed `pnpm dev:doctor --require-running`, local browser `/admin/settings/cache` network/action smoke and storefront `/`/`/search` smoke, full deploy to API `a0fae72a-2d3f-4e9b-acb6-1db7e1ff677d`, admin `9f65099c-d064-4f9c-ad54-608c85a1c8f0`, storefront `6c0da12a-3d87-43e6-a3d8-c4aa228b5a79`, live API setup/storefront/purge HTTP checks, live authenticated `/admin/settings/cache` browser smoke, and live storefront `/`/`/search` browser smoke.
- `PERF-003` ninth slice trimmed the orders list route graph by moving the date-range picker/calendar and bulk-shipping dialog behind user-triggered lazy boundaries. The orders toolbar now renders a lightweight date-range button until first click; `DateRangePickerWithPresets` accepts `initialOpen` so the real picker opens on that first interaction. The bulk ship dialog is only rendered/lazy-loaded once the user opens it. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm --filter @scalius/admin-v2 build`, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`128` files / `802` tests), root `pnpm build`, `git diff --check`, dist scan showing no static orders-route import of `DateRangePickerWithPresets` or `BulkShipDialog`, `pnpm dev:doctor:admin` after warmup, local browser `/admin/orders` smoke proving no initial `DateRangePickerWithPresets`/`react-day-picker`/`BulkShipDialog` module requests, first date-picker click loading the picker/calendar and showing `Quick select`, selected-order bulk ship opening the lazy dialog without console/page errors, full deploy to API `9414dd72-3b67-42cd-9f3d-914482a76579`, admin `797cf037-7158-45a3-81bd-7216810517da`, storefront `427a51c6-7a2e-44f9-afa7-020b346217c5`, live API setup check, live authenticated `/admin/orders` lazy-load smoke, and live storefront `/` plus `/search` browser smoke.
- `PERF-003` tenth slice trimmed per-row orders interaction code by replacing eager order-items and fraud-check popovers with lightweight first-click shells. `OrderItemsPopover` and `FraudCheckIndicator` now accept `initialOpen` so the first click still opens the real control, while the initial route loads only `LazyOrderItemsPopover` and `LazyFraudCheckIndicator`. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm --filter @scalius/admin-v2 build`, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`128` files / `802` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata check, `git diff --check`, production chunk scan showing no static orders-route import/content for `OrderItemsPopover`, `FraudCheckIndicator`, `DateRangePickerWithPresets`, or `BulkShipDialog`, `pnpm dev:doctor:admin`, local Chrome `/admin/orders` smoke proving initial shell-only requests plus first-click lazy loads for item and fraud controls with no console/page errors, full deploy to API `05e91c24-b760-4019-a402-6a0cff4f2b35`, admin `4d9712cf-2a20-4db1-9061-1dee6e2a30e2`, storefront `17df385d-d8c8-4385-b2d1-c0f570cc1db0`, live API setup check, live demo sign-in through `POST /api/auth/sign-in/email`, live authenticated `/admin/orders` smoke proving zero initial heavy module requests and first-click loads of `OrderItemsPopover-TGEk5ypy.js` and `FraudCheckIndicator-BGNu5eia.js`, and live storefront `/` plus `/search` smoke.
- `PERF-003` eleventh slice trimmed orders destructive-action code by lazy-loading `DeleteOrderDialog` only when a single or bulk delete confirmation is opened. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm --filter @scalius/admin-v2 build`, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`128` files / `802` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata check, `git diff --check`, production chunk scan showing no static orders-route import/content for `DeleteOrderDialog` or `alert-dialog` copy while `DeleteOrderDialog-CLejLc-t.js` owns the confirmation UI, `pnpm dev:doctor:admin`, local Chrome `/admin/orders` smoke proving no initial delete dialog request and first delete click loading `DeleteOrderDialog.tsx`, full deploy to API `5dced253-da48-43a2-ad55-c9b0a1364e09`, admin `bc5d7d25-e614-4a4d-9e51-222de052550f`, storefront `3a7a38c6-b4b4-4a1c-873f-a965b13b81bc`, live API setup check, live demo sign-in through `POST /api/auth/sign-in/email`, live authenticated `/admin/orders` smoke proving zero initial heavy module requests and first delete click loading `DeleteOrderDialog-CLejLc-t.js`, and live storefront `/` plus `/search` smoke.
- `PERF-003` twelfth slice split shared admin DataTable drag-and-drop code out of the default table path. `DataTable.tsx` now renders normal tables without importing `@dnd-kit/*`; sortable desktop tables lazy-load `SortableDataTableContent`, which owns the DnD sensors, sortable row, and drag handle. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm --filter @scalius/admin-v2 build`, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`128` files / `802` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata check, `git diff --check`, production chunk scan showing no static ordinary-route import of `sortable.esm`, `pnpm dev:doctor:admin`, local Chrome `/admin/orders` smoke proving zero initial sortable/DnD requests, local drag-enabled `/admin/collections?sort=sortOrder&order=asc` smoke proving `SortableDataTableContent.tsx` and `@dnd-kit` deps load on demand, full deploy to API `cc4da6c9-b1ab-4737-91f0-060716ccecb4`, admin `0dae492a-b87e-4043-9558-a564837ecd7e`, storefront `7a42c38a-3525-4508-a8a3-4c63ccf1c80e`, live API setup/dashboard/storefront HTTP checks, live demo sign-in through `POST /api/auth/sign-in/email`, live authenticated `/admin/orders` smoke proving zero initial sortable/heavy module requests, live drag-enabled `/admin/collections` smoke loading `SortableDataTableContent-BdEuf433.js` and `sortable.esm-CsHTWzZ-.js`, and live storefront `/` plus `/search` smoke.
- `PERF-003` thirteenth slice split discount editing by type. `/admin/discounts/$discountId/edit` lazy-loads only `AmountOffProductsContainer`, `AmountOffOrderForm`, or `FreeShippingForm` for the current discount, and the route warms product/collection selector data only for `amount_off_products`. Discount date text now uses tight hydration-warning suppression around timezone-sensitive `formatDateShort()` output. This slice also fixed `CACHE-012`: catalog writes still await API KV invalidation, but storefront purge now runs in `executionCtx.waitUntil()` so a purge failure cannot turn an already-committed product/category/collection/discount mutation into a false 500. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm --filter @scalius/admin-v2 build`, `pnpm --filter @scalius/api test -- src/utils/cache-invalidation.test.ts`, `pnpm --filter @scalius/api typecheck`, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`128` files / `803` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata check, `git diff --check`, local browser smokes for all three discount types, full deploy to API `f9597a03-f87b-4296-9966-cbfecb999c4c`, admin `0e960b5d-cde9-46a8-88ef-ba01fbd1211c`, storefront `862559f7-d881-4b78-b8a4-6b8a3a68d624`, follow-up admin deploy `80104b0b-2d5c-47a1-a14f-baca5a6fa7e4`, live demo sign-in, live existing `amount_off_order` discount edit smoke with no console/page/request errors, and live API/storefront HTTP smoke. No live discount data was mutated.
- `PERF-003` fourteenth slice trims media and widget cold paths. Product/category/hero-slider media-picker consumers now import a lightweight `MediaManager` wrapper that lazy-loads the real dialog on first trigger click while preserving custom trigger buttons, and the standalone `/admin/media` route lazy-loads `MediaManagerPage`. Widget create/edit now lazy-loads `FullScreenEditor`, history, paste modal, `prompt-helper-v2`, and the standalone prompt wrapper only when preview/history/paste/copy-prompt actions are used. This slice also fixed `CACHE-013`: hero-slider create/update/delete now await API KV invalidation but schedule storefront purge defensively through `triggerStorefrontPurgeForGroups()` with optional Hono `ExecutionContext`, so a missing/down storefront cannot turn an already committed hero-slider write into a 500. Verification: admin/API focused typecheck/lint/build/tests, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`128` files / `803` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `pnpm audit --audit-level moderate`, `pnpm peers check`, frozen install, `git diff --check`, `pnpm dev:doctor:admin --skip-cloudflare`, local browser `/admin/products/new` media picker, `/admin/media`, `/admin/settings/hero-sliders` custom media trigger, `/admin/widgets/create` paste/preview/copy-prompt, full deploy to API `9988d1c2-6058-45f0-b691-acf56652ad60`, admin `166ff295-428e-41c3-b318-41dd296f08eb`, storefront `f886fcbf-7f1e-45b6-9701-d51c0e3a5961`, live API health/storefront/dashboard HTTP checks, and live browser product media picker, standalone media, hero-slider custom picker, widget paste/preview/copy-prompt, and storefront `/` smoke. No live product/widget/slider data was saved.
- `PERF-003` fifteenth slice defers admin rich-text editor loading. Product, category, page, footer, and additional-info form surfaces render `DeferredTiptapEditor`, preserving current HTML values in a sanitized rich preview while the heavy Tiptap/ProseMirror editor remains a separate lazy chunk. The editor now mounts after field hydration or direct interaction, so merchants do not need a separate Edit click before typing and visible toolbars do not depend on idle scheduling. This slice also fixed `CACHE-014`: CSP/security settings now guard the ancillary KV cache write with optional Hono `ExecutionContext`, so a missing local/unit execution context cannot turn an already committed DB write into a false 500 before layout invalidation. Verification: admin/API typecheck/lint/build/tests, production chunk scans showing form chunks import only `DeferredTiptapEditor` while `TiptapEditor` remains a separate lazy chunk, focused API system/auth regression tests, package auth README drift correction, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`128` files / `804` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, database migration metadata guard, `pnpm audit --audit-level moderate`, `pnpm peers check`, frozen install, `git diff --check`, `pnpm dev:doctor:admin --skip-cloudflare`, local browser `/admin/products/new`, `/admin/categories/new`, and `/admin/pages/new` rich-editor smokes, full deploy to API `4f843014-cd3f-4e28-9ba7-2a5ba819e0fe`, admin `5d6450c3-93ef-4333-b042-bb3a83834877`, storefront `06c16889-0599-4198-aeb3-a36a1f3a1bb4`, live API health/OpenAPI checks (`254` paths / `352` operations), live authenticated product/category/page rich-editor smokes, live Tiptap asset-deferral check, and live storefront `/` plus product-page smoke. No production product/category/page data was saved.
- `PERF-003` sixteenth slice defers product-form image drag-and-drop and additional-info builder loading. `ProductImagesSection` lazy-loads `DraggableImageGallery` only when selected images exist, `TitleDescriptionSection` mounts/lazy-loads `AdditionalInfoManager` only after the `Additional Sections` tab is active, and dead legacy product-form section exports/files were removed. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm --filter @scalius/admin-v2 build`, source scans proving no eager product-form `AdditionalInfoManager`/`DraggableImageGallery`/DnD imports, production chunk scan showing `ProductForm-DGIfa6oK.js` has no forbidden static heavy imports while `AdditionalInfoManager-DQUadvsv.js`, `DraggableImageGallery-D6wjPht3.js`, `sortable.esm-CsHTWzZ-.js`, and `TiptapEditor-CG0S1pAl.js` remain separate chunks, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`128` files / `804` tests), `pnpm build`, `pnpm check:env`, `pnpm --filter @scalius/database check:migrations`, `pnpm exec drizzle-kit check --config packages/database/drizzle.config.ts`, `pnpm check:dist-secrets`, `pnpm audit --audit-level moderate`, `pnpm peers check`, `CI=true pnpm install --frozen-lockfile`, `git diff --check`, local `pnpm dev:doctor:admin --skip-cloudflare` and browser `/admin/products/new` smoke, full deploy to API `484f8a9a-65c6-44bf-a6b6-6eec6c1e3cba`, admin `b1091ad4-f457-4ece-9a48-fa1ce5486840`, storefront `7ea49142-4427-44f1-bf8d-831f3adf50f7`, live API health/OpenAPI checks (`254` paths / `352` operations), live authenticated product-form asset-deferral smoke, and live storefront `/` plus `/search` smoke. No production product/storefront data was saved.
- `PERF-003` seventeenth slice defers product variant interaction tools and simplifies CSV import/export parsing. The variant barrel now exports only `VariantManager` and types; `VariantSortModal`, `bulk-generator`, and `utils/csvHelpers` load only through user-triggered lazy/dynamic imports. CSV import now parses once through `parseCsvToVariants()`, preserves barcode/barcodeType fields, validates duplicate/conflicting SKUs, rejects malformed numeric values and unknown discount types, and shares headers with export/template generation. This slice also corrected stale storefront SEO module docs that still claimed `/api/v1/seo` used TTL=0; the route now documents `CACHE_TTLS.STANDARD / 3600s` and the `api:seo:` purge prefix. Verification: focused CSV helper tests, focused API cache-invalidation/site-settings tests, admin typecheck/lint/build, production chunk scan proving product edit/ProductForm chunks have no static `bulk-generator`, `VariantSortModal`, or `csvHelpers` imports, source scans for lazy variant tools and narrow barrel exports, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`129` files / `808` tests), `pnpm build`, `pnpm check:env`, database migration metadata guard, Drizzle check, `pnpm check:dist-secrets`, `pnpm audit --audit-level moderate`, `pnpm peers check`, `CI=true pnpm install --frozen-lockfile`, `pnpm outdated -r` showing only the intentional storefront Vite 7 pin, `git diff --check`, local `pnpm dev` plus warmed `pnpm dev:doctor --require-running`, local browser `/admin/products/prod-test-2/edit` lazy Bulk Variant Generator/Reorder smoke and storefront `/`, full deploy to API `0b6dd797-a59e-4958-8182-21a4fceb6021`, admin `fbf74d2d-46ba-441a-925a-ca3e9feee222`, storefront `b67e5b1f-dbf2-4691-9dc5-6af74d28e02e`, live API setup/products checks, live dashboard auth endpoint check (`403` without `Origin`, `200` with dashboard `Origin` and Set-Cookie), live authenticated `/admin/products/prod_WH2tJTRAzGB4jMTZB8hoX/edit` Bulk Variant Generator/Reorder smoke, and live storefront `/` browser smoke. No production product/storefront data was saved.
- `PERF-003` eighteenth slice defers General Settings Header/Footer hidden subtab modules. `HeaderBuilder` lazy-loads header social links and navigation only when `Contact & Social` or `Navigation` is selected; `FooterBuilder` lazy-loads navigation menus and social media only when those subtabs are selected; `NavigationSection`/`NavigationMenusSection` import `NavigationBuilder` directly; and the header/footer barrels now export only the public builder plus types so hidden sections cannot be pulled in by broad re-exports. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm --filter @scalius/admin-v2 build`, source scans proving only lazy header/footer subtab imports and narrow barrels, production chunk scan proving header/footer builder chunks have no static `NavigationBuilder`, social-section, navigation-menu, sortable, or add-nav-dialog imports, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`129` files / `808` tests), `pnpm build`, `pnpm check:env`, database migration metadata guard, Drizzle check, `pnpm check:dist-secrets`, `pnpm audit --audit-level moderate`, `pnpm peers check`, `CI=true pnpm install --frozen-lockfile`, `pnpm outdated -r` showing only the intentional storefront Vite 7 pin, `git diff --check`, local `pnpm dev` plus warmed `pnpm dev:doctor --require-running`, local browser `/admin/settings` Header Contact & Social/Header Navigation/Footer Branding/Footer Navigation Menus smoke plus storefront `/`, full deploy to API `121e0a9b-ac03-4e97-a7d4-604f171a6e28`, admin `62110591-ee90-4074-b2f9-09c523280b09`, storefront `83a51690-c5b9-4ca5-af4f-2a0125518a85`, live API setup/products checks, live dashboard auth endpoint check (`403` without `Origin`, `200` with dashboard `Origin` and Set-Cookie), live authenticated `/admin/settings` Header/Footer subtab smoke, and live storefront `/` browser smoke. No production settings/storefront data was saved.
- `PERF-003` nineteenth slice replaces broad collection/discount picker preload payloads with targeted lookups and lazy product search. The API now exposes `/api/v1/admin/products/by-ids`, `/api/v1/admin/collections/by-ids`, and `/api/v1/admin/collections/category-options`; collection new/edit route loaders preload only category options plus stored product summaries; collection product and featured-product pickers lazy-load paginated product search on open; discount edit resolves already-selected product/collection labels through by-id endpoints instead of preloading broad form-options/list pages; and product/category/collection mutation hooks invalidate the new lookup query keys when relevant source data changes. Verification: focused API picker lookup route tests, `pnpm generate:sdk`, API/admin/api-client typechecks, admin lint, root `pnpm typecheck`, warning-free `pnpm lint`, `pnpm test` (`130` files / `811` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `pnpm audit --audit-level moderate`, `pnpm peers check`, frozen install, `git diff --check`, local browser `/admin/collections/new` and `/admin/discounts/:id/edit` picker smokes, full deploy to API `d0412eb2-82b4-4e93-932d-0783d8833020`, admin `b34888c2-7fde-425b-b4c6-6154f36a127d`, storefront `7b1e79e1-ba2a-4694-8b18-25e43629ffe8`, live OpenAPI count check (`257` paths / `355` operations), live authenticated by-id/category-options endpoint checks, live browser `/admin/collections/new` product-picker smoke, live browser `/admin/discounts/disc_IcgNztBsUGqxYDHzqDtm1/edit` smoke, and live storefront `/` smoke. No production data was saved.
- `PERF-003` twentieth slice defers remaining admin shell interaction chunks. `AdminHeader` lazy-loads `UserMenu` after idle with a stable initials/name fallback, keeping dropdown/avatar/auth-client code out of first shell load; `sidebar.tsx` lazy-loads `sidebar-mobile-sheet.tsx` only when the mobile drawer first opens and then keeps it mounted for close/reopen smoothness. `route-graph-boundaries.test.ts` now guards the source boundaries. Verification: focused route-graph Vitest (`1` file / `20` tests), admin typecheck/lint/build, admin deploy dry-run with root typecheck, production build scan showing no static admin/sidebar shell imports of `UserMenu`, `sidebar-mobile-sheet`, `dropdown-menu`, `avatar`, or `sheet`, root `pnpm lint`, root `pnpm test` (`197` files / `1169` tests), `pnpm check:env`, `pnpm check:dist-secrets`, local `pnpm dev:admin` smoke (`/auth/login`, anonymous `/admin`, same-origin sign-in, authenticated `/admin`, `/admin/orders`), admin deploy `24d63582-4471-4a1a-88ca-870e4b6e60f1`, live dashboard smoke with demo sign-in, authenticated `/admin` and `/admin/orders`, and live SHA-256 matches for `UserMenu-ka83BRvS.js` and `sidebar-mobile-sheet-q9mWc3ez.js`.
- `CACHE-007` and `PERF-005` are verified and deployed. `GET /api/purge-cache` now rejects query-string tokens without mutation and otherwise returns `405 Allow: POST`; POST remains the only mutating purge path. Dashboard first-paint components no longer import `motion/react` for stat-card, gradient, or word-flip effects. The cache/performance re-audit also opened follow-up rows for delivery-provider cache invalidation, delivery-provider admin query invalidation, order-detail provider warmup resilience, cache UI wording, cache manager duplicate fetches, all-clear timestamps, and the now-verified `ADMIN-013` admin mutation-barrel split. Verification: `pnpm --filter @scalius/storefront exec vitest run src/pages/api/purge-cache.test.ts src/lib/cache-purge-policy.test.ts --passWithNoTests`, `pnpm exec vitest run --config apps/api/vitest.config.ts apps/api/src/utils/cache-invalidation.test.ts`, affected admin/storefront typechecks and lints, `pnpm typecheck`, warning-free `pnpm lint`, `pnpm test` (`126` files / `795` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata check, `pnpm audit --audit-level moderate`, `pnpm peers check`, `git diff --check`, `pnpm outdated -r` with only the intentional storefront Vite 7 pin, local `pnpm dev` plus `pnpm dev:doctor --require-running`, local browser dashboard/products/storefront smoke, full deploy to API `128ebde2-62df-4f03-98f6-e7fa0d37534b`, admin `1e80b617-bffc-46f4-8a7b-b2e0e23ae606`, storefront `f767dd69-aa71-4470-b842-a250283d4b2b`, live storefront purge GET/query-token checks, live API/storefront/dashboard HTTP checks, live browser `/admin`, `/admin/products`, `/admin/orders`, storefront `/`, and storefront `/search` smoke with no console/runtime errors or error-boundary pages, and a dashboard dist scan with no motion runtime markers in the checked dashboard assets.
- `CACHE-008` and `ADMIN-011` are verified and deployed. Delivery-provider create/update/delete/upsert routes now invalidate the checkout cache group, and the delivery-provider admin UI invalidates `queryKeys.settings.deliveryProviders()` after successful save/delete so route-prefetched consumers do not sit on stale 30-minute config. Verification: `pnpm exec vitest run --config apps/api/vitest.config.ts apps/api/src/routes/admin/settings/delivery-providers-cache-invalidation.test.ts`, affected API/admin typechecks and lints, `pnpm typecheck`, warning-free `pnpm lint`, `pnpm test` (`127` files / `799` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata check, `pnpm audit --audit-level moderate`, `pnpm peers check`, `git diff --check`, `pnpm outdated -r` with only the intentional storefront Vite 7 pin, local `pnpm dev` plus `pnpm dev:doctor --require-running`, local browser dashboard/delivery-providers/orders/storefront smoke, full deploy to API `c2dbd146-3d1d-4907-a08c-6dd9f89df83d`, admin `1b04ca74-5c77-4423-ac15-600aee909650`, storefront `420b911c-ea06-48c1-9812-2ee0ad67a90c`, live API/storefront/dashboard HTTP checks, and live browser `/admin`, `/admin/orders`, `/admin/settings/delivery-providers`, storefront `/`, and storefront `/search` smoke with no console/runtime errors or error-boundary pages.
- `ADMIN-012` is verified and deployed. Order detail still requires order and shipment data, but delivery-provider warmup now uses optional `prefetchQuery()` with a logged warning on failure, and the route component consumes providers through a non-suspense query with an empty-list fallback so valid orders do not redirect away when provider config is temporarily unavailable. Verification: `pnpm exec vitest run apps/admin-v2/src/routes/admin/orders/-order-detail-prefetch.test.ts`, admin typecheck/lint, root typecheck/lint/test (`127` files / `800` tests)/build, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata check, `pnpm audit --audit-level moderate`, `pnpm peers check`, `git diff --check`, local `pnpm dev` plus `pnpm dev:doctor --require-running`, local browser `/admin/orders/T1TNN3` smoke, full deploy to API `dac019eb-05ed-4471-a000-a3a0ec545748`, admin `abc5d3fa-dfb2-4d1d-b75a-64088fffbfb2`, storefront `8e8ce72f-9cd7-46ed-8713-16304fef2028`, live HTTP checks for API/dashboard/storefront/purge GET, and live browser `/admin`, `/admin/orders`, `/admin/orders/P8RI6O`, storefront `/`, and storefront `/search` smoke with no error-boundary pages or new console/runtime errors.
- `AUTH-011` and `CACHE-006` are verified and deployed. The admin login 2FA flow now stores Better Auth pending method hints in `sessionStorage`, initializes TOTP/email from those hints before falling back to authenticated 2FA info, avoids silently sending email OTP when that authenticated lookup is unavailable, clears pending hints after verification/sign-out, and rejects `trustDevice: true` at the same-origin Better Auth TOTP/email/backup-code verification paths. Category writes and CMS page writes now also invalidate the `layout` cache group so fallback navigation/header/footer data derived from public categories/pages is purged with the content change. Verification: `pnpm exec vitest run apps/admin-v2/src/lib/auth.server.test.ts apps/admin-v2/src/lib/two-factor-pending.test.ts apps/api/src/routes/admin/auth-management.test.ts --passWithNoTests`, `pnpm --filter @scalius/api exec vitest run src/utils/cache-invalidation.test.ts --passWithNoTests`, affected admin/API typechecks/lints, `git diff --check`, root `pnpm typecheck`, warning-free `pnpm lint`, `pnpm test` (`125` files / `791` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata check, `pnpm audit --audit-level moderate`, `pnpm peers check`, `pnpm outdated -r`, local `pnpm dev` plus `pnpm dev:doctor --require-running`, local browser dashboard/storefront smoke, full deploy to API `e6371993-57e5-4cca-8b06-ffa201b5f1a4`, admin `7d9f3990-6c55-4e16-b095-bc5a6adb2538`, storefront `c6e6eb39-1829-4070-a543-96a1b6e77f13`, live demo sign-in API check, live trusted-device rejection checks for TOTP/email/backup-code, and live browser `/admin`, `/admin/products`, `/admin/orders`, storefront `/`, and storefront `/search` smoke with no console/runtime errors or error-boundary pages. Future remembered-device support should be tracked separately and must prove the full TOTP/email/backup-code/stale-cookie/post-login API matrix before relaxing this policy.
- `ADMIN-009`, `ADMIN-010`, `RBAC-001`, `STORE-007`, `PERF-004`, and `STORE-008` are verified and deployed. The slice centralizes admin page access on `@scalius/core/auth/rbac/page-permissions`, denies unmapped admin pages for non-super-admins, redirects `/admin` users without `dashboard.view` to their first accessible admin page, allows own-account/self-service API endpoints for any verified admin principal, passes KV through API RBAC checks, makes KV misses bypass stale local RBAC memory cache, clears KV permission caches for users assigned to edited roles, stops data tables from forcing refetch on every remount, removes the unused blocking media route loader, extends admin query-cache retention to 30 minutes, makes product-list SSR hydration deterministic, unwraps enveloped storefront CSP settings, avoids eager hidden 1400px product zoom image requests, removes unsupported SWR directives from storefront Cache API writes, rewrites storefront widget links from nonexistent collection-list routes to `/search`, and eliminates all ESLint warnings in the root lint task. Verification: focused `admin-access`, API admin-auth, core RBAC cache, storefront CSP, and shared widget-rendering tests; root `pnpm typecheck`, warning-free `pnpm lint`, `pnpm test` (`123` files / `783` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata check, `pnpm audit --audit-level moderate`, `git diff --check`; full deploy to API `822d3968-ffc0-4280-9286-f161a4096525`, admin `a6701970-d938-4250-a483-3c6155ab0f89`, storefront `9d6a9ea8-6258-487a-85e0-ed4e6f34d72d`; live API health/setup/CSP/auth checks; and live Chrome/CDP smoke for dashboard `/admin`, `/admin/products`, `/admin/orders`, `/admin/media`, `/admin/settings/account`, plus storefront `/`, `/search`, `/categories/men-clothing`, and `/products/monster-energy-drink` with no console/runtime errors or error-boundary pages.
- The stale `docs/codex/audits/` tree was removed because `/audit` is now the maintained audit source and the old tree contained fixed security/dependency/widget/cache claims that conflicted with current code.

2026-06-14:

- `AUTH-010` fixed by forwarding Better Auth session-rotating `Set-Cookie` headers through the API worker and TanStack server-function bridge. Password change now uses Better Auth `returnHeaders: true`, relays replacement cookies, and does not return replacement tokens in JSON. First-time 2FA setup now either proves a same-origin Better Auth `sessionToken` to `/2fa/method` or verifies the target-method code in the API route while forwarding any rotated cookie. The API middleware exempts only the exact method-completion endpoint before `session.twoFactorVerified` is true. Verification: `pnpm exec vitest run apps/admin-v2/src/lib/api.server.test.ts apps/api/src/routes/admin/auth-management.test.ts apps/api/src/middleware/admin-auth.test.ts`, API/admin/api-client typechecks, `pnpm check:env`, SDK regeneration, root `pnpm typecheck`, `pnpm lint`, `pnpm test`, admin/API builds, dist-secret check, local HTTP/browser smoke, full deploy to API `89316428-fc7f-4148-8f56-bb93c6b25c1b`, admin `c144655a-5f96-4741-b001-46926bdb7e2a`, storefront `4ab260a7-39ff-4489-9ef8-3f0370222d00`, and live HTTP/browser smoke.
- `AUTH-011` was later verified on 2026-06-15 for the current conservative policy. Remembered-device login remains intentionally disabled; re-enabling it requires a new tracker item and the full browser/API matrix described in `audit/VERIFICATION_PLAYBOOK.md`.

- `SUPPLY-001` fixed by moving the API Hono dependency out of the advisory range, updating Turbo, and pinning patched transitive advisory packages through `pnpm.overrides` (`esbuild`, `devalue`, `shell-quote`, `@grpc/grpc-js`, `ws`, and `qs`). Verification: `pnpm audit --audit-level moderate` reports no known vulnerabilities. Remaining dependency freshness work is tracked separately as `BUILD-005` and `SDK-001`.
- `AUTH-002` fixed by removing the unauthenticated trust boundary around `/api/v1/admin/auth/2fa/mark-verified`. Admin login/setup/account flows now verify the 2FA code through Better Auth first, then call `/api/v1/admin/auth/2fa/complete-verification` with the session token returned by successful Better Auth verification. The API only sets `session.twoFactorVerified = true` when that token matches the current session id and user id. Verification: focused API auth tests, API/admin/root typechecks, root tests, root lint, full `pnpm run deploy`, direct sign-in endpoint smoke, and browser smoke for `/admin/orders`.
- `AUTH-003` fixed by deleting the browser-callable `markFirstUserAsSuperAdmin` server function and removing the setup-form call site. First-admin promotion remains owned by the guarded `/api/v1/setup` route, and API/admin workers share the same D1 binding. Verification: stale-reference scan for `markFirstUserAsSuperAdmin`, admin typecheck, full `pnpm run deploy`, direct sign-in endpoint smoke, and browser smoke for `/admin/orders`.
- `BUILD-004` fixed by moving `seo-regressions.test.ts` from `apps/storefront/src/pages/` to `apps/storefront/src/lib/` and making the test locate the storefront package root correctly from both package-local and root Vitest runs. Verification: focused Vitest passes, root `pnpm test` passes, storefront typecheck/build pass, rebuilt `dist` has no `seo-regressions` route/chunk, storefront deploy completed as version `decaeaeb-7319-4cf1-965a-d390e8c32759`, live `/seo-regressions.test` returns 404, and browser storefront smoke has no captured console errors.
- `PAY-006` fixed by adding a shared payable-order policy for payment sessions and confirmed gateway processing. Stripe, SSLCommerz, and Polar public session routes now reject soft-deleted, cancelled, returned, refunded, partially-refunded, fully-paid, and payment-refunded orders before gateway settings/provider calls. `processPaymentConfirmed()` applies the same guard before creating/promoting a payment claim and in the final order CAS predicate; non-payable late-success queue results are acknowledged as manual-reconciliation outcomes instead of retried. Verification: `pnpm --filter @scalius/core test -- src/modules/payments/process-payment.test.ts`, `pnpm --filter @scalius/api test -- src/routes/payment/payment-session.test.ts src/queue-consumer.test.ts`, core/API typechecks and lints, `pnpm --filter @scalius/api build`, `pnpm check:dist-secrets`, `pnpm check:env`, `pnpm --filter @scalius/api run deploy` to version `8253ac1e-0053-436e-a20d-d98503c96832`, live API health/OpenAPI checks, authenticated dashboard orders smoke, and storefront browser smoke.
- Dependency/framework modernization pass: package manifests and lockfile were refreshed under pnpm 11.6.0. Local verification passed frozen install, typecheck, lint, tests, build, env checks, dist-secret checks, migration metadata checks, local dev doctor/browser smoke, and live health/browser smoke. Intentional freshness holds remain documented: `@types/node` stays on the Node 22 line, storefront Vite stays on 7.x for Astro/Cloudflare adapter compatibility, `better-call` stays on 1.3.6 for Better Auth 1.6.x / admin auth UI compatibility, and `partyserver` is overridden to 0.5.6 for the same recency guard. The API `ai` dependency is now on 6.0.205, with `ai@6.0.205` and `@ai-sdk/gateway@3.0.131` explicitly listed in `minimumReleaseAgeExclude` after a focused package-currency check. Live deploy versions from the full deploy output: API `28f29a52-9f7e-4cbf-b31e-f24d0f72767c`, admin `50a734d6-d769-45e0-ad55-54733c10db25`, storefront `bc5b4f58-79e5-449a-b03c-ee6acbe065b2`.
- `MEDIA-001` opened after live browser smoke and was fixed through the authenticated admin product update path. Product `prod_abe_OILKCt75z47ppI1Sd` (`khaki-high-top-casual-shoes-for-men`) had primary image `img_Gzbga8t-VI6tIJBR3rfZY` pointing at missing object `https://cloud.scalius.com/zLPBsNbtJCMxTkfPAPHcr.png`. The broken image row was removed from the product gallery and the existing valid image `_P4gWn-fpT60CU0U3Q1R5.png` became primary. Verification: admin API update returned 200, remote D1 SELECT shows six image rows with `_P4gWn-fpT60CU0U3Q1R5.png` primary, product detail and homepage API no longer contain `zLPBsNbtJCMxTkfPAPHcr`, replacement CDN URL returns `200 image/png`, and rendered storefront HTML contains the replacement and not the broken key.
- `CACHE-003` fixed by routing non-widget admin writes through the shared API-KV plus storefront purge helper. Shipping methods, delivery locations, checkout languages, navigation, analytics, header/footer/theme/storefront URL/currency/allowed-country/media/SEO settings, hero sliders, and attributes now invalidate their concrete cache groups instead of missing purge or using stale groups. A 2026-06-15 follow-up fixed hero slider soft-delete to invalidate homepage caches and removed accidental homepage invalidation from hero slider reads. Verification: focused cache route tests, root typecheck/lint/test/env/migration guards, local dev doctor plus local admin/storefront browser smoke, full deploy to API `1b0dbe3e-98fb-4b37-86b0-0db4a4bb111d`, admin `bb5da41d-ae53-4827-8d62-a6120596cdf4`, storefront `93e82ac9-1e3a-4c33-b095-101ba039db77`, and live dashboard/storefront browser smoke with no captured console errors. Widget target-aware purge narrowing is now verified separately as `CACHE-004`.
- `CACHE-004` fixed by replacing widget write invalidation with target-aware cache subjects. Widget create/update/delete/restore/status/history restore now load before/after public widget placement snapshots and purge only `widget_{id}`, exact `widgets_scope_{scope}_{scopeId}`, homepage widget/homepage prefixes when a live homepage placement is affected, and page-render/API page slug prefixes when a live page placement is affected. The dormant duplicate core cache-invalidation helper was removed so the API utility is the single source of truth. Verification: focused widget/cache invalidation tests, root typecheck/lint/test/build/env/dist-secret/audit/peer/frozen-install/migration guards, local dev doctor plus local admin/storefront browser smoke, full deploy to API `a6b3e5ff-1730-4748-a288-d99e51c1fb63`, admin `788dbc42-4852-437e-bf85-2b6a1249087a`, storefront `90ab10e1-74a7-4920-a247-18ef89b06d96`, and live dashboard/storefront browser smoke with no captured console errors.
- `AUTH-005`/`AUTH-006`/`AUTH-007`/`AUTH-008` fixed and verified by adding the Better Auth `two_factor.verified` migration/schema field, requiring target-method code proof before `/api/v1/admin/auth/2fa/method` updates `user.twoFactorMethod`, mapping target-method verification failures to validation errors, verifying duplicate setup-account passwords before first-admin recovery promotion, revoking sessions on password reset, and keying the Better Auth factory cache by secret plus runtime origins. Verification: focused core auth tests, focused API auth-management/admin-auth tests, API/admin/api-client/root typechecks, root test/lint/build/env/dist-secret/audit/peer/frozen-install/migration guards, SDK regeneration, local migration `0040`, remote D1 migration `0040`, remote `PRAGMA table_info(two_factor)` showing `verified`, full deploy to API `4e191a10-8a56-4f60-83c7-ebdd9e431e5e`, admin `1fa6e70f-df67-4282-be0a-dd5abb2aecfa`, storefront `6f5c4744-9fba-4185-8571-9c37140289eb`, direct local/live sign-in checks, and local/live browser dashboard/orders/storefront smoke.
- `PERF-002` fixed and verified by batching dashboard API/service reads with `Promise.all`/`safeBatch` and avoiding lazy chart loading when daily activity rows are all zero. Verification: dashboard chart-data tests, admin/API typechecks, root gates, local empty-dashboard browser check confirming no chart asset loaded, and live dashboard smoke with no console errors.
- `AUTH-009` fixed by tightening the legacy `/api/v1/admin/auth/2fa/verify` endpoint to require an active session, convert Better Auth OTP/TOTP/backup-code verification failures to a 400 validation response, and accept returned session-token proofs only when they match the current session id and user id. Verification: focused API auth tests, API typecheck/build, root `pnpm test`, root `pnpm lint`, targeted API deploy version `76dee35f-507c-4654-a049-d8feb66d63ae`, live demo sign-in/session check, and browser checks for `/admin` and `/admin/orders` with no console errors.
- `CACHE-005` fixed by invalidating the `checkout` cache group after auth/checkout settings saves, invalidating the `layout` cache group after CSP/security settings saves, making `cacheMiddleware({ ttl: 0 })` bypass reads and writes, and counting only successful warm responses in storefront purge logs. Verification: focused cache/auth route tests, API/storefront typechecks, API/storefront builds, root `pnpm test` (117 files, 754 tests), root `pnpm lint`, `pnpm check:env`, `pnpm check:dist-secrets`, targeted API deploy version `76dee35f-507c-4654-a049-d8feb66d63ae`, storefront deploy version `e5765834-61cf-4d8a-80ec-eb70a0c9ad3b`, live API health/analytics/CSP checks, live dashboard sign-in/orders browser smoke, and live storefront browser smoke.
- `AUTH-011` was later verified on 2026-06-15 for the current conservative policy. Preferred-method login now uses pending Better Auth method hints, and trusted-device bypass remains disabled until a future tracked policy change.
- `PERF-003` originated as an admin performance follow-up from the read-only performance audit. Many slices are now verified above; remaining candidate slices include broader route/component bundle slimming, repeated admin guard work, heavier chart/export chunks, and deeper query/module graph splitting.

2026-06-13:

- `SEC-003` fixed by splitting `apps/api/src/routes/checkout-languages.ts` into public active-read and admin CRUD routers, then mounting the public router only at `/api/v1/checkout-languages`. Regenerated `@scalius/api-client` from a live local API server. Verification: `pnpm --filter @scalius/api test -- src/routes/checkout-languages.test.ts`, `pnpm --filter @scalius/api typecheck`, `pnpm --filter @scalius/api-client typecheck`, and `git diff --check`.
- `SEC-002` fixed by requiring `products.view` plus `products.edit`, or super-admin, before `apps/admin-v2/src/routes/api/scanner-token.tsx` writes a QR scanner token. Verification: `pnpm exec vitest run apps/admin-v2/src/routes/api/scanner-token.test.tsx --passWithNoTests` and `pnpm --filter @scalius/admin-v2 typecheck`.
- `SEC-001` fixed by exposing `session.twoFactorVerified` through Better Auth, storing the Better Auth session in Hono context, and rejecting unverified 2FA sessions before admin RBAC except exact 2FA info/verify/complete-verification/method-completion endpoints. Verification: `pnpm --filter @scalius/api test -- src/middleware/admin-auth.test.ts`, `pnpm --filter @scalius/api typecheck`, and `pnpm --filter @scalius/core typecheck`.
- `SEC-004` fixed by rendering checkout summary customer name/address with DOM `textContent` instead of `innerHTML`. Added `happy-dom` to storefront dev dependencies, resolving `TEST-002`. Verification: `pnpm --filter @scalius/storefront exec vitest run src/lib/checkout/render-summary.test.ts --passWithNoTests` and `pnpm --filter @scalius/storefront typecheck`.
- `PRIV-001` fixed by creating a KV-backed receipt token from the checkout token, adding `GET /api/v1/orders/receipt/{id}?token=...`, switching `/order-success` to require `orderId` plus `token`, and rendering a minimal receipt DTO that omits phone, email, customer ID, shipments, delivery providers, and notes. Regenerated `@scalius/api-client`; the OpenAPI surface at that slice was 257 paths / 355 operations. Verification: `pnpm --filter @scalius/api test -- src/routes/orders-receipt.test.ts`, the combined P0 API test run, and the focused typecheck chain through API, admin, storefront, core, and api-client.
- `ORDER-001` fixed by constraining `releaseExpiredReservations()` to orphaned inventory movements whose order row no longer exists, with a second per-candidate order-existence guard before stock release. Active pending/confirmed/incomplete orders now keep their reservations until explicit order transition logic releases or deducts them. Verification: `pnpm --filter @scalius/core test -- src/modules/inventory/expiry.test.ts` and `pnpm --filter @scalius/core typecheck`.
- `ORDER-002` fixed by moving provider shipment creation behind an order-version claim, removing the direct admin shipment route's low-level `createShipment()` bypass, moving COD collection/return writes after successful order CAS, and delaying manual-fulfillment inventory transitions until after the fulfillment claim and shipment/item writes. Verification: `pnpm --filter @scalius/core test -- src/modules/orders/orders.fulfillment.test.ts`, `pnpm --filter @scalius/core typecheck`, `pnpm --filter @scalius/api typecheck`, and `git diff --check`.
- `ORDER-003` fixed by reserving stock per order inside `handleOrderIngestBatch()`, removing reservation-failed order writes before the DB batch, retrying only the message whose reservation failed, and falling back to isolated per-order reserve/write/ack handling after a shared DB batch failure. Verification: `pnpm --filter @scalius/core test -- src/modules/orders/orders.queue.test.ts`, `pnpm --filter @scalius/core typecheck`, and `git diff --check`.
- `ORDER-004` fixed by writing checkout polling KV and receipt-token KV before `ORDER_INGEST_QUEUE.send()`, rewriting checkout status to `failed` if enqueue fails after KV creation, and routing order-ingest queue batches by the actual queue name (`order-ingest`, plus legacy alias) instead of broad mixed-batch body detection. Verification: `pnpm --filter @scalius/api test -- src/routes/orders-create.test.ts src/queue-consumer.test.ts`, `pnpm --filter @scalius/api typecheck`, and `git diff --check`.
- `ORDER-005` fixed by replacing direct hard deletion of stale incomplete orders in admin abandoned-checkout cleanup with `archiveStaleIncompleteOrders()`: reserved/deducted orders first release inventory via `applyInventoryForStatusChange(db, orderId, OrderStatus.CANCELLED)`, then archive an abandoned-checkout snapshot, then mark the order cancelled/restored and soft-deleted. If inventory release fails, the order/items remain present for retry and movement links are not nulled by a hard delete. Current verification entry points are `pnpm --filter @scalius/api test -- src/routes/admin/system-utils-safe-methods.test.ts src/scheduled-maintenance.test.ts`, `pnpm --filter @scalius/core test -- src/modules/orders/stale-incomplete-orders.test.ts src/modules/abandoned-checkout/abandoned-checkout-cleanup.test.ts`, `pnpm --filter @scalius/api typecheck`, and `pnpm --filter @scalius/api lint`.
- `ORDER-012` fixed by making abandoned-checkout cleanup win a version/status/payment-state/stale-cutoff claim before releasing inventory, skip active shipment claims and pending/succeeded payment records, roll the cleanup claim back when inventory release fails, finalize soft-delete with a second guard, and write the archive row only after finalization. Also fixed the live admin SSR failure by skipping server-side prefetch on the abandoned-checkouts route's browser-proxy-only relative fetch. Verification: focused API abandoned-checkout tests, API/admin typechecks and lints, API/admin builds, root `pnpm test`, env/dist-secret checks, API deploy version `1ab100f5-5e2b-4d8a-9fb3-c7ad0a4a3a07`, admin deploy version `af228559-8956-434c-bda7-88ae511cf4c9`, live API/auth/session/admin route/proxy/storefront HTTP smoke, dashboard browser smoke, and storefront browser smoke.
- `ORDER-013` fixed by changing manual fulfillment to acquire a private shipment claim before visible status changes, validate non-empty unique same-order item IDs, scope item updates by `orderId`, and batch the manual shipment insert, item updates, and final order status/fulfillment/claim-clear update together. Batch failure before shipment insert clears the private claim and leaves inventory untouched. Verification: focused core fulfillment tests, core/API typechecks, core lint, API dry-run build, root `pnpm test`, env/dist-secret checks, API deploy version `37698b5e-89fe-4978-b8df-2e94145021c3`, live API/OpenAPI/auth/admin/storefront HTTP smoke, dashboard orders browser smoke, and storefront browser smoke.
- `ORDER-014` fixed by removing COD auto-pay from generic order status updates. Generic COD `delivered`/`completed` transitions now require successful COD payment evidence, collected COD tracking, `paymentStatus = paid`, positive `paidAmount`, and zero balance; otherwise callers must use the COD collection action that writes `cod_tracking`, `order_payments`, and order payment totals together. Verification: focused core fulfillment/COD status tests, core/API typechecks, core lint, API dry-run build, root `pnpm test`, env/dist-secret checks, API deploy version `37698b5e-89fe-4978-b8df-2e94145021c3`, live API/OpenAPI/auth/admin/storefront HTTP smoke, dashboard orders browser smoke, and storefront browser smoke.
- `STORE-005` fixed by routing checkout config inline-script serialization through `serializeJsonForInlineScript()`, escaping `<`, `>`, `&`, U+2028, and U+2029 while preserving parseable JSON. Verification: focused safe-json DOM-parser breakout test, storefront typecheck/build, storefront lint, root `pnpm test`, `pnpm check:dist-secrets`, `git diff --check`, storefront deploy version `3215e5be-1237-47bd-a2b1-ac92c3805a58`, live `/checkout` HTML check for `window.__CHECKOUT_CONFIG__`, and browser smoke with no console or Worker-tail errors.
- `STORE-006` fixed by moving the empty-cart state renderer into `apps/storefront/src/lib/cart/empty-state.ts` and assigning active checkout-language strings through `textContent`/text nodes instead of `innerHTML`. Verification: happy-dom malicious language-string test, focused checkout XSS test set, storefront typecheck/build, storefront lint, root `pnpm test`, `pnpm check:dist-secrets`, `git diff --check`, storefront deploy version `3215e5be-1237-47bd-a2b1-ac92c3805a58`, and live browser inspection of `#cartItems` showing the empty-cart renderer with no injected `img`/`script` nodes.
- `PRIV-004` fixed by removing cart-side standalone `scalius_user_*` analytics PII capture, limiting Meta CAPI default browser user data to `_fbp`, `_fbc`, and user agent, requiring event-specific PII to be explicitly supplied, extending checkout cleanup to remove legacy PII keys, and clearing raw checkout transfer state before SSLCommerz/Polar external redirects while leaving cart intact for cancel/failure recovery. Verification: focused Meta CAPI/session/checkout redirect tests, storefront typecheck/build/lint, all-workspace typecheck during deploy, root `pnpm test` (105 files, 703 tests), `pnpm check:dist-secrets`, `git diff --check`, storefront deploy version `4391b4cd-7a08-438c-be2e-193d8df7a79e`, live HTTP checks for `/`, `/cart`, and `/checkout`, live browser smoke with no console errors, and deployed asset scans confirming the analytics bundle has no legacy session-PII enrichment references.
- `CACHE-001` fixed by making payment-method, Stripe, SSLCommerz, and Polar settings saves invalidate API checkout KV prefixes and purge storefront checkout prefixes, adding `/polar` to checkout cache group routing, aligning the dormant core cache-invalidation copy, and making public checkout config honor `payment_methods.enabled_methods` as the outer gateway allowlist while still requiring each gateway's own settings to be enabled/configured. Verification: focused API payment-settings/cache-invalidation tests, focused core checkout-config tests, API/core typechecks and lints, all-workspace typecheck plus API dry-run build during deploy, root `pnpm test` (107 files, 710 tests), `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, API deploy version `22831cca-fe5d-4df0-a6d7-96bf3237a0ab`, live API health and `/api/v1/checkout/config` smoke, and storefront browser smoke with no console errors.
- `BUILD-005` fixed by adding app-local `.dev.vars`/`.env*` files to Turbo `globalDependencies`, adding build-time env names to `globalEnv`, and adding a regression test for those cache inputs. Verification: `scripts/turbo-config.test.mjs`, Turbo build dry-run JSON showing env files and configured env values in `globalCacheInputs`, root `pnpm test` (115 files, 748 tests), root `pnpm typecheck`, and `git diff --check`.
- `SDK-001` fixed by removing the deprecated `@hey-api/client-fetch` runtime dependency, updating `@hey-api/openapi-ts` to 0.98.2, deleting the post-generation `client-core` shim, importing the generated bundled client directly, and regenerating the SDK from a live local API worker. The regenerated OpenAPI artifact also catches up to the secured 2FA `complete-verification` contract. Verification: npm version/deprecation checks, `pnpm generate:sdk`, API-client/admin/storefront focused typechecks, root `pnpm typecheck`, root `pnpm test` (115 files, 748 tests), root `pnpm lint`, `pnpm audit --audit-level moderate`, and `git diff --check`.
- `PAY-001` fixed by moving Stripe and SSLCommerz webhook ingress to the existing durable `webhook_events` claim-before-enqueue helper, marking queue failures as retryable failed claims, and removing KV-only post-send dedupe. `processPaymentFailed()` now records/claims failed attempts before order updates, duplicate failed attempts no-op, failed attempts can be promoted by later successful gateway events, and payment cancellation releases inventory through the centralized idempotent inventory transition. Verification: `pnpm --filter @scalius/api test -- src/routes/webhooks/stripe.test.ts src/routes/webhooks/sslcommerz.test.ts src/routes/webhooks/polar.test.ts`, `pnpm --filter @scalius/core test -- src/modules/payments/process-payment.test.ts`, `pnpm --filter @scalius/api typecheck`, and `pnpm --filter @scalius/core typecheck`.
- `PAY-002` fixed by making Polar webhook refunds CAS-update `orders.paymentStatus`, `orders.paidAmount`, and allowed `orders.status` transitions before inventory release. Pre-fulfillment full refunds now transition to `cancelled` and release reservations, fulfilled full refunds can transition to `refunded` without auto-restocking, and old rows with `paymentStatus=refunded` but stale order status are repaired on redelivery. Verification: `pnpm --filter @scalius/core test -- src/modules/payments/polar.test.ts src/modules/payments/process-payment.test.ts` and `pnpm --filter @scalius/core typecheck`.
- `PAY-003` fixed by extracting shared receipt-token validation, requiring payment-session `receiptToken` for Stripe/SSLCommerz/Polar, rejecting missing or foreign tokens before gateway settings/provider calls, matching the requested gateway to the order payment method, rejecting invalid Polar paid/cancelled/returned/deposit/balance sessions, deriving SSLCommerz/Polar gateway callback URLs from trusted `PUBLIC_API_BASE_URL`, and regenerating the API client so caller URL fields are no longer advertised. Verification: `pnpm --filter @scalius/api test -- src/routes/payment/payment-session.test.ts src/routes/orders-receipt.test.ts`, `pnpm generate:sdk` against a live local API worker, focused API/storefront/api-client typechecks, and focused API/storefront/api-client lint.
- `PAY-004` fixed by routing Stripe, SSLCommerz, and Polar session creation through a shared server-side payment-session policy. Deposit payments now require enabled partial payment settings and must match the configured partial amount, balance payments require server-side balance state, caller currency is ignored in favor of configured currency, and Stripe manual capture is forced off for public sessions. Verification: `pnpm --filter @scalius/api exec vitest run src/routes/payment/payment-session.test.ts src/routes/webhooks/sslcommerz.test.ts` and `pnpm --filter @scalius/api typecheck`.
- `PAY-005` fixed by making SSLCommerz IPN validate `val_id` first, derive canonical `tran_id`/`val_id`/payment fields from the validation response, claim durable webhook events with canonical identifiers, and resolve payment type against server-side order/payment-plan state instead of trusting form metadata. Verification: `pnpm --filter @scalius/api exec vitest run src/routes/payment/payment-session.test.ts src/routes/webhooks/sslcommerz.test.ts` and `pnpm --filter @scalius/api typecheck`.
- `PAY-007` fixed by using unique scoped SSLCommerz merchant `tran_id` values per full/deposit/balance attempt, carrying canonical order ids through trusted callback `order_id` and validated `value_b`, queueing IPNs with canonical order ids plus full attempt ids, moving confirmed SSLCommerz idempotency to `sslcommerz_val_id`, and normalizing completed payment plans to `completed`. Verification: focused API payment-session/webhook tests, focused core payment processor tests, API/core/database typechecks and lints, database migration metadata check, root `pnpm test`, `pnpm check:env`, `pnpm check:dist-secrets`, API deploy version `9a25a484-b501-4f84-88f5-f3bf4a0b0eba`, live API/OpenAPI/auth HTTP checks, authenticated dashboard `/admin` + `/admin/orders` browser smoke, and storefront browser smoke.
- `ORDER-006` fixed by requiring an active, non-deleted shipping method whenever shipping applies and deriving shipping charge from the selected method fee. Free-delivery product behavior still explicitly waives shipping method and charge. Verification: `pnpm --filter @scalius/core test -- src/modules/orders/orders.storefront.test.ts` and `pnpm --filter @scalius/core typecheck`.
- `ORDER-007` fixed by making status/fulfillment/COD/delivery/refund/return paths reconcile inventory on same-status retries instead of returning before `applyInventoryForStatusChange()`. Delivery webhooks and shipment refreshes now invoke `updateOrderStatusFromShipment()` even when the shipment row already has the provider status, while notifications remain gated on actual order status changes. Already-cancelled full-refund retries release only pre-fulfillment/non-deducted inventory and do not auto-restore fulfilled refunds. Verification: `pnpm --filter @scalius/core exec vitest run src/modules/orders/orders.fulfillment.test.ts src/modules/delivery/tracking.test.ts src/modules/payments/polar.test.ts src/modules/payments/process-payment.test.ts`, `pnpm exec vitest run tests/unit/core/orders/update-order-atomicity.test.ts tests/unit/core/payments/refund-validation.test.ts`, `pnpm --filter @scalius/api exec vitest run src/routes/webhooks/steadfast.test.ts src/routes/admin/system-utils-safe-methods.test.ts src/scheduled-maintenance.test.ts`, `pnpm --filter @scalius/core typecheck`, `pnpm --filter @scalius/api typecheck`, `pnpm --filter @scalius/core lint`, `pnpm --filter @scalius/api lint`, and `git diff --check`.
- `WEBHOOK-001` fixed by adding a five-minute processing lease to `apps/api/src/utils/webhook-idempotency.ts`. Fresh `processing` claims still dedupe, stale `processing` claims are atomically reclaimable by one retry, `failed` claims remain atomically reclaimable, `queued`/`processed` remain terminal for claim purposes, and insert failures with no existing row now throw so providers receive retryable failures instead of false duplicates. Verification: `pnpm --filter @scalius/api exec vitest run src/utils/webhook-idempotency.test.ts src/routes/webhooks/stripe.test.ts src/routes/webhooks/sslcommerz.test.ts src/routes/webhooks/polar.test.ts src/routes/webhooks/steadfast.test.ts`, `pnpm --filter @scalius/api typecheck`, `pnpm --filter @scalius/api lint`, and `git diff --check`.
- `DEL-001` fixed by moving Pathao and Steadfast delivery webhooks to durable `webhook_events` claims before shipment/order side effects, making Steadfast event identity include delivery status or tracking update identity, and expanding `updateOrderStatusFromShipment()` to handle emitted canonical statuses (`pickup_assigned`, `out_for_delivery`, `partial_delivered`, `pickup_failed`, `delivery_failed`, `on_hold`, `unknown`). Failed carrier delivery retry semantics now explicitly allow `shipped -> confirmed`, and direct `confirmed -> delivered` webhooks deduct reserved inventory by treating `delivered` as a stock-deducting transition. Verification: `pnpm --filter @scalius/api test -- src/routes/webhooks/steadfast.test.ts`, `pnpm --filter @scalius/core test -- src/modules/delivery/tracking.test.ts`, `pnpm --filter @scalius/api typecheck`, and `pnpm --filter @scalius/core typecheck`.
- `STORE-001` fixed by making selective storefront prefix purges bump the KV cache version used by L2 Cache API keys. Checkout invalidation can keep `bumpVersion: false` from the API side, but the storefront policy still bumps the global version and warms critical pages when prefixes are present. Verification: `pnpm --filter @scalius/storefront exec vitest run src/lib/cache-purge-policy.test.ts --passWithNoTests`, `pnpm --filter @scalius/api test -- src/utils/cache-invalidation.test.ts`, `pnpm --filter @scalius/storefront typecheck`, and `pnpm --filter @scalius/api typecheck`.
- `OPS-001` fixed by making storefront `typecheck` generate `src/config/build-id.ts` before `astro check` and changing `scripts/generate-build-id.js` from timestamp-only output to deterministic commit/source-hash output. Clean deploy is covered because root deploy runs `pnpm typecheck` before build. Verification: ran the generator twice and confirmed the same `BUILD_ID`, then ran `pnpm --filter @scalius/storefront typecheck`.
- `OPS-002` fixed by routing root `deploy:api`, `deploy:admin`, and `deploy:storefront` through `scripts/deploy.mjs --only ...`; targeted deploys typecheck before building/deploying, and the API target applies remote D1 migrations first. Verification: `node --check scripts/deploy.mjs`, invalid target guard, and script inspection.
- `OPS-003` fixed by making `scripts/deploy.mjs --dry-run` a real non-mutating mode. Dry runs still typecheck, build, and run dist-secret checks, but they skip D1 migrations and Worker deploys for targeted and full deploys; migration-only dry runs print the intended target without applying migrations. Verification: `node --check scripts/deploy.mjs` and `pnpm run deploy:api -- --dry-run`.
- `TEST-001` fixed by making `parseGeneratedWidgetContent()` return normalized widget parts so local-safe HTML-owned script blocks move into widget JS before preview. Queue retry metadata now writes generic buyer-facing retry reasons, and the checkout DOM test runs under `happy-dom`. Verification: focused widget/queue/checkout tests plus root `pnpm test`; current root baseline is recorded above.
- `DEPLOY-001` fixed by detecting recoverable admin route-load failures such as stale dynamic-import/chunk errors, auto-reloading once per route/error signature, and falling back to an explicit reload action instead of leaving users on the generic 500 screen. Dashboard analytics date filters now compare Unix timestamps against Unix timestamp columns. Verification: focused admin route-error and scanner-token tests, admin/core typechecks, admin build, root `pnpm test`, root `pnpm lint`, full `pnpm run deploy`, live API/admin/storefront HTTP checks, browser checks for `https://dashboard.scalius.com/admin` and `https://storefront.scalius.com/`, and `wrangler tail` confirmation that the redeployed admin Worker served `/admin`, `_serverFn`, and `/api/auth/get-session` without exceptions.
- `PERF-003` first slice split the admin dashboard into `/api/v1/admin/dashboard/summary` and `/api/v1/admin/dashboard/activity`, kept the legacy combined endpoint, changed the admin home loader to await only summary data and prefetch activity in the background, added `customers_dashboard_activity_idx` in migration `0041_fixed_whistler.sql`, and regenerated the production-aligned SDK to 254 paths / 352 operations. Verification: `pnpm exec vitest run apps/api/src/routes/admin/dashboard.test.ts`, `pnpm --filter @scalius/database check:migrations`, affected workspace typechecks/lints, root `pnpm test`, root `pnpm typecheck`, root `pnpm build`, local API/admin/storefront browser smoke, `pnpm dev:doctor:all`, full deploy to API `d25ebe15-971e-409e-bcfd-b5d15c6702e6`, admin `3af1362d-cfe5-4722-94f6-0a3c44a5f6ae`, storefront `8976c406-b367-40d8-a39c-a795bd5d6862`, remote D1 index check, live OpenAPI count check, live authenticated split endpoint checks, and live browser `/admin`, `/admin/orders`, and storefront smoke.
- `PERF-003` second slice moved admin order-detail payment history, currency, and COD tracking warming into `apps/admin-v2/src/lib/order-detail-prefetch.ts`. The route now warms order, shipments, delivery providers, payment history, currency settings, and COD tracking for COD orders before render, while optional payment/COD/currency prefetch failures log and continue instead of redirecting away from an otherwise valid order. Component-level order invalidation now uses centralized `queryKeys`, and mounted order/payment/COD observers keep warmed data fresh for the first mount window. Verification: `pnpm exec vitest run apps/admin-v2/src/routes/admin/orders/-order-detail-prefetch.test.ts`, admin/API focused typechecks/lints, root `pnpm test` (120 files, 770 tests), root `pnpm typecheck`, root `pnpm lint`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, local `pnpm dev:doctor --require-running`, local disposable COD order `T1TNN3` order-detail browser smoke, full deploy to API `e1b0a003-9c47-4e7c-9afd-47a209ed3095`, admin `bc36bca6-0628-47dc-8cab-24c6c3bdf9f9`, storefront `d3b9d1e9-795c-44be-99f0-5ab1ae5d7e89`, and live authenticated `/admin`, `/admin/orders`, `/admin/orders/6RMK4J`, plus storefront home/product smoke.
- `PERF-003` third slice trimmed checkout settings route work and started the query-options barrel split for hot shell paths. `/admin/settings/checkout` now preloads only auth settings, `CheckoutFlowSettings` consumes the shared TanStack Query auth-settings cache instead of a duplicate direct fetch, and payment/shipping tab data lazy-loads when those tabs are opened. Always-mounted sidebar, currency, storefront URL, and Firebase-init hooks now import narrow modules under `apps/admin-v2/src/lib/api-query-options/` instead of the broad `api.queries.ts` barrel. Verification: `pnpm exec vitest run apps/admin-v2/src/routes/admin/settings/-checkout-loader.test.ts`, broad-barrel import scan over the checkout/shell hot files, `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, root `pnpm test` (121 files, 771 tests), root `pnpm typecheck`, root `pnpm lint`, root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm audit --audit-level moderate`, `git diff --check`, `pnpm outdated -r` (only storefront `vite` 7.3.5 -> 8.0.16, held because Astro 6.4.6 / `@astrojs/cloudflare` 13.7.0 depend on Vite `^7.3.2`), local `pnpm dev:doctor --require-running`, local browser checkout lazy-load/dashboard/storefront smoke, full deploy to API `d643773b-9e7b-4a9a-b0a7-7725245bd56f`, admin `f31f89ed-909f-4da3-8bba-084a0ea88d12`, storefront `400924eb-01a3-47b5-bb79-8cd5a172dd6e`, live API/storefront/dashboard HTTP smoke, live demo sign-in API check, and live browser `/admin`, `/admin/orders`, `/admin/settings/checkout`, storefront home/cart/checkout checks with no captured console errors.
- `PERF-003` fourth slice reduced admin list-navigation blocking without trading away freshness. The `/admin` route now uses a 15-second client-side `adminRouteGuard()` context cache that is primed from SSR/hydration and cleared on sign-out. Common list loaders use `warmRouteQuery()` so server rendering still awaits data, while cold client navigations prefetch in the background instead of blocking the route transition. Data tables keep previous rows visible and refetch on mount, and `api.queries.ts` lazy-loads domain server-function modules instead of importing every domain at module load. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm exec vitest run apps/admin-v2/src/routes/admin/settings/-checkout-loader.test.ts apps/admin-v2/src/lib/admin-access.test.ts apps/admin-v2/src/lib/api.server.test.ts apps/api/src/routes/admin/auth-management.test.ts`, root `pnpm typecheck`, root `pnpm build`, root `pnpm test` (121 files, 773 tests), `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, full deploy to API `f8513a65-a8e8-463f-98aa-831ca5e3c5a8`, admin `550835d7-3075-486f-88b8-2c07960494be`, storefront `3a8a0294-239d-41c6-9aad-b7c8dd457601`, live API health/storefront home/dashboard sign-in checks, and live browser `/admin`, `/admin/products`, `/admin/orders`, `/admin/customers` smoke.
- `PERF-003` fifth slice fixed freshness gaps introduced or exposed by faster navigation. Profile and 2FA account settings now clear the client admin route-context cache before invalidating the route, so shell/header state refreshes immediately after current-user changes. Product/customer/order mutation hooks now invalidate `queryKeys.dashboard.all`, category mutations invalidate `queryKeys.products.stats()`, direct category creation paths match that policy, and bulk shipment from the orders list invalidates order list/detail/shipment/dashboard keys after successful shipment creation. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm typecheck`, `pnpm lint`, `pnpm test` (121 files, 773 tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, full deploy to API `6c8a5df3-1956-4a98-ade0-abc6cacf021f`, admin `be596a08-09c6-49db-841f-140772fa34e0`, storefront `8eb6cf3a-d57b-4dd4-bc6d-6486a370f3e5`, live API health/storefront home/dashboard sign-in checks, and live browser `/admin`, `/admin/products`, `/admin/orders`, `/admin/customers`, storefront home smoke with no error-boundary page or captured console errors.
- `CACHE-003` 2026-06-15 follow-up fixed hero slider cache behavior: GET-by-id no longer purges homepage caches, while DELETE now invalidates the homepage group like create/update. Verification: `pnpm --filter @scalius/api exec vitest run src/routes/admin/settings/hero-sliders-cache-invalidation.test.ts src/utils/cache-invalidation.test.ts src/routes/admin/widgets-cache-invalidation.test.ts`, API typecheck/lint, root gates, the same full deploy to API `e1b0a003-9c47-4e7c-9afd-47a209ed3095`, admin `bc36bca6-0628-47dc-8cab-24c6c3bdf9f9`, storefront `d3b9d1e9-795c-44be-99f0-5ab1ae5d7e89`, and live dashboard/storefront smoke.
- `AUTH-011` re-audit on 2026-06-15 originally found trusted-device and preferred-method gaps; the later 2026-06-15 hardening slice verified the current conservative policy by rejecting trusted-device attempts at both boundaries and using pending Better Auth method hints. Re-enabling remembered-device behavior still requires a separate future tracker item.
- `BUILD-003` fixed by adding `scripts/clean-dist-env-files.mjs`, running it after admin/storefront framework builds, excluding local env files from Turbo build outputs, excluding ignored OS/generated files from Turbo inputs, routing package-local deploy scripts through the root safety wrapper, and making `scripts/deploy.mjs` fail before deploy if any app `dist/` contains `.dev.vars`, `.env*`, or `*.vars`. Wrangler dry-runs for admin and storefront did not include the pre-existing `.dev.vars` files in uploaded artifacts, but the build invariant is now explicit and enforced. Verification: `node --check scripts/clean-dist-env-files.mjs`, `node --check scripts/deploy.mjs`, `node scripts/clean-dist-env-files.mjs apps/admin-v2 apps/storefront`, `node scripts/clean-dist-env-files.mjs --check apps/admin-v2 apps/storefront apps/api`, `pnpm exec vitest run scripts/clean-dist-env-files.test.mjs`, `pnpm --filter @scalius/admin-v2 build`, `pnpm --filter @scalius/storefront build`, `pnpm check:dist-secrets`, and `pnpm exec turbo run build --dry=json --filter=@scalius/storefront`.
- `AUTH-001` fixed by mapping Better Auth's `twoFactorVerified` additional session field to the Drizzle schema key instead of the physical SQL column name, allowing `/api/auth/sign-in/email` to create sessions again. The first-admin setup route now releases its KV creation lock in `finally` and can complete a partially-created first user when Better Auth already inserted the account but no admin exists. `/admin` now runs its guard during SSR so unauthenticated requests server-redirect to `/auth/login` instead of hydrating from an admin pending shell into the auth layout. Chart containers now start Recharts with a valid initial size. Verification: `pnpm --filter @scalius/core exec vitest run src/auth/auth.test.ts`, `pnpm --filter @scalius/core typecheck`, `pnpm --filter @scalius/api typecheck`, `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm dev:admin:create`, local browser login to `/admin`, unauthenticated `curl -i http://localhost:4323/admin` returning `307 /auth/login`, `pnpm run deploy`, live API health, live dashboard browser login-page check, and live storefront browser homepage check.
- `DEV-002` fixed by adding real lint scripts to admin, api-client, core, database, and shared, registering React Hooks linting for `.ts` helper files, and filtering root lint to the seven code workspaces. Verification: `pnpm lint`.
- `DEV-003` fixed by preserving the original failing exit code in `scripts/dev.sh` cleanup, rejecting valueless CLI options before script side effects, proving `dev:admin:reset` can reach the API before deleting local auth tables, repairing missing build-time env keys in `dev:setup --env-only`, and teaching `dev:doctor` to validate those build-time env keys. Verification: `bash -n scripts/dev.sh`, `node --check` for the changed dev scripts, focused Vitest for dev-admin/dev-doctor/dev-local-utils, direct missing-value smoke tests, and `pnpm dev:doctor`.
- `BUILD-001` fixed by adding `public/**`, workspace `scripts/**`, Wrangler config files, and root config/script globals to Turbo task inputs. Verification: storefront build dry-run includes generated flags, `scripts/generate-build-id.js`, `wrangler.jsonc`, and root `scripts/copy-flags.mjs`.
- `BUILD-002` fixed by declaring `country-flag-icons` directly and making `scripts/copy-flags.mjs` fail on missing/incomplete flag assets. Verification: `node scripts/copy-flags.mjs` copied 265 SVGs into both app public directories.
- `ADMIN-002` fixed by centralizing admin shell access in `apps/admin-v2/src/lib/admin-access.ts`, removing legacy-role-only access from `loadUserPermissions()`, preserving real null roles in the guard context, and redirecting permissionless admin children to `/admin/access-denied`. Verification: `pnpm exec vitest run apps/admin-v2/src/lib/admin-access.test.ts apps/admin-v2/src/routes/api/scanner-token.test.tsx`, `pnpm --filter @scalius/api test -- src/middleware/admin-auth.test.ts`, `pnpm --filter @scalius/admin-v2 typecheck`, and root `pnpm test`.
- `ADMIN-003` fixed by adding `loaderDeps` to search-driven admin list routes and prefetching from validated URL-search deps instead of module defaults. Verification: `pnpm --filter @scalius/admin-v2 typecheck` and root `pnpm test`.
- `STORE-003`, `STORE-004`, `CONTENT-001`, and `CONTENT-002` fixed by preserving redirect-gateway cart/session state until the protected order-success page loads, adding a `LocationSelector` prefill event API for saved customer city/zone/area data, enforcing a shared `publishedAt <= unixepoch()` public page predicate, removing `/cart` from the static sitemap, and passing `noindex` on cart. The cart-preservation detail is historical and is superseded for SSLCommerz/Polar by `CHECKOUT-011` receipt/order-path recovery after durable hosted order commits. Verification: `pnpm --filter @scalius/storefront exec vitest run src/lib/checkout/render-summary.test.ts src/lib/seo-regressions.test.ts src/components/LocationSelector.test.ts`, `pnpm --filter @scalius/core test -- src/modules/pages/pages.service.test.ts`, and `pnpm --filter @scalius/storefront typecheck`.
- `STORE-002`, `CONTRACT-001`, and `CONTRACT-002` fixed by removing browser fallback to nonexistent same-origin `/api/v1`, routing AuthModal/search calls through the shared API URL helper, deleting the stale storefront `/discounts/usage` call, and aliasing `CreateOrderPayload` to the generated SDK `OrderPostRequest`. Verification: `pnpm --filter @scalius/storefront exec vitest run src/lib/api/client-url-policy.test.ts src/lib/checkout/render-summary.test.ts`, `pnpm --filter @scalius/storefront typecheck`, `pnpm --filter @scalius/api-client typecheck`, `pnpm --filter @scalius/api test -- src/routes/orders-create.test.ts`, `pnpm --filter @scalius/core test -- src/modules/orders/orders.queue.test.ts`, and no `discounts/usage`/`recordDiscountUsage()` matches under app/package code.
- `CONTRACT-003` fixed by centralizing API timestamp OpenAPI helpers, normalizing malformed nullable `anyOf` branches in the API-client spec generator before `openapi-ts`, and regenerating the SDK. Generated timestamp fields now use `string | number | null` instead of `string | number | unknown`; the OpenAPI surface at that slice was 257 paths / 355 operations. Verification: `pnpm generate:sdk`, `pnpm --filter @scalius/api typecheck`, `pnpm --filter @scalius/api-client typecheck`, `pnpm --filter @scalius/storefront typecheck`, and an `rg` scan for weak timestamp `unknown` unions.
- `NOTIF-001`, `NOTIF-002`, and `CONF-001` fixed by centralizing `ORDER_NOTIFICATION_TYPES` in core, using that type in order status notifications and API queue messages, rendering all nine customer/admin notification statuses in the admin settings UI, merging notification-channel defaults on read/save, passing the runtime encryption key into SMS notification provider resolution, and changing `getEncryptionKey()` to prefer `CREDENTIAL_ENCRYPTION_KEY` before JWT legacy fallback. SMS secret writes now require the dedicated credential key when new secrets are submitted. Verification: `pnpm --filter @scalius/api test -- src/queue-consumer.test.ts src/utils/encryption-key.test.ts`, `pnpm --filter @scalius/core test -- src/modules/notifications/notifications.service.test.ts`, `pnpm --filter @scalius/core typecheck`, `pnpm --filter @scalius/api typecheck`, and `pnpm --filter @scalius/admin-v2 typecheck`.
- `NOTIF-004` fixed the order-notification channel retry gap by adding `order_notification_delivery_receipts`, claiming one receipt per logical channel target before provider calls, storing accepted/skipped/failed outcomes, keeping parent outbox rows retryable while any enabled target is retryable, threading receipt keys into Resend and GenNet where the providers expose idempotency surfaces, and preserving Cloudflare Email/FCM provider receipt IDs for audit/skip-on-retry. Remaining follow-up: `auth.send_otp` does not yet use this receipt table, and order WhatsApp is still a skipped/not-implemented receipt. Verification: focused Vitest for notifications, queue consumer, email selection, GenNet, FCM, and receipt key helpers; database migration metadata; core/API/database typechecks.
- `CLEAN-001` fixed by removing `apps/admin-v2/src/routes/.DS_Store`; root `.gitignore` already ignores `.DS_Store`. Verification: `find apps/admin-v2/src/routes -name .DS_Store -print` returns no files.
- `DB-001` fixed by adding `packages/database/scripts/check-migration-metadata.mjs`, which verifies SQL migrations, `_journal.json`, snapshots, and an explicit allowlist for the 15 manual migrations without snapshots (`0019`-`0026`, `0030`, `0031`, `0036`-`0040`). Verification: `pnpm --filter @scalius/database check:migrations`, `pnpm exec drizzle-kit check --config packages/database/drizzle.config.ts`, and `pnpm --filter @scalius/database typecheck`.
- `PLAT-001` fixed by adding `scripts/check-worker-env.mjs` and root `pnpm check:env`, making Wrangler binding/var names the checked source of truth for API, admin, and storefront Worker `Env` declarations. Removed stale API/admin `EMAIL` declarations, removed stale API `ASSETS` from `hono-env.d.ts`, and removed the unused API `SESSION` KV binding from production/local API Wrangler configs. Verification: `pnpm check:env`, `node --check scripts/check-worker-env.mjs`, `pnpm --filter @scalius/api typecheck`, `pnpm --filter @scalius/admin-v2 typecheck`, and `pnpm --filter @scalius/storefront typecheck`.
- Historical broad checkpoint verification after the platform/env guard: `pnpm typecheck`, `pnpm lint`, and `pnpm test` passed. Current root test counts are recorded in the baseline above.
- `ADMIN-001` first slice extracted cache server functions from `apps/admin-v2/src/lib/api.functions.ts` into `apps/admin-v2/src/lib/api-functions/cache.ts`, with no file-level `@ts-nocheck` in the extracted module. Verification: `pnpm --filter @scalius/admin-v2 typecheck`.
- `ADMIN-001` second slice extracted analytics-script CRUD/toggle functions from the legacy barrel into `apps/admin-v2/src/lib/api-functions/analytics.ts`, using explicit input/payload types and the existing `AnalyticsScript` response type. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, and stale-reference scans for analytics exports in `api.functions.ts`.
- `ADMIN-001` third slice extracted navigation item/preview server functions from the legacy barrel into `apps/admin-v2/src/lib/api-functions/navigation.ts`, replacing the previous loose navigation payload casts with typed responses.
- `ADMIN-005` fixed by adding `GET /api/v1/admin/navigation/preview-products`, delegating count logic to storefront product filtering through `getNavigationPreviewProductCount()`, validating public category existence, ignoring reserved query keys when building attribute filters, enforcing exact `PRODUCTS_VIEW` RBAC, and regenerating the SDK. Verification: `pnpm --filter @scalius/api test -- src/routes/admin/navigation.test.ts src/middleware/admin-auth.test.ts`, `pnpm --filter @scalius/core test -- src/modules/navigation/navigation.service.test.ts`, `pnpm generate:sdk`, and focused typechecks.
- `ADMIN-001` fourth slice extracted fraud-checker provider/test/lookup server functions from the legacy barrel into `apps/admin-v2/src/lib/api-functions/fraud-checker.ts`, replacing loose component casts with typed provider/test/lookup payloads. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm vitest run packages/core/src/modules/fraud-checker/provider.test.ts --passWithNoTests`, `git diff --check`, and stale-reference scans for fraud-checker exports in `api.functions.ts`.
- `ADMIN-001` fifth slice extracted abandoned-checkout delete into `apps/admin-v2/src/lib/api-functions/abandoned-checkouts.ts`, removed the dormant abandoned-checkout list RPC so the active list stays on the intentional browser proxy path, and aligned the admin `AbandonedCheckout` type/rendering with nullable API `checkoutId`. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `git diff --check`, and stale-reference scans for abandoned-checkout exports in `api.functions.ts`.
- `ADMIN-001` sixth slice extracted RBAC role/permission server functions into `apps/admin-v2/src/lib/api-functions/rbac.ts`, removed loose RBAC casts from role and user-permission admin UI, fixed permission override writes to send `{ permission, granted }` instead of `{ permissionId }`, and fixed account settings to read RBAC permissions from the unwrapped `{ permissions, grouped }` response. This also verifies `ADMIN-006`. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm exec vitest run apps/admin-v2/src/lib/admin-access.test.ts apps/admin-v2/src/routes/api/scanner-token.test.tsx apps/api/src/middleware/admin-auth.test.ts --passWithNoTests`, and stale-reference scans for RBAC exports in `api.functions.ts`.
- `ADMIN-001` seventh slice extracted admin auth/users/profile/2FA/setup server functions into `apps/admin-v2/src/lib/api-functions/auth-management.ts`, removed loose admin-user and 2FA casts, narrowed 2FA method writes to `totp | email`, exposed create-user `emailFailed` to the UI, and changed account settings to reuse the parent admin route's effective permission context instead of all RBAC permission definitions. This also verifies `ADMIN-007` and `ADMIN-008`. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm --filter @scalius/api test -- src/middleware/admin-auth.test.ts`, `pnpm exec vitest run apps/admin-v2/src/lib/admin-access.test.ts apps/admin-v2/src/routes/api/scanner-token.test.tsx --passWithNoTests`, `git diff --check`, and stale-reference scans for auth/setup exports in `api.functions.ts`.
- `ADMIN-001` eighth slice extracted the generic/simple admin settings server functions into `apps/admin-v2/src/lib/api-functions/settings.ts`, using JSON-serializable server-function payload types instead of generated `unknown` index signatures. Allowed-country and notification-channel writes now call the API's PUT routes instead of the stale POST wrappers. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `git diff --check`, and stale-reference scans for extracted settings exports/imports.
- `ADMIN-001` ninth slice extracted admin shipping-method server functions into `apps/admin-v2/src/lib/api-functions/shipping-methods.ts`, with typed list pagination including `hasNextPage`/`hasPrevPage` and numeric/null timestamp types matching the API route schema. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `git diff --check`, and stale-reference scans for extracted shipping-method exports/imports.
- `ADMIN-001` tenth slice extracted admin checkout-language server functions into `apps/admin-v2/src/lib/api-functions/checkout-languages.ts`, replaced the hook's stale `Date`-based local API type with route-shaped timestamp types, and made `languageData`/`fieldVisibility` parsing fall back to defaults instead of crashing on malformed JSON. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `git diff --check`, and stale-reference scans for extracted checkout-language exports/imports.
- `ADMIN-001` eleventh slice extracted admin delivery provider/location server functions into `apps/admin-v2/src/lib/api-functions/delivery.ts`, fixed provider updates to call the real root `PUT /settings/delivery-providers`, replaced the nonexistent locations `GET /all` wrapper with paginated list reads, and made clean-all send `{ confirmDeleteAll: true }`. Delivery provider timestamp types now match serialized API responses, and Pathao import progress is exported from the hook boundary. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `git diff --check`, and stale-reference/route scans for extracted delivery exports/imports.
- `ADMIN-001` twelfth slice extracted header/footer config saves into `apps/admin-v2/src/lib/api-functions/settings.ts` and hero-slider CRUD into `apps/admin-v2/src/lib/api-functions/hero-sliders.ts`, replacing `any`/`unknown[]` wrappers with route-shaped config and slider types. Hero slider updates are narrowed to `images`/`isActive` so admin UI does not expose the backend's conflict-prone `type` update path. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `git diff --check`, and stale-reference/route scans for extracted config/hero-slider exports/imports.
- `ADMIN-001` thirteenth slice extracted AI prompt/context server functions into `apps/admin-v2/src/lib/api-functions/ai.ts` and the public Firebase config server function into `apps/admin-v2/src/lib/api-functions/firebase.ts`. The AI context response now uses the route-shaped product/category/collection/warning DTO instead of `unknown[]`, prompt fetching stays on text responses, and Firebase public config normalizes unknown API values to string config fields before client initialization. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm exec vitest run apps/admin-v2/src/components/admin/widgets/widget-form/ai-context-warnings.test.ts --passWithNoTests`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `git diff --check`, and stale-reference scans for extracted AI/Firebase exports/imports.
- `ADMIN-001` fourteenth slice extracted dashboard data into `apps/admin-v2/src/lib/api-functions/dashboard.ts` and inventory overview/stock adjustment functions into `apps/admin-v2/src/lib/api-functions/inventory.ts`. Dashboard no longer uses generated SDK types with `unknown` index signatures in a server-function return, inventory query results are typed in `InventoryManager`, and the legacy `lowStock` query option now maps to the backend's real `status=low` filter. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `git diff --check`, and stale-reference scans for extracted dashboard/inventory exports/imports.
- `ADMIN-001` fifteenth slice extracted media server functions into `apps/admin-v2/src/lib/api-functions/media.ts`, normalized media DTO timestamps in the media manager client, preserved upload partial-success warnings returned with HTTP 200, and fixed root-folder filtering so `Uncategorized` does not collapse into all files on search/sort/type changes. It also implemented the RBAC-advertised `PUT /api/v1/admin/media/folders/{id}` rename route, corrected the upload OpenAPI response schema, regenerated the API client, refreshed `AGENTS.md` generated-spec guidance, and refreshed `packages/core/src/modules/media/README.md`. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/api typecheck`, `pnpm --filter @scalius/core typecheck`, `pnpm --filter @scalius/api-client typecheck`, `pnpm --filter @scalius/storefront typecheck`, `pnpm generate:sdk` against a live local API worker, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `git diff --check`, and stale-reference scans for extracted media exports/imports.
- `ADMIN-001` sixteenth slice extracted admin attribute server functions into `apps/admin-v2/src/lib/api-functions/attributes.ts`, replacing array/`Record<string, unknown>` casts with route-shaped list/value DTOs in query wrappers, attribute manager dialogs, product-form attribute selection, and navigation dynamic-filter setup. The stale unused `updateAttributeValues` hook/wrapper was removed instead of preserving a broken `{ values }` call to the single-value `POST /values` endpoint. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `git diff --check`, and stale-reference scans for extracted attribute exports/imports.
- `ADMIN-001` seventeenth slice extracted admin page and discount server functions into `apps/admin-v2/src/lib/api-functions/pages.ts` and `apps/admin-v2/src/lib/api-functions/discounts.ts`. Page form submission now serializes `publishedAt`/featured-image timestamps through typed route-shaped payloads, page edit maps API DTOs back into form values explicitly, discount mutation hooks serialize date payloads in one place, and the visible admin discount type filter now reaches the API/core `listDiscounts()` query instead of being a no-op query-key change. Regenerated the API client after adding the `type` query parameter. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/api typecheck`, `pnpm --filter @scalius/core typecheck`, `pnpm --filter @scalius/api-client typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm --filter @scalius/api test -- src/routes/admin/discounts.test.ts`, `pnpm generate:sdk` against a live local API worker, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `git diff --check`, OpenAPI count check, and stale-reference scans for extracted page/discount exports/imports.
- `ADMIN-001` eighteenth slice extracted admin category and collection server functions into `apps/admin-v2/src/lib/api-functions/categories.ts` and `apps/admin-v2/src/lib/api-functions/collections.ts`. Category form submission now strips UI-only fields and serializes image dates before crossing the server-function boundary, update calls strip path IDs from JSON bodies, category detail OpenAPI now matches the actual route shape, category list max-limit behavior matches the documented 500 selector cap, and collection form options now include `product.categoryId` so category-filtered product selection works. Regenerated the API client after the contract fixes. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/api typecheck`, `pnpm --filter @scalius/core typecheck`, `pnpm --filter @scalius/api-client typecheck`, `pnpm generate:sdk` against a live local API worker, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `git diff --check`, OpenAPI count check, and stale-reference scans for extracted category/collection exports/imports.
- `ADMIN-001` nineteenth slice extracted admin product and product-variant server functions into `apps/admin-v2/src/lib/api-functions/products.ts`. Product form submission now constructs a route-shaped payload instead of spreading UI-only form fields, variant list queries use the real `{ variants }` API payload, product image detail reads the API's `alt` field, and product detail OpenAPI now documents returned `discountType`/`discountAmount`. Regenerated the API client after the contract fix. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/api typecheck`, `pnpm --filter @scalius/api-client typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm generate:sdk` against a live local API worker, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `git diff --check`, OpenAPI count check, and stale-reference/count scans for extracted product/variant exports/imports.
- `ADMIN-001` twentieth slice extracted admin order, payment, COD, and shipment server functions into `apps/admin-v2/src/lib/api-functions/orders.ts`. Order create/update now send route-shaped payloads instead of UI-only form fields, order detail builds an explicit `OrderView` model instead of casting API DTOs, shipment refresh/list/create calls no longer rely on `unknown` casts, and the stale unused `updateShipment` wrapper for nonexistent `PUT /admin/shipments/{id}` was deleted. The API OpenAPI schemas for order payments, payment plans, and COD tracking now document the fields actually returned by the routes, and the SDK was regenerated against a live local API worker. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/api typecheck`, `pnpm --filter @scalius/api-client typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm --filter @scalius/api lint`, `pnpm generate:sdk` against a live local API worker, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `git diff --check`, OpenAPI count check, and stale-reference/count scans for extracted order/shipment exports/imports.
- `ADMIN-001` twenty-first slice extracted admin customer server functions into `apps/admin-v2/src/lib/api-functions/customers.ts`. Customer create/update now send route-shaped payloads instead of UI-only location display fields, the dead `deleteCustomerPermanent` alias was removed, customer history routes consume typed data without `Record<string, unknown>` casts, and the customer create/detail/history OpenAPI schemas now match the runtime payloads. Regenerated the API client after the contract fixes. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/api typecheck`, `pnpm --filter @scalius/api-client typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm --filter @scalius/api lint`, `pnpm --filter @scalius/api-client lint`, `pnpm generate:sdk` against a live local API worker, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `git diff --check`, OpenAPI count check, and stale-reference/count scans for extracted customer exports/imports.
- `ADMIN-001` twenty-second slice extracted the final widget server functions into `apps/admin-v2/src/lib/api-functions/widgets.ts` and removed `apps/admin-v2/src/lib/api.functions.ts`. Widget create/update/history/placement-target calls now use generated SDK request/response types, update strips the path `id` out of the JSON body, widget history timestamps match the generated `string | number` shape, and widget OpenAPI response schemas no longer add unnecessary generated index signatures. Regenerated the API client after the schema cleanup. Verification: `pnpm --filter @scalius/admin-v2 typecheck`, `pnpm --filter @scalius/api typecheck`, `pnpm --filter @scalius/api-client typecheck`, `pnpm --filter @scalius/admin-v2 lint`, `pnpm --filter @scalius/api lint`, `pnpm --filter @scalius/api-client lint`, `pnpm generate:sdk` against a live local API worker, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `git diff --check`, and OpenAPI count check.
- `UI-001` fixed by keeping `DashboardChart` from mounting Recharts until the client is mounted and non-empty daily activity exists; empty dashboards render a fixed-height empty state, and chart containers keep stable dimensions. Verification: `pnpm exec vitest run apps/admin-v2/src/components/admin/dashboard-chart-data.test.ts --passWithNoTests` and `pnpm --filter @scalius/admin-v2 typecheck`.
- `DOC-001` fixed by removing volatile generated SDK counts from `packages/api-client/README.md`, documenting the bundled generated Fetch client instead of the deprecated `@hey-api/client-fetch` runtime package, removing fragile database column counts from `packages/database/README.md`, adding the `widgetPlacements` table, updating migration notes through `0041`, and removing a stale singleton-constraint limitation. Verification: stale-count/dependency scans over `packages/api-client/README.md` and `packages/database/README.md` return no matches.
- `ORDER-008` fixed by adding nullable `orders.shipmentClaimId` / `orders.shipmentClaimExpiresAt` columns and migration `0038_order_shipment_claim.sql`, acquiring the order claim before provider shipment calls, linking the claim id to the insert-first `delivery_shipments` row, clearing the claim only after final shipped state succeeds, and marking shipments `reconcile_required` while keeping the order claim active when provider success cannot be finalized locally. Admin order/status/COD/fulfillment/edit/delete/refund/payment-session/shipment-refresh paths now reject active claims; cleanup skips active claims; payment and delivery webhook/queue paths surface retryable failures. Verification: `pnpm --filter @scalius/core exec vitest run src/modules/orders/orders.fulfillment.test.ts src/modules/delivery/tracking.test.ts src/modules/payments/process-payment.test.ts`, `pnpm --filter @scalius/api exec vitest run src/routes/payment/payment-session.test.ts`, `pnpm exec vitest run tests/unit/core/orders/update-order-atomicity.test.ts`, `pnpm --filter @scalius/database check:migrations`, `pnpm exec drizzle-kit check --config packages/database/drizzle.config.ts`, `pnpm --filter @scalius/database typecheck`, `pnpm --filter @scalius/core typecheck`, `pnpm --filter @scalius/api typecheck`, `pnpm typecheck`, and `git diff --check`.
- `ORDER-009` fixed by making admin full order edits fail closed on reserved/deducted negative-delta failures, moving release/restore deltas before order item replacement, sharing the central shipped/delivered inventory predicate, batching item delete+insert so insert failure cannot erase old item context, and compensating pre-write inventory deltas if later writes fail. Verification: `pnpm exec vitest run tests/unit/core/orders/update-order-atomicity.test.ts`, `pnpm --filter @scalius/core exec vitest run src/modules/orders/orders.fulfillment.test.ts src/modules/delivery/tracking.test.ts src/modules/payments/process-payment.test.ts`, and `pnpm --filter @scalius/core typecheck`.
- `ORDER-010` fixed by removing the shared-batch-failure pattern that released all reservations best-effort and then re-reserved each isolated order. The fallback now re-checks whether the shared D1 batch actually committed an order, reuses the reservation already acquired for isolated replay, releases only that order's original reservation if isolated writes fail, treats `releaseMultiple()` returning `success: false` as rollback failure, and fails checkout closed for manual reconciliation instead of retrying into a second reservation when release is uncertain. Verification: `pnpm --filter @scalius/core test -- src/modules/orders/orders.queue.test.ts`, `pnpm exec vitest run tests/unit/core/orders/order-ingest-queue.test.ts`, `pnpm exec vitest run tests/unit/core/payments/process-payment.test.ts`, `pnpm --filter @scalius/core typecheck`, `pnpm --filter @scalius/core lint`, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test`, `pnpm run deploy:api` to API version `b361f707-6450-42f0-9f88-a80d0b799d14`, live HTTP checks for API/dashboard/storefront, and browser checks for dashboard/storefront with no console errors.
- `ORDER-011` fixed by making `restoreOrder()` apply an explicit trash-restore inventory matrix instead of blindly re-reserving every `inventoryAction = "restored"` order. Incomplete/pending/processing/confirmed orders re-reserve variant inventory and become `reserved`, or become `none` when no variant inventory exists; cancelled/returned/refunded stay `restored`; invalid existing `reserved`/`deducted` pairs reject; shipped/delivered/completed/partially-refunded restored orders reject until deliberate inventory/status reconciliation is performed. Successful re-reservations are released if the final restore CAS fails. `buildInventoryStatements()` now re-reserves only `isStockReservableStatus()` statuses. Verification: `pnpm exec vitest run tests/unit/core/orders/update-order-atomicity.test.ts`, `pnpm exec vitest run tests/unit/core/inventory/reserve-deduct-release.test.ts`, `pnpm --filter @scalius/core typecheck`, `pnpm --filter @scalius/core lint`, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` at the then-current suite size, `pnpm run deploy:api` to API version `c36bc4ca-bccf-4276-9be9-5c0f86e562ea`, live HTTP checks for API/dashboard/storefront, direct demo email sign-in/session check, and browser checks for dashboard orders/storefront with no console errors.
- `DEL-002` fixed by centralizing shipment-delete safety in `deleteShipmentRecord()`: `creating` and `reconcile_required` rows reject, any active order shipment claim rejects, expired matching claims reject unless the shipment is failed/cancelled, and stale failed/cancelled matching claims are cleared before the shipment row is deleted. Verification: `pnpm --filter @scalius/core exec vitest run src/modules/delivery/delivery.service.test.ts`, `pnpm --filter @scalius/core typecheck`, `pnpm --filter @scalius/core lint`, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` at the then-current suite size, `pnpm run deploy:api` to API version `5a206ef1-adf4-42f3-bcab-ffe13c7d1e40`, live HTTP checks for API/dashboard/storefront, direct demo email sign-in/session check, and browser checks for dashboard orders/storefront with no console errors.

- `PERF-003` rich-text UX follow-up removes the extra manual Edit button from deferred rich-text fields. `DeferredTiptapEditor` still keeps Tiptap in a lazy chunk, requests the real editor immediately after field hydration, and autofocuses only after direct click/focus. This affects product descriptions, category/page/footer content, and additional-info sections. Verification: local Chrome screenshots for product, category, page, footer, and mobile product forms showed no manual edit button, mounted editor surfaces, and no horizontal overflow; `pnpm --filter @scalius/admin-v2 typecheck`; `pnpm --filter @scalius/admin-v2 lint`; `pnpm --filter @scalius/admin-v2 build`; focused route-graph boundary test (`22` tests); `pnpm check:env`; `pnpm check:dist-secrets`; production dist marker scan proving old `Edit rich text content`/`PencilLine` markers were absent; and `git diff --check`.
- `CHECKOUT-039` fixed the multi-issue stale-cart repair gap. Cart validation summary copy now keeps the server/cart handoff message for one validation pass only, renders bulk repair buttons for removable, reducible, and price-refreshable issue groups, and clears repaired line warnings immediately while final validation stays authoritative. COD-only server failures now preserve all structured `details.itemIssues` with the original browser cart keys, and `/cart` can consume a safe inline `codCartRepairState` JSON island when the failure happened during server-side POST handling. Verification: focused cart/client/server/repair-state tests (`3` files / `16` tests), expanded checkout/cart tests (`7` files / `33` tests), storefront typecheck/lint/build, root typecheck/lint/test (`260` files / `1659` tests)/build, env/dist/migration/diff guards, local D1-backed cart-validation smoke returning remove/reduce/refresh actions, local screenshots at `/tmp/scalius-cart-repair-20260622/cart-repair-desktop.png` and `/tmp/scalius-cart-repair-20260622/cart-repair-mobile.png`, storefront deploy `1b0f4231-c971-4aaa-8f35-4530c1afdeff`, live `/cart` `200`, and deployed asset markers for bulk actions plus `codCartRepairState`.
- `CHECKOUT-015` fixed the browser-only allowed-country policy gap. `@scalius/shared/customer-utils` now centralizes include/exclude phone-country policy checks; public checkout config reuses `getAllowedCountries()` instead of duplicating parser logic; API checkout enforces the policy after replay/cart/delivery preflight and before gateway/session/rate/claim/order work; customer auth enforces it on OTP send, OTP verify pinned contacts, and profile completion. Verification: focused shared/API/core tests, affected typecheck/lint, root typecheck/lint/test/build, env/dist/migration/diff guards, API deploy `421f8b05-0413-491e-894c-9b500319e1a2`, live `POST /api/v1/customer-auth/send-otp` with French phone returned `400 VALIDATION_ERROR`, and live storefront checkout proxy returned `400` with the same country-policy message before order creation.
- `AUTH-022` fixed checkout account ownership under guest-enabled stores. API order policy now treats a forwarded customer session as authoritative even when guests are allowed, rejects signed-in phone mismatches, and returns `CUSTOMER_SESSION_STALE` for expired sessions so the storefront proxy clears `cs_tok`/`cs_auth` before retry. Storefront order building no longer resolves customers from submitted phone; authenticated payloads carry the session `customerId`, true guest payloads keep `existingCustomer: null`, and commit fresh-reads the active customer before setting `orders.customerId`. Verification: focused API/core/storefront tests, affected workspace typecheck/lint, root typecheck/lint/test (`264` files / `1689` tests)/build, env/dist/migration/diff guards, API deploy `38d80904-2f64-4830-b155-adb94a4d6c48`, storefront deploy `380a1160-4b7c-4a18-ae9c-8dc5cd546160`, live API health `200`, and live storefront home/cart `200`.

- `STORE-012` removes storefront route-test files from Astro's production route tree. Logout, checkout order, cart-validation, cache-purge, and buy-now tests now live under `apps/storefront/src/lib/route-tests/**`, so `.test` pages and Vitest helpers cannot be bundled as customer-facing Worker routes. Verification: route-tree scan for `test|spec|boundaries`, moved route tests (`5` files / `27` tests), storefront typecheck/lint/build, root typecheck/lint/test (`267` files / `1719` tests)/build, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, storefront dist scans for `.test` route entries, `vitest`, `route-tests`, and boundary-test markers, storefront deploy `09621a14-a939-47db-9627-0f644ea5c84d`, live old `.test` API route URLs returning `404`, live product detail flags for simple/optioned products, live cart-validation valid/stale-line smokes, live optioned quick-buy `variant_required` redirect, live login form POST/no-credential-marker check, and browser DOM/console smokes for storefront home/product/cart plus authenticated admin product edit.
- `CACHE-019` is verified and deployed. Product and discount writes now invalidate the `api:categories:` cache family through the existing `products` invalidation group, so rewarming `/categories/{slug}` cannot refill storefront HTML from stale `/api/v1/categories/{slug}/products` KV data after a product/discount catalog write. The fix intentionally avoids adding the full `categories` group, keeping navigation/layout/category-metadata purges out of product writes while still clearing the product-list API source. Storefront source-boundary tests now use cwd-aware source paths so both repo-root and package-filtered Vitest commands cover the same files. Verification: focused API cache tests (`2` files / `45` tests), focused storefront cache/page-boundary tests in both package and root modes (`3` files / `28` tests; `7` files / `33` tests), root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`267` files / `1720` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, API deploy `9c52d40a-d0f4-417a-a06d-d1bf12ff9576`, live API health `200`, live category-products API `200` with repeat `x-cache: HIT`, and live storefront category/product pages `200` without error shells.

## Local Dev Verification Notes

2026-06-29:

- `ADMIN-029` is verified and deployed. Admin order detail hydration drift came from locale-sensitive amount/timestamp rendering, a render-time relative shipment timestamp, and optional payment/COD/notification/provider reads that could populate between SSR dehydration and the browser's first render. The order detail path now uses deterministic `Asia/Dhaka` order formatters, removes render-time relative dates and blanket hydration suppression from the render tree, and gates optional panels through `useHydrated()` with card-local loading/error states. Verification: focused admin order route tests, admin typecheck/lint, local fresh-tab `/admin/orders/SNJU2N?hydration=timestamp-filtered` browser smoke with no new console errors, root typecheck/lint/test/build/env/dist-secret/diff guards, admin deploy `abb3cb5f-30e0-4e18-9c03-9f2648ffa028`, and live authenticated `/admin/orders/DW8W05?hydration=admin029` smoke with no new console errors and visible COD/notification panels.
- `NOTIF-009` is verified and deployed. Merchant setup failures in order notification dispatch now stop retrying until settings change: missing email/SMS providers, credential decrypt/auth/token/sender/account/balance blockers, status-only provider setup responses such as Resend/SMS `401`/`405`, and Firebase service-account failures become skipped receipts, while transient provider/network failures stay retryable. FCM throws after receipt claims now settle every claimed push receipt immediately. Admin order detail now groups duplicate receipt rows and renders merchant-facing issue summaries instead of raw provider strings. Verification: root gates passed, API deployed `b8ab2f35-c840-4936-b7ac-8b64320332b2`, admin deployed `3801b15a-8781-439e-bdc5-bb881625724a`, local order-detail smoke showed `Delivery settled` with zero console warnings/errors, live `DW8W05` D1 proof showed two terminal sent outboxes plus the historical SMS.net.bd `56`-attempt skipped receipt, live Chrome smoke showed `10 admin devices`, `Stopped after 56 attempts`, no `delivery_receipt_busy`, no raw `error=405`, and zero console warnings/errors, and live `/admin/settings/notifications` rendered without error-boundary or raw dump.
- `ADMIN-031` rich-content media flicker slice is verified and deployed. The screenshot product `prod_abe_OILKCt75z47ppI1Sd` kept the editor shell stable on cold load, but its transformed shoe image had no intrinsic HTML dimensions and briefly collapsed to `0x0` while Cloudflare image redirect/canonicalization resolved. Shared sanitizer coverage now adds dimensions from Cloudflare image transform URLs, preserves merchant-provided dimensions, and leaves generic/incomplete image URLs alone. Verification: shared sanitizer tests/typecheck, admin typecheck/build, dist-secret and diff checks, admin deploy `a7eb1418-df97-47de-9c4e-9842c0c954fb`, and live Chrome smoke showing `width="600"`, `height="600"`, `0` `.ProseMirror` editors, `70/70` image samples at `283x283`, no zero-size collapse, and zero console warnings/errors.
- `PERF-015` storefront auth runtime slice is verified and deployed. Global `AuthModal` no longer creates guest-side checkout-config or customer-session reads after hydration: it reuses `window.__CHECKOUT_CONFIG__` when a page already fetched checkout config, fetches policy only when the auth modal is actually opened, reads `/me` only when the readable `cs_auth=1` mirror exists, clears that mirror after an authoritative unauthenticated session read, and dedupes concurrent `/me` reads in the customer-auth API helper. Cart SSR now injects its server-fetched checkout config for the modal. Verification: focused auth helper/source tests (`2` files / `24` tests), storefront typecheck/lint/build, root deploy storefront typecheck/build/dist-secret checks, local Chrome smoke proving home/cart guest idle made `0` config and `0` `/me` reads, cart-open made `0` extra config reads, home-open made exactly `1` on-demand config read, storefront deploy `a344e39c-a6fb-4328-b856-7b5ef7fd6a19`, live `/health` 200, warmed critical HTML paths, and live browser smoke with the same network counts plus stale-mirror self-clear proof and zero console/page/request failures.

2026-06-28:

- `ADMIN-020` shipment-sync recovery slice is verified and deployed. `updateOrderStatusFromShipment()` now wraps inventory reconciliation after a carrier-driven order-status CAS: if inventory repair fails, it marks the related shipment `reconcile_required` with `reason = "order_status_inventory_reconcile_failed"` and sanitized `orderStatusSync` metadata; if the carrier webhook/admin status check retries after the order already has the mapped status, successful inventory reconciliation clears the shipment marker back to the carrier shipment status and removes stale recovery metadata. Verification: focused delivery tracking + fulfillment tests (`2` files / `39` tests), core typecheck/lint, root typecheck, root lint, root test (`289` files / `1861` tests), root build, `pnpm check:env`, migration metadata guard, `pnpm check:dist-secrets`, `git diff --check`, API deploy `45adc4bf-444a-431d-bd85-d00e923d4bab`, live API health `200`, readyz `ready`, unauthenticated admin orders `401`, and live OpenAPI shipment status/refresh route smoke. Remaining `ADMIN-020` scope is generic admin status changes plus COD collection/return status+inventory+ledger atomicity.
- `ADMIN-020` generic admin-status rollback slice is verified and deployed. `updateOrderStatus()` now rolls back the buyer/merchant-visible status claim when the order-status CAS wins but inventory reconciliation throws before the order's `inventoryAction` changes. The rollback is guarded by order id, claimed status, claimed version, and previous `inventoryAction`; if inventory already finalized, it leaves the status alone so retry/repair semantics remain intact. Verification: focused fulfillment test (`30` tests), combined delivery tracking + fulfillment tests (`2` files / `40` tests), core typecheck/lint, root typecheck, root lint, root test (`289` files / `1862` tests), root build, `pnpm check:env`, migration metadata guard, `pnpm check:dist-secrets`, `git diff --check`, API deploy `4a571118-13ba-42c8-8c33-d1e63dbf1af3`, live API health `200`, readyz `ready`, unauthenticated admin orders `401`, and live OpenAPI admin status/shipment route smoke.
- `ADMIN-020` return-status rollback slice is verified and deployed. `processReturn()` now uses the shared `rollbackOrderStatusIfInventoryUnchanged()` helper when a `RETURNED` status claim wins but inventory restoration throws before `inventoryAction` changes. The same helper now also backs generic admin status rollback, so future status-before-inventory paths have one boring guard to reuse. Verification: focused refund-validation test (`27` tests), focused fulfillment test (`30` tests), core typecheck/lint, root typecheck, root lint, root test (`289` files / `1863` tests), root build, `pnpm check:env`, migration metadata guard, `pnpm check:dist-secrets`, `git diff --check`, API deploy `1aaa87ea-3646-403d-92fa-c79f9e096a83`, live API health `200`, readyz `ready`, unauthenticated admin orders `401`, and live OpenAPI admin return/status route smoke.
- `ORDER-019` COD collection recovery slice is verified and deployed. `processCodAction("collected")` now detects existing succeeded COD payment plus collected tracking as retry evidence, skips duplicate collection writes in that state, records COD payment/tracking before delivered inventory reconciliation, and rolls back the delivered status claim if collection evidence or inventory repair fails before `inventoryAction` changes. Verification: focused fulfillment test (`33` tests), COD idempotency test (`7` tests), core typecheck/lint, root typecheck, root lint, root test (`289` files / `1866` tests), root build, `pnpm check:env`, migration metadata guard, `pnpm check:dist-secrets`, `git diff --check`, API deploy `1306c5d2-24d9-4dca-a43a-d41db7717d8d`, live API health `200`, readyz `ready`, unauthenticated admin orders `401`, and live OpenAPI COD route smoke. Remaining `ORDER-019` scope is COD return marker/inventory ordering and recovery semantics.
- `ORDER-019` COD return/tracking completion slice is verified and deployed. `markCODReturned()` now fails closed when no COD tracking row is updated, `recordCODCollection()` fails closed when tracking is missing and no longer treats a succeeded COD payment as idempotent unless collected tracking exists, and `processCodAction("returned")` records the COD return marker before inventory restoration while rolling back the visible returned claim on marker/restoration failure. Verification: focused COD + fulfillment tests (`2` files / `46` tests), core typecheck/lint, root typecheck, root lint, root test (`289` files / `1872` tests), root build, `pnpm check:env`, migration metadata guard, `pnpm check:dist-secrets`, `git diff --check`, API deploy `08274388-bb9e-4d9d-aea7-d76267b54aa2`, live API health `200`, readyz `ready`, unauthenticated admin orders `401`, and live OpenAPI COD route smoke. `ORDER-019` and the COD remainder of `ADMIN-020` are now verified.
- `PAY-008` payment webhook queue-closure slice is deployed. Stripe, SSLCommerz, and Polar now include the source durable `webhookEventId` on payment queue messages; the queue consumer marks the source event `processed` only after side effects finish, `manual_reconciliation` for non-retryable confirmation guard conflicts, and `failed` only on the terminal payment queue delivery attempt. Verification: focused queue/webhook/idempotency tests (`5` files / `48` tests), API typecheck/lint, root typecheck/lint/test (`289` files / `1875` tests), root build, env/migration/dist-secret guards, API deploy `08a55ffc-f56e-49b4-a625-35ee2354528f`, live API health `200`, readyz `ready`, and unauthenticated admin orders `401`. Remaining `PAY-008` scope is stale queued-event sweeps, DLQ recovery, and admin reconciliation surfacing.
- `PAY-008` stale queued-event recovery slice is deployed. Added migration `0065_webhook_events_status_processed_at_index` plus a scheduled six-hour, payment-provider-only queued webhook sweep that marks genuinely stranded rows `failed` in bounded batches, letting provider retries reclaim the event instead of leaving it permanently `queued`. Verification: focused scheduled/webhook/queue tests (`3` files / `31` tests), API/database typecheck/lint, root typecheck/lint/test (`289` files / `1876` tests), root build, env/migration/dist-secret guards, remote migration `0065`, API deploy `e71d221d-a6b2-4336-b12b-3475350faa8a`, live API health `200`, readyz `ready`, unauthenticated admin orders `401`, and remote D1 index proof. Remaining `PAY-008` scope is DLQ recovery and admin reconciliation surfacing.
- `PAY-008` DLQ/admin reconciliation slice closes the tracked webhook terminal-state gap. Wrangler now consumes `payment-events-dlq`; the queue handler routes that queue before normal dispatch, records compact `payment_events_dlq` evidence in `webhook_events` (or a deterministic fallback row for legacy messages), preserves previous result/status, avoids downgrading `processed`/`manual_reconciliation` rows, acks only after the D1 evidence write succeeds, and never replays DLQ messages through payment processors. Admin `GET /orders/{id}/payments` returns sanitized `paymentWebhookIssues`, and the order Payment card renders a refreshable warning for failed/manual webhook states without raw payloads or fake resolution actions. Verification: focused queue/webhook issue/idempotency tests (`3` files / `38` tests), focused admin boundary test (`29` tests), API/admin/api-client typecheck, API/admin lint, SDK regeneration from local OpenAPI (`261` paths), root typecheck/lint/test (`290` files / `1885` tests), root build, env/migration/dist-secret/diff guards, API deploy `ecad6cfa-76fe-4f0d-bff6-8b0b8df8e32f` with live `payment-events-dlq` consumer, admin deploy `356278b8-e17a-4346-a1c4-f154f852ca38`, live API health `200`, readyz `ready`, OpenAPI `paymentWebhookIssues` marker, admin login redirect smoke, and unauthenticated admin payments API `401`.

2026-06-28:

- `NOTIF-006` admin delivery-state slice is deployed. Admin order detail now shows durable notification outbox rows plus channel delivery receipts and can safely retry only failed/pending parent rows through the existing outbox enqueue helper; history reads require `orders.view`, retry requires `orders.edit`, and already-terminal accepted/skipped/sent notifications are not resent. Verification: focused core/API/admin tests, core/API/admin typecheck+lint, root typecheck/lint/test (`295` files / `1944` tests)/build, env/dist/migration/diff guards, API deploy `5d8a0461-44a0-4276-a78f-c78e5c3f4c32`, admin deploy `b69f9603-d329-4b2e-af27-8d2d282c72e1`, live API health/readyz, dashboard order-list redirect smoke, and deployed admin asset markers for notification history/retry code.
- `ARCH-002` is verified and deployed. Scheduled notification outbox flushing now replays parent rows that remain `queued` for more than one hour, using the same outbox enqueue claim and same `outboxId` so delivery receipts keep accepted/skipped targets terminal. The scheduler logs `staleQueued`, and migration `0066` adds `order_notification_outbox_queued_idx` for cheap stale scans. Verification: focused core/API tests, affected core/API/database typecheck+lint, root typecheck/lint/test (`295` files / `1947` tests)/build, env/dist/migration/diff guards, remote migration `0066`, API deploy `2706e20b-6e21-453e-ae8f-7d3e85b642f9`, live API health/readyz, unauthenticated admin API `401`, and remote D1 index proof.

2026-06-24:

- `ADMIN-019` is verified and deployed. Admin order edits now commit scalar order fields, optional new-customer creation, replacement item inserts, and old item deletion inside one guarded D1 `safeBatch()`. The CAS order update remains the authority; replacement writes are guarded with SQLite `changes() = 1` plus committed order-version existence so stale/concurrent edits cannot mutate items, and inventory compensation still runs for pre-write reservations/deductions when the batch fails or CAS misses. Verification: focused update-order atomicity test (`33` tests), surrounding order tests (`5` files / `79` tests), core typecheck/lint, root typecheck, root lint, root test (`289` files / `1859` tests), root build, `pnpm check:env`, migration metadata guard, `pnpm check:dist-secrets`, `git diff --check`, API deploy `bd4ff09f-be8f-4a52-aa49-99740d8448a7`, live health `200`, readyz `ready`, and unauthenticated admin orders `401` fail-closed.
- `ADMIN-017` is verified and deployed. Manual admin order create/update now uses `packages/core/src/modules/orders/delivery-location-validation.ts`, the same active city→zone→area type/parent resolver used by storefront delivery preflight, instead of trusting submitted `cityName`/`zoneName`/`areaName` or falling back to raw IDs. Invalid delivery-location chains reject before customer/order/inventory side effects, and successful writes persist canonical D1 names. Verification: focused admin/storefront/update-order tests (`3` files / `64` tests), core/API typecheck and lint, root typecheck, root lint, root test (`289` files / `1859` tests), root build, `pnpm check:env`, `pnpm --filter @scalius/database check:migrations`, `pnpm check:dist-secrets`, `git diff --check`, API deploy `1f327db4-a571-476f-8a92-5d02989d8643`, live API health `200`, live readyz `ready`, and unauthenticated live admin orders read `401` fail-closed.
- `CHECKOUT-039` follow-up tightened stale-cart repair row identity after an independent read-only audit. Late tracked-inventory reservation failures now carry the original browser `cartKey` through the order ingest payload and structured `itemIssues`, and storefront cart repair/free-delivery reconciliation no longer falls back by array index when a server issue explicitly names a cart key that is no longer in the current cart. Older no-cart-key payloads still fall back by product/variant identity. Verification: focused core order tests (`71` files / `440` tests), focused storefront cart/checkout tests (`4` files / `30` tests), affected core/storefront typecheck and lint, root typecheck, root lint, root test (`289` files / `1853` tests), root build, env/dist-secret/diff guards, API deploy `5aa85b36-19c7-4f96-9078-5b6e2601f08d`, storefront deploy `ff5e3048-c650-453a-831b-cbe5dd95741e`, live API health `200`, live `/cart` `200`, and live storefront `validate-cart` proxy smoke returning a cart-keyed `PRODUCT_UNAVAILABLE` repair issue.
- `PAY-016` fixed split-payment refund allocation. `processRefund()` no longer refunds only the latest succeeded payment; it loads all successful source payments newest-first, subtracts previous source-linked refund rows, claims one refund row per allocation, passes deterministic idempotency/reference metadata to providers, and applies order payment-state changes only for provider-successful allocation amounts behind the refund-claim order version. Stripe full-order split refunds now always pass explicit per-charge amounts instead of relying on Stripe's full-charge shorthand, and SSLCommerz gets deterministic `refund_trans_id` values. Verification: focused refund/payment tests (`5` files / `66` tests), root typecheck, root lint, root test (`285` files / `1828` tests), root build, env/dist/migration/diff guards, API deploy `e596020b-0594-418d-a20d-b2f10a5d9d21`, and live health/readyz smokes. `PAY-009` remains the broader provider-timeout/crash-after-provider-accepts durability gap.
- `PAY-009` fail-closed slice started refund ambiguity hardening. Online provider refund calls now run behind a bounded deadline; if Stripe/SSLCommerz/Polar throws or times out after dispatch starts, unresolved allocation rows remain `pending` with `metadata.providerOutcome = "unknown"` instead of becoming retryable `failed` rows. Completed allocations still update order paid/balance state, and pre-provider/settings failures still mark claims failed. Verification: focused refund tests (`3` files / `38` tests), wider payment/refund tests (`5` files / `67` tests), root typecheck, root lint, root test (`285` files / `1829` tests), root build, env/dist/migration/diff guards, API deploy `ee084153-fb1a-46ac-b725-876e6a4eef1e`, and live health/readyz smokes. Remaining scope: first-class refund-attempt/outbox reconciliation, provider probing, admin mutation blockers, and admin visibility for pending unknown refunds.
- `PAY-009` durable refund-attempt foundation added migration `0064_refund_attempts` and schema `refundAttempts`. `processRefund()` now claims one attempt row beside each refund payment allocation, records request/source/provider identity, blocks new refunds on active attempts, moves attempts through `pending`/`processing`/`provider_unknown`/`refunded`/`failed`, and marks provider-accepted but locally unreconciled allocations as `reconcile_required`. Verification: focused refund/schema tests (`3` files / `16` tests) plus affected refund-validation test (`26` tests), root typecheck, root lint, root test (`285` files / `1831` tests), root build, env/dist/migration/diff guards, local D1 migration proof, remote migration/table proof, API deploy `a9736eb9-34d9-46e0-8c4b-dbf81b8af8c9`, and live health/readyz smokes. Remaining scope: provider probing/reconciliation worker, admin recovery UI/API, and `ORDER-018` mutation blockers for shipment/status/COD/delete/edit races.
- `ORDER-018` active-refund mutation blocker slice added shared `refund-attempt-guard.ts`. Fulfillment status/order status updates, bulk shipping, manual fulfillment shipments, COD collection/return, admin order edit/delete/restore/permanent/bulk delete, shipment-driven order-status sync, and direct fulfillment-status API updates now reject active `refund_attempts` or legacy pending refund rows before order/payment/inventory mutation; COD failure logging stays allowed. Focused coverage exercises the guarded fulfillment, COD, delivery tracking, admin edit/restore, refund-service legacy fallback, and API route-boundary paths. Verification: focused refund/order/delivery/API tests (`5` files / `81` tests), core/API typecheck and lint, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`285` files / `1841` tests), root build, env/dist/migration/diff guards, API deploy `55bb9327-8f65-42fd-8f10-1c49ddf33bf9`, and live health/readyz smokes. Remaining scope: durable provider probing/reconciliation and admin recovery UX/API for `provider_unknown`/`reconcile_required` attempts.
- `PAY-009` scheduled refund reconciliation slice added `refund-reconciliation.ts`, Stripe/Polar provider lookup helpers, refund metadata propagation, and a shared `finalizeAcceptedRefundAttemptIds()` local finalizer used by both normal refunds and scheduled recovery. Scheduled maintenance now claims due refund attempts in bounded batches (`5`), fails expired pre-dispatch attempts, probes Stripe/SSLCommerz/Polar for `provider_unknown`, locally finalizes `reconcile_required`, defers unknown outcomes with probe timestamps, and invalidates product availability caches for finalized orders. Verification: focused reconciler/finalizer/scheduled tests (`3` files / `6` tests), focused refund regression tests (`3` files / `40` tests), root typecheck, root lint, root test (`287` files / `1845` tests), root build, `pnpm check:env`, `pnpm --filter @scalius/database check:migrations`, `pnpm check:dist-secrets`, `git diff --check`, API deploy `a03ad5a2-01e4-4879-aa1a-f881d164d8cd`, live health/readyz, and live OpenAPI refund-route smoke. Remaining scope: admin/customer visibility and recovery UX for pending/manual-review refund attempts.
- `PAY-009`/`ORDER-018` recovery visibility slice added the shared `refund-attempt-visibility.ts` read model, admin order-detail/payment-history refund recovery panels, admin action locks while active refund attempts exist, and customer order-detail refund progress/timeline copy that hides raw `provider_unknown`/`reconcile_required` names, provider payloads, request hashes, idempotency keys, claim fields, and raw metadata. Admin payment history now selects safe columns instead of returning raw `orderPayments.metadata`. Verification: focused visibility/customer API tests, affected core/API/admin/storefront/api-client typechecks, affected lints, SDK regeneration from local OpenAPI, root typecheck/lint/test (`288` files / `1849` tests)/build, `pnpm check:env`, migration metadata guard, `pnpm check:dist-secrets`, `git diff --check`, local admin order-detail browser smoke with no console errors, generated storefront preview account-order smoke, full deploy to API `b318ee81-c518-4cee-a001-2f641ccc9864`, admin `6d7d092e-97e7-4ff4-a326-a771392a3552`, storefront `26356fb4-5d3e-48a8-b259-6e8fd046db47`, live API health/readyz, live OpenAPI schema smoke proving refund fields are present and payment metadata is absent, live admin orders/detail browser smoke, and live storefront account-order browser smoke. Local `astro dev` uncovered separate `DEV-006` React invalid-hook errors after Vite dependency optimization; generated preview did not reproduce them.
- `DEV-006` is verified and deployed to storefront version `cd0cc66c-b926-443f-af74-de4bdd7dcc9a`. Storefront `astro.config.mjs` now keeps React, React DOM, `react-dom/server`, and JSX runtimes in one `resolve.dedupe` singleton list so Vite 8 dev SSR does not split `react-dom/server` from SSR-rendered React islands. A focused source-boundary test guards that list and the storefront README documents the Cloudflare dev-runner nuance: do not put React in `ssr.optimizeDeps.exclude`, because that externalizes `react` through `require()` and makes workerd-style local dev exit before readiness. Verification: focused boundary Vitest, storefront lint/typecheck/build, local `astro dev` `/account/orders/SNJU2N` smoke (`200`, full order-detail shell, no `Invalid hook call`/`useContext`/`require("react")` log signatures), generated `astro preview` `/health` and `/account/orders/SNJU2N` smokes, root typecheck/lint/test (`289` files / `1850` tests)/build, `pnpm check:env`, `pnpm check:dist-secrets`, deploy-time typecheck/build/dist-secret checks, live `/health` 200, live `/account/orders/SNJU2N` 200 with order-detail shell, live `/` and `/search` 200 on build `src-b60c2a68c66a2b01`, and `git diff --check`.
- `PAY-017` fixed order payment-state drift by introducing `packages/core/src/modules/payments/payment-state.ts` as the shared `paidAmount`/`balanceDue`/`paymentStatus` arithmetic helper. Payment confirmation, COD collection, admin manual order create/edit, admin refunds/rollback, and Polar refund webhooks now use the shared state result or restore prior `balanceDue`; COD collection falls back to computed outstanding balance when stored `balanceDue` is stale. Added `PAY-022` for the separate Polar cumulative partial-refund idempotency gap. Verification: focused payment/order tests, core typecheck/lint, root typecheck/lint/test (`284` files / `1822` tests), root build, env/dist/migration/diff guards, API deploy `88d2e4d8-13b8-48c8-931c-04b39285cf12`, live API health/readyz, and live OpenAPI refund-route smoke.

2026-06-23:

- `DEV-004` full deploy verification after the Corepack `pnpm.mjs` resolver fix: `pnpm run deploy` passed root typecheck, root build, dist secret checks, remote D1 migration check (`No migrations to apply`), and deployed API `cfef47b8-52c7-4fdc-927d-46751fa2fe89`, admin `91c5f827-5d5b-4e31-8ff2-e3730d9d71e9`, and storefront `e4153b3a-eb20-41f5-ab5b-794eb07e3564`. Live checks after deploy: API `/api/v1/health` 200, storefront `/health` 200, public checkout config `success: true`, storefront homepage browser render 200 with no console/page errors, dashboard login 200 with POST form/no credential URL leakage, demo admin sign-in reached `/admin`, and account profile edit controls rendered correctly; human-like name editing enabled `Save changes`, `Cancel` restored the original name, and no console/page errors or horizontal overflow were observed.
- `DEV-004` follow-up: plain root Turbo scripts (`pnpm lint`, `pnpm typecheck`, `pnpm build`, migration helpers, and SDK generation) now use `scripts/turbo-run.mjs`, which resolves pnpm and prepends its directory to PATH before invoking `pnpm exec turbo run ...`. This closes the same Corepack `pnpm.mjs` binary-discovery failure for everyday root quality gates, not just deploy/dev wrappers.
- Vite/toolchain freshness follow-up: admin/storefront/root now resolve Vite `8.1.0`; admin uses `@vitejs/plugin-react` `6.0.3`; Cloudflare Vite integration is `@cloudflare/vite-plugin` `1.42.1`; Wrangler is pinned/overridden to `4.103.0`; and all Worker packages use `@cloudflare/workers-types` `4.20260623.1`. The Workers type update required narrowing app cache helpers to a structural `WaitUntilExecutionContext` instead of requiring the full Cloudflare tracing context from Hono route calls. Verification: `pnpm peers check`, `pnpm typecheck`, `pnpm lint`, focused order-route regression (`31` tests), root `pnpm test` (`271` files / `1758` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, and `git diff --check`. Full deploy passed root typecheck/build, dist-secret check, remote D1 migration check (`No migrations to apply`), and deployed API `dad28c88-6021-405f-8a67-19321f8facfe`, admin `5d3d34cc-a9a6-42c5-ae08-b493725f0156`, and storefront `80295ea8-c0d7-4cf9-b86d-3e99fc233b6f` at 100%. Live checks after deploy: API `/api/v1/health` 200, API `/api/v1/readyz` 200 with `status: "ready"` and all D1/KV/R2/DO/queue/runtime checks ok, storefront `/health` 200, and dashboard `/auth/login` 200. Oxfmt/Oxlint research conclusion: keep ESLint and Prettier for now because official Oxc compatibility still says Astro formatting needs Prettier plugin support that Oxfmt does not yet provide; revisit as an additive non-Astro formatting experiment only after a focused parity diff.
- `PERF-012` first runtime slice: `reserveStockBatch()` now loads all reservation-time variant states with one joined `IN (...)` read while preserving active product/non-deleted SKU guards, and API local/remote Wrangler configs lower the legacy `order-ingest` consumer batch size from `100` to `10` to reduce serial Worker invocation blast radius. Verification: focused inventory/order queue/storefront order tests (`3` files / `37` tests), core/API/root typecheck, root lint, root Vitest (`268` files / `1743` tests), root build, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, `pnpm run deploy:api -- --dry-run`, API deploy `7d215190-c7a5-45cc-a431-120d507bc7c6`, live API `/api/v1/health` 200, and live checkout config `success: true` with `activeDefaultMethod: "cod"`. The broad `PERF-012` item remains `In Progress` because durable cache-sweep retry paths, legacy queue retirement after drain proof, and load/perf measurement are still open.

2026-06-13:

- `pnpm dev:admin:status` starts a temporary API worker with `apps/api/wrangler.local.jsonc`, reaches `GET /api/v1/setup`, reports local admin status, and stops without a Cloudflare remote proxy session.
- `pnpm dev:admin` starts API first, then admin. Unauthenticated `http://localhost:4323/admin` returns a server-side `307` to `/auth/login`; authenticated browser login with the default local admin renders `/admin` dashboard data from API `200` responses.
- Disposable full reset verified with `SCALIUS_WRANGLER_STATE=/tmp/scalius-commerce-lite-disposable-state pnpm dev:reset --admin-email disposable@local.test --admin-password 'Disposable123!' --admin-name 'Disposable Admin'`. It applied all then-current migrations to the disposable state path and created the admin through the real `/api/v1/setup` endpoint.
- Disposable admin login verified in the browser: `http://localhost:4323/auth/login` accepted the disposable credentials and rendered `/admin` with dashboard data from API `200` responses.
- Admin local API transport verified after fix: server functions loaded dashboard data through HTTP fallback, and a curl sign-in plus cookie jar request to `http://localhost:4323/api/v1/admin/dashboard` returned `200 OK` with `x-proxy-base-url: http://localhost:8787/api/v1`.
- `pnpm dev:storefront` now applies pending local D1 migrations before API starts. It applied `0037_widget_js_content.sql` to the stale local DB, then started API and storefront.
- Browser check of `http://localhost:4322/` rendered the storefront homepage with no console errors. Direct API check of `http://localhost:8787/api/v1/storefront/homepage` returned 200 after migrations.
- `pnpm dev:admin:create --help`, `pnpm dev:setup --help`, and `pnpm dev:reset --help` print the new local admin options.
- `pnpm dev:setup` now reuses existing shared local secrets when filling missing `.dev.vars` files, writes API `PURGE_TOKEN`/`PURGE_URL`, and fails loudly on existing secret drift instead of silently creating a broken local environment.
- `pnpm dev:setup --env-only` repairs missing or blank local `.dev.vars` and `.env.development` keys without installing dependencies, applying migrations, or creating an admin.
- Local admin/status/reset helpers now avoid irrelevant password validation: `dev:admin:status` works even if `LOCAL_ADMIN_PASSWORD` is invalid, `dev:reset --skip-admin` skips password validation, and validation failures print concise messages instead of Node stack traces.
- `pnpm dev:admin:create/reset/status` apply local migrations before starting a temporary API worker unless `--skip-migrations` is passed. `--api` custom origins now require `--no-start`, and `--state` paths resolve from the repo root.
- `pnpm dev:doctor` is a non-mutating readiness check for repo shape, tooling, env files, local URL values, shared secrets, Wrangler state, ports, and API/admin/storefront responses. On this machine it reports stopped services and the Node-version mismatch as warnings without modifying secrets.
- `pnpm dev:doctor:api`, `pnpm dev:doctor:admin`, `pnpm dev:doctor:storefront`, and `pnpm dev:doctor:all` now wrap `dev:doctor --profile ... --require-running`, so partial local stacks have matching verification commands instead of expected stopped-service warnings.
- `pnpm dev:api` now runs through the same local wrapper as `pnpm dev`, applying local migrations and waiting for API `/api/v1/setup`. Combined admin/storefront modes also wait for API readiness before starting dependents.
- Live `SCALIUS_SKIP_DEV_MIGRATIONS=1 pnpm dev:api` verified the wrapper waits until Wrangler serves `GET /api/v1/setup 200 OK`; direct checks of `/api/v1/setup` and `/api/v1/health` returned successful JSON responses before cleanup stopped the server.
- Local-dev helper regressions now cover valueless option flags, reset-before-delete ordering, build-time env repair expectations, local URL validation without value leakage, doctor service profiles, API-only wrapper startup, API-readiness ordering, and `scripts/dev.sh` preserving failing exit codes through cleanup.
- Empty dashboard chart warning is fixed by rendering a stable empty state until daily activity data exists.
- `CACHE-016` durable purge first slice: API cache invalidation now enqueues normalized `storefront.cache_purge` messages on `STOREFRONT_CACHE_QUEUE` after API KV/fence invalidation, with direct `waitUntil()` purge only as the missing/broken-queue fallback. The queue consumer retries failed storefront purge HTTP responses instead of acking them, and `/readyz` now requires `storefront_cache_queue`, `PURGE_URL`, and `PURGE_TOKEN`. Verification: focused API cache/queue/readiness tests, storefront purge route test, root typecheck/lint/test/build, env/dist/migration/diff guards, created remote `storefront-cache` and `storefront-cache-dlq`, API deploy `f02b3733-b896-4eac-ac77-323d0381509d`, live health/readyz, and queue metadata proving `scalius-api` is attached as producer and consumer. Remaining scope is durable post-purge warming and operator/DLQ replay visibility.
- `OPS-008` first D1 background-budget slice is deployed to API `da70f637-3d10-4a4b-aee2-2a5b5dc637c3`. D1 readiness now has a D1-specific 3s budget while KV/R2 stay at 1.5s, storefront cache queue and DLQ batches are capped at 3, and exact storefront queue warming fetches paths in batches of 2. Verification: focused cache/readiness tests (`2` files / `41` tests), API/root typecheck and lint, root test (`294` files / `1947` tests), root build, env/migration/dist-secret/diff gates, API deploy, live health `200`, direct remote D1 `SELECT 1` sub-millisecond, and one live `/readyz` `200 ready`. A later spaced `/readyz` still hit Cloudflare D1 overload, so `OPS-008` remains open for correlation logs and cron/dashboard/background workload budgeting.
- `OPS-008` scheduled abandoned-checkout cleanup index slice is deployed to API `e426895e-d8ae-45fe-b250-e8101b51376e`. Migration `0068_abandoned_checkout_cleanup_indexes` adds `(created_at, id)` and `(customer_phone, updated_at, id)` indexes, and `cleanupStaleAbandonedCheckouts()` now uses deterministic oldest-first indexed scans plus `customer_phone IS NULL` before JSON empty checks. Verification: core abandoned-checkout cleanup package tests (`72` files / `467` tests), database boundary test (`4` tests), root typecheck/lint/test (`294` files / `1948` tests)/build, env/migration/dist-secret/diff gates, local D1 migration, remote migration, live health `200`, remote index-list proof, and remote `EXPLAIN QUERY PLAN` proof for both cleanup queries. Live `/readyz` still hit Cloudflare D1 overload, so next `OPS-008` work should cap payment/notification/OTP queue concurrency and add correlation logs.
- `OPS-008` queue fan-out budget slice is deployed to API `af99a6af-0366-4ca3-afdf-77a007ab00fb`. `handleQueueBatch()` now processes normal payment/notification/OTP/storefront-cache messages through a bounded all-settled helper capped at 3 concurrent messages per invocation, and payment/storefront DLQ archive paths are capped at 2 concurrent evidence writes. Per-message ack/retry loops remain the only delivery-outcome authority: normal failures still retry after 30 seconds, DLQ evidence failures after 300 seconds, and successful messages are acked. Verification: focused queue consumer tests (`37` tests) including normal/DLQ concurrency assertions, API typecheck/lint, root typecheck/lint/test (`294` files / `1950` tests), root build, env/migration/dist-secret/diff gates, `pnpm run deploy:api`, live API health `200`, live `/readyz` `200 ready` with D1 `143ms`, and unauthenticated admin orders API `401`.
- `OPS-008` background correlation/timing slice is deployed to API `cd929245-e2b1-4270-91eb-30b7630079af`. Queue batches now emit `queue_batch_started` / `queue_batch_completed` with a batch id, queue metrics, concurrency limit, ack/retry counts, attempts, first/last message ids, and duration, while scheduled maintenance emits run-level and per-operation timing/failure events with cron metadata. The generic queue-event privacy regression asserts OTP identifiers/codes are not present in those logs. Verification: focused queue/scheduled tests (`2` files / `42` tests), API typecheck/lint, root typecheck/lint/test (`294` files / `1952` tests), root build, env/migration/dist-secret/diff gates, `pnpm run deploy:api`, live API health `200`, live `/readyz` `200 ready` with D1 `77ms`, and unauthenticated admin orders API `401`.
- `OPS-008` admin dashboard activity-budget slice is deployed to admin `a9d47be2-4519-42fb-a334-04815884e7b0`. The dashboard loader now prefetches only the lightweight home summary; the heavier 90-day activity chart query is enabled after browser idle with a timeout fallback, reducing first-paint and long-idle tab recovery contention with D1 reads. Verification: focused admin route-boundary test (`33` tests), admin typecheck/lint, root typecheck/lint/test (`294` files / `1952` tests), root build, env/migration/dist-secret/diff gates, `pnpm run deploy:admin`, live dashboard login `200`, and unauthenticated `/admin` `307 /auth/login`. Remaining `OPS-008` scope: use the new timing logs and D1 query evidence to decide whether the activity aggregate needs query/index reshaping beyond deferred fetch.
- `OPS-008` dashboard query observability slice is deployed to API `4b3fb941-327e-40c2-b6c7-3a423d06f1ce`. Core dashboard reads now emit `[dashboard-query]` events for `summary_stats`, `full_stats`, `recent_orders`, and `daily_activity_{days}d` with duration, attempt counts, retry delays, and capped error text only. Verification: focused dashboard service test, core typecheck/lint, root typecheck/lint/test (`295` files / `1953` tests), root build, env/migration/dist-secret/diff gates, `pnpm run deploy:api`, live API health `200`, live `/readyz` `200 ready` with D1 `145ms`, and unauthenticated dashboard activity API `401`.
- `OPS-008` dashboard evidence/runtime simplification slice is deployed to API `588809b2-3532-46d2-94b2-f40be602c416` and admin `b73205ca-a9c2-4fc7-b634-4b8d5fac62be`. Parallel read-only audits plus remote D1 `EXPLAIN QUERY PLAN` checks showed current `daily_activity_90d` order/customer queries already use `orders_dashboard_agg_idx` and `customers_dashboard_activity_idx` with sub-millisecond plans on production data, so no speculative dashboard revenue index was added. `getRecentOrders()` now excludes soft-deleted orders, which is correct for merchants and uses the dashboard aggregate index remotely. Admin `DashboardStats` removed the second chart-level `requestIdleCallback`; the route-level activity request remains the only idle gate, and the lazy chart renders via Suspense as soon as data is ready. Verification: focused core/admin tests, local browser dashboard smoke with no console errors or stuck loading state, root typecheck/lint/test (`309` files / `2051` tests), root build, env/migration/dist-secret/diff gates, live API health `200`, live `/readyz` `200 ready` with D1 `62ms`, unauthenticated `/admin` `307 /auth/login`, and demo authenticated `/admin` `200` with dashboard HTML.
- `OPS-008` remains relevant after the PAY-022 API deploy: Cloudflare rejected the first `wrangler deploy` validation attempt with startup CPU error `10021`, then the retry deployed API version `0cb35740-cb0f-4f07-815f-9789433279f3` with reported Worker startup time `684 ms`. Treat this as evidence for continued API startup/import-budget review, not as proof of a PAY-022 regression.
- `OPS-008` API startup/import-budget slice is deployed to API `bec414e5-b60a-4674-bedc-75c962cf420d` with Cloudflare-reported Worker startup time `560 ms`. The `/admin/ai` route and `WidgetDesignAgent` still share the main API Worker graph, but AI SDK/provider runtime imports (`ai`, OpenAI, Gemini, OpenRouter, Workers AI provider) now initialize only when AI generation/model resolution is requested. `ai-startup-boundaries.test.ts` proves importing the admin AI route does not load those packages and that `streamWidgetContent()` / `getLanguageModel()` load only the requested runtime dependency. Verification: focused startup-boundary test (`3` tests), API test (`69` files / `581` tests), API typecheck/lint/build, root deploy API typecheck/build/dist-secret/migration/deploy path, `pnpm check:env`, `git diff --check`, current Cloudflare Workers docs and `@cloudflare/workers-types@4.20260630.1` review, live health `200`, live `/readyz` `200 ready` with D1 `122ms`, and unauthenticated `/admin/ai/models` `401`.
- `OPS-008` API handler-graph lazy entrypoint slice is deployed to API `032d668a-d918-4ecf-9917-0bf031d689c8` with Cloudflare-reported Worker startup time `127ms`. `worker.ts` now lazy-loads the HTTP app, queue consumer, and scheduled-maintenance graphs only inside `fetch`, `queue`, and `scheduled`, while keeping the `WidgetDesignAgent` named Durable Object export stable. `worker-startup-boundaries.test.ts` proves entrypoint import does not evaluate `app.ts`, `queue-consumer.ts`, or `scheduled-maintenance.ts`. Verification: focused boundary test through the API Vitest package run, API typecheck/lint, root typecheck/lint/test (`317` files / `2137` tests)/build/env/dist-secret/migration/diff gates, API deploy, live health `200`, live `/readyz` `200 ready` with total `327ms` and D1 `136ms`, live checkout config sane, and live OpenAPI `269` paths. Dry-run evidence showed Wrangler still produced a single Worker bundle and upload weight increased (`11241.40 KiB` before to `12148.14 KiB` after), so this phase should be treated as startup/evaluation improvement only; remaining OPS-008 import-budget work is to isolate the DO/AI graph or split modules when the evidence says bundle weight is the bottleneck.
- `OPS-008` WidgetDesignAgent/import-budget slice is deployed to API `4b52f832-7330-4d00-b16d-e1ad6b3c6569` with Cloudflare-reported Worker startup time `89ms`. The named Durable Object export now stays as a thin shell with local status handling, while heavy AI generation and provider/runtime imports live in `widget-design-agent-runtime.ts` and load only for POST generation requests. The same slice fast-fails no-cookie admin API requests before Better Auth/RBAC, preventing unauthenticated probes from timing out and spending auth/D1 budget. Verification: focused API startup/admin-auth/agent-boundary tests (`76` files / `632` tests), root typecheck/lint/test (`322` files / `2173` tests)/build/env/dist-secret/migration/diff gates, final API deploy, live health `200`, live `/readyz` `200 ready` with D1 `353ms` and R2 `196ms`, live OpenAPI `269` paths / `367` operations, no-cookie `/api/v1/admin/orders` and `/api/v1/admin/ai/models` `401` smokes. One live `/readyz` sample before the final deploy still showed an R2 timeout, so OPS-008 stays open for dependency-variance monitoring/runbook work.
- `OPS-008` API post-deploy readiness-sampler slice is deployed to API `6b377fa7-6429-450c-8469-e3d79e5e184c`. `scripts/deploy.mjs` now verifies API deploys by proving the latest Cloudflare deployment is serving one version at `100%`, checking live `/api/v1/health`, then sampling `/api/v1/readyz` four times and failing persistent degraded readiness. Verification: deploy-sampler unit tests (`3` tests), focused readiness tests (`4` tests), root typecheck/lint/test (`327` files / `2201` tests)/build/env/dist-secret/diff gates, API dry-run, live `pnpm run deploy:api`, live health `200`, four live `/readyz` samples `200 ready` with D1 between `52ms` and `112ms`, and no-cookie `/api/v1/admin/orders` `401`.
- `OPS-008` repo-owned ops smoke slice adds `pnpm ops:check` as the default read-only production API operator check. It verifies `/health`, samples `/readyz`, counts OpenAPI paths, proves the latest API Worker deployment through app-local Wrangler (`pnpm --dir apps/api exec wrangler deployments list --json`), and optionally checks all configured queues with `--queues`. Verification: `node --check scripts/ops-check.mjs`, focused script/deploy tests (`2` files / `13` tests), `pnpm ops:check --help`, `pnpm check:env`, `git diff --check`, live `pnpm ops:check --json` request id `ops-check-20260705t043025249z-6337d319` with `4/4` ready samples, OpenAPI `275` paths, and API version `a750ed55-fce8-42e0-bd13-91528fbc7def` at `100%`; live `pnpm ops:check --queues --samples 1 --timeout-ms 20000` passed all eight normal/DLQ queues. This did not deploy Workers because only repo scripts/docs/package metadata changed. Continuous monitoring remains open for the intended scheduled ops-monitor Worker: `/readyz`, `Queue.metrics()` backlog/DLQ checks, KV streak/cooldown state, structured redacted logs, deploy proof, Wrangler tail/cron evidence, and optional alert-channel verification.
- `OPS-008` queue provider-wiring smoke follow-up makes `pnpm ops:check --queues` parse Wrangler queue metadata and verify the expected `worker:scalius-api` producers/consumers from `apps/api/wrangler.jsonc` instead of only proving queue names. It also records the ops-monitoring contract in `audit/ops-monitoring-contract.md`; current direction is a scheduled Worker using queue bindings and `Queue.metrics()` rather than Cloudflare API tokens. Verification: focused ops/deploy script tests (`2` files / `15` tests), app-local Wrangler `queues info --help`, live `pnpm ops:check --queues --samples 1 --timeout-ms 20000` request id `ops-check-20260705t062801202z-749b9218` with all eight normal/DLQ queues matching expected provider wiring, plus admin/core/API/storefront typecheck and lint for the integrated hardening batch.
- 2026-07-05 integrated hardening batch commit `cff6436e` deployed API `3246448c-1d48-49dd-a2bb-a01a10a184c1`, admin `a02d670d-c98f-4f79-b69f-2018234a18c6`, and storefront `b7309bb4-3299-4060-a7e2-18b9bd81f711`. Post-deploy proof: built-in deploy sampler had 4/4 live `/readyz` ready samples, `pnpm ops:check --queues --samples 1 --timeout-ms 20000` request id `ops-check-20260705t063757924z-c4485419` passed all eight queues, and live Playwright smoke loaded `/admin/settings`, `/admin/settings/checkout`, `/admin/products`, and `/products/fish` with zero console warnings/errors, page errors, or failed requests.
- `ORDER-018` order-list active-refund lock slice is deployed to API `4ef7298c-0fbb-4d47-8f19-1932eadf8999` and admin `0039f18b-accb-451e-bee0-3d2d2573b62a`. `GET /api/v1/admin/orders` now includes only a compact `activeRefundOperation` lock summary, while raw refund attempts/provider evidence stay on detail/payment panels. Admin desktop/mobile order rows route active recovery rows to detail, show a refund lock badge, disable status/delete affordances before a rejected mutation, and block bulk shipment/delete when selected rows have active refund recovery or active hosted-payment setup. Verification: parallel code/UI/backend guard audits; focused core/API/admin tests; affected core/API/admin/api-client typecheck+lint; SDK regeneration; root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`327` files / `2204` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, and `git diff --check`; local Chrome order-list smoke screenshot `/tmp/scalius-local-order-list-smoke.png` with no console warnings/errors; API deploy readiness sampler with four live `/readyz` samples all `200 ready`; live Chrome `/admin/orders` smoke screenshot `/tmp/scalius-live-order-list-smoke.png` with `62 orders`, filters/actions rendered, and zero console warnings/errors. Remaining `ORDER-018` scope is the backend guard sweep for non-delete active payment-session mutations plus COD failure evidence, refund repair, shipment status refresh, and shipment delete paths that can still start work before the active-refund guard.
- `ORDER-018` backend guard sweep is verified and deployed to API `4e956a17-df5e-44de-a533-267c83c2a031`. Shared active hosted-payment setup guards now live in `payment-session-attempts.ts`; admin order edit/restore/delete/permanent/bulk delete, status/fulfillment changes, shipment create/refresh/delete, shipment-driven status sync, COD collection/failure/return, refunds, and returns reject active refund attempts or active hosted-payment setup leases before provider/payment/inventory side effects. COD failure evidence is now blocked during active refund recovery, and refund repair/return paths also reject active hosted-payment setup. Verification: focused core/API tests, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`327` files / `2216` tests), root `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata guard, `git diff --check`, API deploy dry-run + no remote migrations, API deploy readiness sampler with all four live `/readyz` samples `200 ready`, live no-cookie `/api/v1/admin/orders` `401`, and live OpenAPI smoke (`271` paths, order status and fulfillment-status routes present).
- `PAY-009` explicit manual refund recovery is verified and deployed to API `0428a210-b70a-4265-8383-607fd0ba91cd` and admin `fa6562c0-ec23-46cd-9d28-ce31f9740d1f`. Refund-capable admins can now click `Check now` on active refund attempts from order payment detail; the backend route validates the order/attempt pair, claims the attempt in D1 before reconciliation, force-checks provider-visible states safely, refuses active leases, keeps fresh `pending` attempts deferred until due, performs cache/notification side effects after local reconciliation without leaking notification facts, and keeps failed attempts retryable through the normal refund flow once the active lock clears. Verification: focused core/API/admin/RBAC tests, SDK regeneration, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`327` files / `2223` tests), `pnpm build`, env/dist/migration/diff guards, live API health/readyz, live OpenAPI route smoke (`272` paths, recovery route present), and dashboard unauthenticated redirect smoke.
- `ADMIN-016` shipment recovery visibility/lock slice is deployed to API `f92af064-6fad-426b-b314-1d3995e66da6` and admin `cf9bef24-a7f3-42c8-88a5-8f10ad7e2673`. `GET /api/v1/admin/orders`, `/payment-recovery`, and `/:id` now expose a sanitized `shipmentRecovery` read model with `state`, `severity`, `activeLock`, and retry-safe copy, while claim ids/provider metadata stay private. Admin list/detail/mobile/bulk flows show shipment recovery context and block edit/status/delete/refresh/bulk delete/bulk ship/manual fulfillment/provider shipment creation before a backend rejection when active shipment finalization or reconciliation is in progress. Failed provider rows are visible as retryable and failed shipment creation invalidates order/detail/shipment queries so the operator sees the durable evidence immediately. Verification: parallel read-only audits, SDK regeneration, affected/root typecheck+lint, root test (`327` files / `2224` tests), root build, env/migration/dist-secret/diff guards, API deploy readiness sampler with four live `200 ready` samples, live admin orders asset/guard smoke, and live OpenAPI field smoke proving `shipmentRecovery` on order list, payment recovery list, and order detail.
- `ADMIN-016` explicit shipment reconciliation repair slice is verified and deployed to API `ebd3aad0-afd5-45fb-b52c-a13dc3008538` and admin `70a4d167-0746-4f91-802e-19921c3a094c`. `POST /api/v1/admin/orders/{id}/shipments/{shipmentId}/reconcile` validates the order/shipment pair, refuses active refund/payment-session conflicts, repairs `orderStatusSync` inventory failures or provider-success/local-finalization failures from persisted shipment evidence, invalidates product availability caches, emits a deduped buyer status notification for repaired shipment-driven statuses, and clears only the matching order shipment claim after local repair succeeds. Admin order detail now shows an RBAC-gated `Repair shipment` action inside the sanitized shipment recovery notice. Verification: parallel read-only backend/admin audits; SDK regeneration; focused core/API/admin/RBAC tests; affected typecheck/lint; root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`327` files / `2228` tests), `pnpm build`, `pnpm check:env`, migration metadata guard, `pnpm check:dist-secrets`, `git diff --check`; API deploy readiness sampler with four live `200 ready` samples; live health/readyz; live OpenAPI route smoke (`273` paths / `371` operations); live no-cookie admin orders and reconcile POST `401`; and live dashboard canonical route redirect to `/auth/login`.
- `PERF-010` storefront product-listing indexes are verified and deployed to API `f9cdb388-9e46-4e52-890e-0db1752dcea4`. Migration `0075_storefront_listing_indexes` added the measured global newest, category newest, and attribute-value covering indexes, and the single-attribute filter helper now avoids the grouped intersection path. Remote D1 plans show no temp sort for global/category newest and no temp group for `brand=Apple`; live API/storefront smokes passed. Remaining future optimization, if production traffic proves it matters: pre-resolve attribute IDs before multi-attribute intersections instead of broadening indexes speculatively.
- `PERF-010` storefront cold/static route audit follow-up is verified and deployed to storefront `95dd055c-6890-43e7-81d9-0056838a44aa` / version `4d46c4fe-6187-4469-bfb9-1b9c2f3ac53b` with build `src-394b7dd5f1c74988`. Evidence pass traced category/search cold paths through layout + listing + attribute metadata (+ scoped widgets for category after category id), CMS pages through consolidated page-render data, collections through collection + scoped widget reads, and HTML/API cache keys through global version plus exact generations for product/category/collection/CMS HTML paths. Low-risk fixes: CMS and collection pages now return non-cacheable temporary-unavailable responses when required layout data is missing instead of caching a fallback shell; `sitemap-pages.xml` now fails closed with non-cacheable `503` when any CMS page batch is unavailable instead of caching a partial/empty XML sitemap; and `sitemap-categories.xml` uses the same `xmlDataUnavailableResponse()` no-store path for category read failures. Verification: focused storefront sitemap/cache/listing/page-data tests, storefront typecheck/lint/build, root gates, `git diff --check`, deploy-script `/health` plus warm-path proof, and live read-only smokes for `/sitemap-pages.xml`, `/sitemap-categories.xml`, and `/search?sortBy=newest` returning `200` with browser-safe no-store headers. No SQL/index changes were made because the existing PERF-010 query-plan evidence still owns listing DB shape.
- `CHECKOUT-042` is verified and deployed to storefront `95dd055c-6890-43e7-81d9-0056838a44aa` / version `4d46c4fe-6187-4469-bfb9-1b9c2f3ac53b`. Storefront checkout/order helpers and same-origin order/payment-session proxies now call the public proof/origin-guarded backend paths without the authenticated API helper or service-token branch, while keeping customer session forwarding for order creation. Verification: focused storefront order/proxy tests, API public order/payment boundary tests, root typecheck/lint/test/build/env/dist/migration/diff guards, local post-sale smoke (`CC64H3`) before deploy, and live invalid create-order/Stripe/SSLCommerz/Polar proxy smokes returning `400`/`404` with private no-store headers and no auth/service-token failures.
- `PERF-003` dashboard chart renderer slice is verified and deployed to admin `9171367d-5949-4bf0-b235-f0e98241757c`. Recharts and the shared chart wrapper were removed from the dashboard lazy chart path; the admin dashboard now renders daily activity through a small inline SVG renderer with measured pixel coordinates, non-finite API value guards, one keyboard focus target with arrow-key date inspection, and tooltip placement that avoids the card header on top-edge points. `recharts`, `victory-vendor`, D3 Recharts-only transitive packages, and `apps/admin-v2/src/components/ui/chart.tsx` are gone from the workspace. Verification: focused dashboard/chart boundary tests (`3` files / `50` tests), admin typecheck/lint/build, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, production dist scans proving no Recharts/chart-wrapper references and no dashboard `viewBox`/`preserveAspectRatio`, client chart chunk `9.8 kB` raw / `3.8 kB` gzip, local Chrome dashboard smokes at `1440x1100` and `430x920` with one SVG, one keyboard focus target, arrow-key tooltip details, and zero console/page errors, plus live demo-account Chrome smokes at the same viewports loading `DashboardChart-CMOE39xk.js` with zero console/page errors.
- `PERF-003` products-list interaction boundary slice is verified and deployed to admin `4479e7a4-a6df-4d31-aa71-97f564c3f1b9` (`100%` in deployment `cf792551-6438-43ed-8973-dbda2f64d842`). The route lazy-loads `-ProductDeleteDialog` only after a delete interaction, while the dialog chunk owns `AlertDialog`, `AlertTriangle`, `Loader2`, and dialog-specific styling. Verification: focused route/dashboard tests (`2` files / `44` tests), root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`344` files / `2364` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, dist marker scan proving the products client route has no static alert-dialog import markers and `-ProductDeleteDialog` owns the dialog copy, local browser smoke at `/admin/products` with screenshot `/tmp/scalius-products-delete-dialog-local.png`, and live demo-session smoke at `/admin/products` showing 10 row menus, one `Move to Trash` menu item, one confirmation dialog with title/description/cancel/action, no new `dashboard.scalius.com` warnings/errors, and screenshot `/tmp/scalius-products-delete-dialog-live.png`.
- `PERF-003` dashboard activity query fallback is verified and deployed to admin `4479e7a4-a6df-4d31-aa71-97f564c3f1b9`. The admin dashboard activity query now normalizes an undefined local server-function/API result to `{ dailyActivityData: [] }` before React Query sees it, preventing the `Query data cannot be undefined` warning for `["dashboard","activity"]` after idle/local recovery edge cases. Verification: focused query-options coverage for normal data and undefined fallback, root gates listed above, and a fresh live `/admin` dashboard smoke starting `2026-07-05T08:04:19.847Z` with dashboard/recent-orders content present and no new query-undefined warnings.
- `PERF-003` dashboard activity query fallback hardening is verified and deployed to admin `c4fbe55b-daee-400f-9450-988d9c90e6f1`. The admin dashboard activity query now also normalizes truthy malformed activity payloads without an array `dailyActivityData` to `{ dailyActivityData: [] }`, keeping React Query data defined and routing the chart to the normal empty state. Verification: focused dashboard query-options/chart/route-boundary tests, API dashboard route package test, admin/root gates, local authenticated browser smoke with "No daily activity yet" and no warning/error console logs after the idle activity fetch window, plus live dashboard smoke showing settled Daily Activity/Recent Orders with no new `dashboard.activity` undefined warning.
- `PERF-003` order/discount list menu boundary slice is verified and deployed to admin `313230b9-2d1e-4ce2-8a18-17f0b4bd8467` (`100%` in deployment `5441d9f7-4754-4020-825f-e56771b50bde`, Worker startup `17ms`). `OrderStatusSelector` and `DiscountTableToolbar` now keep only the hot triggers in the route/list path and lazy-load `OrderStatusSelectorMenu` / `DiscountTypeFilterMenu` on first interaction; the lazy files own `DropdownMenu*`, order transitions, and radio menu content. Verification: focused boundary tests (`3` files / `4` tests), admin typecheck/lint/build, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`346` files / `2366` tests), `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, source/dist marker scans proving hot files have no static dropdown/menu-helper imports and dedicated client chunks exist, local browser smokes for discounts and order status menus with screenshots `/tmp/scalius-local-discount-type-menu.png` and `/tmp/scalius-local-order-status-menu.png`, plus live demo-session smokes showing the discount type radio options and order status transitions without mutation or new `dashboard.scalius.com` warnings/errors, with screenshots `/tmp/scalius-live-discount-type-menu.png` and `/tmp/scalius-live-order-status-menu.png`.
- `PERF-003` first-time 2FA setup bundle slice is verified and deployed to admin `c4fbe55b-daee-400f-9450-988d9c90e6f1`. `components/auth/TwoFactorSetup.tsx` now dynamic-imports the Better Auth 2FA client only inside enable/verify/resend handlers, preserving the same email-code setup UX while keeping `/auth/setup-2fa` off the eager auth-client edge; route-boundary coverage also guards the owned setup component against static `qrcode` imports because the profile-settings QR runtime is outside this slice. Verification: focused route-boundary test, admin/root gates, source scan showing no static `auth-client`/`qrcode` imports in the setup route/component, and build output showing `auth-client` remains a separate dynamic chunk.
- `PERF-003` account/scanner QR-runtime bundle slice is verified and deployed to admin `51127823-1626-4b1a-b726-3f36852cc682` / version `fa00baa0-4ff5-4d7d-8171-7ff9e6c93896`. Profile account 2FA setup and General Settings scanner-token generation now lazy-load `qrcode` only after a TOTP URI or scanner token exists, with cancellation guards so stale QR work cannot repaint after state changes. Verification: route-boundary test (`45` tests), admin typecheck/lint/build, root gates, source scan showing only `import("qrcode")`, client dist scan proving account and scanner chunks only point at lazy QR chunks, server dist scan proving account/scanner chunks only lazy-import QR runtime, `git diff --check`, and live dashboard login HTML smoke. Authenticated QR/TOTP mutation was intentionally not run against production.
- `PERF-003` categories/customers/pages delete-dialog slice is verified and deployed to admin `c4fbe55b-daee-400f-9450-988d9c90e6f1`. The sibling list routes now lazy-load `-CategoryDeleteDialog`, `-CustomerDeleteDialog`, and `-PageDeleteDialog` only after a row delete/permanent-delete interaction, while the new dialog chunks own `AlertDialog`, `AlertTriangle`, `Loader2`, and dialog-specific styling. Existing single-delete UX/RBAC mutation behavior is preserved; bulk delete behavior is unchanged. Verification: focused route-boundary test, admin/root gates, source scan proving the hot category/customer/page routes have no static dialog markers, deployed asset list containing the three new dialog chunks, and local/live cancel-only browser smokes for categories/customers/pages row delete confirmations with no horizontal overflow or console warnings/errors; screenshots `/tmp/scalius-local-list-delete-dialog-lead.png` and `/tmp/scalius-live-list-delete-dialog-lead.png`.
- `PERF-003` product option/navigation boundary slice is verified and deployed to admin `9ce2801e-f868-4460-afa9-2614d85ff61d` / version `6c5f1768-f015-46b8-9a12-4a34e7f98619` with Worker startup `18ms`. Product edit now keeps the heavy `VariantManager` off the SSR document path until hydration, product-option CSV import/export helpers load only after the merchant opens or uses those tools, `NavigationBuilder` renders compact non-DnD rows by default and lazy-loads `SortableNavigationEditor` only after Reorder, and hot admin route graph coverage guards against eager command-palette UI. Verification: focused boundary tests (`4` files / `59` tests), admin typecheck/lint/build, root `pnpm typecheck`, `pnpm lint`, `pnpm test` (`353` files / `2387` tests), root `pnpm build`, `pnpm check:env`, migration metadata guard (`78` SQL / `78` journal / `32` snapshots / `46` allowed gaps), `pnpm check:dist-secrets`, `git diff --check`, chunk scans over `42` product edit chunks and `2` navigation chunks, local product edit document fetch `200` in `147ms`, local Chrome product-import/settings-navigation smokes with screenshots `/tmp/scalius-local-variant-import-dialog.png` and `/tmp/scalius-local-settings-navigation-reorder.png`, admin deploy plus dry-run, and live Chrome smoke opening the option import dialog plus settings navigation in `2.4s`/`2.6s` with zero console/page/request errors and screenshots `/tmp/scalius-live-variant-import-dialog.png` and `/tmp/scalius-live-settings-navigation.png`.
- `NOTIF-010` provider setup-failure follow-up is verified and deployed to API `85e3db08-62b9-48c8-8359-d6eaac7dd2bc` and admin `c6c9e368-f13e-4583-9f9c-d3f593ebcea7`. Resend now preserves the HTTP status when the JSON body has a provider message, provider-health classification pauses merchant-actionable `400`/`404`/`422` setup failures the same way existing `401`/`403`/`405` failures were paused, and Notification Settings renders sanitized merchant-facing readiness copy instead of long raw provider strings. Verification: focused notification/provider tests (`81` core files / `614` tests due package runner expansion), focused notification display tests (`2` files / `49` tests), core/admin typecheck and lint, admin build, root `pnpm typecheck`, root `pnpm lint`, root `pnpm test` (`346` files / `2367` tests), root `pnpm build`, `pnpm check:env`, migration metadata guard, `pnpm check:dist-secrets`, `git diff --check`, local notification smoke screenshot `/tmp/scalius-local-notifications-friendly-copy.png`, API deploy readiness sampler with four live `/readyz` `200 ready` samples, and live notification settings smoke showing `SMS via smsnetbd sending is paused after a provider setup failure...` with screenshot `/tmp/scalius-live-notifications-friendly-copy.png`.
- `INV-011` variant terminology follow-up is verified and deployed to admin `c6c9e368-f13e-4583-9f9c-d3f593ebcea7`. Admin product option surfaces now keep the SKU-first data model while teaching generic merchant-defined choices: read-only product detail labels option attributes as `Option 1`/`Option 2`, row/bulk editors use `choice` hints and examples like `2KG`, `XL`, `100ml`, `Red`, `Blue`, and `Pro`, CSV templates include three non-size-only example rows, and stock-limit controls say `No stock limit` instead of the shorter ambiguous `No limit`. Verification: focused variant/product-view tests (`3` files / `17` tests), admin typecheck/lint/build, root gates listed in `NOTIF-010`, local simple-SKU and optioned-product smokes with screenshots `/tmp/scalius-local-simple-sku-copy.png` and `/tmp/scalius-local-option-copy.png`, and live optioned-product edit smoke for `prod_abe_OILKCt75z47ppI1Sd` showing `Product Options`, `Option 1`, `Option 2`, no old `No limit` copy, and screenshot `/tmp/scalius-live-product-variant-copy.png`.
- `PERF-003` Tiptap media-trigger ownership slice is verified and deployed to admin version `70cbb18d-6527-44b3-b048-93b74185fcc4`. `TiptapMenuBar` no longer uses a page-global hidden media-manager wrapper or `document.getElementById(...).querySelector("button")` bridge; each editor toolbar now renders `MediaManager` through its own custom trigger, and `ToolbarButton` forwards React click events for cloned trigger compatibility. Verification: focused rich-text/route-boundary tests (`3` files / `49` tests), admin typecheck/lint/build, root typecheck/lint/test (`361` files / `2419` tests)/build, env/migration/dist-secret/diff guards, source/dist scan proving the old global trigger bridge is gone outside negative assertions, local admin browser smoke opening product edit, the description Media Library dialog, and the Additional Sections tab with zero captured browser warnings/errors, unauthenticated live product edit redirecting to `/auth/login`, live demo sign-in returning `200` with session cookies, authenticated live product edit returning `200`, and deployed `TiptapEditor`/media-manager asset marker scans showing no old wrapper or query-selector bridge. Handoff note: `audit/parallel/PERF-003-tiptap-media-trigger.md`.
- `PERF-003` admin performance confidence gate added root `pnpm check:admin-perf` as a read-only source/dist subset of the manual hot-path checklist. It checks the deleted query barrel, major list route warming, DataTable DnD split, product form/image/variant/navigation/settings/order lazy boundaries, and optional admin dist manifest/ProductForm chunk markers. Verification: `node --check scripts/admin-perf-check.mjs scripts/admin-perf-check.test.mjs`, focused script Vitest, and local `pnpm check:admin-perf`.
- `CACHE-022` cache operations recovery slice is verified and deployed to API `ea298d04-aec8-4c37-8f36-a4ddd63e33b0` and admin `067cfbb1-2a06-4efc-a4b4-72ab8257ee74`. API clear-all keeps API fence/global invalidation but now schedules all storefront groups through the durable storefront cache queue path with sanitized direct fallback if the queue binding is missing. Admin cache settings shows pending storefront purge/warm queue failures with safe error copy and replay/ignore actions, while the global header cache action is hidden unless the admin has `settings.cache.manage`. Current Cloudflare Workers Cache research decision: do not enable it globally before stable release, because a hit can bypass storefront middleware and the current Cache API plus KV generation freshness contract. Verification: focused cache route/DLQ/boundary tests, API/admin/storefront/core/database/shared/ops-monitor typechecks as applicable to the Worker toolchain bump, API/admin lint and build, `pnpm peers check`, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, API deploy readiness sampler with four live `/readyz` `200 ready` samples, admin deploy, live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` request id `ops-check-20260707t191101906z-07258ee1`, direct `wrangler queues info storefront-cache-dlq` proof showing consumer `worker:scalius-api`, and `pnpm release:check --timeout-ms 30000` passing tracker/docs, API ops, dashboard redirect, storefront, SEO discovery/feed/sitemap, UCP catalog discovery, and product-route schema checks.

## Tracker Update Template

```md
ID:
Status:
Owner:
Files changed:
Verification:
Remaining risk:
PR/commit:
```
