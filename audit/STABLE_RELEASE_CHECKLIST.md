# Stable Release Checklist

Last reviewed: 2026-07-08

Use this file before claiming the platform is ready for a stable merchant-facing release. The tracker is the source of truth for defects; this checklist is the release gate.

## Latest Gate Evidence

- 2026-07-08: Admin MCP foundation is now deployed and release-gated without widening admin authority into the agent Worker. `apps/admin-v2` `/api/assistant/mcp` proxies to `apps/agent` `/mcp/admin`, strips bearer auth, forwards only cookie/MCP protocol headers, and the only tool is read-only `admin_session_context` backed by API RBAC session context. Verification passed: `pnpm --filter @scalius/agent test` (`15`), focused admin proxy/release/deploy tests (`59`), agent/admin typecheck/lint/build, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, `pnpm release:check --skip-live --json`, agent deploy `38a75356-f7ba-4896-afc9-67429aa7b86d`, admin deploy `fe3c357d-8cb4-4fd8-be09-ad1e4ecc3b4a`, live `pnpm release:check --timeout-ms 30000`, and authenticated demo-admin live MCP initialize/list/call proof showing only `admin_session_context` and `Cache-Control: no-store` on every MCP response. Known release warnings remain only OPS-005 Email alert setup and OPS-010 legacy `worker:testdash` queue producers.
- 2026-07-08: OPS-010 live read-only investigation clarified the legacy queue-producer topology and the release gate now emits required actions for unexpected queue producers. `testdash` is still deployed at `100%` (`0f3f0b9a-6af9-458a-b1b6-09a7b2408b6a`, uploaded `2026-03-12T17:23:53Z`) with fetch+queue handlers and producer bindings for `payment-events`, `order-notifications`, `auth-otp`, and legacy `order-ingest`; `dash.wrygo.com` routes directly to it. `store.wrygo.com` routes to `storefront-test`, which still service-binds `BACKEND_API` to `testdash`, so deleting `testdash` first is unsafe. Live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` passed as `ops-check-20260708t011934777z-a67e57d0` and reports required actions for each `worker:testdash` producer binding. Follow-up live `pnpm release:check --timeout-ms 30000` now runs the queue-aware ops path too, prints `queues 8 checked`, and surfaces the three queue cleanup actions for `payment-events`, `order-notifications`, and `auth-otp` alongside the known OPS-005 Email actions.
- 2026-07-08: ops-monitor routed-alert proof path is deployed in Worker version `485b6d3f-0b8d-4daf-b623-0ba1f3c62968`. The scheduled Worker can now consume a one-shot `OPS_MONITOR_STATE` marker at `ops-monitor:v1:control:routed-alert-proof`, route one synthetic proof issue through the existing Cloudflare Email path, and write a redacted result key under `ops-monitor:v1:control:routed-alert-proof:result:<nonce>` without exposing a public route or requiring a temporary bad readiness deploy. Verification passed: ops-monitor tests (`21`), ops-monitor typecheck/lint/build, all-workspace typecheck during deploy, `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, live tail run `ops-monitor-20260708t010230572z-83258db4` on the new version with `/readyz` ok, all eight queues `0`, `alertCount:0`, `routedAlertCount:0`, and `deliveryFailureCount:0`, live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` (`ops-check-20260708t010007493z-251f45e5`), and live `pnpm release:check --timeout-ms 30000` with only the known logs-only Email and `worker:testdash` warnings.
- 2026-07-08: `deploy:agent` post-deploy verification now uses the same real read-only `catalog_profile` MCP `tools/call` smoke as `release:check`, so an agent deploy fails if the deployed Worker can list catalog tools but cannot execute the UCP profile tool or returns checkout/cart/order/payment/customer/recovery capabilities. Verification passed: `node --check scripts/deploy.mjs`, `node --check scripts/deploy.test.mjs`, focused `pnpm exec vitest run scripts/deploy.test.mjs scripts/release-check.test.mjs --passWithNoTests` (`2` files / `48` tests), `git diff --check`, and direct live `verifyAgentDeploy({ storefrontUrl: "https://storefront.scalius.com", timeoutMs: 30000 })` showing `catalog_profile call ok` for `https://storefront.scalius.com/ucp`.
- 2026-07-08: release agent MCP smoke now performs a real read-only `catalog_profile` `tools/call` after MCP initialize/tools-list and validates the returned structured UCP profile against the catalog-only HTTPS/storefront-origin rules. This keeps stable limited to the stateless catalog MCP Worker while proving deployed tool execution still works. Verification passed: `node --check scripts/release-check.mjs`, `node --check scripts/release-check.test.mjs`, focused `pnpm exec vitest run scripts/release-check.test.mjs scripts/deploy.test.mjs --passWithNoTests` (`2` files / `47` tests), `git diff --check`, live `pnpm release:check --timeout-ms 30000` showing `catalog_profile call ok`, and live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` (`ops-check-20260708t003815458z-ce036a6a`) passing with only the known OPS-005/OPS-010 warnings.
- 2026-07-08, code commit `d545fbc9`: fixed the collection edit runtime/late-label regression and deployed admin `a35880e8-7e3d-4a50-b4dc-d2967a5dc568`. The form now keeps canonical `config.productIds` / `featuredProductId` strings but renders a neutral loading label while product lookup data arrives, so older or late-loading collection references do not crash the edit page or leak raw `prod_...` IDs as labels. Verification passed: focused `pnpm exec vitest run apps/admin-v2/src/components/admin/collection-form/CollectionFormContainer.test.tsx apps/admin-v2/src/components/admin/collection-form/types.test.ts apps/admin-v2/src/lib/api-query-options/lookup-options.test.ts --passWithNoTests` (`3` files / `11` tests), `pnpm build`, `pnpm check:dist-secrets`, `git diff --check`, `pnpm run deploy:admin`, live `pnpm release:check --timeout-ms 30000`, live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` (`ops-check-20260708t001328598z-3cb76d4f`), and live in-app browser smoke on `/admin/collections/mnLNrND_Ci0ki_4deGpTf/edit` showing the edit form and save action render with no fatal text, no browser warnings/errors, no visible `prod_...` IDs, and no horizontal overflow.
- 2026-07-08, HEAD `b177a096`: hardened `pnpm dev:admin:browser-smoke` after review. The smoke now refuses to treat arbitrary non-JSON/404 services on the admin port as ready before sign-in, refuses to silently reuse already-running local workers unless `--no-start` is explicit, and cleans up Chrome/profile state when DevTools startup fails. Verification passed: `node --check scripts/dev-admin-browser-smoke.mjs`, `pnpm exec eslint scripts/dev-admin-browser-smoke.mjs scripts/dev-admin-browser-smoke.test.mjs`, focused `pnpm exec vitest run scripts/dev-admin-browser-smoke.test.mjs scripts/dev-admin-smoke.test.mjs --passWithNoTests` (`2` files / `22` tests), full local browser run `rm -rf /tmp/scalius-admin-browser-smoke-state-codex && pnpm dev:admin:browser-smoke --state /tmp/scalius-admin-browser-smoke-state-codex --reset-admin` with marker `rt-smoke-1783468868852-kkp1nd` persisted on product `prod_VomsQJZS11na1IFbpvMFr`, root `pnpm test` (`410` files / `2977` tests), `git diff --check`, and live `pnpm release:check --timeout-ms 30000` with only the known logs-only ops Email warnings.
- 2026-07-08, HEAD `a66243c4` plus local browser-smoke patch: added `pnpm dev:admin:browser-smoke` as the executable local admin product rich-text browser gate. Focused checks passed: `node --check scripts/dev-admin-browser-smoke.mjs`, `pnpm exec eslint scripts/dev-admin-browser-smoke.mjs scripts/dev-admin-browser-smoke.test.mjs`, and `pnpm exec vitest run scripts/dev-admin-browser-smoke.test.mjs scripts/dev-admin-smoke.test.mjs --passWithNoTests` (`2` files / `17` tests). Fresh local run `rm -rf /tmp/scalius-admin-browser-smoke-state-codex && pnpm dev:admin:browser-smoke --state /tmp/scalius-admin-browser-smoke-state-codex --reset-admin` passed: it started disposable local API/admin workers, created first admin, created inactive/noindex/feed-excluded category/product fixtures, opened `/admin/products/prod_2bqeRBOR6W0cY2wDTfT8b/edit` in headless Chrome, inserted marker `rt-smoke-1783468116393-blqc41`, saved through `PUT /api/v1/admin/products/prod_2bqeRBOR6W0cY2wDTfT8b`, and verified the marker persisted with `consoleErrorCount:0` and `pageErrorCount:0`. Local storefront cache purge queue messages logged retryable network warnings during fixture invalidation, but the browser/product-edit assertion passed and no production data was touched. Follow-up `git diff --check` and live `pnpm release:check --timeout-ms 30000` passed with the same known logs-only ops Email warnings.
- 2026-07-08, HEAD `ee453af4`: current release-candidate smoke refresh passed with disposable local mutation state plus live read-only production gates. `pnpm dev:admin:smoke --state /tmp/scalius-stable-admin-state --reset-admin` created the first admin, signed in, proved account-security/RBAC, and round-tripped the scoped business-settings `invoicePrefix` write through the admin proxy. `pnpm dev:post-sale:smoke --state /tmp/scalius-stable-postsale-state --json` seeded a local SKU-backed COD checkout fixture, validated cart state, created and replayed order `JQ3016`, fetched the receipt, and created support request `osr_H48SRO15GW9BFYXE`. Follow-up local checks on the same state passed: `pnpm dev:post-sale:load --state /tmp/scalius-stable-postsale-state --skip-migrations --orders 10 --concurrency 3 --json` returned `10/10` `201` responses with p95 `102.6ms`; `pnpm dev:post-sale:otp --state /tmp/scalius-stable-postsale-state --skip-migrations --json` failed closed for the dummy local email provider with `503` and `0` pending challenges; `pnpm dev:post-sale:payment-readiness --state /tmp/scalius-stable-postsale-state --skip-migrations --json` failed Stripe, SSLCommerz, and Polar closed with clear setup messages and `0` payment-plan/session-attempt side effects. Live `pnpm release:check --timeout-ms 30000` passed, including agent MCP catalog-only tools, SEO/feed/sitemap/UCP/product-route proof, and only the known ops Email warnings. Live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` passed as `ops-check-20260707t233356288z-61735425` with healthy API/deployment/queue consumers and only the known logs-only ops Email plus external `worker:testdash` producer warnings.
- 2026-07-08, HEAD `2e632cf7` plus release-gate hardening patch: live in-app browser smoke found no console warnings/errors, fatal render text, or horizontal overflow on `/admin/settings/checkout`, `/admin/settings/notifications`, `/admin/settings`, `/admin/settings/cache`, `/admin/orders/DW8W05`, `/admin/products/prod_abe_OILKCt75z47ppI1Sd/edit`, `/admin/products/prod_WH2tJTRAzGB4jMTZB8hoX/edit`, `/admin/collections`, `/admin/collections/mnLNrND_Ci0ki_4deGpTf/edit`, storefront `/`, `/search?q=fish&sortBy=newest`, `/products/test-productss`, `/products/fish?size=xl&color=black`, `/collections/mnLNrND_Ci0ki_4deGpTf`, `/cart`, `/checkout`, and `/account`. The account page rendered the expected visible sign-in prompt; stale "temporarily unavailable" copy was present only in hidden fallback markup.
- 2026-07-08: first safe `@scalius/agent` Worker slice passed local and live verification. The new tracked Worker exposes only `GET /health` and stateless `/mcp` catalog tools backed by storefront UCP catalog/profile endpoints, with no Durable Object or commerce-authority bindings. Verification passed: blocker scans for unsafe bindings/imports/tool terms, `pnpm --filter @scalius/agent typecheck`, `pnpm --filter @scalius/agent lint`, `pnpm --filter @scalius/agent test` (`7` tests), deploy/turbo config tests (`8` tests), `pnpm --filter @scalius/agent build`, `pnpm check:env`, `pnpm install --frozen-lockfile`, `git diff --check`, `pnpm run deploy:agent -- --dry-run`, and live deploy `scalius-agent` version `2e53ea83-72be-4866-abb5-89ce88653595`. Live smokes proved `/health`, JSON 404, MCP tools/list catalog-only read tools, and `catalog_profile`/`catalog_search`/`catalog_lookup`/`catalog_product` calls against storefront UCP. The release/deploy scripts now automate `/health` plus MCP initialize/tools-list as the bounded agent gate; focused script tests and live `pnpm release:check --timeout-ms 30000` exercised the new gate. Post-deploy `pnpm release:check --timeout-ms 30000` passed, and `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` passed as `ops-check-20260707t230804721z-87fd5721` with only the known logs-only Email alert and stale external `worker:testdash` queue warnings.
- 2026-07-08: release UX/ops-warning/product-edit slice passed and deployed to admin `765d1478-88b2-428b-a727-a6455a8a6458`. Focused tests covered release-check warning propagation, product edit deferred-rich-text boundaries, merchant-defined variant option labels, admin route graph boundaries, and Auth & Access SMS readiness failures (`5` files / `104` tests); admin typecheck/lint/build, `git diff --check`, `pnpm check:dist-secrets`, live `pnpm release:check --timeout-ms 30000`, and live `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` (`ops-check-20260707t223230238z-2a2ffd63`) passed. The release gate now shows the known logs-only Email alert warning/action instead of hiding it.
- 2026-07-08, HEAD `49f7d26f`: missing release hygiene gates passed: `pnpm exec drizzle-kit check --config packages/database/drizzle.config.ts`, `pnpm --filter @scalius/database check:migrations`, `pnpm install --frozen-lockfile`, `pnpm audit --audit-level moderate`, `pnpm peers check`, and `git diff --check`.
- 2026-07-08, HEAD `49f7d26f`: local post-sale smokes passed. `pnpm dev:post-sale:smoke --json` seeded the disposable local fixture, created/replayed one COD order, fetched the receipt, and submitted a support request. `pnpm dev:post-sale:otp --json` failed closed once for the dummy local email provider with `503`, no pending OTP challenges, and clear customer-safe copy. `node scripts/dev-postsale.mjs payment-readiness --json` failed Stripe, SSLCommerz, and Polar closed with clear readiness messages and zero payment-plan/session side effects.
- 2026-07-08: `pnpm dev:admin:smoke --state /tmp/scalius-admin-smoke-state-codex-20260708 --reset-admin` passed. It used disposable local Wrangler state, created the first admin through setup, signed in through `/api/auth/sign-in/email`, proved admin session/RBAC with `/api/v1/admin/auth/account-security`, and GET/POST/GET verified `/api/v1/admin/settings/business` through the admin proxy.
- 2026-07-08: `pnpm release:check --timeout-ms 30000` now also proves invalid admin cookies fail fast against `/api/v1/admin/settings/business` through both the API and dashboard proxy. Live proof returned `401` on both surfaces with no `ADMIN_API_READ_TIMEOUT`/`504`.
- 2026-07-08: fresh ops evidence confirms the remaining stable acceptances are external/account-side, not repo-code blockers. `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` passed as `ops-check-20260707t210503184z-848d6340` with healthy `/readyz`, all required `worker:scalius-api` queue consumers/producers present, and only the known stale `worker:testdash` producer warnings on `payment-events`, `order-notifications`, and `auth-otp`. Ops-monitor tail run `ops-monitor-20260707t210227309z-b1fd6197` on version `ed91cdf7-3aaa-4012-b655-c8071f60c4fc` reported `/readyz` ok, all eight queues/DLQs backlog `0`, `alertCount:0`, `routedAlertCount:0`, and `deliveryFailureCount:0`; dry-run config still shows unrestricted `ALERT_EMAIL` with empty `ALERT_EMAIL_FROM` / `ALERT_EMAIL_TO`, so routed alert proof remains account setup work.
- 2026-07-08, cache-release hardening commits `317f333d`, `b0c9b60a`, and `6d54e37d`: admin cache settings actions are permission-gated and confirmed, stock-only availability invalidation now covers all-product and category-product storefront list cache families without a global HTML version bump, and `pnpm release:check` now includes storefront cache-header/private-checkout/purge-method smokes. Deployed API `8385676f-7148-43b7-a659-3dedc36f892f`, storefront `0b150bd2-bec0-4a76-a981-098cf8bfa0d5`, and admin `0f8ce437-405c-4254-b60d-bd440a3bdf90`. Final live `pnpm release:check --timeout-ms 30000` passed, including `PASS storefront cache headers`, and `pnpm ops:check --queues --samples 1 --timeout-ms 30000 --json` passed as `ops-check-20260707t212722153z-8f1e057f` with only the known stale `worker:testdash` producer warnings.
- 2026-07-08, HEAD `49f7d26f`: live browser route smoke found no console errors, fatal render text, or horizontal overflow on `/admin/settings/checkout`, `/admin/orders/DW8W05`, `/admin/products`, `/admin/products/prod_abe_OILKCt75z47ppI1Sd/edit`, `/admin/settings/notifications`, `/admin/settings`, `/admin/settings/cache`, `/admin/collections`, `/admin/analytics`, storefront `/`, `/search`, `/cart`, `/checkout`, `/account`, and `/products/test-productss`. `/admin/settings/payments` is not a dashboard route; payment gateway settings are rendered inside `/admin/settings/checkout`.
- 2026-07-08, HEAD `49f7d26f`: `pnpm release:check --timeout-ms 30000` passed after the local smokes with tracker/docs/API/dashboard/storefront/discovery/UCP/product-route checks green.
- Remaining release acceptances before a stable tag: `OPS-010` stale external `worker:testdash` queue producer warning and `OPS-005` routed ops alerting, which still requires Cloudflare Email Service sender/destination setup outside the current code path.

## Non-Negotiable Gates

- No open P0/P1 item in `audit/REMEDIATION_TRACKER.md` for checkout, auth, payments, orders, inventory, notifications, cache freshness, product/variant management, first-admin setup, or dashboard/storefront runtime.
- Current branch has passed root `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, `pnpm check:env`, `pnpm check:dist-secrets`, database migration metadata checks, frozen install, dependency audit, peer checks, and `git diff --check`.
- Current branch has passed `pnpm release:check` as the compact read-only production release smoke, including public SEO policy, homepage global JSON-LD proof, feeds/sitemaps, deployed agent `/health` plus catalog-only MCP tools/list, and UCP profile/search/lookup/product coverage. UCP release proof must require a publicly cacheable HTTPS profile and, when a product URL is known from sitemap/feed proof, lookup/product correlation must still run even if catalog search is empty. Use `--skip-wrangler` only when Cloudflare auth is unavailable and record that the agent smoke was skipped. Do not use `--allow-strict-seo-policy-fallback` for release candidates unless the public SEO policy outage is separately accepted and documented.
- Current branch has passed local buyer and merchant smokes for admin login/setup, product create/edit, cart, checkout, order success/receipt, customer auth/profile completion, order detail, notifications, and settings saves.
- Current branch has been deployed through the normal Cloudflare path and live read-only smokes pass for API health/readyz, OpenAPI, dashboard login/critical routes, storefront home/search/product/category/cart/checkout, queues/ops check, and discovery XML.
- Dummy or unreadable provider credentials fail closed with clear dashboard copy, no hot retry loops, no raw provider dumps, no noisy queue churn, and no runaway compute.
- Hosted payment-session `202 processing` responses are retried only by the bounded storefront helper, with server-provided retry hints honored and a short hard stop before recovery copy. Do not add unbounded polling after order commit.
- Cache invalidation evidence shows scoped API KV invalidation plus storefront purge/warm for affected content, not accidental global purge behavior.

## SEO, Feed, AEO, And AIO Gate

Treat "AIO" as crawlable, trustworthy commerce data for search and assistants, not as a magic markup layer.
Official Google Search/Merchant guidance was rechecked on 2026-07-07: Product structured data still recommends combining merchant-listing JSON-LD with Merchant feeds, ProductGroup remains the current variant model, and Google generative-AI search guidance still depends on crawlable, useful, well-structured human-facing content.

- `/robots.txt`, `/sitemap.xml`, `/sitemap-static.xml`, `/sitemap-products.xml`, `/sitemap-categories.xml`, `/sitemap-collections.xml`, `/sitemap-pages.xml`, canonical `/api/product-feed.xml?limit=5`, and compatibility `/api/facebook-feed.xml?limit=5` return valid XML/text with absolute URLs and production-safe cache headers. The canonical feed must use Google/Base availability values (`in_stock`/`out_of_stock`); the compatibility feed must keep Meta-style values (`in stock`/`out of stock`).
- Static sitemap-advertised URLs have canonical URLs; search/listing query, sort, filter, and paginated variants must be canonicalized or noindexed with follow.
- Product feed availability, Product JSON-LD availability, storefront availability UI, and checkout validation are all derived from buyer-resolvable SKU truth.
- Feed items must have absolute `http(s)` primary images, non-empty plain descriptions, valid price/currency, SKU-aware availability, and no invalid zero-price fallback unless the catalog policy explicitly supports it.
- Feed XML, UCP catalog media, Product JSON-LD/Open Graph images, and admin/feed diagnostics must use the shared catalog-discovery image contract. Do not let UI placeholders, protocol-relative URLs, unsafe base URLs, or non-primary image fallbacks enter machine-readable catalog surfaces.
- Dashboard SEO controls must remain the source of truth for sitemap sections, robots sitemap advertising, product-feed exposure, sold-out inclusion, feed title/description, schema-family toggles, product-level sitemap/feed XML exclusions, and product/category/collection/page `noindex`, sitemap-exclusion, plus route-shaped same-store canonical path controls. Collection canonical paths must follow the current ID-routed `/collections/<collectionId>` route until collection slugs or aliases exist.
- `pnpm release:check` must fail if the public sitemap index advertises a child sitemap section that the public SEO policy says is disabled.
- Resource `noindex` means public-but-not-indexed: page renders `noindex,follow`, sitemap XML excludes it, and resource-specific JSON-LD is suppressed. Sitemap exclusion alone is XML-only and must not hide or noindex the page.
- Public schema should match real merchant content: OnlineStore/Organization, WebSite/SearchAction, MerchantReturnPolicy, Product/Offer, BreadcrumbList, ProductGroup, CollectionPage/category pages. `pnpm release:check` must fail malformed emitted homepage schema, but absence is valid when the merchant has not configured prerequisites such as business identity, logo, or return policy. ProductGroup variant semantics must use the product's saved option labels/schema mappings; never assume merchant options are always Size and Color. Do not invent Product/Offer facts such as price expiry, item condition, seller, or brand when the merchant has not provided that data. Add richer contact, address, shipping, FAQ, or ProductGroup/variant schema only when the admin has real fields and tests proving the public output stays accurate.

Primary references for future changes:

- Google Product structured data and variants/ProductGroup: https://developers.google.com/search/docs/appearance/structured-data/product and https://developers.google.com/search/docs/appearance/structured-data/product-variants
- Google Merchant product data specification: https://support.google.com/merchants/answer/7052112
- Google canonical and noindex guidance: https://developers.google.com/search/docs/crawling-indexing/consolidate-duplicate-urls and https://developers.google.com/search/docs/crawling-indexing/block-indexing
- Google generative AI Search guidance: https://developers.google.com/search/docs/fundamentals/ai-optimization-guide

## Known P2 Release Follow-Ups

- `SEO-011`: remaining structured-data preview UX and richer FAQ/AEO controls where dashboard-owned data exists; emitted homepage/global schema and feed XML essentials are now covered by release/dashboard proof.
- `SEO-026`: before Google Merchant's announced January 31, 2027 enforcement date, feed diagnostics should verify that primary catalog images meet the 500x500 minimum when dimensions are known or can be derived from first-party media metadata.
- SEO dashboard live probes now cover `robots.txt`, the sitemap index, every enabled child sitemap, canonical product feed, and compatibility Facebook feed from the saved Store URL. Feed proof must warn on non-empty feeds missing per-item link, `image_link`, or availability, and must flag non-absolute product/image links instead of reporting green from HTTP 200 alone. The dashboard also has bounded aggregate product-feed diagnostics for emitted rows, skipped rows, reason counts, and safe product samples.
- Feed diagnostics: `/api/product-feed.xml` paginates the final flattened feed rows so skipped products and variant expansion cannot drop rows, while admin diagnostics explain which bounded catalog rows are skipped and why.
- `UCP-001`/`UCP-002`/`UCP-004`: read-only catalog discovery is present without checkout/cart/order/payment advertisement, and `pnpm release:check` now verifies the HTTPS catalog-only profile plus opportunistic search, lookup correlation, and product-detail first-variant behavior. Remaining UCP work is a P2/P1 roadmap item for dashboard control, D1-backed sessions, idempotency, signing/profile verification, and supported payment completion.
- `ANALYTICS-003`: provider health/test-send UX, TikTok Events API/server-side adapter, and broader server-side attribution.
- `OPS-005`/ops alerting: routed email/notification alerts for production ops signals.
- Broad admin performance hardening remains ongoing under `PERF-003`; do not let it block a stable release unless a concrete route regresses or stalls.
