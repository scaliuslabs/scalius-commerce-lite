# Scalius Commerce Agent Notes

This repository uses [AGENTS.md](AGENTS.md) as the canonical agent guide for architecture, commands, conventions, local development, deployment, and current limitations.

Do not duplicate the full guide here. Update `AGENTS.md` when an agent-facing rule changes, and update focused package READMEs only when a package-specific convention changes.
