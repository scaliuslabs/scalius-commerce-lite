diff --git a/dist/agent-execution-store-BCmrE5Jm.d.mts b/dist/agent-execution-store-BCmrE5Jm.d.mts
index ace28ee..9e4d254 100644
--- a/dist/agent-execution-store-BCmrE5Jm.d.mts
+++ b/dist/agent-execution-store-BCmrE5Jm.d.mts
@@ -428,0 +428 @@ interface DispatchInput {
+  generation?: number;
@@ -679 +680 @@ interface AgentSubmissionStore {
-   * Only a running direct submission owned by `attempt` may transition to
+   * Only a running direct or dispatch submission owned by `attempt` may transition to
diff --git a/dist/conversation-projections-XMug3C6A.mjs b/dist/conversation-projections-XMug3C6A.mjs
index d052200..92abc7b 100644
--- a/dist/conversation-projections-XMug3C6A.mjs
+++ b/dist/conversation-projections-XMug3C6A.mjs
@@ -611 +611 @@ function isDispatchInput(value) {
-	return typeof input.dispatchId === "string" && input.dispatchId.trim() !== "" && typeof input.agent === "string" && input.agent.trim() !== "" && typeof input.id === "string" && input.id.trim() !== "" && input.input !== void 0 && typeof input.acceptedAt === "string" && input.acceptedAt.trim() !== "";
+	return typeof input.dispatchId === "string" && input.dispatchId.trim() !== "" && typeof input.agent === "string" && input.agent.trim() !== "" && typeof input.id === "string" && input.id.trim() !== "" && input.input !== void 0 && typeof input.acceptedAt === "string" && input.acceptedAt.trim() !== "" && (input.generation === void 0 || Number.isSafeInteger(input.generation) && input.generation > 0);
diff --git a/dist/conversation-stream-store-Bitz7UoW.mjs b/dist/conversation-stream-store-Bitz7UoW.mjs
index 9403255..7c2f9bb 100644
--- a/dist/conversation-stream-store-Bitz7UoW.mjs
+++ b/dist/conversation-stream-store-Bitz7UoW.mjs
@@ -1844,2 +1844,3 @@ var Session = class {
-		const frameworkTools = (taskTool) => [
-			taskTool,
+		const taskTool = Object.keys(this.config.subagents ?? {}).length > 0 ? createTaskTool(runTask, this.config.subagents ?? {}) : void 0;
+		const frameworkTools = [
+			...taskTool ? [taskTool] : [],
@@ -1866 +1867 @@ var Session = class {
-				tools: frameworkTools(createTaskTool(runTask, this.config.subagents ?? {}))
+				tools: frameworkTools
@@ -1877 +1878 @@ var Session = class {
-			tools: frameworkTools(createTaskTool(runTask, this.config.subagents ?? {}))
+			tools: frameworkTools
@@ -3134,2 +3135 @@ async function reconcileInterruptedSubmission(submissions, submission, agent, cr
-		if (submission.kind === "direct") await settleDirectSubmission(submissions, attempt, ctx, "completed", inspected.result, void 0, conversationWriter);
-		else await submissions.completeSubmission(attempt);
+		if (!await settleSubmission(submissions, attempt, ctx, "completed", inspected.result, void 0, conversationWriter)) return { disposition: "stale" };
@@ -3308 +3308 @@ async function processSubmission(opts) {
-			const settled = submission.kind === "direct" ? await settleDirectSubmission(submissions, attempt, ctx, "failed", void 0, error, opts.conversationWriter) : await submissions.failSubmission(attempt, error);
+			const settled = await settleSubmission(submissions, attempt, ctx, "failed", void 0, error, opts.conversationWriter);
@@ -3312 +3312 @@ async function processSubmission(opts) {
-		const settled = submission.kind === "direct" ? await settleDirectSubmission(submissions, attempt, ctx, "completed", result, void 0, opts.conversationWriter) : await submissions.completeSubmission(attempt);
+		const settled = await settleSubmission(submissions, attempt, ctx, "completed", result, void 0, opts.conversationWriter);
@@ -3334 +3334 @@ async function failInterruptedSubmission(submissions, submission, attempt, agent
-	if (!(submission.kind === "direct" ? await settleDirectSubmission(submissions, attempt, ctx, "failed", void 0, error, conversationWriter) : await submissions.failSubmission(attempt, error))) return { disposition: "stale" };
+	if (!await settleSubmission(submissions, attempt, ctx, "failed", void 0, error, conversationWriter)) return { disposition: "stale" };
@@ -3347,4 +3347,4 @@ async function failInterruptedSubmission(submissions, submission, attempt, agent
-* so the abort is always visible in the message timeline. Direct submissions
-* additionally settle through the two-phase outbox with `outcome: 'aborted'`,
-* the durable terminal record a reconnecting waiter observes; dispatch
-* submissions settle the operational row with `failSubmission`.
+* so the abort is always visible in the message timeline. Direct and dispatch
+* submissions additionally settle through the two-phase outbox with
+* `outcome: 'aborted'`, the durable terminal record a reconnecting waiter
+* observes.
@@ -3367,2 +3367 @@ async function settleAbortedWithContext(submissions, submission, attempt, agent,
-	if (submission.kind === "direct") return settleDirectSubmission(submissions, attempt, ctx, "aborted", void 0, error, conversationWriter);
-	return submissions.failSubmission(attempt, error);
+	return settleSubmission(submissions, attempt, ctx, "aborted", void 0, error, conversationWriter);
@@ -3370 +3369 @@ async function settleAbortedWithContext(submissions, submission, attempt, agent,
-async function settleDirectSubmission(submissions, attempt, ctx, outcome, result, error, conversationWriter) {
+async function settleSubmission(submissions, attempt, ctx, outcome, result, error, conversationWriter) {
diff --git a/dist/flue-app-DweeRG3g.mjs b/dist/flue-app-DweeRG3g.mjs
index c0cd7c7..9a7abc6 100644
--- a/dist/flue-app-DweeRG3g.mjs
+++ b/dist/flue-app-DweeRG3g.mjs
@@ -10 +10 @@ async function enqueueDispatch(options) {
-	const input = validateAndCloneDispatchRequest(options.request, agent, options.rt);
+	const validated = validateAndCloneDispatchRequest(options.request, agent, options.rt);
@@ -15 +15,2 @@ async function enqueueDispatch(options) {
-		input,
+		input: validated.input,
+		...validated.generation === void 0 ? {} : { generation: validated.generation },
@@ -23,0 +24 @@ function validateAndCloneDispatchRequest(request, agent, rt) {
+	if (request.generation !== void 0 && (!Number.isSafeInteger(request.generation) || request.generation <= 0)) throw new Error("[flue] dispatch() generation must be a positive safe integer.");
@@ -24 +26,4 @@ function validateAndCloneDispatchRequest(request, agent, rt) {
-	return cloneJsonSerializable(request.input, "dispatch().input");
+	return {
+		input: cloneJsonSerializable(request.input, "dispatch().input"),
+		generation: request.generation
+	};
diff --git a/dist/internal.mjs b/dist/internal.mjs
index 1147b97..9125994 100644
--- a/dist/internal.mjs
+++ b/dist/internal.mjs
@@ -1 +1 @@
-import { E as SessionAlreadyExistsError, N as SubmissionAbortedError, O as SessionNotFoundError, X as toHttpResponse, w as RuntimeUnavailableError } from "./errors-DUgRtE8e.mjs";
+import { E as SessionAlreadyExistsError, N as SubmissionAbortedError, O as SessionNotFoundError, X as toHttpResponse, h as InvalidRequestError, w as RuntimeUnavailableError } from "./errors-DUgRtE8e.mjs";
@@ -763,0 +763,27 @@ function createSqlAgentExecutionStore(storage, className) {
+function createSqlAgentAdmissionFence(storage, className) {
+	const sql = storage?.sql;
+	if (!sql || typeof sql.exec !== "function") throw new Error(`[flue] Cloudflare durable agent class "${className}" requires Durable Object SQLite for its admission fence.`);
+	sql.exec(`CREATE TABLE IF NOT EXISTS flue_agent_admission_fence (
+		singleton INTEGER PRIMARY KEY CHECK (singleton = 1),
+		cutoff INTEGER NOT NULL CHECK (cutoff >= 0)
+	)`);
+	const readCutoff = () => {
+		const row = sql.exec("SELECT cutoff FROM flue_agent_admission_fence WHERE singleton = 1 LIMIT 1").toArray()[0];
+		if (!row) return 0;
+		if (!Number.isSafeInteger(row.cutoff) || row.cutoff < 0) throw new Error("[flue] Persisted agent admission fence is malformed.");
+		return row.cutoff;
+	};
+	return {
+		allows(generation) {
+			return generation === void 0 || generation > readCutoff();
+		},
+		advance(cutoff) {
+			const row = sql.exec(`INSERT INTO flue_agent_admission_fence (singleton, cutoff)
+				VALUES (1, ?)
+				ON CONFLICT(singleton) DO UPDATE SET cutoff = max(cutoff, excluded.cutoff)
+				RETURNING cutoff`, cutoff).toArray()[0];
+			if (!row || !Number.isSafeInteger(row.cutoff) || row.cutoff < cutoff) throw new Error("[flue] Agent admission fence could not be advanced.");
+			return row.cutoff;
+		}
+	};
+}
@@ -766,0 +793,2 @@ const CLOUDFLARE_AGENT_INTERNAL_DISPATCH_PATH = "/__flue/internal/dispatch";
+const FLUE_ADMISSION_GENERATION_HEADER = "x-flue-admission-generation";
+const FLUE_ABORT_THROUGH_GENERATION_HEADER = "x-flue-abort-through-generation";
@@ -784,0 +813 @@ function createCloudflareAgentRuntime(options) {
+				admissionFence: createSqlAgentAdmissionFence(storage, className),
@@ -849,0 +879,3 @@ var CloudflareAgentCoordinator = class {
+			const cutoff = readOptionalFenceGeneration(request.headers, FLUE_ABORT_THROUGH_GENERATION_HEADER);
+			if (!cutoff.ok) return invalidFenceGenerationResponse(FLUE_ABORT_THROUGH_GENERATION_HEADER);
+			if (cutoff.value !== void 0) this.admissionFence.advance(cutoff.value);
@@ -870,0 +903,2 @@ var CloudflareAgentCoordinator = class {
+		const generation = readOptionalFenceGeneration(request.headers, FLUE_ADMISSION_GENERATION_HEADER);
+		if (!generation.ok) return invalidFenceGenerationResponse(FLUE_ADMISSION_GENERATION_HEADER);
@@ -875 +910 @@ var CloudflareAgentCoordinator = class {
-			admitAttachedSubmission: (payload, onEvent, waitForResult, traceCarrier) => this.admitAttachedSubmission(payload, onEvent, waitForResult, traceCarrier)
+			admitAttachedSubmission: (payload, onEvent, waitForResult, traceCarrier) => this.admitAttachedSubmission(payload, onEvent, waitForResult, traceCarrier, generation.value)
@@ -901,0 +936,3 @@ var CloudflareAgentCoordinator = class {
+	get admissionFence() {
+		return this.prepared.admissionFence;
+	}
@@ -997 +1035,2 @@ var CloudflareAgentCoordinator = class {
-					}
+					}
+					if (settlement.record.outcome === "aborted") this.observers.fail(settlement.submissionId, new SubmissionAbortedError());
@@ -1187 +1226 @@ var CloudflareAgentCoordinator = class {
-	async admitAttachedSubmission(payload, onEvent, waitForResult, traceCarrier) {
+	async admitAttachedSubmission(payload, onEvent, waitForResult, traceCarrier, generation) {
@@ -1199,0 +1238 @@ var CloudflareAgentCoordinator = class {
+			if (!this.admissionFence.allows(generation)) throw new InvalidRequestError({ reason: "Agent admission generation was fenced by a later abort." });
@@ -1226,0 +1266 @@ var CloudflareAgentCoordinator = class {
+		if (!this.admissionFence.allows(input.generation)) return Response.json({ error: "Agent dispatch generation was fenced by a later abort." }, { status: 409 });
@@ -1250,0 +1291,10 @@ var CloudflareAgentCoordinator = class {
+function readOptionalFenceGeneration(headers, name) {
+	const raw = headers.get(name);
+	if (raw === null) return { ok: true, value: void 0 };
+	if (!/^[1-9][0-9]{0,15}$/.test(raw)) return { ok: false };
+	const value = Number(raw);
+	return Number.isSafeInteger(value) && value > 0 ? { ok: true, value } : { ok: false };
+}
+function invalidFenceGenerationResponse(name) {
+	return Response.json({ error: `Invalid ${name} header.` }, { status: 400 });
+}
@@ -1585 +1636 @@ function createNodeAgentCoordinator(options) {
-			if (!submission || submission.kind !== "direct") continue;
+			if (!submission) continue;
@@ -1601 +1652,2 @@ function createNodeAgentCoordinator(options) {
-				}
+				}
+				if (settlement.record.outcome === "aborted") observers.fail(settlement.submissionId, new SubmissionAbortedError());
diff --git a/dist/types-USSZhfC6.d.mts b/dist/types-USSZhfC6.d.mts
index e41ab79..c8b792a 100644
--- a/dist/types-USSZhfC6.d.mts
+++ b/dist/types-USSZhfC6.d.mts
@@ -62,0 +62,5 @@ interface AgentDispatchRequest {
+  /**
+   * Optional trusted admission generation. Cloudflare Agent Durable Objects
+   * reject generations fenced by a later abort before durable admission.
+   */
+  generation?: number;
diff --git a/dist/sql-run-store-DRLffFXh.mjs b/dist/sql-run-store-DRLffFXh.mjs
index 777173d..2aa5dfa 100644
--- a/dist/sql-run-store-DRLffFXh.mjs
+++ b/dist/sql-run-store-DRLffFXh.mjs
@@ -275 +275 @@ var AgentSubmissionStoreImpl = class {
-					 WHERE submission_id = ? AND kind = 'direct' AND status = 'running' AND attempt_id = ?
+					 WHERE submission_id = ? AND kind IN ('direct', 'dispatch') AND status = 'running' AND attempt_id = ?
@@ -282 +282 @@ var AgentSubmissionStoreImpl = class {
-					 WHERE submission_id = ? AND kind = 'direct' AND status = 'terminalizing'
+					 WHERE submission_id = ? AND kind IN ('direct', 'dispatch') AND status = 'terminalizing'
