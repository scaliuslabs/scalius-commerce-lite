-- No-op migration: schema TypeScript files were updated (UNIX_NOW constant,
-- FK declarations) but these produce identical SQL output and do not require
-- any database changes. This migration exists only to sync Drizzle's snapshot
-- state with the current schema definitions.
SELECT 1;