---
// Owned by B5 (warranty): the warranty trust row in the buy box, under DeliveryFacts; renders nothing until B5 ships it.
import type { Product } from "@/lib/api";

export interface Props {
  product: Product;
}
---
