---
// Owned by B2 (reviews storefront): the star summary under the product title; renders nothing until B2 ships it.
import type { Product } from "@/lib/api";

export interface Props {
  product: Product;
}
---
