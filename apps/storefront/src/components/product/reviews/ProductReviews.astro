---
// Owned by B2 (reviews storefront): the product page's `reviews` module (summary, histogram, list, pager); renders nothing until B2 ships it.
import type { Product } from "@/lib/api";

export interface Props {
  product: Product;
}
---
