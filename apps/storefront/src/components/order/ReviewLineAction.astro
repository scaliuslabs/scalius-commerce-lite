---
// Owned by B2 (reviews storefront): the line's review form (works without JavaScript) or the buyer's review; renders nothing until B2 ships it.
import type { LineExtrasContext, OrderLine } from "@/lib/order-line-extras";

export interface Props {
  line: OrderLine;
  context: LineExtrasContext;
}
---
