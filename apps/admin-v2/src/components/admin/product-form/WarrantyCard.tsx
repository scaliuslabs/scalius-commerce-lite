// Owned by B5 (warranty): the product's warranty policy select and preview line; nothing until B5 ships it.
import type { UseFormReturn } from "react-hook-form";
import type { ProductFormValues } from "./types";

export function WarrantyCard(_props: {
  form: UseFormReturn<ProductFormValues>;
  readOnly: boolean;
}): null {
  return null;
}
