// Owned by B3 (digital goods): files and licence keys of a digital product; nothing until B3 ships it.
import type { UseFormReturn } from "react-hook-form";
import type { ProductFormValues } from "./types";

export function DigitalDeliveryCard(_props: {
  form: UseFormReturn<ProductFormValues>;
  /** Unset while the product is being added (files attach to a saved product). */
  productId: string | undefined;
  readOnly: boolean;
}): null {
  return null;
}
