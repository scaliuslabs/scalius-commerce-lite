// Owned by B4 (gift cards): the gift-card product switch (variants are denominations); nothing until B4 ships it.
import type { UseFormReturn } from "react-hook-form";
import type { ProductFormValues } from "./types";

export function GiftCardProductCard(_props: {
  form: UseFormReturn<ProductFormValues>;
  productId: string | undefined;
  readOnly: boolean;
}): null {
  return null;
}
