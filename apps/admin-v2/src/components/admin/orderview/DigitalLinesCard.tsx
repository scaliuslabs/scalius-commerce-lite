// Owned by B3 (digital goods): "Digital · Delivered automatically" (entitlements, keys by last 4, resend); nothing until B3 ships it.
import type { Order } from "./types";

export function DigitalLinesCard(_props: { order: Order }): null {
  return null;
}
