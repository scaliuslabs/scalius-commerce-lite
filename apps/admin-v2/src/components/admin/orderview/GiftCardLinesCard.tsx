// Owned by B4 (gift cards): the gift cards this order issued (last 4, balance, resend); nothing until B4 ships it.
import type { Order } from "./types";

export function GiftCardLinesCard(_props: { order: Order }): null {
  return null;
}
