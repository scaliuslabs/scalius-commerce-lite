// Owned by B5 (warranty): each line's warranty, its expiry and "Open claim"; nothing until B5 ships it.
import type { Order } from "./types";

export function WarrantyLinesCard(_props: { order: Order }): null {
  return null;
}
