// Owned by B5 (warranty): the Claim card in a claim thread's context rail (policy, dates, status actions); nothing until B5 ships it.
import type { StaffThread } from "~/lib/api-query-options/inbox";

export function WarrantyClaimContext(_props: { thread: StaffThread }): null {
  return null;
}
