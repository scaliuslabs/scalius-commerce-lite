// Owned by B1 (reviews): the Review card in a review thread's context rail (stars, text, state, reply); nothing until B1 ships it.
import type { StaffThread } from "~/lib/api-query-options/inbox";

export function ReviewContext(_props: { thread: StaffThread }): null {
  return null;
}
