// Agent operation registry rows for the dashboard digital-goods routes (Wave B).
// Empty until B3 adds its routes; every documented route needs exactly one row.
import type { OperationRegistryEntry } from "./entry";

export const DASHBOARD_DIGITAL_OPERATIONS = {} satisfies Record<string, OperationRegistryEntry>;
