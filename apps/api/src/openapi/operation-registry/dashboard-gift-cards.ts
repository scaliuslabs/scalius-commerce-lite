// Agent operation registry rows for the dashboard gift-card routes (Wave B).
// Empty until B4 adds its routes; every documented route needs exactly one row.
import type { OperationRegistryEntry } from "./entry";

export const DASHBOARD_GIFT_CARD_OPERATIONS = {} satisfies Record<string, OperationRegistryEntry>;
