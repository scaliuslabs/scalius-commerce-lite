// Agent operation registry rows for the storefront digital-goods routes (Wave B).
// Empty until B3 adds its routes; every documented route needs exactly one row.
import type { OperationRegistryEntry } from "./entry";

export const STOREFRONT_DIGITAL_OPERATIONS = {} satisfies Record<string, OperationRegistryEntry>;
