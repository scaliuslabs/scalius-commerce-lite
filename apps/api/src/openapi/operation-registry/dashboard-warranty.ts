// Agent operation registry rows for the dashboard warranty routes (Wave B).
// Empty until B5 adds its routes; every documented route needs exactly one row.
import type { OperationRegistryEntry } from "./entry";

export const DASHBOARD_WARRANTY_OPERATIONS = {} satisfies Record<string, OperationRegistryEntry>;
