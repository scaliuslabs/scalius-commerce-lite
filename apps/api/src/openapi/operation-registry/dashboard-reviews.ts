// Agent operation registry rows for the dashboard review routes (Wave B).
// Empty until B1 adds its routes; every documented route needs exactly one row.
import type { OperationRegistryEntry } from "./entry";

export const DASHBOARD_REVIEW_OPERATIONS = {} satisfies Record<string, OperationRegistryEntry>;
